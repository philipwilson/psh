# Improvement Program 2026-09 — Unified Ledger

- **Created:** 2026-09-06 (Wave 0.1, package D). Maintained by the program integrator.
- **Launch base:** `origin/main` @ `6459f1a6` (v0.779.0). Wave 0 branch `fix/wave0-oracle-5.3`.
- **Oracle ruling acknowledged (launch checklist item 1):** `/opt/homebrew/bin/bash` =
  GNU bash **5.3.15(1)-release** (aarch64-apple-darwin) is THE oracle. Never `/bin/bash`
  (3.2), never bare `bash` on PATH. "Verified against 5.2" is provenance, not a claim (D12).
- **This is THE registry** (program §2, §3 item 7): one row per canonical inventory cid
  (Part A, 245), one per red local-gate node at `6459f1a6` (Part B, 51), one per side
  finding registered at Wave 0 (Part C, W0-N1..N10), plus the Wave 0 rulings and the
  Park register (Part D). A row leaves this ledger only as **CLOSED** (release named in
  `closure`, with probe + flipped/added pin + guard), **PARKED** (§15 ruling with a
  successor owner) or **EXCLUDED** (§15; `fixed` / `not_reproducible`). Nothing labelled
  partial, carried, watch or to-be-decided counts as closed (§3 item 7); no row has an
  empty or undecided owner (D10 / §6 exit criteria).
- **Owner derivation:** Parts A, B and E are GENERATED by
  [`tools/make_ledger.py`](tools/make_ledger.py) from `INVENTORY.json`, `gate_triage.json`
  and the program's own slot headings (§6–§13), §12, §15 and §16 — cid → slot comes from
  the headings, not from the §16 map, and Part E prints the cross-check. Regenerate with
  `python3 docs/reviews/evidence/improvement_program_2026_09/tools/make_ledger.py`;
  hand-edit only the header, Part C and Part D prose — Part D also holds the generated C241
  census block (marked `generated:c241`), which the script rewrites like the others.
- **Columns:** `owner slot` = the wave.slot (or `Park P-n` / `Excluded` / `R`) that closes the
  row; `owner symbol` = the `file.py#symbol` the slot brief names as the single holder of the
  rule (D2); `guard` = the ratchet / drift-lock / matrix / counter that forbids re-duplication
  (run against a synthetic offender in the shipping slot); `closure` = empty until a release
  closes the row ("release vX.Y.Z" + evidence dir).
- **Status vocabulary (Part A):** `live` (re-reproduced at HEAD vs 5.3.15 on 2026-09-06),
  `n/a` (design / theme / doc row — not verifiable as a defect; closed by its slot's guard or
  ruling), `oracle_changed` (bash 5.3 moved; ruled in Part D), `not_reproducible` / `fixed`
  (excluded, §15).

## Part A — Canonical inventory (245 rows, C001–C245)

<!-- generated:part-a:start -->
| id | title | sev / kind | status | owner slot | owner symbol | guard | closure |
|---|---|---|---|---|---|---|---|
| C001 | Function/eval/source body as pipeline member drops every command after the first external… | P0 defect | live → closed v0.788.0 | 1.1 | `psh/executor/context.py#ExecutionContext.for_pipeline_member` (one-shot `in_pipeline`) | ratchet — `in_pipeline` referenced only in `context.py`/`pipeline.py` (offender); `--debug-exec` shows no exec; golden rows | slot 1.1: one-shot `ExecutionContext.for_pipeline_member` token consumed at `CommandExecutor.execute`; `in_pipeline` gone (ratchet `test_in_pipeline_ratchet.py`); pins `test_pipeline_member_one_shot.py`, `test_pipeline_member_conformance.py`, golden `pipeline_member_*`; PTY pins for the job-control guard |
| C002 | `((` lexed as DOUBLE_LPAREN without checking for a matching `))` — `((cmd); cmd)` mis-lexed as… | P0 defect | live | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C003 | Cascade hardening: unbalanced DOUBLE_LPAREN leaves arithmetic_depth > 0 for the rest of the… | P0 design | n/a | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C004 | is_comment_start keys on the preceding character: `)` missing (`(cmd)#c` rejected) and `{`… | P0 defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C005 | fd-prefix redirect recognized mid-word: digit or `{` after a quote/expansion in the same word… | P0 defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C006 | `>&` branch backtracks INTO the previous token (overlapping spans) — a2>&1 treated as fd-2… | P0 defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C007 | $((…)) extent scan is quote-blind: quoted ')'/'(' inside arithmetic ends it early | P0 defect | live | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C008 | $'\uXXXX'/$'\UXXXXXXXX' call chr() with no surrogate/range guard → UnicodeEncodeError (internal… | P0 defect | live | 3.1 | `psh/utils/escapes.py#decode_ansi_c_escapes` (one digit reader; `unicode_escape_char` with surrogate/range guards) | ratchet forbidding `chr(int(` in `psh/lexer/` and `psh/utils/` outside the owner (offender); cross-entry NUL matrix |  |
| C009 | ')' closing a function-definition header inside $(…) drops command position → case in function… | P0 defect | live | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C010 | for/select loop variable corrupted when the header comes from an alias | P0 defect | live → closed v0.787.0 | 1.7 | `psh/parser/recursive_descent/parsers/control_structures.py` (`ForLoop.variable` = token value; `session.py#token_lexeme`) | three-mode pins with `--debug-ast` | slot 1.7: `psh/lexer/token_types.py#slice_renders_token` / `token_lexeme`; for/select variable + heredoc delimiter gated; 43 pins red on base; guard offender mutation |
| C011 | _is_shell_token_delimiter includes `{ } [ ]`, so `!` glued to a word (`![`, `![[`, `!{`) is… | P1 defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C012 | Heredoc-discovery loop re-lexes the entire pending logical command per additional line — O(N^2)… | P1 perf | live | 5.2 | `psh/lexer/heredoc_lexer.py` — skip the re-lex while the failure is `UnclosedQuoteError` | `test_heredoc_scaling.py` extended with a one-logical-command source |  |
| C013 | ANSI-C octal/hex escapes ($'\377', $'\xff', $'\303\251') produce codepoints where bash produces… | P1 defect | live | 3.1 | `psh/utils/escapes.py#decode_ansi_c_escapes` (one digit reader; `unicode_escape_char` with surrogate/range guards) | ratchet forbidding `chr(int(` in `psh/lexer/` and `psh/utils/` outside the owner (offender); cross-entry NUL matrix |  |
| C014 | Heredoc spec ids matched to << tokens positionally, but registration skips operators with… | P1 defect | live | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C015 | `{ list; }` rejected as the body of a traditional `for` and of `select` | P1 defect | live | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C016 | `name=(...)` accepted as an argument to any command, not just declaration builtins | P1 defect | live | 4.2 | `psh/parser/recursive_descent/parsers/arrays.py#_candidate_initializer`; `psh/ast_nodes/command_head.py#CommandHead.of` | ratchet forbidding `.args[0]`/`.words[0]` head inspection in `psh/parser/` and `psh/visitor/` outside `CommandHead` (offender) |  |
| C017 | Newline between `for`/`select` and the loop variable is accepted; bash rejects it | P1 defect | live | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C018 | `a[0]=(1 2)` rejected at parse time; bash parses it and errors only at execution | P1 defect | live | 4.2 | `psh/parser/recursive_descent/parsers/arrays.py#_candidate_initializer`; `psh/ast_nodes/command_head.py#CommandHead.of` | ratchet forbidding `.args[0]`/`.words[0]` head inspection in `psh/parser/` and `psh/visitor/` outside `CommandHead` (offender) |  |
| C019 | Exponential (~4^n) backtracking caused by one redundant pipeline-element retry… | P1 perf | live | 5.3 | `psh/parser/combinators/commands/pipelines.py` (redundant retry deleted; optional packrat memo keyed `(id(parser), pos)`) | counter pin linear in nesting (≤ 2× per +2 levels); memory pin |  |
| C020 | Trailing redirect on [[ ]] / (( )) silently split into a second statement, discarding exit… | P1 defect | live → closed v0.786.0 | 1.9 | `_parse_trailing_redirects` in `psh/parser/combinators/special_commands.py` | matrix — every AST class with `redirects` has a combinator production consuming them (RD↔combinator AST equality); direct-API pins (D6) | slot 1.9: `TrailingRedirectMixin._parse_trailing_redirects` (two copies deleted); RD↔combinator redirect matrix guard; 57 pins red on base |
| C021 | Bare process substitution at command position parses as a pipeline element, yielding a… | P1 defect | live | 3.5 | `psh/parser/combinators/special_commands.py` (`special_command` without `.or_else(self.process_substitution)`); `build_statement_list` depth vs the ONE `MAX_NESTING_DEPTH` | direct-API pins under strict-errors |  |
| C022 | Backgrounded command inside pipeline/redirected compound gets /dev/null stdin | P1 defect | live | 1.12 | `psh/executor/process_launcher.py#AsyncJobPolicy.apply` (fd 0 origin from the stream bindings) | pins read the actual bytes |  |
| C023 | Unquoted $*/${a[*]} is join-then-split; diverges from bash under empty IFS | P1 defect | live | 1.19 | `psh/expansion/word_expander.py#_project_star_fields(parts, quoted)`; `DEFAULT_IFS` and `IFS_WHITESPACE` constants | ratchet forbidding `' \t\n'` as a class test and `' '.join(` on field lists in `psh/expansion/` and `read_builtin.py` (offender); compare-bash IFS matrix |  |
| C024 | "${!x}" where x names @ collapses positionals to one field under quoting | P1 defect | live | 1.19 | `psh/expansion/word_expander.py#_project_star_fields(parts, quoted)`; `DEFAULT_IFS` and `IFS_WHITESPACE` constants | ratchet forbidding `' \t\n'` as a class test and `' '.join(` on field lists in `psh/expansion/` and `read_builtin.py` (offender); compare-bash IFS matrix |  |
| C025 | Backslash-escaped } not unescaped in DQ context; escaped text stored into variable by := | P1 defect | live | 1.19 | `psh/expansion/word_expander.py#_project_star_fields(parts, quoted)`; `DEFAULT_IFS` and `IFS_WHITESPACE` constants | ratchet forbidding `' \t\n'` as a class test and `' '.join(` on field lists in `psh/expansion/` and `read_builtin.py` (offender); compare-bash IFS matrix |  |
| C026 | IFS whitespace hardcoded as ' \t\n' (missing \v \f \r); literal duplicated in word_splitter,… | P1 defect | live | 1.19 | `psh/expansion/word_expander.py#_project_star_fields(parts, quoted)`; `DEFAULT_IFS` and `IFS_WHITESPACE` constants | ratchet forbidding `' \t\n'` as a class test and `' '.join(` on field lists in `psh/expansion/` and `read_builtin.py` (offender); compare-bash IFS matrix |  |
| C027 | set -u consults state.env, reinstating the env fallback the subsystem declares it removed | P1 defect | live | 1.15 | `scope_manager.lookup(name).is_set` in `psh/core/options.py`; `parameter_expansion.py` drops the env union | ratchet forbidding `state.env` reads for set-ness/enumeration in `psh/core/options.py`, `psh/expansion/` (offender); user-guide `:957` |  |
| C028 | set -a (allexport) applies only to plain assignments, missing all four declaration builtins | P1 defect | live | 1.16 | `ScopeManager.set_variable`/`create_local` (allexport at the write site); `state.py:1047` consumer deleted | conformance across all five spellings; `CLAIM_TESTS` mapped; user-guide `:961` |  |
| C029 | printf hand-rolls option parsing; accepts any `-X` as the format string (rc 0); attached… | P1 defect | live | 4.7 | `Builtin.parse_flags` — the only option walker (printf/times/eval/builtin/source migrated; six declaration parsers use `self.error`+`self.usage`) | `test_builtin_help_sync.py` (c): builtins scanning a leading `-` without `parse_flags` on an explicit justified allowlist (the C135 drift-lock; offender); user-guide `:976` |  |
| C030 | Python int() semantics (underscores, 0o prefix) leak into 8 builtins' numeric operands;… | P1 defect | live | 1.14 | `psh/builtins/numeric.py#legal_number` (+ `legal_octal`, `legal_fd`, `finite_timeout`) | ratchet — no bare `int(`/`float(` on argv-derived operands under `psh/builtins/` outside the owner (offender); user-guide `:990` |  |
| C031 | Redirect targets for fd >= 3 expanded twice for in-process builtins (cmdsubs run twice,… | P1 defect | live → closed v0.784.0 | 1.3 | the resolved `RedirectPlan`; `psh/io_redirect/manager.py#_builtin_redirect_fd_level` applies it | counter — `planner.plan` runs once per `RedirectOp` (instrumented; mutation re-adds the second call) | slot 1.3: `_builtin_redirect_fd_level` applies the resolved `RedirectPlan`; `FileRedirector.apply_plan_saving`; plan-once counter guard (24 nodes); pins in three modes + conformance + golden `c031_*` |
| C032 | In every in-process compound, `1>&-` closing fd 1/2 defeats a LATER reopen in the same redirect… | P1 defect | live → closed v0.781.0 | 1.2 | `psh/io_redirect/manager.py#_swap_closed_output_streams` installs `_RawFdStream` | pins assert file contents after `1>&- 1>f` / `2>&- 2>f` in compound forms | slot 1.2: `_swap_closed_output_streams` installs `_RawFdStream`; pins `tests/integration/redirection/test_close_then_reopen_c032.py`, `tests/unit/io_redirect/test_close_reopen_stream_shapes_c032.py`, `tests/conformance/bash/test_close_then_reopen_conformance.py`, golden `c032_*`; verify round 1 PASS |
| C033 | Formatter drops ${v} braces before a brace expansion, changing which variables are read… | P1 defect | live | 1.6 | `VariableExpansion.braced` (`psh/visitor/formatter_visitor.py#_needs_brace_disambiguation`, `psh/ast_nodes/words.py` `__str__`) | `tests/unit/visitor/test_executable_roundtrip.py` corpus: `--format` and `eval "$(declare -f f)"` reproduce stdout/rc |  |
| C034 | History numbers are list positions, not bash's absolute number; !n wrong after any trim | P1 defect | live | 4.18 | `ShellState.history_base` (monotonic; owner of `!n`, `\!`, listing numbers); `LOCK_EX` in `write_history`/`append_history`; `_editable` at `_replace_line`; `CTRL_UNDERSCORE` → undo | pins via `history -s`/`-p`/`${PS1@P}` + one PTY leg; user-guide `:983` |  |
| C035 | Tab completion cannot round-trip its own escaping (2nd Tab on any path with a space fails);… | P1 defect | live | 4.19 | `psh/interactive/tab_completion.py#CompletionContext` (raw span, decoded lookup text, quote mode) | PTY workflow tests (escaped spaces/quotes/`$`/backticks/backslashes, cursor mid-word) |  |
| C036 | Under set -o vi, Ctrl-D does nothing; ignoreeof path unreachable | P1 defect | live | 4.20 | one canonical action table in `psh/interactive/keybindings.py`; `prompt.py#_get_cwd` (shell `HOME`, component boundary, `PROMPT_DIRTRIM`, octal 1–3 digits) | completeness test — mode-independent actions bound in every table |  |
| C037 | \w/\W read process HOME and match as bare string prefix | P1 defect | live | 4.20 | one canonical action table in `psh/interactive/keybindings.py`; `prompt.py#_get_cwd` (shell `HOME`, component boundary, `PROMPT_DIRTRIM`, octal 1–3 digits) | completeness test — mode-independent actions bound in every table |  |
| C038 | Terminal title writes $PWD/command text into OSC 0 unsanitized (escape injection) | P1 defect | live | 3.6 | `psh/interactive/title.py#sanitize_title` at the single OSC write site | PTY pin with a crafted `$PWD` basename; CHANGELOG flags security |  |
| C039 | Diagnostics + $LINENO report wrong line inside any continuation-joined command | P1 defect | live | 4.4 | `process_line_continuations` → `(joined_text, LineMap)`; `psh/parser/recursive_descent/support/nested_parse.py#parse_nested_command` with the enclosing source | pins BOTH `$LINENO` and the command-not-found line; `psh/parser/CLAUDE.md` coordinate invariant |  |
| C040 | `set -n` (noexec) inert in -c mode; commands after it still execute | P1 defect | live | 1.10 | `psh/executor/core.py` per-statement `noexec` check; `command.py#_run_command` sets `$?` from the last substitution | independent three-mode pins; user-guide `17_differences_from_bash.md:25` corrected in the same diff |  |
| C041 | A bare command substitution does not set $? | P1 defect | live | 1.10 | `psh/executor/core.py` per-statement `noexec` check; `command.py#_run_command` sets `$?` from the last substitution | independent three-mode pins; user-guide `17_differences_from_bash.md:25` corrected in the same diff |  |
| C042 | Tilde not expanded in case patterns → wrong branch silently taken | P1 defect | live | 1.11 | `psh/expansion/pattern_words.py#expand_pattern_word` (tilde + quote-aware; `case`, `[[ == ]]`, `${var#pat}`) | pins `~`, `~/x`, `~+`, alternation, quoted control; user-guide rows `:934`/`:941` |  |
| C043 | Logical cd can enter a different directory from the one it reports | P1 defect | live | 1.4 | `psh/builtins/navigation.py#resolve_cd_target(operand, mode)`; `ShellState.__init__` PWD/OLDPWD validation (W0-N7) | D3 pins: `pwd -P`, `os.getcwd()`, a file placed after `cd ..`; `case_env` stale-PWD/bogus-OLDPWD rows in three modes |  |
| C044 | Returning from a function can leave executable resolution using its local PATH | P1 defect | live → closed v0.789.0 | 1.5 | `psh/core/scope.py#ScopeManager._effective_binding_changed(name)`; `CommandHashTable` sole subscriber | counter test over every write path — one observer call per binding change; `execve` target asserted | slot 1.5: `ScopeManager._effective_binding_changed` (pops included); `CommandHashTable` sole subscriber; counter guard; matrix C044 cells flipped |
| C045 | WordShapeTracker advertises five forward properties but three and the WordShape enum have no… | P2 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C046 | Lexer no-progress crash (RuntimeError) is CLI-reachable at v0.779.0; the guard comment asserts… | P2 defect | live | 3.2 | the failing recognizer (backtick-in-subscript extent) fixed; no-progress guard KEPT as an internal-defect raise; `psh/lexer/recognizers/registry.py` carve-out `(RecursionError, PshError, SyntaxError)` | reproducer pinned against 5.3.15; fault-injected pin for the carve-out |  |
| C047 | Two ${…} extent scanners disagree: find_closing_delimiter (via skip_expansion_region) nests a… | P2 defect | live | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C048 | Identifier-prefixed unmatched '[' triggers whole-line lookahead + full text[pos:] copy → O(n²)… | P2 perf | live | 5.1 | `psh/lexer/modular_lexer.py#emit_token` builds the frozen `Token` once; literal collector segment accumulation; cursor-indexed `[` lookahead | scaling pins (one long word; 2000 `[`-words on a line); replace-count = 0 per recognizer token; `tools/regen_lexer_corpus.py` diff reviewed in the ledger |  |
| C049 | Inside ((…)) the lexer doesn't tokenize (whole expression is one WORD) and three downstream… | P2 design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C050 | Collecting one long unquoted literal has avoidable quadratic copying (literal.py:190) | P2 perf | live | 5.1 | `psh/lexer/modular_lexer.py#emit_token` builds the frozen `Token` once; literal collector segment accumulation; cursor-indexed `[` lookahead | scaling pins (one long word; 2000 `[`-words on a line); replace-count = 0 per recognizer token; `tools/regen_lexer_corpus.py` diff reviewed in the ledger |  |
| C051 | Parse errors inside `$(...)` bodies mix two coordinate systems (file line, body-local… | P2 defect | live | 4.4 | `process_line_continuations` → `(joined_text, LineMap)`; `psh/parser/recursive_descent/support/nested_parse.py#parse_nested_command` with the enclosing source | pins BOTH `$LINENO` and the command-not-found line; `psh/parser/CLAUDE.md` coordinate invariant |  |
| C052 | `for ( (` (non-adjacent parens) and unbalanced `for ((…) )` accepted as C-style for headers | P2 defect | live | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C053 | Dead grammar path: WORD-shaped fd duplication (parse_fd_dup_word / _is_fd_duplication)… | P2 design | n/a | 4.2 | `psh/parser/recursive_descent/parsers/arrays.py#_candidate_initializer`; `psh/ast_nodes/command_head.py#CommandHead.of` | ratchet forbidding `.args[0]`/`.words[0]` head inspection in `psh/parser/` and `psh/visitor/` outside `CommandHead` (offender) |  |
| C054 | parse_for_statement and parse_select_statement duplicate their whole tail | P2 design | n/a | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C055 | Three routes construct a ParseError, two through a private method on another class | P2 design | n/a | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C056 | time -p ! cmd silently runs `!` as a command | P2 defect | live | 4.15 | `TokenGroups.WORD_LIKE` (every word-like set derives from it); `[[` operator table imported from the one RD/test-evaluator table; `build_statement_list` stamps `.line` | ratchet forbids local `frozenset({TokenType.WORD, …})` in `psh/parser/combinators/` (offender); committed RD↔combinator corpus parity test; direct-API pins (D6) |  |
| C057 | Four parallel word-like token sets drifted from TokenGroups.WORD_LIKE; `{`/`}` unusable as… | P2 defect | live | 4.15 | `TokenGroups.WORD_LIKE` (every word-like set derives from it); `[[` operator table imported from the one RD/test-evaluator table; `build_statement_list` stamps `.line` | ratchet forbids local `frozenset({TokenType.WORD, …})` in `psh/parser/combinators/` (offender); committed RD↔combinator corpus parity test; direct-API pins (D6) |  |
| C058 | Over-acceptance family (latent, shielded by RD trial gate): no separator required, `if` needs… | P2 defect | live | 4.15 | `TokenGroups.WORD_LIKE` (every word-like set derives from it); `[[` operator table imported from the one RD/test-evaluator table; `build_statement_list` stamps `.line` | ratchet forbids local `frozenset({TokenType.WORD, …})` in `psh/parser/combinators/` (offender); committed RD↔combinator corpus parity test; direct-API pins (D6) |  |
| C059 | No nesting-depth guard: deeply nested input raises RecursionError instead of a clean ParseError | P2 defect | live | 3.5 | `psh/parser/combinators/special_commands.py` (`special_command` without `.or_else(self.process_substitution)`); `build_statement_list` depth vs the ONE `MAX_NESTING_DEPTH` | direct-API pins under strict-errors |  |
| C060 | [[ ]] rejects -ef/-nt/-ot; diagnostic names three causes none of which is real | P2 defect | live | 4.15 | `TokenGroups.WORD_LIKE` (every word-like set derives from it); `[[` operator table imported from the one RD/test-evaluator table; `build_statement_list` stamps `.line` | ratchet forbids local `frozenset({TokenType.WORD, …})` in `psh/parser/combinators/` (offender); committed RD↔combinator corpus parity test; direct-API pins (D6) |  |
| C061 | Bash-style syntax messages built by f-string instead of shared unexpected_token_message: "token… | P2 defect | live | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C062 | Combinator stamps no .line on statements: $LINENO wrong inside every compound body [previously… | P2 defect | live | 4.15 | `TokenGroups.WORD_LIKE` (every word-like set derives from it); `[[` operator table imported from the one RD/test-evaluator table; `build_statement_list` stamps `.line` | ratchet forbids local `frozenset({TokenType.WORD, …})` in `psh/parser/combinators/` (offender); committed RD↔combinator corpus parity test; direct-API pins (D6) |  |
| C063 | POSIX mode: `VAR=v exec` does not persist the assignment; persistence code inert | P2 defect | live | 4.11 | `ExecutionResult.assignments_persist` (posix `VAR=v exec`); `control_flow.py` select `if reply == '': continue` | conformance pins; user-guide `:942` if landing last |  |
| C064 | `select`: empty input line runs body instead of redisplaying menu | P2 defect | live | 4.11 | `ExecutionResult.assignments_persist` (posix `VAR=v exec`); `control_flow.py` select `if reply == '': continue` | conformance pins; user-guide `:942` if landing last |  |
| C065 | Compound background jobs recorded under placeholder text, breaking %?str jobspecs | P2 defect | live | 4.12 | `psh/executor/job_control.py#job_command_text(node, source)` (AST source extents); `JobManager.create_job` burns no number for foreground commands | flips the Wave 0 signal-death FLIP-PIN to parity (`strsignal.ljust(27) + text`) |  |
| C066 | Fifteen executor diagnostics bypass state.error_location_prefix() | P2 defect | live | 4.3 | `ShellState.error_location_prefix()` / `Builtin.report_error` (all 18 sites migrated) | `tests/unit/tooling/test_error_prefix_ratchet.py` — AST walk forbids `"psh: "` literals to stderr under executor/expansion/builtins (allowlist frozen to C205; offender) |  |
| C067 | `wait` interrupted by trapped signal returns job status instead of 128+N | P2 defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C068 | "${!arr[*]}" joins indices/keys with a literal space instead of IFS[0] | P2 defect | live | 1.19 | `psh/expansion/word_expander.py#_project_star_fields(parts, quoted)`; `DEFAULT_IFS` and `IFS_WHITESPACE` constants | ratchet forbidding `' \t\n'` as a class test and `' '.join(` on field lists in `psh/expansion/` and `read_builtin.py` (offender); compare-bash IFS matrix |  |
| C069 | Expansion diagnostics (arith evaluator.py:808, operators.py:99, arrays.py:146) hardcode "psh: "… | P2 defect | live | 4.3 | `ShellState.error_location_prefix()` / `Builtin.report_error` (all 18 sites migrated) | `tests/unit/tooling/test_error_prefix_ratchet.py` — AST walk forbids `"psh: "` literals to stderr under executor/expansion/builtins (allowlist frozen to C205; offender) |  |
| C070 | A temp-env prefix over eval/source/. is enumeration-invisible, but bash pushes a real scope… | P2 defect | live | 4.9 | `push_temp_env_scope` (eval/source/`.` push a real `is_temp_env` scope); `_remove_export` resolves the nameref first; `SHLVL` seeded in `ShellState.__init__`; `type` under unset PATH | enumeration test (`set`, `export -p`) over the temp-env scope; 1.15 `state.env` ratchet extended to `psh/builtins/environment.py` |  |
| C071 | export -n through a nameref is a silent no-op | P2 defect | live | 4.9 | `push_temp_env_scope` (eval/source/`.` push a real `is_temp_env` scope); `_remove_export` resolves the nameref first; `SHLVL` seeded in `ShellState.__init__`; `type` under unset PATH | enumeration test (`set`, `export -p`) over the temp-env scope; 1.15 `state.env` ratchet extended to `psh/builtins/environment.py` |  |
| C072 | ${!prefix*} lists opaque non-identifier environment entries, breaking the H3 partition | P2 defect | live | 1.15 | `scope_manager.lookup(name).is_set` in `psh/core/options.py`; `parameter_expansion.py` drops the env union | ratchet forbidding `state.env` reads for set-ness/enumeration in `psh/core/options.py`, `psh/expansion/` (offender); user-guide `:957` |  |
| C073 | Half of every variable write is spent in enum.Flag arithmetic | P2 perf | live | 5.5 | `VarAttributes.is_*` against raw ints; PEP-562 lazy `psh/__init__.py` (+ `__main__.py` handles `--version`/`--help` before importing `Shell`); `_read_line_block` chunk accumulator; `args` snapshot per handler | `python -c "import psh"` does not import `psh.shell`; `--version` wall ≤ 40% of the Wave 0 baseline; microbench ≥ 1.6× recorded |  |
| C074 | cd treats an EMPTY HOME / OLDPWD as unset (errors rc 1 where bash no-ops rc 0) | P2 defect | live | 1.4 | `psh/builtins/navigation.py#resolve_cd_target(operand, mode)`; `ShellState.__init__` PWD/OLDPWD validation (W0-N7) | D3 pins: `pwd -P`, `os.getcwd()`, a file placed after `cd ..`; `case_env` stale-PWD/bogus-OLDPWD rows in three modes |  |
| C075 | source/. with no filename returns 1 not 2, skips the usage line and the POSIX-mode shell exit | P2 defect | live | 4.8 | per builtin: `source` usage + `SpecialBuiltinUsageError(2, suppressible=True)`; `kill -n`; `wait` malformed id; `psh/utils/printf_formatter.py` `_CONVERSIONS` without `%` | conformance status/message rows; user-guide `:976`/`:953` |  |
| C076 | Seven builtins (printf, times, eval, builtin, source, pushd, popd, dirs) do not reject invalid… | P2 defect | live | 4.7 | `Builtin.parse_flags` — the only option walker (printf/times/eval/builtin/source migrated; six declaration parsers use `self.error`+`self.usage`) | `test_builtin_help_sync.py` (c): builtins scanning a leading `-` without `parse_flags` on an explicit justified allowlist (the C135 drift-lock; offender); user-guide `:976` |  |
| C077 | `kill -n SIGNUM` unimplemented; `-n` parsed as a signal NAME | P2 defect | live | 4.8 | per builtin: `source` usage + `SpecialBuiltinUsageError(2, suppressible=True)`; `kill -n`; `wait` malformed id; `psh/utils/printf_formatter.py` `_CONVERSIONS` without `%` | conformance status/message rows; user-guide `:976`/`:953` |  |
| C078 | wait returns 127 where bash returns 1 for a syntactically malformed id | P2 defect | live | 4.8 | per builtin: `source` usage + `SpecialBuiltinUsageError(2, suppressible=True)`; `kill -n`; `wait` malformed id; `psh/utils/printf_formatter.py` `_CONVERSIONS` without `%` | conformance status/message rows; user-guide `:976`/`:953` |  |
| C079 | Failing dup2 onto out-of-range target fd prints the errno number (builtin) or filename… | P2 defect | live | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C080 | fd prefix >= 2^31 escapes as a raw Python OverflowError (fcntl raises OverflowError, guards… | P2 defect | live | 3.3 | `psh/io_redirect/file_redirect.py#fd_from_text` (int32 range; `(OSError, OverflowError)` caught) | pins `exec 4294967296>f`, `echo x 2147483648>f` |  |
| C081 | >(cmd) is FIFO-backed on EVERY platform (no platform branch) although root CLAUDE.md/code call… | P2 defect | live | 1.13 | `psh/io_redirect/process_sub.py` (`ExitStack`-owned acquisition; blocking open or diagnostic, never silent `/dev/null`) | fault injection at pipe/flag/FIFO/fork/launch; `/dev/fd` census asserts no leak; 6 s late-open pin; Linux nightly (D11) |  |
| C082 | >(cmd) child gives up after a fixed 5s SIGALRM: opens /dev/null, unlinks the FIFO and exits 0… | P2 defect | live | 1.13 | `psh/io_redirect/process_sub.py` (`ExitStack`-owned acquisition; blocking open or diagnostic, never silent `/dev/null`) | fault injection at pipe/flag/FIFO/fork/launch; `/dev/fd` census asserts no leak; 6 s late-open pin; Linux nightly (D11) |  |
| C083 | DebugASTVisitor silently drops redirects on 10 of 13 redirect-carrying node classes | P2 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C084 | MetricsVisitor substitution counts are word-anchored not node-anchored; under-counts nested… | P2 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C085 | Validator '>&1' advisory is inverted: never fires on the mistake, always on the correct spelling | P2 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C086 | Invocation -o/+o reads cluster remainder as option NAME; bash takes next argv element and keeps… | P2 defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C087 | Ten in-code evidence citations point into gitignored tmp/ (five already gone) plus ~96 opaque… | P2 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C088 | psh/__init__.py eager `from .shell import Shell` forces the entire module tree; 79% of… | P2 perf | live | 5.5 | `VarAttributes.is_*` against raw ints; PEP-562 lazy `psh/__init__.py` (+ `__main__.py` handles `--version`/`--help` before importing `Shell`); `_read_line_block` chunk accumulator; `args` snapshot per handler | `python -c "import psh"` does not import `psh.shell`; `--version` wall ≤ 40% of the Wave 0 baseline; microbench ≥ 1.6× recorded |  |
| C089 | printf with any flag/width/precision/length before % prints a literal % and exits 0; comment at… | P2 defect | live | 4.8 | per builtin: `source` usage + `SpecialBuiltinUsageError(2, suppressible=True)`; `kill -n`; `wait` malformed id; `psh/utils/printf_formatter.py` `_CONVERSIONS` without `%` | conformance status/message rows; user-guide `:976`/`:953` |  |
| C090 | mapfile consumes input before rejecting its destination and can corrupt array attributes | P2 defect | live | 1.17 | `psh/builtins/mapfile_builtin.py` preflight (nameref target resolved, writability/indexed compatibility validated BEFORE reading) | D3 pins: fd position after rejection; untouched assoc contents |  |
| C091 | Process-substitution acquisition leaks resources when fork fails | P2 defect | live | 1.13 | `psh/io_redirect/process_sub.py` (`ExitStack`-owned acquisition; blocking open or diagnostic, never silent `/dev/null`) | fault injection at pipe/flag/FIFO/fork/launch; `/dev/fd` census asserts no leak; 6 s late-open pin; Linux nightly (D11) |  |
| C092 | A script-file read error is silently converted to EOF (LazyFileInput._read_line_block) | P2 defect | live | 3.7 | `psh/executor/pipeline.py` (`except BaseException: rollback; raise`; explicit empty-status branch); `psh/scripting/input_sources.py` read error propagates | both pipeline fault-injection probes promoted to tests; `/dev/fd` census asserts no leak |  |
| C093 | Arithmetic promotion from scalar to array discards the scalar value | P2 defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C094 | Explicit array indices incorrectly set a high-water mark for subsequent elements | P2 defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C095 | Integer-array initialization applies attributes at the wrong phase | P2 defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C096 | Associative initializers accept invalid empty keys and mixed forms | P2 defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C097 | Terminal input and layout conflate bytes, code points, and display columns: per-read decode… | P2 defect | live | 4.21 | the 4B.2 incremental UTF-8 decoder reused by `KeyDecoder`; `line_layout` single width policy | ratchet: no second decoder; PTY pin delivering split 2/3/4-byte sequences |  |
| C098 | Invalid read options escape through Python numeric/platform exceptions (read -t inf, huge -u →… | P2 defect | live | 1.14 | `psh/builtins/numeric.py#legal_number` (+ `legal_octal`, `legal_fd`, `finite_timeout`) | ratchet — no bare `int(`/`float(` on argv-derived operands under `psh/builtins/` outside the owner (offender); user-guide `:990` |  |
| C099 | Static security analysis loses the command behind assignment prefixes and wrappers (X=1 eval /… | P2 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C100 | handle_ansi_c_escape repeats three near-identical 'read up to N hex digits' loops plus an octal… | P3 design | n/a | 3.1 | `psh/utils/escapes.py#decode_ansi_c_escapes` (one digit reader; `unicode_escape_char` with surrogate/range guards) | ratchet forbidding `chr(int(` in `psh/lexer/` and `psh/utils/` outside the owner (offender); cross-entry NUL matrix |  |
| C101 | Duplicated balanced-[ / ] matcher in _parse_dollar_bracket_arithmetic and… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C102 | Recognizer registry converts EVERY recognizer exception into RuntimeError (latent… | P3 design | n/a | 3.2 | the failing recognizer (backtick-in-subscript extent) fixed; no-progress guard KEPT as an internal-defect raise; `psh/lexer/recognizers/registry.py` carve-out `(RecursionError, PshError, SyntaxError)` | reproducer pinned against 5.3.15; fault-injected pin for the carve-out |  |
| C103 | Every recognizer-produced token is rebuilt with dataclasses.replace — largest cost in a lexing… | P3 perf | live | 5.1 | `psh/lexer/modular_lexer.py#emit_token` builds the frozen `Token` once; literal collector segment accumulation; cursor-indexed `[` lookahead | scaling pins (one long word; 2000 `[`-words on a line); replace-count = 0 per recognizer token; `tools/regen_lexer_corpus.py` diff reviewed in the ledger |  |
| C104 | WORD_TERMINATORS lists `{`/`}` while _is_word_terminator unconditionally returns False for them | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C105 | Dead configuration: allows_newlines always True, QUOTE_RULES['… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C106 | psh/lexer/CLAUDE.md drift: '>>=' shown as operator but absent from OPERATORS table; stale line… | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C107 | Dead branch: `!` as a command argument (EXCLAMATION guard after args can never fire) | P3 design | n/a | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C108 | create_parser's docstring asserts ZERO production callers, but the parse-tree builtin calls it | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C109 | arithmetic.py:26 comment claims 'legacy tokenization' for a live straddle-split close branch | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C110 | _parse_c_style_for_brace_body duplicates parse_brace_group's brace-body rule | P3 design | n/a | 4.1 | `psh/parser/recursive_descent/parsers/control_structures.py#_parse_word_list_loop(keyword, node_cls)`; `commands.py#_parse_brace_body` | three-mode pins; user-guide `:942` (here or in 4.11) |  |
| C111 | Two diagnostic vocabularies (consume() 'Expected X' vs unexpected_token_message bash-style) →… | P3 defect | live | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C112 | Truncated comment left mid-sentence in unclosed_expansion.py | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C113 | Defensive getattr on fields declared on the Token dataclass | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C114 | Test-operator sets rebuilt per call, next to hoisted frozensets in the same file | P3 perf | not_reproducible | Excluded | §15 Excluded — recorded in `INVENTORY.json` with the verify note; not queued | — |  |
| C115 | `materialize`'s docstring names call sites that do not exist | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C116 | --debug-ast=dot silently drops EVERY scalar field | P3 defect | live | 4.17 | `psh/parser/visualization/dot_generator.py` (scalar else arm; DOT escaping); `_render_sexp_list` indent threading | renderer pins (escaping, indentation) |  |
| C117 | DOT labels are HTML-escaped, not DOT-escaped; backslash unescaped | P3 defect | live | 4.17 | `psh/parser/visualization/dot_generator.py` (scalar else arm; DOT escaping); `_render_sexp_list` indent threading | renderer pins (escaping, indentation) |  |
| C118 | Combinator error path (parser.py:229-241) builds its message from result.error alone: leaks… | P3 defect | live | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C119 | Combinator algebra largely inert: .then/.map used at ONE site tree-wide; 11 of 12 keyword()… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C120 | Constant 4-6x parse cost vs RD, and every input parsed twice under --parser combinator… | P3 perf | live | Park P-2 | by design (educational parser); one sentence in `psh/parser/CLAUDE.md` via 6.1 | C178 is D6 |  |
| C121 | 16 sites construct a fresh Parser INSIDE a parse closure, contradicting the 'built exactly… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C122 | SExpressionRenderer loses all indentation inside &&/\|\| operator trees | P3 defect | live | 4.17 | `psh/parser/visualization/dot_generator.py` (scalar else arm; DOT escaping); `_render_sexp_list` indent threading | renderer pins (escaping, indentation) |  |
| C123 | Doc drift in combinator_parser_guide.md contradicted by the code's own docstrings | P3 doc | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C124 | `( cmd ) \| cat` emits signal-death diagnostic that bash omits | P3 defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C125 | Dead setpgid with misleading rationale inside exec-failure try | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C126 | Pipeline rollback except-clause too narrow — non-OSError/ValueError exceptions leak sync pipes | P3 defect | live | 3.7 | `psh/executor/pipeline.py` (`except BaseException: rollback; raise`; explicit empty-status branch); `psh/scripting/input_sources.py` read error propagates | both pipeline fault-injection probes promoted to tests; `/dev/fd` census asserts no leak |  |
| C127 | all_statuses[-1] unguarded against mark_orphaned_completed empty-list state | P3 defect | live | 3.7 | `psh/executor/pipeline.py` (`except BaseException: rollback; raise`; explicit empty-status branch); `psh/scripting/input_sources.py` read error propagates | both pipeline fault-injection probes promoted to tests; `/dev/fd` census asserts no leak |  |
| C128 | Value-operand builder is quadratic in operand length | P3 perf | live | 5.4 | `psh/expansion/operands.py` list accumulation, flushed per protection change | scaling pin |  |
| C129 | Subscripted VariableExpansion in arithmetic rendered back to source text and re-parsed at… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C130 | readonly diagnostic for an array element through a nameref names the target, not the reference | P3 defect | live | 4.5 | `unexpected_token_message` (via `ctx.consume()` default and both combinator sites); one `make_error_context`; `variable_store.py` raises with the pre-resolution name; `RedirectPlan.target_fd` threaded into the OSError | ratchet forbidding `f"…unexpected token…"`/`"Expected "` literals outside helpers (offender); wording/caret parity rows across modes |  |
| C131 | ShellState.__init__ is a 320-line ordered startup whose constraints live only in comments | P3 design | n/a | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C132 | VariableStore is a façade, not the transaction boundary it is documented as | P3 design | n/a | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C133 | Subsystem prose in psh/core is indexed by campaign codename rather than by behaviour | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C134 | Six hand-rolled declaration-family parsers print bash's invalid-option message backwards and… | P3 defect | live | 4.7 | `Builtin.parse_flags` — the only option walker (printf/times/eval/builtin/source migrated; six declaration parsers use `self.error`+`self.usage`) | `test_builtin_help_sync.py` (c): builtins scanning a leading `-` without `parse_flags` on an explicit justified allowlist (the C135 drift-lock; offender); user-guide `:976` |  |
| C135 | builtins/CLAUDE.md's hand-rolled-parser inventory is wrong; the promised drift-lock would have… | P3 doc | n/a | 4.7 | `Builtin.parse_flags` — the only option walker (printf/times/eval/builtin/source migrated; six declaration parsers use `self.error`+`self.usage`) | `test_builtin_help_sync.py` (c): builtins scanning a leading `-` without `parse_flags` on an explicit justified allowlist (the C135 drift-lock; offender); user-guide `:976` |  |
| C136 | declare -p prints `=()` for a declared-but-never-assigned array where bash prints no value | P3 defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C137 | show-ast/ast-dot/parser-mode construct fresh builtin instances instead of registry.get(...),… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C138 | parser-select writes primary output via shell.stdout.write instead of self.write_line | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C139 | `test -v` bad-subscript diagnostic bypasses self.report_error, hand-inlining… | P3 design | n/a | 4.3 | `ShellState.error_location_prefix()` / `Builtin.report_error` (all 18 sites migrated) | `tests/unit/tooling/test_error_prefix_ratchet.py` — AST walk forbids `"psh: "` literals to stderr under executor/expansion/builtins (allowlist frozen to C205; offender) |  |
| C140 | stdin-source decision duplicated verbatim: ReadBuiltin._should_use_sys_stdin vs… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C141 | _evaluate_binary repeats a five-line try/except six times for -eq/-ne/-lt/-le/-gt/-ge | P3 design | n/a | 1.14 | `psh/builtins/numeric.py#legal_number` (+ `legal_octal`, `legal_fd`, `finite_timeout`) | ratchet — no bare `int(`/`float(` on argv-derived operands under `psh/builtins/` outside the owner (offender); user-guide `:990` |  |
| C142 | setup/restore_builtin_redirections is the one save/restore pair NOT expressed as a context… | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C143 | Docstring mass has outgrown the code in manager.py (~half of 1,181 lines is prose) | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C144 | MetricsVisitor counts each backtick as a separate command substitution in case subjects | P3 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C145 | Validator compound handlers pop context before traversing redirects; swept edges lose their… | P3 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C146 | No analysis mode reports a source location; LintIssue.line/.column dead; ASTNode.line docstring… | P3 design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C147 | SecurityVisitor ARITHMETIC_INJECTION fires for (( )) command, never for $(( )) expansion | P3 defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C148 | Dead branches in FormatterVisitor.visit_SimpleCommand | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C149 | Comments-only visit_ArrayElementAssignment in ValidatorVisitor is dead weight | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C150 | f-string without placeholders in DebugASTVisitor (ruff F541 not selected) | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C151 | Meta-< and Ctrl-R accept skip cmdhist join → raw newlines in buffer, renderer desync | P3 defect | live | 4.18 | `ShellState.history_base` (monotonic; owner of `!n`, `\!`, listing numbers); `LOCK_EX` in `write_history`/`append_history`; `_editable` at `_replace_line`; `CTRL_UNDERSCORE` → undo | pins via `history -s`/`-p`/`${PS1@P}` + one PTY leg; user-guide `:983` |  |
| C152 | Prompt octal \nnn requires exactly 3 digits; bash accepts 1-2 at prompt end | P3 defect | live | 4.20 | one canonical action table in `psh/interactive/keybindings.py`; `prompt.py#_get_cwd` (shell `HOME`, component boundary, `PROMPT_DIRTRIM`, octal 1–3 digits) | completeness test — mode-independent actions bound in every table |  |
| C153 | \# is one greater than bash's command number | P3 defect | oracle_changed → still divergent: `-c` always, nested reads in every mode (R-C153) | 4.24 | one command-number owner (the main-input reader in `psh/scripting/source_processor.py`; `psh/interactive/prompt.py` only renders it): one increment per top-level command read from the main input; `-c` strings never; nested reads (`eval`, `.`/`source`, `$(…)`, subshell/function/loop bodies) never (ruling R-C153) | `${PS1@P}` pins in `-c`/script/stdin with nested-read rows (eval/source/cmdsub/function/loop) + a PTY leg with the same nested rows |  |
| C154 | REPL sketch in interactive/CLAUDE.md drifted and not drift-locked | P3 doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C155 | Emacs undo/redo unreachable; undo stack survives across reads | P3 defect | live | 4.18 | `ShellState.history_base` (monotonic; owner of `!n`, `\!`, listing numbers); `LOCK_EX` in `write_history`/`append_history`; `_editable` at `_replace_line`; `CTRL_UNDERSCORE` → undo | pins via `history -s`/`-p`/`${PS1@P}` + one PTY leg; user-guide `:983` |  |
| C156 | history -a/-w write without the lock save_to_file takes; docstring over-claims concurrency-safe | P3 defect | live | 4.18 | `ShellState.history_base` (monotonic; owner of `!n`, `\!`, listing numbers); `LOCK_EX` in `write_history`/`append_history`; `_editable` at `_replace_line`; `CTRL_UNDERSCORE` → undo | pins via `history -s`/`-p`/`${PS1@P}` + one PTY leg; user-guide `:983` |  |
| C157 | Bare `+` argument rejected; bash treats it as an empty flag cluster | P3 defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C158 | InvocationConfig.argv0 uses "psh" as the no-name sentinel; an explicitly empty $0 is silently… | P3 defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C159 | input_source parameter unannotated in ~29 signatures despite an import-safe InputSource ABC | P3 design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C160 | psh/utils/__init__.py docstring describes 4 of 7 modules; partial facade reads as complete | P3 design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C161 | psh rejects bash's -l/-r/-D/--login/--noprofile/--noediting/--verbose/--restricted with rc2;… | P3 defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C162 | has_unclosed_heredoc is public API with zero production callers | P3 design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C163 | --rcfile with no value exits 2 where bash warns and continues | P3 defect | not_reproducible | Excluded | §15 Excluded — recorded in `INVENTORY.json` with the verify note; not queued | — |  |
| C164 | Bare `]]` outside `[[`: bash syntax error rc=2 vs psh ']]: command not found' rc=127 | unrated defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C165 | coproc absent from the grammar (documented difference) | unrated defect | live | Park P-3 | not a feature-parity sweep; funsub pinned as a declared divergence behind `oracle_feature('funsub')` (0.1); 6.3 verifies the doc rows | `$(< file)` may be pulled in ONLY by a written ruling after Wave 4 |  |
| C166 | `a=(1 2)x`: bash assigns the literal scalar "(1 2)x", psh errors and then runs `x` | unrated defect | live | 4.2 | `psh/parser/recursive_descent/parsers/arrays.py#_candidate_initializer`; `psh/ast_nodes/command_head.py#CommandHead.of` | ratchet forbidding `.args[0]`/`.words[0]` head inspection in `psh/parser/` and `psh/visitor/` outside `CommandHead` (offender) |  |
| C167 | [META] Lexer reviewer's declared coverage gaps: Linux-only paths, interactive/PTY lexing,… | unrated doc | n/a | R | Checkpoint R report (`checkpoint-r/`) — explicit coverage scope | attack rounds to zero; every ratchet re-run against its offender; Linux nightly green at the checkpoint tree |  |
| C168 | CRLF handling divergence (psh strips one trailing CR per line; bash keeps CR) absent from the… | unrated doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C169 | Untracked junk files in repo root: f, f1, f2 | unrated design | n/a → already gone (R-C169) | 0.1 | repo root (`f`, `f1`, `f2`) | ruling R-C169: absent and untracked at `788ffe41`; delete on sight if they reappear |  |
| C170 | Lexer accepts an fd prefix > INT_MAX as a redirect; bash re-lexes it as an ordinary word | unrated defect | live | 1.8 | `psh/lexer/state_context.py#LexicalState.at_word_start` | `tests/unit/tooling/test_lexer_word_start_authority.py` — AST ratchet on `text[pos-1]`/`prev_char in` under `psh/lexer/recognizers/` (offender) + corpus differential vs the `$(…)` scanner |  |
| C171 | ParseSession re-parses the growing non-heredoc logical command on every input line — O(k²) /… | unrated perf | live | Park P-1 | RESUMABLE-PARSER successor campaign (measured cost target + own verification model) | characterization pins become upper-bound tests when it lands |  |
| C172 | Nested command-substitution re-parse is mildly super-linear (capped at… | unrated perf | live | Park P-1 | RESUMABLE-PARSER successor campaign (measured cost target + own verification model) | characterization pins become upper-bound tests when it lands |  |
| C173 | Combinator parser has its own _parse_fd_dup_word — same dead-path question as the RD one… | unrated design | n/a | 4.2 | `psh/parser/recursive_descent/parsers/arrays.py#_candidate_initializer`; `psh/ast_nodes/command_head.py#CommandHead.of` | ratchet forbidding `.args[0]`/`.words[0]` head inspection in `psh/parser/` and `psh/visitor/` outside `CommandHead` (offender) |  |
| C174 | `cat <<` INCOMPLETE-vs-bash classification [previously registered successor row] | unrated defect | live | 4.22 | the `cat <<` classification rule (interactive INCOMPLETE vs non-interactive syntax error) | PTY pin |  |
| C175 | Aliases expanded in scripts/-c by default (documented divergence; escape hatch verified) | unrated doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C176 | ascii_tree.py dead branch: elif always true, else unreachable | unrated design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C177 | sexp_renderer.py leaves quotes unescaped, producing malformed s-expressions | unrated defect | live | 4.17 | `psh/parser/visualization/dot_generator.py` (scalar else arm; DOT escaping); `_render_sexp_list` indent threading | renderer pins (escaping, indentation) |  |
| C178 | FRAMING: _trial_parse always builds an RD Parser, so under --parser combinator RD validates and… | unrated theme | n/a | 6.1 | tree-wide ratchets (error-prefix; one-extent-authority census; `TokenGroups.WORD_LIKE` derivation; duplicated literals; `chr(int(`/bare `int(` census); C178 sentence in `psh/parser/CLAUDE.md` | each ratchet run against its offender |  |
| C179 | Three executor comments describe behavior the code does not produce (textbook drift) | unrated theme | n/a → closed v0.788.0 | 1.1 | `psh/executor/context.py#ExecutionContext.for_pipeline_member` (one-shot `in_pipeline`) | ratchet — `in_pipeline` referenced only in `context.py`/`pipeline.py` (offender); `--debug-exec` shows no exec; golden rows | slot 1.1: the "nothing left to do" and setpgid comments corrected; the exec-assignment-loop drift is W1-N5 (6.2) |
| C180 | Unverified: nested ( ) in a foreground pipeline member may transfer terminal control to its own… | unrated defect | live | 3.9 | `JobManager` (tty handed to the pipeline pgid once; nested members never re-fork a pgid / `tcsetpgrp`) | PTY pin checking `tcgetpgrp` from inside the grandchild; Linux nightly watch row |  |
| C181 | -c + set -m job notice [previously registered divergence] | unrated defect | oracle_changed → closed by 0.2 (R-C181) | 0.2 | `psh/builtins/job_control.py:96-98` `command_mode` DONE filter deleted (0.2) | `test_jobs_completed_listing_modes.py` `_listed_once_c_mode` rows against the live oracle; CHANGELOG discharges the v0.692 deferral |  |
| C182 | Bare `wait` does not reach process-substitution children [previously registered divergence] | unrated defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C183 | PIPESTATUS collapse in brace groups [previously registered divergence] | unrated defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C184 | Pipeline member EXIT trap [previously registered divergence] | unrated defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C185 | TIMEFORMAT %P CPU-measure residue [previously registered divergence] | unrated defect | live | 4.13 | trap dispatcher "handler fired" record → `wait` 128+N; procsub-wait list registered at fork; PIPESTATUS re-stamp removed inside groups; pipeline member runs its own EXIT trap | each 2026-07 LEDGER successor row closes with a pin; 138-vs-158 nightly row; user-guide `:953` |  |
| C186 | %Q lenient divergence [previously registered, documented] | unrated defect | live | Park P-4 | declared divergence documented at `psh/builtins/core.py:380-390` | 6.3 verifies the both-sides pin |  |
| C187 | Async-reaper family (CR-D1: bg-job reap-timing / absent general async reaper) [previously… | unrated doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C188 | Docstrings in psh/expansion/ have grown into campaign changelogs (textbook deduction) | unrated doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C189 | [Coverage note] Areas the expansion reviewer did NOT independently verify (pattern_engine,… | unrated theme | n/a | R | Checkpoint R report (`checkpoint-r/`) — explicit coverage scope | attack rounds to zero; every ratchet re-run against its offender; Linux nightly green at the checkpoint tree |  |
| C190 | $(< file) command-substitution shortcut unsupported (documented / deferred) | unrated defect | live | Park P-3 | not a feature-parity sweep; funsub pinned as a declared divergence behind `oracle_feature('funsub')` (0.1); 6.3 verifies the doc rows | `$(< file)` may be pulled in ONLY by a written ruling after Wave 4 |  |
| C191 | NUL bytes retained in $'...' and by read/mapfile where bash drops them (read/mapfile face =… | unrated defect | live | 3.1 | `psh/utils/escapes.py#decode_ansi_c_escapes` (one digit reader; `unicode_escape_char` with surrogate/range guards) | ratchet forbidding `chr(int(` in `psh/lexer/` and `psh/utils/` outside the owner (offender); cross-entry NUL matrix |  |
| C192 | Locale/collation behavior needs a Linux oracle | unrated doc | n/a | R | Checkpoint R report (`checkpoint-r/`) — explicit coverage scope | attack rounds to zero; every ratchet re-run against its offender; Linux nightly green at the checkpoint tree |  |
| C193 | [Coverage note] Lightly-probed core areas and un-re-verified carry '1.4 locale warn wider… | unrated theme | n/a | R | Checkpoint R report (`checkpoint-r/`) — explicit coverage scope | attack rounds to zero; every ratchet re-run against its offender; Linux nightly green at the checkpoint tree |  |
| C194 | [Coverage note] executor/array.py alias-mutation write-ban gap (known Phase-4 P1, out of scope) | unrated defect | live | 1.18 | `psh/core/variable_store.py#VariableStore.promote_to_indexed`; `psh/executor/array.py#ArrayBuilder` phases (`IndexedInitializerCursor`, `VarAttributes.UNSET`) | 1.0 matrix extended with array cells + ratchet: `VarAttributes.ARRAY` construction outside the store fails (offender); `test_no_derived_variables_writes.py` extended |  |
| C195 | SHLVL never set | unrated defect | live | 4.9 | `push_temp_env_scope` (eval/source/`.` push a real `is_temp_env` scope); `_remove_export` resolves the nameref first; `SHLVL` seeded in `ShellState.__init__`; `type` under unset PATH | enumeration test (`set`, `export -p`) over the temp-env scope; 1.15 `state.env` ratchet extended to `psh/builtins/environment.py` |  |
| C196 | BASH_SOURCE unset (already documented as missing) | unrated defect | live | Park P-3 | not a feature-parity sweep; funsub pinned as a declared divergence behind `oracle_feature('funsub')` (0.1); 6.3 verifies the doc rows | `$(< file)` may be pulled in ONLY by a written ruling after Wave 4 |  |
| C197 | set -opipefail: bash lists options then parses 'pipefail' as flags; psh rejects outright (set… | unrated defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C198 | set -o table diverges from bash: missing… | unrated defect | live | 4.10 | `psh/invocation.py#_parse_cluster` consumed by invocation AND the `set` builtin; `InvocationConfig.argv0: Optional[str]` | `test_invocation_argv_guard.py` extended so `set` cannot grow a second cluster parser (offender); golden rows `-opipefail`, `-oe pipefail`, `-ox`, `set -opipefail` |  |
| C199 | Shell-identity variables $0 / BASH_SOURCE / BASH_VERSINFO / BASH_ARGV0 differ (by-design) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C200 | Several builtin usage strings do not match bash's (passing remark, no location) | unrated defect | live | 4.7 | `Builtin.parse_flags` — the only option walker (printf/times/eval/builtin/source migrated; six declaration parsers use `self.error`+`self.usage`) | `test_builtin_help_sync.py` (c): builtins scanning a leading `-` without `parse_flags` on an explicit justified allowlist (the C135 drift-lock; offender); user-guide `:976` |  |
| C201 | _declare_bare_name has a 125-line body (sprawl) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C202 | Stringly-typed option dicts in the declaration family | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C203 | Deliberate/documented builtin divergences explicitly NOT counted (ulimit -p, declare -A key… | unrated doc | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C204 | declare -f / type function pretty-print formatting differs (by-design) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C205 | "environment:" prefix on FUNCNEST diagnostic differs from bash (by-design) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C206 | exec {v}>&- with v unset silently succeeds [previously registered D-5C.2-d2] | unrated defect | live | 4.14 | `fd_from_text` (from 3.3): `{v}` unset → `NAME: ambiguous redirect` rc 1; move form closes the SOURCE in the parent | conformance pins (bash 5.3 confirmed) |  |
| C207 | n<&m- move-form fd lifetime [previously registered] | unrated defect | live | 4.14 | `fd_from_text` (from 3.3): `{v}` unset → `NAME: ambiguous redirect` rc 1; move form closes the SOURCE in the parent | conformance pins (bash 5.3 confirmed) |  |
| C208 | Write-side process-substitution nightly race [previously registered] | unrated defect | fixed | Excluded | §15 Excluded — recorded in `INVENTORY.json` with the verify note; not queued | — |  |
| C209 | SimpleCommand.args recomputed per access (36x in one visit_SimpleCommand) | unrated perf | live | 5.5 | `VarAttributes.is_*` against raw ints; PEP-562 lazy `psh/__init__.py` (+ `__main__.py` handles `--version`/`--help` before importing `Shell`); `_read_line_block` chunk accumulator; `args` snapshot per handler | `python -c "import psh"` does not import `psh.shell`; `--version` wall ≤ 40% of the Wave 0 baseline; microbench ≥ 1.6× recorded |  |
| C210 | Three visitors carry per-handler blind spots that no guard covers (coverage matrix parametrizes… | unrated theme | n/a | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C211 | Security analysis baseline labels a quoted variable as unquoted (false positive) | unrated defect | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C212 | CTRL_R (reverse-i-search) missing from vi-insert bindings | unrated defect | live | 4.20 | one canonical action table in `psh/interactive/keybindings.py`; `prompt.py#_get_cwd` (shell `HOME`, component boundary, `PROMPT_DIRTRIM`, octal 1–3 digits) | completeness test — mode-independent actions bound in every table |  |
| C213 | PROMPT_DIRTRIM unimplemented for \w | unrated defect | live | 4.20 | one canonical action table in `psh/interactive/keybindings.py`; `prompt.py#_get_cwd` (shell `HOME`, component boundary, `PROMPT_DIRTRIM`, octal 1–3 digits) | completeness test — mode-independent actions bound in every table |  |
| C214 | psh -i with piped stdin never enters the REPL (bash prompts there) | unrated defect | live | 4.23 | user-guide §17 row "no REPL with non-tty stdin (deliberate, `__main__.py:279-290`)" | No-row probe in `test_claims_have_tests.py` |  |
| C215 | Real-terminal rendering not assessed (SIGWINCH repaint, completion column listing, Ctrl-R… | unrated design | n/a | R | Checkpoint R report (`checkpoint-r/`) — explicit coverage scope | attack rounds to zero; every ratchet re-run against its offender; Linux nightly green at the checkpoint tree |  |
| C216 | Three of five shell.py function-level imports defer modules that are already loaded (no-op… | unrated design | n/a | 6.2 | coverage-instrumented deletion sweep (dead code / duplicated rules) | census tests enumerate the surviving allowlisted sites with reasons |  |
| C217 | 48% of in-scope top-level/scripting/utils lines are prose (comment/docstring density) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C218 | Richer parse-error message format than bash (by-design classification) | unrated design | n/a | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C219 | THEME: error_location_prefix() has drifted back with no guard (15 executor sites, 3 expansion… | unrated theme | n/a | 4.3 | `ShellState.error_location_prefix()` / `Builtin.report_error` (all 18 sites migrated) | `tests/unit/tooling/test_error_prefix_ratchet.py` — AST walk forbids `"psh: "` literals to stderr under executor/expansion/builtins (allowlist frozen to C205; offender) |  |
| C220 | THEME: Python stdlib semantics leaking where bash's C semantics are required (int(), chr(), IFS… | unrated theme | n/a | 6.1 | tree-wide ratchets (error-prefix; one-extent-authority census; `TokenGroups.WORD_LIKE` derivation; duplicated literals; `chr(int(`/bare `int(` census); C178 sentence in `psh/parser/CLAUDE.md` | each ratchet run against its offender |  |
| C221 | THEME: Multiple ${…}/$((…)) extent scanners that disagree | unrated theme | n/a | 3.4 | `psh/lexer/pure_helpers.py#scan_double_paren_arithmetic` (always quote-aware); `#validate_brace_expansion` sole `${` authority; `recognizers/operator.py` `((` only when balanced | ratchet forbidding `find_closing_delimiter(` with `'{'` and any second `))`-search loop in `psh/lexer/` (offender); `grep -rc track_quotes psh/lexer` = 0; 1.8 corpus differential extended; user-guide `:931` |  |
| C222 | THEME: Silent data loss from ordering/re-entry bugs (second pass contradicts a correct first… | unrated theme | n/a | 6.1 | tree-wide ratchets (error-prefix; one-extent-authority census; `TokenGroups.WORD_LIKE` derivation; duplicated literals; `chr(int(`/bare `int(` census); C178 sentence in `psh/parser/CLAUDE.md` | each ratchet run against its offender |  |
| C223 | THEME: Duplicated logic that has drifted | unrated theme | n/a | 6.1 | tree-wide ratchets (error-prefix; one-extent-authority census; `TokenGroups.WORD_LIKE` derivation; duplicated literals; `chr(int(`/bare `int(` census); C178 sentence in `psh/parser/CLAUDE.md` | each ratchet run against its offender |  |
| C224 | THEME: Documentation/comment drift (the textbook-quality tax) | unrated theme | n/a | 6.1 | tree-wide ratchets (error-prefix; one-extent-authority census; `TokenGroups.WORD_LIKE` derivation; duplicated literals; `chr(int(`/bare `int(` census); C178 sentence in `psh/parser/CLAUDE.md` | each ratchet run against its offender |  |
| C225 | cd resolved through the empty CDPATH component wrongly prints the destination | unrated defect | live | 1.4 | `psh/builtins/navigation.py#resolve_cd_target(operand, mode)`; `ShellState.__init__` PWD/OLDPWD validation (W0-N7) | D3 pins: `pwd -P`, `os.getcwd()`, a file placed after `cd ..`; `case_env` stale-PWD/bogus-OLDPWD rows in three modes |  |
| C226 | DESIGN: Enforce mutation invariants where the write happens (VariableStore/scope/array… | unrated design | live → closed v0.783.0 | 1.0 | `tests/unit/core/test_write_authority_matrix.py` (entry points × observations) | the matrix itself: cells owned by later slots ship `xfail(strict=True, reason="C0xx → slot y")` | slot 1.0: write-authority matrix `tests/unit/core/test_write_authority_matrix.py` (134 cells; strict xfails per owning slot); enforcement lands per write-site slot (1.4/1.5/1.15/1.16/1.17/1.18/2.4/4.5/4.9) |
| C227 | DESIGN: Preserve the good typed models; simplify large coordinators (_run_command,… | unrated design | live | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C228 | DESIGN: Separate algorithm explanation from development history in code comments | unrated design | live | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C229 | DESIGN: Target demonstrated costs (literal.py:190, ParseSession, LazyFileInput, mapfile), not… | unrated design | live | 5 (design input, wave charter, no slot by design) | every slot of the wave — a design input the program deliberately places at wave level, not a slot (§11 Owned findings; §16 wave 5) | wave exit criteria (§11: before/after numbers + scaling/counter pin per row) |  |
| C230 | Entry points: duplicate legacy configuration paths remain alongside the frozen invocation config | unrated design | live | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C231 | AST: no separate structural vs executable round-trip contracts (source provenance excluded from… | unrated design | live | 1.6 | `VariableExpansion.braced` (`psh/visitor/formatter_visitor.py#_needs_brace_disambiguation`, `psh/ast_nodes/words.py` `__str__`) | `tests/unit/visitor/test_executable_roundtrip.py` corpus: `--format` and `eval "$(declare -f f)"` reproduce stdout/rc |  |
| C232 | Lexer: command-position/quote-state ownership is hard to follow without historical context | unrated design | live | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C233 | Parser: two grammar implementations (RD + combinator) impose real parity maintenance | unrated design | live | 6.3 | `tests/unit/tooling/test_comment_hygiene.py` (campaign-ID ceiling that only decreases; `tmp/` pointers forbidden); `test_doc_pointers` gains the `#symbol` half | documented-by-design rows stated in `17_differences_from_bash.md` with both-sides pins; `test_doc_snippets.py` green |  |
| C234 | Builtins: behavior uneven; navigation, mapfile, numeric parsing, declaration/array interactions… | unrated design | live | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C235 | Visitors: formatting, command-head analysis, and feature counting each discard or reconstruct… | unrated design | live | 4.16 | `_render_redirects` helper on DebugASTVisitor; node-anchored `visit_CommandSubstitution`/`visit_ProcessSubstitution`; security command head via 4.2's `CommandHead.of` | `test_ast_coverage_matrix.py` parametrized over EVERY analysis visitor |  |
| C236 | Protocols: some role protocols expose concrete ShellState and subsystem implementations | unrated design | live | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C237 | Combinator parser: heterogeneous or_else returns a loose Parser instead of a typed union | unrated design | live | 6.4 | named `ShellState.__init__` phases; VariableStore completes its transaction contract or is retitled; legacy config-path census; protocol census | ratchet forbidding new `Shell`/`ShellState`-typed protocol members; no new import cycles; functions ≥ 100 lines ≤ the Wave 0 baseline |  |
| C238 | Documentation indexes carry stale claims of which appraisal/campaign is 'latest' or 'active' | unrated doc | live | 0.1 | `docs/reviews/README.md` Live-references rows (the 2026-07 campaign and the #19/#22 audits demoted to historical wording) | §13 6.3 exit criterion: the reviews index carries no stale "active" claim (re-checked at Wave 6 and Ceremony C) |  |
| C239 | LazyFileInput._read_line_block repeatedly concatenates and rescans the tail of a very long… | unrated perf | live | 5.5 | `VarAttributes.is_*` against raw ints; PEP-562 lazy `psh/__init__.py` (+ `__main__.py` handles `--version`/`--help` before importing `Shell`); `_read_line_block` chunk accumulator; `args` snapshot per handler | `python -c "import psh"` does not import `psh.shell`; `--version` wall ≤ 40% of the Wave 0 baseline; microbench ≥ 1.6× recorded |  |
| C240 | mapfile unbounded read path holds the whole input and then splits records | unrated perf | live | 5.6 | `psh/builtins/mapfile_builtin.py` incremental records on the unbounded path | peak-memory pin |  |
| C241 | Many existing oracle comments target Bash 5.2 while the measured oracle is 5.3.15 | unrated doc | live | 0.1 | provenance stamps ("verified against bash 5.2") rewritten only in files a slot touches (D12) | `tests/unit/tooling/test_oracle_version_comments.py` ratchet — frozen baseline (Part D census) that only decreases |  |
| C242 | Local gate not green at 6459f1a6: 23,985 passed, 52 failed (oracle drift to bash 5.3.15 per… | unrated theme | live | 0.3 | the 0.3 tree: `python run_tests.py --parallel --write-attestation` green, unsandboxed; `gate_attestation.json` schema 2 with `oracle.version == "5.3.15(1)-release"` | runner preflight refuses under `BASH_PATH=/bin/bash`; `tools/verify_gate_attestation.py`; three seeded gates recorded in `oracle-baseline.md` |  |
| C243 | docs/reviews index test fails on the pre-existing untracked reappraisal #23 report | unrated doc | live | 0.1 | `docs/reviews/README.md` rows for r23 and the program under "Current appraisal" | `tests/unit/tooling/test_reviews_index.py::test_every_review_file_is_indexed` |  |
| C244 | Test-suite limits: structural/setter/return-code tests miss executable round-trips, scope… | unrated theme | live | 6.5 | cross-entry-point matrix where the 1.0 matrix did not reach; written D3 audit per closed Wave 1 row | the audit itself (D3 compliance per row) |  |
| C245 | No fast automated pre-merge smoke check alongside the local gate and nightly | unrated design | live | 0.1 | D8 pre-handoff smoke (`run_tests.py --quick`, touched modules, ruff, mypy; four tails pasted in the handoff) + the CLAUDE.md Development Principles line | the integrator rejects a handoff without the four tails (no new CI by user decision) |  |
<!-- generated:part-a:end -->

## Part B — Gate triage nodes (51 rows, red at `6459f1a6`)

Category per `gate_triage.json`: FORMAT (22, bash 5.3 presentation), SEMANTIC (19, bash
5.3 semantics psh should follow), PREMISE (4, the test's bash-side premise changed), ENV
(6, sandbox / process-state artifacts). Wave 0 sub-slot per program §6: 0.1 = S-edits,
ENV skips, declared-divergence notes; 0.2 = one-site psh presentation retunes; 0.3 = status
retunes + the FLIP-PINS declared-divergence pins (owned by Wave 2 / 4.12 for the flip).
The 52nd local failure (`test_every_review_file_is_indexed` on the untracked r23 report) is
C243 in Part A, not a triage node.

<!-- generated:part-b:start -->
| id | node (red at `6459f1a6`) | triage / effort | status | owner slot | owner symbol | guard | closure |
|---|---|---|---|---|---|---|---|
| G01 | `conformance/bash/test_error_prefix_conformance.py::test_multiline_line_number_matches_bash` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/signal_handling.py` — synopsis `trap [-Plp] [[action] signal_spec ...]` + `-P` implemented (flags `lpP`) | live-oracle stderr pins (no test edit); `test_builtin_help_sync.py` green WITHOUT an allowlist entry |  |
| G02 | `conformance/bash/test_error_prefix_conformance.py::test_prefix_matches_bash_exactly[trap -x]` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/signal_handling.py` — synopsis `trap [-Plp] [[action] signal_spec ...]` + `-P` implemented (flags `lpP`) | live-oracle stderr pins (no test edit); `test_builtin_help_sync.py` green WITHOUT an allowlist entry |  |
| G03 | `conformance/bash/test_error_prefix_conformance.py::test_usage_line_stays_unprefixed_like_bash` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/signal_handling.py` — synopsis `trap [-Plp] [[action] signal_spec ...]` + `-P` implemented (flags `lpP`) | live-oracle stderr pins (no test edit); `test_builtin_help_sync.py` green WITHOUT an allowlist entry |  |
| G04 | `conformance/bash/test_trap_signal_spec_conformance.py::TestTrapPosixNumericForms::test_single_invalid_operand_usage_error` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/signal_handling.py` — synopsis `trap [-Plp] [[action] signal_spec ...]` + `-P` implemented (flags `lpP`) | live-oracle stderr pins (no test edit); `test_builtin_help_sync.py` green WITHOUT an allowlist entry |  |
| G05 | `conformance/bash/test_cmdsub_errexit_conformance.py::TestInheritErrexitShopt::test_shopt_query_states` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/shell_options.py#_print_option` width 15→20 for shopt-table prints (`set -o` / `-o` listings stay 15) | live-oracle pins; `test_shopt.py:79`, `test_shopt_set_o.py`, golden rows `:2378`/`:8910` retuned |  |
| G06 | `conformance/bash/test_locale_conformance.py::TestRangesAndGlobasciiranges::test_shopt_globasciiranges_query` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/shell_options.py#_print_option` width 15→20 for shopt-table prints (`set -o` / `-o` listings stay 15) | live-oracle pins; `test_shopt.py:79`, `test_shopt_set_o.py`, golden rows `:2378`/`:8910` retuned |  |
| G07 | `conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_after_unset_exits_1` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/shell_options.py#_print_option` width 15→20 for shopt-table prints (`set -o` / `-o` listings stay 15) | live-oracle pins; `test_shopt.py:79`, `test_shopt_set_o.py`, golden rows `:2378`/`:8910` retuned |  |
| G08 | `conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_set_exits_0` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/shell_options.py#_print_option` width 15→20 for shopt-table prints (`set -o` / `-o` listings stay 15) | live-oracle pins; `test_shopt.py:79`, `test_shopt_set_o.py`, golden rows `:2378`/`:8910` retuned |  |
| G09 | `conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_unset_exits_1` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/builtins/shell_options.py#_print_option` width 15→20 for shopt-table prints (`set -o` / `-o` listings stay 15) | live-oracle pins; `test_shopt.py:79`, `test_shopt_set_o.py`, golden rows `:2378`/`:8910` retuned |  |
| G10 | `system/test_stdin_startup_robustness.py::TestClosedFd0Startup::test_plain_with_closed_fd0` | SEMANTIC / M | RED @6459f1a6 | 0.3 | `psh/__main__.py` STDIN non-interactive branch: `sys.stdin is None` → `error creating buffered stream: Bad file descriptor`, exit 126 | `test_plain_with_closed_fd0` / `test_dash_s_with_closed_fd0` → 126; W0-N1 (`read` with fd 0 closed at start) registered → 3.8 |  |
| G11 | `behavioral/test_golden_behavior.py::test_golden[r18t2_builtins_history_write_to_stdout]` | ENV / S | RED @6459f1a6 | 0.1 | golden row gains `requires_dev_fd: true` (probe-open `/dev/stdout`) | ENV skip (D4): SKIP under a sandbox, never FAIL |  |
| G12 | `conformance/bash/test_cv_carry_characterization.py::TestExecutableSpecialFileEarlier::test_socket_earlier_bash_126_psh_runs_later` | ENV / S | RED @6459f1a6 | 0.1 | `except PermissionError: pytest.skip` around the AF_UNIX bind; docstring "5.2- and 5.3.15-verified" | ENV skip (D4) |  |
| G13 | `unit/tooling/test_shell_oracle_harness.py::test_cap_kill_reaches_a_writer_that_left_the_process_group` | ENV / S | RED @6459f1a6 | 0.1 | skip when `ps -eo pid=,ppid=` cannot spawn | ENV skip (D4) |  |
| G14 | `conformance/bash/test_variable_projection_reads_conformance.py::TestNotFoundMessageVariableTruth::test_explicit_empty_path_is_no_such_file` | SEMANTIC / M | RED @6459f1a6 | 0.3 | `psh/executor/strategies.py#format_exec_failure` — `unset_path = not state.scope_manager.lookup('PATH').is_set` (bash 5.3.15 CHANGES line 848, 5.3-alpha item p: a NULL `$PATH` is treated as `.`) | renamed `…_is_command_not_found` twins + `local PATH=` row; W0-N5 registered → 4.9 |  |
| G15 | `unit/executor/test_command_resolution_r3.py::TestEmptyPathNotFoundMessage::test_empty_path_says_no_such_file` | SEMANTIC / M | RED @6459f1a6 | 0.3 | `psh/executor/strategies.py#format_exec_failure` — `unset_path = not state.scope_manager.lookup('PATH').is_set` (bash 5.3.15 CHANGES line 848, 5.3-alpha item p: a NULL `$PATH` is treated as `.`) | renamed `…_is_command_not_found` twins + `local PATH=` row; W0-N5 registered → 4.9 |  |
| G16 | `conformance/bash/test_hash_conformance.py::TestHashBuiltin::test_dash_d_deletes` | SEMANTIC / M | RED @6459f1a6 | 0.2 | `psh/builtins/hash_builtin.py:80-83` empty-table short-circuit deleted (bash 5.3 CHANGES calls 5.2 a bug) | conformance row uses `2>/dev/null`; `test_hash_builtin.py::…_silently_succeeds` → `_reports_miss` |  |
| G17 | `conformance/bash/test_export_env_sync_conformance.py::TestExportAttributeLifecycle::test_declare_i_on_readonly_succeeds` | SEMANTIC / M | RED @6459f1a6 → closed v0.785.0 | 0.3 | declared-divergence pin (both sides, `oracle_min("5.3")`) — FLIP-PINS row owned by 2.4 | flips in 2.4 (`ScopeManager.apply_attribute` refusal) | slot 2.4 (v0.785.0): `ScopeManager.check_readonly_attribute_change` + `READONLY_LOCKED_ATTRIBUTES`; flipped to `TestReadonlyAttributeRefusal` parity pins (3 modes), unit twin `test_attrs_only_add_integer_refused`, golden `local_readonly_attrs_only_add_integer_refused` |
| G18 | `conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_export_bad_identifier_survives` | SEMANTIC / M | RED @6459f1a6 → closed v0.790.0 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.2 | flip in 2.2 (`SpecialBuiltinUsageError(1, suppressible=True)`; eval/dot boundary transparent) | slot 2.2 (v0.790.0): `special_builtin_usage_exit` / `SpecialBuiltinUsageError(1, suppressible=True)`; flipped to `TestPosixSpecialBuiltinExitParity` (three modes), golden `posixexit_*_exits` |
| G19 | `conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_readonly_bad_identifier_survives` | SEMANTIC / M | RED @6459f1a6 → closed v0.790.0 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.2 | flip in 2.2 (`SpecialBuiltinUsageError(1, suppressible=True)`; eval/dot boundary transparent) | slot 2.2 (v0.790.0): `special_builtin_usage_exit` / `SpecialBuiltinUsageError(1, suppressible=True)`; flipped to `TestPosixSpecialBuiltinExitParity` (three modes), golden `posixexit_*_exits` |
| G20 | `conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_unset_readonly_survives` | SEMANTIC / M | RED @6459f1a6 → closed v0.790.0 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.2 | flip in 2.2 (`SpecialBuiltinUsageError(1, suppressible=True)`; eval/dot boundary transparent) | slot 2.2 (v0.790.0): `special_builtin_usage_exit` / `SpecialBuiltinUsageError(1, suppressible=True)`; flipped to `TestPosixSpecialBuiltinExitParity` (three modes), golden `posixexit_*_exits` |
| G21 | `conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSuppressibleExit::test_eval_boundary_not_suppressed` | SEMANTIC / M | RED @6459f1a6 → closed v0.790.0 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.2 | flip in 2.2 (`SpecialBuiltinUsageError(1, suppressible=True)`; eval/dot boundary transparent) | slot 2.2 (v0.790.0): `special_builtin_usage_exit` / `SpecialBuiltinUsageError(1, suppressible=True)`; flipped to `TestPosixSpecialBuiltinExitParity` (three modes), golden `posixexit_*_exits` |
| G22 | `conformance/bash/test_identifier_policy_conformance.py::TestPosixRestrictsUnicodeLikeBash::test_declare_export_read_report_and_continue` | SEMANTIC / M | RED @6459f1a6 → closed v0.790.0 | 0.3 | test split: `declare é`/`read é` stay report-and-continue; `export é=1` half becomes a declared-divergence pin — FLIP-PINS row owned by 2.2 | flip in 2.2 | slot 2.2 (v0.790.0): `special_builtin_usage_exit` / `SpecialBuiltinUsageError(1, suppressible=True)`; flipped to `TestPosixSpecialBuiltinExitParity` (three modes), golden `posixexit_*_exits` |
| G23 | `conformance/bash/test_identifier_policy_conformance.py::TestPosixRestrictsUnicodeLikeBash::test_for_and_function_rejected_by_both` | PREMISE / M | RED @6459f1a6 | 0.2 | `psh/executor/function.py:47-55` posix-mode function-name rejection deleted; `function_support.py` stops pre-validating `-f` operands | split test: keep `for é`; new `test_function_names_unrestricted_in_posix`; user-guide `17_differences_from_bash.md:504-540` narrowed |  |
| G24 | `integration/job_control/test_pipeline_signal_death.py::TestPipelineLastMemberSignalDeath::test_sigterm_last_member_announced` | FORMAT / S | RED @6459f1a6 | 0.1 | S path: `bash.stderr == strsignal(SIGTERM).ljust(27) + '<job text>\n'` with psh's bare-form pin kept; docstrings + `psh/executor/job_control.py:626-628` reworded | FLIP-PINS row owned by 4.12 (C065 job text) for the L path |  |
| G25 | `integration/job_control/test_pipeline_signal_death.py::TestPipelineLastMemberSignalDeath::test_sigterm_no_trailing_command` | FORMAT / S | RED @6459f1a6 | 0.1 | S path: `bash.stderr == strsignal(SIGTERM).ljust(27) + '<job text>\n'` with psh's bare-form pin kept; docstrings + `psh/executor/job_control.py:626-628` reworded | FLIP-PINS row owned by 4.12 (C065 job text) for the L path |  |
| G26 | `integration/job_control/test_pipeline_signal_death.py::TestPipelineNonLastMemberSignalDeath::test_pipefail_announces_status_determining_member` | FORMAT / S | RED @6459f1a6 | 0.1 | S path: `bash.stderr == strsignal(SIGTERM).ljust(27) + '<job text>\n'` with psh's bare-form pin kept; docstrings + `psh/executor/job_control.py:626-628` reworded | FLIP-PINS row owned by 4.12 (C065 job text) for the L path |  |
| G27 | `integration/job_control/test_signal_killed_diagnostic.py::TestAbnormalTerminationDiagnostic::test_reported_in_explicit_subshell` | FORMAT / S | RED @6459f1a6 | 0.1 | S path: `bash.stderr == strsignal(SIGTERM).ljust(27) + '<job text>\n'` with psh's bare-form pin kept; docstrings + `psh/executor/job_control.py:626-628` reworded | FLIP-PINS row owned by 4.12 (C065 job text) for the L path |  |
| G28 | `integration/job_control/test_signal_killed_diagnostic.py::TestAbnormalTerminationDiagnostic::test_sigterm_prints_bare_signal_description` | FORMAT / S | RED @6459f1a6 | 0.1 | S path: `bash.stderr == strsignal(SIGTERM).ljust(27) + '<job text>\n'` with psh's bare-form pin kept; docstrings + `psh/executor/job_control.py:626-628` reworded | FLIP-PINS row owned by 4.12 (C065 job text) for the L path |  |
| G29 | `integration/job_control/test_bg_resume_refreshes_state.py::test_bg_actually_resumes_a_job_stopped_behind_the_shells_back` | ENV / S | RED @6459f1a6 | 0.1 | skip when `ps -o stat= -p $$` is empty | ENV skip (D4) |  |
| G30 | `conformance/bash/test_exit_cd_options_conformance.py::TestCdOptions::test_cd_too_many_arguments` | SEMANTIC / M | RED @6459f1a6 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.3 | flip in 2.3 (`special_builtin_usage_discard` → rc 2 on the next line); W0-N4 golden `bcontract_exit_bad_first_operand_exits_two` |  |
| G31 | `conformance/bash/test_exit_cd_options_conformance.py::TestExitStatus::test_exit_too_many_args_does_not_exit` | SEMANTIC / M | RED @6459f1a6 | 0.3 | declared-divergence pins (both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.3 | flip in 2.3 (`special_builtin_usage_discard` → rc 2 on the next line); W0-N4 golden `bcontract_exit_bad_first_operand_exits_two` |  |
| G32 | `conformance/bash/test_exit_trap_status_precedence_conformance.py::test_bare_exit_in_a_signal_trap_still_uses_current_status` | SEMANTIC / M | RED @6459f1a6 → closed v0.782.0 | 0.3 | declared-divergence pins (4 nodes incl. the must-hold; both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.1 | flip in 2.1 (trap entry status, POSIX interp 1602 / bash 5.3 NEWS uu) | slot 2.1 (v0.782.0): `TrapManager` frames + `bare_status_entry_value`; flipped to `test_trap_entry_status_matches_bash[...]` / `test_bare_exit_in_a_signal_trap_uses_entry_status[...]`; verify r1 BOUNCE → r2 PASS |
| G33 | `conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-command]` | SEMANTIC / M | RED @6459f1a6 → closed v0.782.0 | 0.3 | declared-divergence pins (4 nodes incl. the must-hold; both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.1 | flip in 2.1 (trap entry status, POSIX interp 1602 / bash 5.3 NEWS uu) | slot 2.1 (v0.782.0): `TrapManager` frames + `bare_status_entry_value`; flipped to `test_trap_entry_status_matches_bash[...]` / `test_bare_exit_in_a_signal_trap_uses_entry_status[...]`; verify r1 BOUNCE → r2 PASS |
| G34 | `conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-script]` | SEMANTIC / M | RED @6459f1a6 → closed v0.782.0 | 0.3 | declared-divergence pins (4 nodes incl. the must-hold; both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.1 | flip in 2.1 (trap entry status, POSIX interp 1602 / bash 5.3 NEWS uu) | slot 2.1 (v0.782.0): `TrapManager` frames + `bare_status_entry_value`; flipped to `test_trap_entry_status_matches_bash[...]` / `test_bare_exit_in_a_signal_trap_uses_entry_status[...]`; verify r1 BOUNCE → r2 PASS |
| G35 | `conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-stdin]` | SEMANTIC / M | RED @6459f1a6 → closed v0.782.0 | 0.3 | declared-divergence pins (4 nodes incl. the must-hold; both sides, `oracle_min("5.3")`) — FLIP-PINS rows owned by 2.1 | flip in 2.1 (trap entry status, POSIX interp 1602 / bash 5.3 NEWS uu) | slot 2.1 (v0.782.0): `TrapManager` frames + `bare_status_entry_value`; flipped to `test_trap_entry_status_matches_bash[...]` / `test_bare_exit_in_a_signal_trap_uses_entry_status[...]`; verify r1 BOUNCE → r2 PASS |
| G36 | `conformance/bash/test_bash_compatibility.py::TestBashJobControl::test_job_control_commands` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G37 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_done_label_script` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G38 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_listed_once_script` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G39 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_listed_once_stdin` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G40 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_suppressed_c_mode` | SEMANTIC / M | RED @6459f1a6 | 0.2 | `psh/builtins/job_control.py:96-98` `command_mode` DONE filter deleted (C181 closed); tests renamed `_listed_once_c_mode` | CHANGELOG discharges the v0.692 deferral; module docstring rewritten |  |
| G41 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_listed_once_script` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G42 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_listed_once_stdin` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G43 | `integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_suppressed_c_mode` | SEMANTIC / M | RED @6459f1a6 | 0.2 | `psh/builtins/job_control.py:96-98` `command_mode` DONE filter deleted (C181 closed); tests renamed `_listed_once_c_mode` | CHANGELOG discharges the v0.692 deferral; module docstring rewritten |  |
| G44 | `integration/job_control/test_jobs_completed_listing_modes.py::test_jobs_n_completion_listed_once_script` | FORMAT / M | RED @6459f1a6 | 0.2 | `psh/executor/job_control.py:279/:280/:683` status column `:<24` → `:<27` | live-oracle pins (no test edit) |  |
| G45 | `conformance/bash/test_bad_substitution_conformance.py::TestBadSubstitutionRejected::test_rejected_cases_match_bash` | SEMANTIC / L | RED @6459f1a6 | 0.1 | `${ }` / `${ :-x}` dropped from BAD_CASES; funsub declared-divergence pin guarded by `oracle_feature('funsub')` | Park P-3 (no flip in this program); FLIP-PINS notes the row |  |
| G46 | `conformance/bash/test_subscript_keying_conformance.py::test_divergence_procsub_compound_render_residual` | FORMAT / S | RED @6459f1a6 | 0.1 | PREMISE S-edit: 4th tuple → bash 5.3's `'<(case x in y)\n        echo n\n    ;;\nesac)'`; trailing-space docstring phrase dropped | pin retuned in place |  |
| G47 | `conformance/bash/test_subscript_keying_conformance.py::test_divergence_sq_in_dq_readback_outcome` | PREMISE / S | RED @6459f1a6 | 0.1 | PREMISE: parity pin renamed `test_sq_in_dq_readback_round_trips` (CHANGES line 1382 is 5.2-alpha item k, "subscripts expanded only once" — cited as a 5.2 item; the read-back change between 5.2.26 and 5.3.15 is empirical) | oracle-side closure recorded in FLIP-PINS (no flip) |  |
| G48 | `conformance/bash/test_subscript_keying_conformance.py::test_unlexable_subscript_route_audit[let_arith]` | PREMISE / S | RED @6459f1a6 | 0.1 | PREMISE S-edit: bash branch → rc 0 / `declare -A a` / `not a valid identifier` | pin retuned in place |  |
| G49 | `conformance/bash/test_subscript_keying_conformance.py::TestCompositeQuoting::test_tilde_expands_in_key` | ENV / S | RED @6459f1a6 | 0.1 | ENV: `env={'HOME': '/probe-home'}` with the D14 comment (Homebrew 5.3.15 bottle resolves `~` from the startup environment) | pin retuned in place (D14) |  |
| G50 | `conformance/bash/test_syntax_template_timing_conformance.py::test_redirect_procsub_suppression_is_a_declared_divergence` | ENV / S | RED @6459f1a6 | 0.1 | ENV: write-row poll lengthened 10×0.1 s → 30×0.1 s | pin retuned in place |  |
| G51 | `conformance/bash/test_typed_expansion_errors_conformance.py::TestDeclaredDivergences::test_invalid_regex_diagnostic_is_psh_only` | PREMISE / S | RED @6459f1a6 | 0.1 | PREMISE: both-diagnose wording pin (`invalid regular expression` in bash, `invalid regex` in psh, rc 2 both) | W0-N3 (`[[ x =~ a{1 ]]` rc 1 vs 2) registered → 4.6 |  |
<!-- generated:part-b:end -->

## Part C — Side findings registered at Wave 0 (D9 N-rows)

Registered on the day they were found (2026-09-06, gate triage / 0.3 probe matrix, then the
Wave 0 package handoffs, verifier reports and the compare-bash ceremony leg, W0-N11..N37); text per program §6 0.3 and §8 2.3. Each has an owning slot named in that slot's heading
(W0-N1 → 3.8, N2 → 4.12, N3 → 4.6, N5 → 4.9, N6 → 4.7) or brief body (N4, N8, N9, N10 →
2.3; N7 → 1.4). Status: OPEN unless a closure is named.

| id | title | severity / class | status | owner slot | owner symbol | guard | closure |
|---|---|---|---|---|---|---|---|
| W0-N1 | `read` with fd 0 closed at process START (`sys.stdin is None`; `psh -c 'read x' <&-`, `psh s.sh <&-`) escapes under strict-errors as `AttributeError: 'NoneType' object has no attribute 'fileno'` at `psh/builtins/input_reader.py:429`, chained into `ValueError: InputCursor needs exactly one of fd or stream` at `:124` (strict off: `read: InputCursor needs exactly one of fd or stream` rc 1); bash 5.3.15 `read: 0: read error: Bad file descriptor` rc 1 and continues; the per-command `read x <&-` form already returns rc 1 (psh wording `read: read error: 0: …`, bash `read: 0: read error: …`) — package F re-verified 71d03038 | P1 / internal defect (crash under strict-errors) + wording | OPEN (registered 0.3 triage) | 3.8 | `psh/builtins/input_reader.py#InputCursor` + the `read` stream selection handle `sys.stdin is None` | pins in `-c` and script modes with fd 0 closed on the psh process; strict-errors pin: no traceback; must NOT become the 0.3 exit-126 path (bash runs `-c` normally with fd 0 closed) |  |
| W0-N2 | A foreground external command consumes a job number (the next `&` job is numbered one higher than bash's) | P2 / conformance (jobs) | OPEN (registered 0.3 triage) | 4.12 | `JobManager.create_job` does not burn a job number for foreground commands | jobs-numbering pin beside the 4.12 job-text pins |  |
| W0-N3 | `[[ x =~ a{1 ]]`: psh silent rc 1 vs bash 5.3.15 `invalid regular expression … braces ({ }) not balanced` rc 2 | P2 / conformance (diagnostic + status) | OPEN (registered 0.3 triage) | 4.6 | `psh/expansion/enhanced_test_evaluator.py` compile diagnostic through `error_location_prefix`, rc 2 | conformance pin (both shells rc 2, wording per side) |  |
| W0-N4 | golden `bcontract_exit_bad_first_operand_exits_two`: bash 5.3.15 `exit abc; echo rc=$?` → `rc=2` and CONTINUES (5.2 exited 2); psh exits | P2 / bash-5.3 semantic (usage status) | OPEN (declared-divergence golden pin at 0.3, `min_bash: "5.3"`) | 2.3 | `psh/core/internal_errors.py#special_builtin_usage_discard` (numeric-argument cells continue on the same line with rc 2 in every mode) | FLIP-PINS row; the golden row flips to equality in 2.3 |  |
| W0-N5 | Under an unset (or `local`) PATH a cwd hit is reported the wrong way round: `unset PATH; type cwdcmd` — bash 5.3.15 prints the ABSOLUTE path, psh prints `./cwdcmd` (same for `command -v`, `type -P/-p`; `hash` table bash `cwdcmd` vs psh `./cwdcmd`); the hit RUNS with `$0`=`./hit` in psh vs `hit` in bash; `unset PATH; exec nosuchcmd` bash `/abs/cwd/nosuchcmd: No such file or directory` vs psh `exec: nosuchcmd: not found` (carry #24 wording). `PATH= type cwdcmd` is parity (`./cwdcmd`). Package F, verified round 2 | P3 / conformance (lookup truth) | OPEN (registered 0.3, PATH-wording retune) | 4.9 | `type` under unset PATH prints bash's form (4.9 lookup-truth residue) | conformance pin beside the 4.9 enumeration test |  |
| W0-N6 | `declare -c` rejected as an invalid option (bash 5.3.15 accepts it) | P3 / conformance (option walker) | OPEN (registered 0.3 triage) | 4.7 | `Builtin.parse_flags` (declare accepts `-c` or declares it explicitly) | `test_builtin_help_sync.py` (c) allowlist drift-lock |  |
| W0-N7 | Startup imports an inherited `PWD` unvalidated and `OLDPWD` unchecked (`psh/core/state.py:227-228` seeds `PWD` only when absent): `env PWD=/nonexistent/zz psh -c 'echo $PWD'` prints `/nonexistent/zz` where bash prints the real cwd; a bogus `OLDPWD` survives to `cd -`; a stale `PWD` yields a non-canonical `cd ..` path — masked by design in `tests/harness/shell_oracle.py:297` | P1 / wrong-target (cwd) | OPEN (registered 0.3; §17 user-guide row until closed) | 1.4 | `ShellState.__init__` seeds `PWD` from the inherited value only if it names the cwd (same dev/ino as `os.getcwd()`), else `os.getcwd()`; drops an `OLDPWD` that is not a directory | three-mode pins with `case_env={'PWD': stale}` / `{'OLDPWD': bogus}` asserting `$PWD`, `pwd`, `pwd -P`, the `cd -` target and a file placed after `cd ..` (D3); `shell_oracle.py:297` points at the pin |  |
| W0-N8 | `shift abc` → psh rc 1 (bash 5.3.15 rc 2, continues on the same line) | P3 / bash-5.3 semantic (usage status) | OPEN (registered for the 2.3 brief) | 2.3 | `psh/core/internal_errors.py#special_builtin_usage_discard` | 2.3 probe matrix pins (three modes) |  |
| W0-N9 | script-mode `shift 1 2` / `return 1 2` / `exit 7 8` → psh rc 1 on the next line (bash 5.3.15 rc 2; rest of the input line dropped; `-c` exits 1 in both) | P3 / bash-5.3 semantic (usage status) | OPEN (registered for the 2.3 brief) | 2.3 | `psh/core/internal_errors.py#special_builtin_usage_discard` (`TopLevelAbort(2, errexit_immune=True)` outside command_mode) | 2.3 probe matrix pins (three modes; `-c` pins stay) |  |
| W0-N10 | `break abc` / `continue abc` inside a loop → psh exits 128 where bash 5.3.15 EXITS the shell with rc 2 in EVERY input mode (script/stdin/-c, even under `|| echo caught`) — package G + its verifier corrected the program's earlier 'drops the line' wording | P3 / bash-5.3 semantic (usage status) | OPEN (registered for the 2.3 brief) | 2.3 | `psh/core/internal_errors.py#special_builtin_usage_discard` (bad-count `break`/`continue` drop the line; `-c` exits 2) | 2.3 probe matrix pins (three modes) |  |

| W0-N11 | `tests/integration/control_flow/test_for_loops.py::test_for_loop_with_glob` touches `file1-3.txt` in the bare `shell` fixture cwd (= the repo root under `run_tests.py`) and relies on `rm -f *.txt`; an EPERM flake leaves junk (the C169 shape) | P3 / test hygiene | OPEN (found by package C, confirmed by its verifier) | 6.5 | test uses `isolated_shell_with_temp_dir` | `test_no_junk_in_repo_root` census (6.5 test-shape audit) |  |
| W0-N12 | `tests/unit/builtins/test_directory_stack.py::test_popd_invalid_index` leaves an empty `test_invalid` directory every run (its `finally` reads the cwd AFTER the pushd, so the rmdir never runs) | P3 / test hygiene | OPEN (found by package C's verifier) | 6.5 | compute `original` before the pushd; `isolated_shell_with_temp_dir` | same census as W0-N11 |  |
| W0-N13 | `unset PATH; command -p nosuchcmd` (and `local PATH`) said "No such file or directory" where bash 5.3.15 says "command not found" (the default `CS_PATH` list WAS searched) | P3 / conformance (PATH-miss wording) | CLOSED by 0.3 (package F bounce B2, 71d03038: `search_override` threaded into `report_exec_failure`; 11 pins in three modes + golden row) | 0.3 | `psh/executor/strategies.py#report_exec_failure(search_override=)` | conformance rows `test_unset_path_command_p_is_command_not_found` / `_local_…` | release v0.780.0 |
| W0-N14 | No default PATH is seeded when `PATH` is absent from the environment at startup: `env -u PATH psh -c 'echo "[${PATH-unset}]"; ls'` → `[unset]`, `ls: No such file or directory` rc 127; bash 5.3.15 seeds `/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin:/bin:/sbin:.` and runs `ls` | P2 / startup env seeding | OPEN (package F verifier N-a) | 1.4 | `psh/core/state.py#ShellState.__init__` startup-variable seeding (with W0-N7) | three-mode pins with `case_env` lacking PATH |  |
| W0-N15 | fd 0 open WRITE-ONLY at startup: bash 5.3.15 `script file read error: Bad file descriptor` rc 128 (rc 1 under `-e`); psh exits 0 silently for plain/`-s`/`--`/flag forms, and `--validate`/`--lint` traceback `OSError: [Errno 9]` from `_read_all_stdin` | P2 / startup fd state (same family as the 0.3 closed-fd guard) | OPEN (package F verifier N-b) | 3.8 | `psh/__main__.py` stdin-source selection + `InputCursor` boundary | system rows with `stdin=os.open(devnull, O_WRONLY)` |  |
| W0-N16 | fd 0 = a DIRECTORY at startup: CPython aborts before psh runs (`Fatal Python error: init_sys_streams: <stdin> is a directory`, rc 1) for EVERY invocation incl. `-c`/script; bash runs `-c`/script normally and reports `script file read error: Is a directory` rc 128 for stdin forms | P3 / interpreter limitation | OPEN — ruling needed: document as a known limitation in §17 or handle in a launcher | 6.3 | user guide §17 row (documented-by-design) unless a launcher shim is ruled | `NO_ROW_PROBES` entry |  |
| W0-N17 | `type -P nosuchcmd` prints `type: nosuchcmd: not found` where bash 5.3.15 is silent (rc 1 both); independent of PATH state | P3 / conformance (lookup truth) | OPEN (package F verifier N-d) | 4.9 | `type` builtin `-P` miss path | conformance row |  |
| W0-N18 | `env` is a psh BUILTIN (`type env` → "env is a shell builtin"; bash `/usr/bin/env`), so `unset PATH; env` succeeds rc 0 where bash reports `env: No such file or directory` rc 127 | P3 / documented-difference check | OPEN — ruling needed (deliberate builtin? then a §17 row with a `NO_ROW_PROBES` probe) | 6.3 | user guide §17 | `NO_ROW_PROBES` entry |  |
| W0-N19 | `shopt -s lastpipe` is unsupported (`invalid shell option name`); slot 1.1's pin list names `shopt -s lastpipe`, so 1.1 must implement or declare it before pinning | P3 / feature gap surfaced by the PATH battery | CLOSED v0.788.0 — DECLARED: premise pinned by `test_psh_has_no_lastpipe_option` (slot 1.1); implementing `lastpipe` is a Wave 4 options item | 1.1 | `psh/builtins/shell_options.py` shopt table (+ pipeline last-member exec policy) | 1.1 pins |  |
| W0-N20 | bash 5.3.15 STILL identifier-checks `declare -f é` / `-F é` / `typeset -f é` under `set -o posix` (rc 1 `not a valid identifier`, even when é is defined) while psh prints the definition rc 0; the 0.2 brief's clause about removing `-f` operand pre-validation in `function_support.py` was wrong about the tree (no such code) | P3 / conformance (declaration family) | OPEN — pinned both sides by package E (`test_declare_f_operand_check_is_a_declared_divergence`) and documented in guide §17 | 4.7 | `DeclareBuiltin` `-f`/`-F` operand validation in posix mode | the E pin flips to parity |  |
| W0-N21 | posix-mode slash names: bash 5.3.15 DEFINES `a/b() { …; }` but never looks it up (CHANGES 5.3-beta ww: `a/b` → `No such file or directory` 127); psh defines AND runs it | P3 / conformance (function lookup) | OPEN (package E side finding b) | 4.7 | function lookup rejects slash-bearing names under posix | conformance row |  |
| W0-N22 | `readonly -f é` on an undefined name: bash `readonly: é: not a function`, psh `readonly: é: not found` (`export -f` already says "not a function") | P3 / diagnostic wording | OPEN (package E side finding c) | 4.7 | declaration-family `-f` miss message | conformance row |  |
| W0-N23 | Async completion notice for the PREVIOUS job: bash 5.3.15 marks it `[1]-  Done`, psh prints `[1]   Done` (`psh/executor/job_control.py` comment pins "never '-'" against 5.2.26) — PTY-observed | P3 / conformance (job notice text) | OPEN (package E side finding d) | 4.12 | `psh/executor/job_control.py#job_command_text` / notice marker | PTY leg |  |
| W0-N24 | In-process capture artefact: after a DEBUG trap fires for the query command, `captured_shell` misses the builtin's own stdout (`trap 'echo a' DEBUG; trap -P DEBUG` captured only `a`); the subprocess output matches bash — possible trap-runner stream-restore bug | P3 / internal (test harness or trap runner) | OPEN — investigate (package E side finding e) | 3.7 | trap dispatcher stream save/restore | fault-injection pin if a real bug |  |
| W0-N25 | posix `unset -f` on a READONLY function: bash 5.3.15 exits the non-interactive posix shell, psh continues (already pinned both sides by package G as `test_unset_readonly_function_exits_in_posix`) | P3 / bash-5.3 semantic (special-builtin exit) | CLOSED v0.790.0 (slot 2.2) | 2.2 | `UnsetBuiltin` readonly-function branch raises `SpecialBuiltinUsageError(1, suppressible=True)` | the G pin flips |  |
| W0-N26 | `wait %1` after `kill -STOP` + `kill -9` under `set -m`: bash 5.3.15 returns 145/146, psh 1 | P3 / job bookkeeping | OPEN (package E verifier) | 4.13 | `wait` status derivation from the job's last known state | integration row |  |
| W0-N27 | `$(jobs)` shows the parent's jobs in bash, nothing in psh; `jobs \| cat` lists a stale `Running` in psh's child | P3 / job table in substitution children | OPEN (package E verifier) | 4.12 | job-table snapshot handed to substitution/pipeline children | integration row |  |
| W0-N28 | `export -f` never reaches a child bash from psh for ANY function (no `BASH_FUNC_name%%` environment export) | P2 / feature gap — ruling needed (implement in 4.9's lookup-truth residue, or a §17 documented difference with a `NO_ROW_PROBES` probe) | OPEN (package E verifier) | 6.3 | user guide §17 unless ruled into 4.9 | `NO_ROW_PROBES` entry |  |
| W0-N29 | `shopt -o interactive-comments` is unsupported | P3 / option table | OPEN (package E verifier) | 4.10 | `set -o` table (4.10 names `interactive-comments`) | 4.10 golden rows |  |
| W0-N30 | `exit 99999999999999999999`: bash 5.3.15 `numeric argument required` rc 2 and CONTINUES; psh SILENTLY exits 255 | P2 / usage status (silent wrong exit) | OPEN (package G verifier) | 2.3 | `legal_number`-style operand validation in `exit` (int64 range) | 2.3 probe-matrix pin |  |
| W0-N31 | W0-N9 sub-row: script/stdin with NO line after an abandoned `exit 1 2` inside a function (`f(){ exit 1 2; echo in=$?; }; f; echo out=$?` as the whole input) → bash exits rc 2, psh rc 1 (the `-c` parity control does not carry) | P3 / usage status | OPEN (package G verifier N1) | 2.3 | as W0-N9 | both-sides pin recommended |  |
| W0-N32 | 2.2 cells from the G verifier: `unset -v 1bad` bash exits 1 `not a valid identifier` / psh silent rc 0 (bare `unset 1bad` is a silent no-op in both); `export -n 1bad` exits; `export ok=1 1bad=x` exits at the first bad operand; unguarded export / eval-export inside a function exits, guarded → caught in both | P3 / bash-5.3 semantic (special-builtin exit) | CLOSED v0.790.0 (slot 2.2) | 2.2 | as the 2.2 brief | 2.2 rows |  |
| W0-N33 | 2.4 cells from the G verifier: bash 5.3.15 also refuses `-u`/`-n`/`typeset -i`/`local -l`/`-n` on a readonly, refuses `declare -i` on an already `-ir` variable, refuses a mixed `-ix` WHOLE (no `-x` applied) and continues to later operands; `declare -a/-A` on a readonly scalar rc 1 in both; bash quirk `local -a x` on a readonly local (rc 1 yet becomes `declare -ar x=()`) is NOT to be adopted | P3 / bash-5.3 semantic (attribute refusal) | OPEN | 2.4 | `ScopeManager.apply_attribute` refusal set | 2.4 rows |  |
| W0-N34 | Family-1 novel cells from the G verifier: `while`-body / `\|\|` / `&&`+brace resolve `exit` from the ENTRY status (bash 0 / psh 1); an external child exiting 9 before `exit` → bash 9 / psh 0; a function body with entry 5 → both 0 | P3 / trap entry status | OPEN | 2.1 | `TrapManager` entry-status top-level boundary | 2.1 rows |  |
| W0-N35 | `cd ""` was a no-op success (5.2) — bash 5.3.15 says `cd: null directory` rc 1 before any CDPATH search; surfaced as the unowned compare-bash red golden `r18t2_glue_cd_empty_operand_noop` by the package G verifier | P3 / conformance (cd) | CLOSED by 0.3 (integrator b8332a3a: `navigation.py` rejects the empty operand; golden row retuned to `…_null_directory` with `min_bash: "5.3"`; `TestCdEmptyOperand` re-pinned; conformance row) | 0.3 | `psh/builtins/navigation.py` empty-operand branch | conformance `TestCdOptions::test_cd_empty_operand_is_null_directory` | release v0.780.0 |

| W0-N36 | bash 5.3 no longer propagates `export X=2` inside a function back over the call-time temporary `X` (`X=outer; f() { export X=2; }; X=1 f; declare -p X` → bash `declare -- X="outer"`, psh `declare -x X="2"`; CHANGES 5.3-alpha jjjj) — surfaced by the Wave 0 compare-bash leg (golden row now `psh_only`) | P3 / bash-5.3 semantic (temporary environment) | OPEN (FLIP-PINS row) | 4.9 | temp-env scope teardown vs `export` (`psh/core/scope.py` temp-env layer) | golden row flips to parity |  |
| W0-N37 | `read` with an unescaped backslash before EOF: bash 5.3 drops the trailing IFS whitespace / delimiter (`printf 'a \\' \| read x` → `[a]`; `[a][b]`; `[a][b]`), psh keeps the 5.2 shape (`[a ]`, `[a][b ]`, `[a][b:]`; CHANGES 5.3-alpha uuuu) — Wave 0 compare-bash leg (three golden rows now `psh_only`) | P3 / bash-5.3 semantic (read builtin) | OPEN (FLIP-PINS row) | 1.19 | `psh/builtins/input_reader.py` backslash-EOF handling + IFS trailing-delimiter rule | golden rows flip to parity |  |
| W1-N6 | Undeliverable diagnostic lost when fd 2 is closed for the WHOLE compound (`{ cd /nonexistent_zz; } 2>&-; echo end >&2`): bash's stdio buffer flushes `cd`'s message after the shell's own restore, psh writes diagnostics straight to the fd and drops the undeliverable one; status and restore identical (found by slot 1.2, 2026-09-07) | P3 / declared divergence (diagnostic delivery) | DECLARED — user guide §17 "buffered diagnostics across a stderr close+reopen" widened to the per-command spelling in v0.781.0; no flip planned | — (declared) | `psh/io_redirect/manager.py` (per-command region restore) | `tests/integration/redirection/test_close_then_reopen_c032.py::test_undeliverable_diagnostic_is_lost_declared_divergence` | FLIP-PINS "Declared, NOT flipped" row |
| W1-N1 | Bare `exit` (and the ERR shape) inside a COMMAND SUBSTITUTION or a PROCESS SUBSTITUTION `<( )` within a trap action takes the trap's ENTRY status in bash 5.3.15 (`trap 'x=$(false; exit); echo c=$?' EXIT; exit 3` → `c=3`; USR1/ERR shapes `c=0`/`c=9`), current `$?` in psh (`c=1`); a forked SIMPLE `exit` (`exit &`) already agrees — pre-existing at base; outside the interp-1602 wording (found by slot 2.1's verifier, 2026-09-07) | P3 / bash-5.3 semantic (trap status) | OPEN | 2.1-r (rider after v0.782.0: copy the parent's frame list into the substitution child via an owner method at the comsub/procsub body in `psh/expansion/command_sub.py` / `psh/io_redirect/process_sub.py`; NOT in `Shell.for_subshell`, which `( )`/env children share and which must not inherit) | `TrapManager` frame stack | three-mode pins for comsub, backtick, procsub, ERR shape | |
| W1-N22 | A backgrounded pipeline loses a BUILTIN member's status: `true; false \| exit 6 & wait $!; echo c=$?` → bash 5.3.15 `c=6`, psh `c=0` (PIPESTATUS too; no trap needed; identical at base) — found by slot 2.1's round-2 verifier | P2 / wrong status (silent) | OPEN | 1.1-r (rider after slot 1.1 merges; `psh/executor/pipeline.py` background member status collection) | `pipeline.py` background job status | three-mode pins incl. `PIPESTATUS` | |
| W1-N23 | A pipeline COMPOUND member (`{ … }` / loop / `if`) keeps the parent's signal and ERR traps where bash resets non-ignored traps on entry to the forked member; `{ } &` and `( )` already reset — pre-existing at base (slot 2.1's round-2 verifier) | P3 / conformance (trap inheritance) | OPEN | 2.1-r (same rider: call `enter_subshell_trap_environment()` for compound pipeline members in `pipeline.py`, kept separate from the entry-status frame drop) | `TrapManager.enter_subshell_trap_environment` | three-mode pins: signal + ERR trap visible/invisible inside a `{ }` member | |
| W1-N24 | psh lets `trap` install a handler for a signal that was IGNORED at shell entry (bash refuses: an inherited SIG_IGN signal cannot be trapped in a non-interactive shell) — pre-existing (slot 2.1's round-2 verifier) | P3 / conformance (signals) | OPEN | 4.13 | `psh/core/trap_manager.py` entry-ignored set | pin: `(trap '' USR1; exec psh -c 'trap "echo got" USR1; kill -USR1 $$; sleep 0.2; echo end')` → bash prints only `end`; Linux nightly watch | |
| W1-N2 | `a=(1 2); declare a[1]=q` → bash rc 0, `[1]="q"`; psh rc 1 ``declare: `a[1]=q': not a valid identifier``, array unchanged (slot 1.0, 2026-09-07) | P3 / conformance (declaration builtin subscript) | OPEN | 1.18 | `psh/builtins/function_support.py` declare operand parsing → store element write | matrix cell `W1-N2 → slot 1.18` | |
| W1-N3 | `readonly a; getopts x a[0]` → bash `` `a[0]': not a valid identifier`` (name check first), psh `getopts: a: readonly variable`; rc 1 and untouched array in both — wording only (slot 1.0) | P4 / diagnostic wording | OPEN | 4.5 | `psh/builtins/getopts` operand validation order | cell asserts rc + untouched array, not the wording | |
| W1-N7 | `for` over a NAMEREF control variable: bash rebinds the reference per word (`x=0; y=0; declare -n r=x; for r in x y; do r=5; done` → x=5 y=5; `for r in 1 2` refused rc 1, body not run); psh writes every word INTO the target and runs the body (slot 1.0 verifier r1) | P2 / wrong target (silent) | OPEN | 1.18 | for-loop control-variable write → `VariableStore` nameref rule | matrix cells `W1-N7 → slot 1.18` (3 + spawn) | |
| W1-N8 | Identifier validation bypassed at three write sites: `getopts a bad-name`/`a.b`/`1x`/`""` create the name (bash rc 1); `getopts a 'a[0]'` writes an element bash refuses; `printf -v 'bad-name' x` creates it (bash rc 2); `printf -v 'a[]'` writes `[0]` (bash refuses); `declare -n r; r=bad-name` binds (bash rc 1) — all silent rc 0 in psh (slot 1.0 verifier r1) | P2 / wrong target (silent) | OPEN | 1.18 | `VariableStore.set_variable` / nameref-target write: one name-validation owner | matrix cells `W1-N8 → slot 1.18` (5 + spawn) | |
| W1-N25 | The READ of a nameref bound to an array ELEMENT is empty/0 in arithmetic and read-modify-write contexts (plain `$r` expansion is correct): `arr=(a b c); declare -n r='arr[1]'; r+=X` → psh `[1]="X"` (bash `bX`); `(( r += 10 ))` on `(1 2 3)` → 10 (bash 12); `$(( r ))` → 0 (bash 2), `(( r > 1 ))` takes the wrong branch, `[[ r -eq 2 ]]`, `let`, arith-`for`; under `set -u` psh aborts `r: unbound variable`; `let`, `(( r++ ))`, assoc, element 0, `local -n` likewise. Root cause: the store's nameref walk hands back the target NAME `arr[1]` unresolved — only the expansion path parses the subscript (slot 1.0 verifiers r2/r3) | P2 / wrong value (silent) | OPEN | 1.18 | `VariableStore` nameref resolution of subscripted targets (one owner) | matrix cells `W1-N25 → slot 1.18` | |
| W1-N28 | An attribute-only declaration through a nameref bound to an array ELEMENT overwrites the element with "" and applies no attribute: `arr=(1 2 3); declare -n r='arr[1]'; declare -i r` → bash `declare -ai arr=(… [1]="2" …)`, psh `declare -a arr=(… [1]="" …)`; same for -x/-u/-l/-r, `typeset`, assoc key, element 0, `local -n`; an unset element is CREATED as `[7]=""`; `readonly r`/`export r` silently clear it where bash refuses (same root cause as W1-N25; slot 1.0 verifier r3) | P1 / silent data loss | OPEN | 1.18 | `VariableStore` nameref resolution of subscripted targets | matrix cells `W1-N28 → slot 1.18` (5) | |
| W1-N29 | `mapfile` through a nameref bound to an array ELEMENT consumes the input it then refuses to store (the C090 shape) (slot 1.0 verifier r3) | P3 / consumed input | OPEN | 1.17 | `mapfile` preflight (resolve the nameref target BEFORE reading) | matrix cell `W1-N29 → slot 1.17` (input position) | |
| W1-N30 | `tests/unit/builtins/test_test_builtin.py::TestFileTests` writes fixed-name files into the SHARED cwd through the bare `shell` fixture — the xdist collision the root CLAUDE.md forbids (surfaced as a --quick failure during slot 1.0; pre-existing) | P4 / test hygiene (parallel safety) | OPEN | 6.5 (C244 holder) — or the next hygiene sweep | the module's fixture (use `isolated_shell_with_temp_dir`) | xdist run of the module | |
| W1-N26 | `export -f f` / `declare -fx f` never reaches the child environment: the flag is set but no `BASH_FUNC_f%%` entry exists, so `bash -c f` in a child → 127 (slot 1.0 verifier r2) | P3 / missing mechanism (function export) | OPEN | 1.16 (allexport/env write site) — or document as unsupported | environment materialisation of exported functions | matrix green control + N-row | |
| W1-N27 | `-i`/`-u`/`-l` attributes are not applied on builtin ELEMENT writes (`printf -v 'a[0]'`, `mapfile`, `read -a` into a `declare -ai`/`-au` array) where bash applies them (slot 1.0 verifier r2) | P3 / wrong value (silent) | OPEN | 1.18 | `VariableStore` element write applies the array's attributes | matrix cells `W1-N27 → slot 1.18` (4 + control) | |
| W1-N31 | psh cannot parse an explicit fd before the clobber-override operator: `echo hi 3>| f` → `Parse error … Expected file name` rc 2 (bash writes `f`); same for `1>|`, `2>|`, on compounds and `exec`; bare `>|` parses and applies correctly (slot 1.3; the 2026-07 boundary FLIP-PINS successor row R10-C/N3 records the same gap) | P3 / parse gap (redirect operator) | OPEN | 1.8 | `psh/lexer/recognizers/operator.py` fd-prefix + `>|` recognition | three-mode parse + apply pins | |
| W1-N32 | An `eval` whose OWN stderr is redirected drops its body's failing exit status: `exec 3> t3; eval "echo c >&3" 3>&- 2> err; echo rc=$?` → bash `rc=1`, psh `rc=0` (without `2> err` both print 1; also with `2>/dev/null`); identical at base (slot 1.3) | P3 / wrong status (silent) | OPEN | 1.3-r (io_redirect rider — same mechanism as W1-N33: the `2> err` open takes the freed fd 3 and the body's output lands in err) | `_open_output_off_low_fds` / `_dup_output_fd_for_children` | three-mode pins | |
| W1-N33 | A builtin's fd 1/2 stream open lands on the LOWEST FREE fd, so a later op in the same list naming that fd re-points it: `echo hi > o1 3> o3` writes hi into o3 (bash: o1); `echo hi > o1 3>&-` → write error rc 1; builtin diagnostics under `2> e 3> o3` land in o3 — pre-existing at base (slot 1.3 verifier) | P1 / wrong target (silent) | OPEN | 1.3-r (io_redirect rider after v0.78x: open the stream-side fd off the low range or apply the list's fd ops before the stream open) | `_open_output_off_low_fds` / `_dup_output_fd_for_children` | three-mode pins asserting file bytes | |
| W1-N15 | bash 5.3.15 refuses `local -a`/`local -A` on a readonly local but still DESTROYS its value (`declare -ar x=()`); psh refuses and keeps the value — ruled a bash bug, not emulated (slot 2.4) | P4 / declared divergence | DECLARED (v0.785.0; both-sides pin `TestReadonlyLocalArrayDeclaredDivergence`, oracle_min 5.3) | — (declared) | `ScopeManager.create_local` | FLIP-PINS "Declared, NOT flipped" row | |
| W1-N16 | `readonly -a NAME` is a selector in bash (no assignment → nothing happens: `R=1; readonly -a R` keeps `declare -r R="1"`, `readonly R=1; readonly -a R` rc 0 silent); psh array-ifies / refuses with an unlabelled `R: readonly variable` (slot 2.4) | P3 / conformance (declaration options) | OPEN | 4.7 | `readonly` builtin `-a`/`-A` option semantics | conformance rows | |
| W1-N17 | Bare `declare -n R` / `local -n` skip the nameref SHAPE checks (value must be a valid name; target must not be an array): `R=1; declare -n R` → bash rc 1 `invalid variable name for name reference`, psh rc 0 `declare -n R="1"`; `local -n x` on a readonly local says `readonly variable` where bash reports the shape (slot 2.4) | P3 / conformance (nameref) | OPEN | 1.18 | `VariableStore` nameref binding validation | conformance rows + matrix cells | |
| W1-N18 | `create_local`: a value-less redeclare over a declared-but-unset local DROPS the tombstone's attributes — `f(){ local -u x; local -x x; declare -p x; }; f` → bash `declare -xu x`, psh `declare -x x`; with `local -r x; local -x x` READONLY is silently dropped (slot 2.4) | P2 / wrong state (silent) | OPEN | 1.16 | `ScopeManager.create_local` attribute merge | matrix cells + conformance rows | |
| W1-N19 | `local` rejects `-t`/`+t`/`-g` (`local: invalid option: -t` rc 2) where bash's `local` accepts every `declare` option (slot 2.4) | P3 / conformance (option table) | OPEN | 4.7 | `local` flag table = `declare`'s | conformance rows | |
| W1-N20 | `declare -c` (capcase attribute) is unimplemented: rc 2 `invalid option: -c` where bash rc 0 `declare -c R="1"` (slot 2.4) | P3 / missing feature | OPEN | 4.7 | `VarAttributes.CAPCASE` | conformance rows or documented gap | |
| W1-N21 | `_declare_bare_name`'s `-a`/`-A` branch does not resolve namerefs, so `T=1; declare -n r=T; declare -a r` array-ifies the reference TEXT: psh `declare -a T=([0]="T")`, bash `([0]="1")` (slot 2.4; the readonly guard now resolves, the conversion does not) | P2 / wrong value (silent) | OPEN | 1.18 | `_declare_bare_name` → store conversion through the nameref rule | matrix cell + conformance row | |
| W1-N47 | `declare +g` is rejected by psh (rc 2, invalid option) where bash 5.3.15 accepts it as a no-op — pre-existing (slot 2.4 verifier r2) | P4 / option table | OPEN | 4.7 | `declare` flag table (`+g`) | conformance row | |
| W1-N46 | Combinator-parser AST nodes carry no source position, so `--parser combinator --debug-ast` omits the `@lineN` annotations the RD parser prints, on any input — pre-existing (slot 1.9) | P4 / combinator parity (debug output) | OPEN | combinator parity / 6.x textbook owner | combinator node construction (position field) | RD↔combinator `--debug-ast` differential | |
| W1-N48 | The combinator statement list accepts a compound followed by a bare word with NO separator as two statements (`[[ a == b ]] g`, `{ :; } g`, `while …; done g`, also with redirects) where the RD parser and bash reject rc 2 — hidden at the CLI by the RD trial parse (C178); pre-existing (slot 1.9 verifier) | P3 / combinator over-acceptance | OPEN | combinator parity / 6.x textbook owner | combinator statement-list separator rule | direct-API rows (D6) | |
| W1-N49 | psh refuses reserved words as alias NAMES (`alias for='for x in'` → error) where bash 5.3.15 accepts the definition (slot 1.7 verifier) | P3 / conformance (alias names) | OPEN | 4.x alias/builtin owner (register; ruling on adoption) | `alias` builtin name validation | conformance row | |
| W1-N50 | The combinator parser lacks the `select v; do …` (no `in` list) form that RD and bash accept (slot 1.7 verifier; pre-existing) | P3 / combinator parity | OPEN | combinator parity / 6.x owner | combinator `select` production | direct-API row | |
| W1-N44 | `for $'if' in a; do :; done` / `for $'a\tb' …`: psh's `not a valid identifier` diagnostic shows the raw `$'…'` source spelling where bash 5.3.15 shows the ANSI-C-processed value re-wrapped in plain single quotes — wording only (slot 1.7) | P4 / diagnostic wording | OPEN | 4.5 | identifier-error rendering of the subject word | conformance wording row | |
| W1-N45 | A heredoc operator introduced by an ALIAS (`alias hd='cat <<EOF'; hd` + body + `EOF`) is refused by psh with a loud explanatory error ("here-document `<<EOF` was never collected … an ALIAS whose expansion introduces the `<<` operator") and the body lines run as commands rc 127; bash prints the body — the referred half of C010 at `psh/scripting/command_accumulator.py`'s lex seam (slot 1.7) | P3 / conformance (alias/heredoc lex seam; loud, not silent) | OPEN | 1.8-r (lexer/accumulator seam rider) — or a Wave 3 lexer slot | `command_accumulator.py` heredoc collection after alias expansion | three-mode pins | |
| W1-N5 | `psh/executor/command.py#_handle_exec_builtin`'s `if not args:` branch says "make variable assignments permanent" and loops `set_variable`, but the assignments do not persist — behavior is bash-correct (`FOO=bar exec; echo "[$FOO]"` → `[]` in both); the comment (and possibly the loop) is drift (slot 1.1) | P4 / comment drift | OPEN | 6.2 | `_handle_exec_builtin` | dead-code sweep | |
| W1-N13 | ERR-trap scoping in pipeline members: an ERR trap inside a function body never fires, and fires inside a member where bash resets it (slot 1.1 verifier r1 N-A/N-B) | P3 / conformance (trap scoping) | OPEN | 4.13 | `TrapManager` ERR dispatch vs function/member scope | three-mode pins | |
| W1-N14 | `set +m` is ignored when the shell is interactive: job control stays on (slot 1.1 verifier r1 N-C) | P3 / conformance (options) | OPEN | 3.9 | `set -m`/`+m` → `JobManager.does_job_control` | PTY pin | |
| W1-N41 | Pipeline rollback `killpg` on the ERROR path now targets the forked child's inherited process group (after the forked-child job-control guard); harmless on the success path — audit the rollback target (slot 1.1 verifier r3 N-6) | P4 / error-path signal target | OPEN | 1.1-r | `psh/executor/pipeline.py` rollback | fault-injection row | |
| W1-N51 | At an interactive prompt psh prints a `[1] pid` job notice for `&` INSIDE a forked child (`( sleep 5 & )`, `f(){ sleep 5 & }; f | cat`) where bash prints nothing — pre-existing at base (slot 1.1 verifier r4) | P4 / interactive notice (jobs) | OPEN | 4.12 (job listing/notice owner) | async notice inside `in_forked_child` | PTY row | |
| W1-N52 | Attribute-only `declare -x PATH` / `declare -t PATH` do not empty the command hash table where bash 5.3.15 flushes it (an attribute-only `export PATH` must NOT flush — needs its own probe pass); `apply_attribute`/`remove_attribute` reach no observer (slot 1.5) | P3 / stale dispatch (attribute writes) | OPEN | core declaration owner (1.16 or 1.18) | `apply_attribute`/`remove_attribute` → the PATH observer | counter rows | |
| W1-N56 | bash BYPASSES the command hash table while PATH is a TEMPORARY-environment variable (findcmd.c `search_for_command`: no `phash_search`/`phash_insert` when PATH is a tempvar); psh consults and populates it — `PATH=$PWD/a; f(){ hash -p $PWD/b/probe probe; probe; }; PATH=$PWD/a f` → bash A, psh B; `f(){ probe; hash -t probe; }; PATH=$PWD/a f` → bash not found, psh …/a/probe (slot 1.5 verifier) | P3 / lookup truth (temp-env PATH vs hash) | OPEN | 4.9 | command search under a temp-env PATH | actual-dispatch rows | |
| W1-N57 | PATH converted to an ARRAY: `PATH[0]=$PWD/b; probe` → bash `declare -ax PATH=([0]=…)` and `command not found` rc 127 (bash finds nothing through an array PATH); psh resolves through element 0 and runs B; `mapfile -t PATH < f; probe` → bash keeps the hashed entry (no flush), psh flushes — informational edge (slot 1.5 verifier) | P4 / PATH-as-array edge | OPEN | 1.18 or 4.9 (informational) | PATH array conversion vs command search | conformance rows | |
| W1-N53 | A command temp-env prefix shadows a function's OWN `local`: `PATH=$PWD/a eval "g"` with `g(){ local PATH=$PWD/b; probe; }` runs A inside g (bash B) — `get_variable_object` consults `command_temp_env` before the scope stack; the value after the pop is correct (slot 1.5) | P2 / wrong target (name-lookup precedence) | OPEN | 4.9 | `get_variable_object` lookup order | actual-dispatch rows | |
| W1-N34 | `set -o posix; f(){ return abc; echo in=$?; }; f; echo after=$?` → bash 5.3.15 EXITS rc 2 in every mode (an outer guard suppresses the exit and does not return from the function); psh prints `after=2` rc 0 — `return` keeps `FunctionReturn` in slot 2.3 and borrows only the status (slot 2.3 N-1) | P3 / bash-5.3 semantic (posix special-builtin exit) | OPEN | 2.2-r (rider on the special-builtin exit owner; needs the `command`/`builtin` stripping the builtin guard sees) | `special_builtin_usage_exit` for `return` | three-mode pins under posix + guard | |
| W1-N42 | bash 5.3.15 reports child status 7 (or 4) for a FORKED posix special-builtin failure in EVERY status-1 class — `( export 1bad=x )`, `export 1bad=x | cat`, `$(export 1bad=x)`, `readonly r=1; unset r & wait $!`, `unset -v 1bad & wait $!` — while the status-2 usage class (`set -q & wait $!`) agrees at 2; a bash longjmp artifact, psh reports 1 (slot 2.2 verifier C4/C6/C15 + round-3 probe table) | P4 / bash artifact | OPEN | Park (ruling needed: declare, do not emulate) | — | declared-divergence row if kept | |
| W1-N43 | `-c`-mode subshell readonly ASSIGNMENT: the child reports rc 127 in psh where bash reports 1 (slot 2.2 verifier) | P3 / wrong status | OPEN | 2.3-r (usage/assignment status rider) | subshell child status for a readonly assignment failure | three-mode pins | |
| W1-N64 | With an ERR trap INSTALLED, bash 5.3.15 runs the trap and does NOT exit for the suppressible special-builtin classes (export/readonly/unset/`set -q`/return) where it exits without the trap; psh exits either way. Discriminators (whitespace-only, comment-only and subshell-only actions still exit) show a bash flag accident — DECLARED, not emulated (slot 2.2 verifier r2) | P4 / declared divergence (bash artifact) | OPEN | — (declared; both-sides pin, oracle_min 5.3; user guide §17 line) | `special_builtin_usage_exit` under an ERR trap | FLIP-PINS "Declared, NOT flipped" row | |
| W1-N65 | `psh -c 'set -o posix; : $((1/0)); echo survived'` → psh rc 1 where bash 5.3.15 reports 127; script-file and stdin modes agree at 1 in both shells — the `-c` command-mode discard path reports the wrong status (slot 2.2 verifier r2 N-4; pre-existing at base) | P3 / wrong status | OPEN | 2.3-r (command-mode discard / child status) | `-c` command-mode discard status | three-mode pins | |
| W1-N36 | `a=1; unset 'a[]]'` → bash 5.3.15 silent rc 0; psh ``unset: `a[]]': not a valid identifier`` rc 1 — the psh branch cites a bash-5.2 probe (`environment.py` `_unset_array_element`, R4-2): oracle drift (slot 2.2) | P4 / oracle drift (subscript keying) | OPEN | 4.x subscript-keying family owner (re-baseline on 5.3) | `_unset_array_element` | conformance row | |

Also registered by §6 0.3 without an N-number: the readonly-function `unset -f` wording
(psh `unset: f: readonly function` vs bash 5.3.15 `unset: f: cannot unset: readonly
function`) → **4.7** (named in the 4.7 heading; the exit-on-error half of the same cell is
2.2's).

## Part D — Wave 0 rulings (2026-09-06, package D; probe transcripts in `wave0-rulings/`)

Probe invocation (D15): every probe below ran from a fresh `mktemp -d` with `PWD`/`OLDPWD`
unset — psh as `env -u PWD -u OLDPWD PYTHONPATH=<worktree> PSH_STRICT_ERRORS=1 python -m psh …`
(discriminator `psh.__file__` printed under the worktree), bash as
`env -u PWD -u OLDPWD /opt/homebrew/bin/bash …` (`$BASH_VERSION` = `5.3.15(1)-release`).
Transcripts: `wave0-rulings/probe_c153_c181.out`, `probe_c153_multiline.out`, `probe_c153_pty.out`
(scripts beside them), plus the nested-read legs added after the package-D bounce:
`probe_c153_nested.sh/.out` and `probe_c153_nested_pty.py/.out`.

**R-C153 (`\#` command number; inventory `oracle_changed`) — STILL DIVERGENT → owner slot 4.24;
the divergence is WIDER than the r23 finding, not narrower.** bash 5.3 CHANGES (5.3-alpha
section, item `rrrrrr`, lines 719–720: "Fix for history and command number expansion when
expanding a prompt string that contains the @P variable transformation") moved the oracle,
so the r23 claim ("psh is one greater in script mode") no longer reproduces on FLAT command
lists — but psh's counter is wrong in a different way in every mode. Two probe families:

*Flat lists* (`PS1="\#> "`, then `echo "${PS1@P}"` ×2, `true`, `echo "${PS1@P}"`):

| mode | transcript | bash 5.3.15 | psh | verdict |
|---|---|---|---|---|
| `-c`, one line (`;`-joined) | `probe_c153_c181.out` | `1> 1> 1>` | `2> 2> 2>` | DIVERGENT |
| `-c`, multi-line string | `probe_c153_multiline.out` | `1> 1> 1>` | `3> 4> 6>` | DIVERGENT |
| script file, one line | `probe_c153_c181.out` | `2> 2> 2>` | `2> 2> 2>` | parity |
| script file, one command per line | `probe_c153_multiline.out` | `3> 4> 6>` | `3> 4> 6>` | parity |
| stdin, one line / multi-line | both transcripts | `2> 2> 2>` / `3> 4> 6>` | same | parity |
| PTY, flat (`--norc -i`, `PS1='[\#]$ '`; `true`, `echo one`, `echo two`, `echo "${PS1@P}"`, `exit`) | `probe_c153_pty.out` | prompts `[1]…[5]`, the echo prints `[5]$` | identical | parity |

*Nested reads* (`probe_c153_nested.sh/.out`: one script whose top-level lines are `echo`
tags interleaved with `eval '…; true; true'`, `. ./inc.sh` (two lines, tags `src`/`src2`),
`x=$(…; true)`, `( …; true )`, a function definition then its call, a three-iteration `for`,
and `source ./inc.sh`, every tag echoing `${PS1@P}`):

| mode | bash 5.3.15 | psh | verdict |
|---|---|---|---|
| script | `a:3 ev:4 b:5 src:6 src2:6 c:7 cs:8 d:9 sub:10 e:11 fn:13 g:14 h:16 src:17 src2:17 i:18` | `a:3 ev:5 b:6 src:8 src2:9 c:10 cs:12 d:12 sub:13 e:14 fn:16 g:17 h:19 src:21 src2:22 i:23` | DIVERGENT |
| stdin | identical to script | identical to psh script | DIVERGENT |
| `-c` (same text) | every tag `1` | identical to psh script (`a:3 … i:23`) | DIVERGENT |
| PTY (`probe_c153_nested_pty.py/.out`; `--norc -i`, `PS1='#\## '`; typed: `echo`, `eval '…; true; true'`, `source ./inc.sh`, `x=$(…); echo "$x"`, `f(){…}; f`, `echo`, `exit`) | prompts `#1# … #7#`; nested tags `top:2 ev:3 src:4 cs:5 fn:6 end:7` | prompts `#1# #2# #4# #6# #7# #8# #9#`; nested `top:2 ev:4 src:6 cs:8 fn:8 end:9` | DIVERGENT |

**The rule (bash 5.3.15, every mode):** `\#` advances exactly **once per top-level command
read from the main input** (a script line, a stdin line, an interactive line — the number a
command sees is already the incremented one, so the first interactive command prints 2 in
both shells); a **`-c` string is never read that way, so it stays at 1**; commands executed
by **`eval`, `.`/`source`, `$(…)`, a subshell, a function body or a loop body never advance
it** (both sourced lines print the same number; `eval 'x; true; true'` adds nothing). psh
advances it once per executed command from ANY reader (`eval '…; true; true'` adds two,
each sourced line adds one, the cmdsub body adds one), which is why flat lists happen to
agree and everything else does not.

**Ruling:** the row transfers to **4.24** (§10 heading "only if Wave 0 ruled it still
divergent" — it is) with the scope stated above: *one increment per top-level command read
from the main input; `-c` strings never; nested reads never*. Part A's 4.24 owner symbol and
guard carry the widened rule; §16's Wave 0 listing of C153 is superseded (the one expected
Part E disagreement). Provenance note (D7): the first version of this ruling under-scoped
the divergence to "`-c` only" because its probes were flat lists; the package-D bounce
verifier's independent nested-read rows (script/stdin/`-c` and a PTY leg) exposed the
wider rule, and the tables above are an independent re-derivation of those rows, not a copy.

**R-C181 (`-c` + `set -m` job notice; inventory `oracle_changed`) — CONFIRMED: bash 5.3.15
prints no notice; closed by 0.2 (jobs `-c` listing retune).** Probe `set -m; sleep 0.01 &
wait; echo end`: bash `-c` → stdout `end`, stderr EMPTY, rc 0; psh `-c` → identical
(`end`, empty stderr, rc 0); script mode → identical in both. The v0.692 deferral ("`-c` +
monitor boundary notice, bash read-loop artifact, timing-flaky") is therefore moot on the
5.3 oracle — bash 5.3 dropped the `-c` eager reap. What 5.3.15 DOES do that psh does not:
`set -m; sleep 0.1 & sleep 0.3; jobs; echo end` in `-c` lists the finished job on stdout
(`[1]+  Done                       sleep 0.1` then `end`) while psh prints only `end`
(`command_mode` DONE filter, `psh/builtins/job_control.py:96-98`). That is exactly the 0.2
retune (delete the filter; rename the `_suppressed_c_mode` tests `_listed_once_c_mode`;
CHANGELOG discharges the v0.692 deferral) — so C181 CLOSES in **0.2**, with the +3 status
column width (`:<24` → `:<27`) landing in the same slot. Closure evidence: the 0.2 commit +
Part B rows G40/G43.

**R-C169 (junk files `f`, `f1`, `f2`) — CLOSED as already-gone.** At `788ffe41`
(`w0/pkg-d` base) none of `f`, `f1`, `f2` exists in the working tree (`ls` → No such file),
none is tracked (`git ls-files f f1 f2` empty), none ignored-but-present. Nothing to delete;
no successor. (If they reappear they are a sibling agent's redirection probes — delete on
sight.)

**R-C241 (oracle comments target 5.2) — census baseline for the D12 ratchet.** Owner 0.1;
per-file counts generated below; the ratchet (`test_oracle_version_comments.py`) freezes
the total and only lets it decrease; rewrites happen only in files a slot touches (D12) —
the 0.2/0.3 triage files first.

<!-- generated:c241:start -->
`git grep -n 'bash 5\.2' 788ffe41 -- tests psh` (tracked text only, pattern = the regex `bash 5\.2`): **599 lines in 370 files** at `788ffe41` (the D12 ratchet baseline; only decreases; rewritten only in files a slot touches). Files with ≥ 2 lines are tabled; the 255 single-line files follow as a list.

| file | lines |
|---|---:|
| `tests/behavioral/golden_cases.yaml` | 17 |
| `psh/builtins/environment.py` | 8 |
| `psh/core/internal_errors.py` | 7 |
| `tests/conformance/bash/test_cv_carry_characterization.py` | 7 |
| `tests/conformance/bash/test_subscript_keying_conformance.py` | 7 |
| `tests/system/test_analysis_state_aware.py` | 7 |
| `psh/core/trap_manager.py` | 6 |
| `tests/conformance/bash/test_history_p_interactive_conformance.py` | 6 |
| `tests/integration/test_posix_special_builtin_exit.py` | 6 |
| `psh/expansion/pattern_engine.py` | 5 |
| `psh/interactive/history_manager.py` | 5 |
| `psh/utils/escapes.py` | 5 |
| `tests/unit/visitor/test_security_missed_positions.py` | 5 |
| `psh/core/state.py` | 4 |
| `psh/scripting/program_source.py` | 4 |
| `tests/integration/arrays/test_array_element_word_values.py` | 4 |
| `tests/integration/job_control/test_job_notice_channel.py` | 4 |
| `tests/system/interactive/test_pty_smoke.py` | 4 |
| `tests/unit/builtins/test_bcontract_serialization.py` | 4 |
| `tests/unit/builtins/test_local_builtin.py` | 4 |
| `tests/unit/expansion/test_arithmetic_characterization.py` | 4 |
| `tests/unit/expansion/test_parameter_expansion.py` | 4 |
| `tests/unit/expansion/test_pattern_operand_expansion.py` | 4 |
| `tests/unit/utils/test_printf_formatter.py` | 4 |
| `psh/builtins/loop_control.py` | 3 |
| `psh/core/scope.py` | 3 |
| `psh/executor/command_assignments.py` | 3 |
| `psh/executor/job_control.py` | 3 |
| `psh/expansion/CLAUDE.md` | 3 |
| `psh/expansion/param_parser.py` | 3 |
| `psh/expansion/word_expander.py` | 3 |
| `psh/expansion/word_expansion_types.py` | 3 |
| `psh/scripting/analysis_session.py` | 3 |
| `psh/scripting/input_sources.py` | 3 |
| `psh/scripting/source_processor.py` | 3 |
| `psh/utils/printf_formatter.py` | 3 |
| `tests/conformance/bash/test_array_init_conformance.py` | 3 |
| `tests/integration/job_control/test_debug_err_traps.py` | 3 |
| `tests/integration/job_control/test_exit_trap_paths.py` | 3 |
| `tests/integration/redirection/test_builtin_redirect_nesting.py` | 3 |
| `tests/system/test_cli_argument_parsing.py` | 3 |
| `tests/unit/builtins/test_ansi_c_reuse_surfaces.py` | 3 |
| `tests/unit/builtins/test_declaration_family_r19_t2.py` | 3 |
| `tests/unit/builtins/test_history_flags.py` | 3 |
| `tests/unit/core/test_special_registry.py` | 3 |
| `tests/unit/core/test_variable_store_truth_table.py` | 3 |
| `tests/unit/expansion/test_assignment_word_splitting.py` | 3 |
| `tests/unit/expansion/test_patsub_nocase_and_anchoring.py` | 3 |
| `tests/unit/expansion/test_pattern_bash_composition_differential.py` | 3 |
| `tests/unit/expansion/test_pattern_relations.py` | 3 |
| `tests/unit/utils/test_escape_dialects.py` | 3 |
| `psh/builtins/declare_format.py` | 2 |
| `psh/builtins/job_control.py` | 2 |
| `psh/builtins/positional.py` | 2 |
| `psh/builtins/test_command.py` | 2 |
| `psh/core/exceptions.py` | 2 |
| `psh/executor/command.py` | 2 |
| `psh/executor/control_flow.py` | 2 |
| `psh/executor/strategies.py` | 2 |
| `psh/expansion/manager.py` | 2 |
| `psh/expansion/operands.py` | 2 |
| `psh/expansion/subscript.py` | 2 |
| `psh/utils/heredoc_detection.py` | 2 |
| `psh/visitor/security_visitor.py` | 2 |
| `tests/conformance/bash/test_absent_features.py` | 2 |
| `tests/conformance/bash/test_cmdsub_case_conformance.py` | 2 |
| `tests/conformance/bash/test_resolution_timing_conformance.py` | 2 |
| `tests/conformance/bash/test_syntax_template_timing_conformance.py` | 2 |
| `tests/conformance/bash/test_trap_signal_spec_conformance.py` | 2 |
| `tests/conformance/bash/test_variable_truth_conformance.py` | 2 |
| `tests/conformance/posix/test_readonly_conformance.py` | 2 |
| `tests/integration/arrays/test_arrays_comprehensive.py` | 2 |
| `tests/integration/control_flow/test_break_continue_levels.py` | 2 |
| `tests/integration/control_flow/test_c_style_for_loops.py` | 2 |
| `tests/integration/job_control/test_bg_child_trap_discipline.py` | 2 |
| `tests/integration/job_control/test_signal_handling.py` | 2 |
| `tests/integration/job_control/test_trap_actions.py` | 2 |
| `tests/integration/parsing/test_bang_prefix_compound.py` | 2 |
| `tests/integration/parsing/test_statement_separators.py` | 2 |
| `tests/integration/redirection/test_builtin_dup_source_reassigned.py` | 2 |
| `tests/integration/redirection/test_exec_permanent_redirect.py` | 2 |
| `tests/integration/redirection/test_redirect_failure_paths.py` | 2 |
| `tests/integration/test_arith_readonly_continue.py` | 2 |
| `tests/integration/test_fatal_expansion_model.py` | 2 |
| `tests/integration/test_ps4_expansion.py` | 2 |
| `tests/integration/test_time_keyword.py` | 2 |
| `tests/parser_differential/test_input_contract_parity.py` | 2 |
| `tests/system/source_service/test_source_service_matrix.py` | 2 |
| `tests/system/test_eof_dangling_continuation.py` | 2 |
| `tests/system/test_source_error_rc.py` | 2 |
| `tests/unit/builtins/test_alias_builtins.py` | 2 |
| `tests/unit/builtins/test_bcontract_argument_policy.py` | 2 |
| `tests/unit/builtins/test_directory_stack.py` | 2 |
| `tests/unit/builtins/test_export_builtin.py` | 2 |
| `tests/unit/builtins/test_function_builtins.py` | 2 |
| `tests/unit/builtins/test_printf_enhanced.py` | 2 |
| `tests/unit/builtins/test_shopt_set_o.py` | 2 |
| `tests/unit/core/test_append_engine_characterization.py` | 2 |
| `tests/unit/executor/test_command_assignments.py` | 2 |
| `tests/unit/expansion/test_arith_recursion_variable_eval.py` | 2 |
| `tests/unit/expansion/test_arithmetic_integration.py` | 2 |
| `tests/unit/expansion/test_braceexpand_option.py` | 2 |
| `tests/unit/expansion/test_subscript_evaluator.py` | 2 |
| `tests/unit/interactive/test_histcontrol_histignore.py` | 2 |
| `tests/unit/interactive/test_history_state_machine_4b3.py` | 2 |
| `tests/unit/lexer/test_arith_gate_semantic_n2.py` | 2 |
| `tests/unit/lexer/test_post_lex_fusion_order_b3.py` | 2 |
| `tests/unit/parser/combinators/test_composite_conditional_patterns.py` | 2 |
| `tests/unit/parser/test_escaped_dollar_syntax.py` | 2 |
| `tests/unit/parser/test_word_fusion_improvement_surface_n3.py` | 2 |
| `tests/unit/scripting/test_analysis_session.py` | 2 |
| `tests/unit/scripting/test_program_source_unit.py` | 2 |
| `tests/unit/test_line_editor_helpers.py` | 2 |
| `tests/unit/test_parse_invocation.py` | 2 |
| `tests/unit/utils/test_signal_listing.py` | 2 |

One line each: `psh/builtins/core.py`, `psh/builtins/declaration_engine.py`, `psh/builtins/directory_stack.py`, `psh/builtins/function_support.py`, `psh/builtins/hash_builtin.py`, `psh/builtins/io.py`, `psh/builtins/navigation.py`, `psh/builtins/shell_options.py`, `psh/builtins/shell_state.py`, `psh/builtins/source_command.py`, `psh/builtins/type_builtin.py`, `psh/core/command_hash.py`, `psh/core/getopts_state.py`, `psh/core/variables.py`, `psh/executor/array.py`, `psh/executor/command_resolution.py`, `psh/executor/process_launcher.py`, `psh/expansion/arithmetic/evaluator.py`, `psh/expansion/arithmetic/tokenizer.py`, `psh/expansion/extglob.py`, `psh/expansion/operators.py`, `psh/expansion/parameter_expansion.py`, `psh/expansion/procsub_render.py`, `psh/expansion/tilde.py`, `psh/interactive/CLAUDE.md`, `psh/interactive/edit_buffer.py`, `psh/interactive/eof_policy.py`, `psh/interactive/line_editor_helpers.py`, `psh/interactive/repl_loop.py`, `psh/invocation.py`, `psh/io_redirect/file_redirect.py`, `psh/io_redirect/redirect_program.py`, `psh/lexer/cmdsub_scanner.py`, `psh/lexer/heredoc_lexer.py`, `psh/parser/recursive_descent/support/syntax_templates.py`, `psh/scripting/input_preprocessing.py`, `psh/scripting/script_validator.py`, `psh/utils/signal_utils.py`, `psh/visitor/formatter_quoting.py`, `psh/visitor/formatter_visitor.py`, `tests/conformance/bash/test_ansi_c_control_escape_conformance.py`, `tests/conformance/bash/test_array_case_attr_conformance.py`, `tests/conformance/bash/test_bad_substitution_conformance.py`, `tests/conformance/bash/test_bash_compatibility.py`, `tests/conformance/bash/test_case_toggle_conformance.py`, `tests/conformance/bash/test_cmdsub_errexit_conformance.py`, `tests/conformance/bash/test_command_resolution_conformance_r3.py`, `tests/conformance/bash/test_computed_special_vars_conformance.py`, `tests/conformance/bash/test_declare_attributes_conformance.py`, `tests/conformance/bash/test_dynamic_special_scoping_conformance.py`, `tests/conformance/bash/test_echo_double_dash_conformance.py`, `tests/conformance/bash/test_errexit_brace_group_conformance.py`, `tests/conformance/bash/test_exec_error_message_conformance.py`, `tests/conformance/bash/test_exit_trap_status_precedence_conformance.py`, `tests/conformance/bash/test_export_env_sync_conformance.py`, `tests/conformance/bash/test_field_splicing_conformance.py`, `tests/conformance/bash/test_grammar_boundaries_conformance.py`, `tests/conformance/bash/test_hash_conformance.py`, `tests/conformance/bash/test_heredoc_delimiter_conformance.py`, `tests/conformance/bash/test_heredoc_transaction_conformance.py`, `tests/conformance/bash/test_history_state_machine_conformance.py`, `tests/conformance/bash/test_identifier_policy_conformance.py`, `tests/conformance/bash/test_keyword_word_boundary_conformance.py`, `tests/conformance/bash/test_lineno_conformance.py`, `tests/conformance/bash/test_nameref_array_append_conformance.py`, `tests/conformance/bash/test_nameref_attribute_conformance.py`, `tests/conformance/bash/test_nounset_arithmetic_conformance.py`, `tests/conformance/bash/test_nounset_array_conformance.py`, `tests/conformance/bash/test_opaque_environment_conformance.py`, `tests/conformance/bash/test_param_transform_keyvalue_conformance.py`, `tests/conformance/bash/test_parse_continuation_s4_conformance.py`, `tests/conformance/bash/test_posixly_correct_conformance.py`, `tests/conformance/bash/test_prefix_assignment_conformance.py`, `tests/conformance/bash/test_printf_float_format_conformance.py`, `tests/conformance/bash/test_prompt_expansion_conformance.py`, `tests/conformance/bash/test_read_escapes_conformance.py`, `tests/conformance/bash/test_read_exact_chars_conformance.py`, `tests/conformance/bash/test_read_ifs_split_conformance.py`, `tests/conformance/bash/test_resolver_conformance.py`, `tests/conformance/bash/test_set_o_history_conformance.py`, `tests/conformance/bash/test_temporary_env_conformance.py`, `tests/conformance/bash/test_trap_flags_conformance.py`, `tests/conformance/bash/test_typed_expansion_errors_conformance.py`, `tests/conformance/bash/test_unset_nameref_conformance.py`, `tests/conformance/bash/test_v_array_conformance.py`, `tests/conformance/bash/test_variable_projection_reads_conformance.py`, `tests/conformance/bash/test_wait_unset_conformance.py`, `tests/conformance/posix/test_posix_special_builtin_exit_conformance.py`, `tests/integration/arrays/test_array_init_word_expansion.py`, `tests/integration/arrays/test_associative_array_bug.py`, `tests/integration/builtins/test_bcontract_dirstack_transactions.py`, `tests/integration/command_resolution/test_command_resolution.py`, `tests/integration/command_resolution/test_exec_failure_wording.py`, `tests/integration/command_resolution/test_hash_execution.py`, `tests/integration/control_flow/test_break_continue_as_builtins.py`, `tests/integration/control_flow/test_case_subject_quoting.py`, `tests/integration/control_flow/test_eval_control_flow.py`, `tests/integration/control_flow/test_for_select_item_expansion.py`, `tests/integration/control_flow/test_loop_control_scope_boundary.py`, `tests/integration/control_flow/test_tier3_executor_fixes.py`, `tests/integration/control_flow/test_while_loops.py`, `tests/integration/functions/test_funcnest.py`, `tests/integration/functions/test_recursion_depth.py`, `tests/integration/functions/test_wait_n.py`, `tests/integration/job_control/test_background_jobs.py`, `tests/integration/job_control/test_boundary_j1_lifecycle.py`, `tests/integration/job_control/test_jobs_completed_listing_modes.py`, `tests/integration/job_control/test_jobs_n_changed.py`, `tests/integration/job_control/test_jobs_x_substitution.py`, `tests/integration/job_control/test_jobspec_operands.py`, `tests/integration/job_control/test_noninteractive_job_refresh.py`, `tests/integration/job_control/test_pending_signal_trap_eof.py`, `tests/integration/job_control/test_pipeline_signal_death.py`, `tests/integration/job_control/test_signal_killed_exit_status.py`, `tests/integration/job_control/test_stopped_job_current_marker.py`, `tests/integration/job_control/test_wait_disown_bg_fg.py`, `tests/integration/parser/test_heredoc_error_lineno.py`, `tests/integration/parsing/test_amp_command_position.py`, `tests/integration/parsing/test_brace_extent_literal_brace.py`, `tests/integration/parsing/test_cmdsub_grammar.py`, `tests/integration/parsing/test_quoting_escaping.py`, `tests/integration/parsing/test_r16_command_position.py`, `tests/integration/parsing/test_r17_parser_cluster.py`, `tests/integration/parsing/test_word_splitting.py`, `tests/integration/redirection/test_builtin_redirect_child_visibility.py`, `tests/integration/redirection/test_child_fd_inheritance.py`, `tests/integration/redirection/test_compound_redirect_failure.py`, `tests/integration/redirection/test_exec_close_output_leak.py`, `tests/integration/redirection/test_external_redirect_once.py`, `tests/integration/redirection/test_fd_move_and_csh_redirect.py`, `tests/integration/redirection/test_here_string_tilde.py`, `tests/integration/redirection/test_here_string_word_quoting.py`, `tests/integration/redirection/test_heredoc_shared_cursor_r1.py`, `tests/integration/redirection/test_noclobber_targets.py`, `tests/integration/redirection/test_process_sub_closed_fds.py`, `tests/integration/redirection/test_process_sub_embedded.py`, `tests/integration/redirection/test_redirect_diagnostic_prefix_r1.py`, `tests/integration/redirection/test_redirection_restore.py`, `tests/integration/redirection/test_self_dup_leniency_r1.py`, `tests/integration/shell_options/test_errexit_script_mode.py`, `tests/integration/shell_options/test_nounset_script_mode.py`, `tests/integration/subshells/test_subshell_basics.py`, `tests/integration/subshells/test_subshell_implementation.py`, `tests/integration/test_assignment_error_abort.py`, `tests/integration/test_brace_adjacency_idioms.py`, `tests/integration/test_enhanced_test_unary_word_operand.py`, `tests/integration/test_eval_line_discard.py`, `tests/integration/test_extglob_nonfinal_path.py`, `tests/integration/test_scripting_idioms.py`, `tests/integration/test_xtrace_format.py`, `tests/integration/variables/test_variable_assignment.py`, `tests/performance/benchmarks/test_pattern_engine_performance.py`, `tests/system/interactive/test_heredoc_detection_interactive_pty.py`, `tests/system/interactive/test_substitution_abort_interactive_pty.py`, `tests/system/invocation/test_invocation_matrix.py`, `tests/system/source_service/test_nul_channel_matrix.py`, `tests/system/test_analysis_mode_line_continuation.py`, `tests/system/test_r16_scripting.py`, `tests/system/test_read_malformed_bytes_i1.py`, `tests/system/test_script_input_sources.py`, `tests/system/test_unterminated_heredoc.py`, `tests/unit/builtins/test_bcontract_nameref_riders.py`, `tests/unit/builtins/test_builtin_base_helpers.py`, `tests/unit/builtins/test_candidate_banner.py`, `tests/unit/builtins/test_declare_bare_name_locality.py`, `tests/unit/builtins/test_exec_builtin.py`, `tests/unit/builtins/test_exec_flags.py`, `tests/unit/builtins/test_hash_builtin.py`, `tests/unit/builtins/test_io_builtins.py`, `tests/unit/builtins/test_navigation.py`, `tests/unit/builtins/test_pwd_stack_convergence.py`, `tests/unit/builtins/test_r16t2_builtins_flags.py`, `tests/unit/builtins/test_r16t2_core_options.py`, `tests/unit/builtins/test_read_error_messages.py`, `tests/unit/builtins/test_read_mapfile_streaming.py`, `tests/unit/builtins/test_read_option_parsing.py`, `tests/unit/builtins/test_read_remainder.py`, `tests/unit/builtins/test_read_unified_quirks.py`, `tests/unit/builtins/test_readonly_export_attribute_flags.py`, `tests/unit/builtins/test_set_builtin.py`, `tests/unit/builtins/test_source_return.py`, `tests/unit/builtins/test_system_builtins.py`, `tests/unit/builtins/test_tier3_builtin_array_fixes.py`, `tests/unit/builtins/test_trap_flags.py`, `tests/unit/core/test_array_scalar_overwrite.py`, `tests/unit/core/test_environ_policy.py`, `tests/unit/core/test_funcname_call_stack.py`, `tests/unit/core/test_getopts_state_p1.py`, `tests/unit/core/test_ifs_seed.py`, `tests/unit/core/test_option_reflection.py`, `tests/unit/core/test_pep538_locale.py`, `tests/unit/core/test_r18t2_corestate.py`, `tests/unit/core/test_scope_tombstones.py`, `tests/unit/core/test_variable_lookup.py`, `tests/unit/executor/test_executor_error_guards.py`, `tests/unit/executor/test_legacy_ast_fallbacks.py`, `tests/unit/executor/test_nocasematch_posix_classes.py`, `tests/unit/expansion/test_arith_compound_assign_order.py`, `tests/unit/expansion/test_arith_no_rescan_stored_value.py`, `tests/unit/expansion/test_arith_readonly_nameref.py`, `tests/unit/expansion/test_arithmetic_command_form_dollar.py`, `tests/unit/expansion/test_arithmetic_comprehensive.py`, `tests/unit/expansion/test_arithmetic_division_semantics.py`, `tests/unit/expansion/test_arithmetic_dollar_expansion.py`, `tests/unit/expansion/test_arithmetic_integration_advanced_todo.py`, `tests/unit/expansion/test_array_index_arith_errors.py`, `tests/unit/expansion/test_case_toggle.py`, `tests/unit/expansion/test_command_sub_bytes.py`, `tests/unit/expansion/test_command_sub_closed_fds.py`, `tests/unit/expansion/test_dollar_bracket_arithmetic.py`, `tests/unit/expansion/test_expand_aliases_option.py`, `tests/unit/expansion/test_expansion_correctness_sweep.py`, `tests/unit/expansion/test_extglob_negation.py`, `tests/unit/expansion/test_globstar_symlinks.py`, `tests/unit/expansion/test_multi_field_expansion.py`, `tests/unit/expansion/test_param_parser_behavior_fixes.py`, `tests/unit/expansion/test_param_parser_differential.py`, `tests/unit/expansion/test_parameter_transform.py`, `tests/unit/expansion/test_posix_char_classes.py`, `tests/unit/expansion/test_process_sub_quoting.py`, `tests/unit/expansion/test_slice_semantics.py`, `tests/unit/expansion/test_special_variables.py`, `tests/unit/expansion/test_substitution_empty_match_pins.py`, `tests/unit/expansion/test_substring_lazy_arithmetic.py`, `tests/unit/expansion/test_tilde_prefix_boundary.py`, `tests/unit/expansion/test_value_operand_quoting.py`, `tests/unit/expansion/test_view_operators_joined_nullness.py`, `tests/unit/expansion/test_word_expansion_policy.py`, `tests/unit/interactive/test_edit_buffer_killring.py`, `tests/unit/interactive/test_eof_policy.py`, `tests/unit/interactive/test_history_nav.py`, `tests/unit/io_redirect/test_named_fd_heredoc.py`, `tests/unit/io_redirect/test_redirect_predicates.py`, `tests/unit/lexer/test_ansi_c_quoting.py`, `tests/unit/lexer/test_arith_cmdsub_disambiguation.py`, `tests/unit/lexer/test_backtick_dquote_escapes.py`, `tests/unit/lexer/test_bracket_quote_words.py`, `tests/unit/lexer/test_case_mapping.py`, `tests/unit/lexer/test_command_position_after_amp.py`, `tests/unit/lexer/test_fallback_words.py`, `tests/unit/lexer/test_heredoc_brace_expansion.py`, `tests/unit/lexer/test_heredoc_lexer.py`, `tests/unit/lexer/test_word_scanners.py`, `tests/unit/parser/test_background_lists.py`, `tests/unit/parser/test_c_style_for_body_forms.py`, `tests/unit/parser/test_case_pattern_empty_alternatives.py`, `tests/unit/parser/test_case_subject.py`, `tests/unit/parser/test_composite_headed_assignment_b1.py`, `tests/unit/parser/test_misplaced_case_terminators.py`, `tests/unit/parser/test_r18t2_lexparse_fixes.py`, `tests/unit/parser/test_regex_operand_policy.py`, `tests/unit/scripting/test_heredoc_alias_route.py`, `tests/unit/scripting/test_line_continuation_contexts.py`, `tests/unit/tooling/test_operand_projection_guard.py`, `tests/unit/utils/test_heredoc_detection.py`.
<!-- generated:c241:end -->

### Park register (program §15, ruled at Wave 0; none counts as closed)

| Park | Rows | Ruling | Successor owner |
|---|---|---|---|
| P-1 RESUMABLE-PARSER successor campaign | C171 `ParseSession` O(k²) (registered carry; prior ruling R1), C172 nested-cmdsub super-linear (capped at `MAX_SUBSTITUTION_NESTING=100`, documented), C012 retry-from-seed variant if 5.2 ships only the cheap variant | Campaign-scale; needs a measured cost target and its own verification model. Characterization pins become upper-bound tests when it lands. | the successor RESUMABLE-PARSER campaign (carried from the 2026-07 LEDGER, ruling R1); C012's cheap variant stays with slot 5.2 |
| P-2 Combinator constant factor + double parse | C120 | By design (educational parser); one sentence in `psh/parser/CLAUDE.md` via 6.1; C178 is D6. | 6.1 (the sentence); no code owner |
| P-3 Missing bash features | C165 `coproc`, C190 `$(< file)` (documented at `06_expansions.md:254`), C196 `BASH_SOURCE` (`docs/missing_features.md` §1), funsub `${ cmd; }` / `${\| cmd; }` (bash 5.3, L) | Not a feature-parity sweep. Wave 0 pins funsub as a declared divergence behind `oracle_feature('funsub')`; 6.3 verifies the doc rows. `$(< file)` is the one S-sized candidate: it may be pulled in as a rider ONLY by a written ruling after Wave 4, never silently. | 0.1 (funsub declared-divergence pin, FLIP-PINS "noted" row) + 6.3 (doc rows); `$(< file)` rider = integrator ruling after Wave 4 |
| P-4 `%Q` lenient TIMEFORMAT | C186 | Declared divergence documented at `core.py:380-390`; 6.3 verifies the both-sides pin. | 6.3 |
| P-5 Unicode width model | the display-column/grapheme model beyond 4.21's single width policy; C215 real-terminal rendering beyond Checkpoint R's PTY scope | Requires a defined width policy and a rendering harness. | successor (post-program); 4.21 ships the single width policy, Checkpoint R the PTY scope for C215 |
| P-6 Platform rows | psh emulating x86-64 `long double` `%a` output | Platform, not psh; classified by `oracle_feature('x87_long_double')` in 0.1. | 0.1 (classifier) + the nightly (`nightly-status.md`) |
| Excluded (status) | C114, C163 (not reproducible on 5.3.15), C208 (fixed — harness race) | Not queued; recorded in `INVENTORY.json` with the verify note. | none (terminal) |

## Part E — Derivation report and §16 cross-check (generated)

<!-- generated:part-e:start -->
| wave | derived (headings/§12/§15/§6) | §16 map | agree |
|---|---:|---:|---|
| 0 | 7 | 8 | NO: C153 |
| 1 | 44 | 44 | yes |
| 2 | 0 | 0 | yes |
| 3 | 21 | 21 | yes |
| 4 | 85 | 84 | NO: C153 |
| 5 | 12 | 12 | yes |
| R | 5 | 5 | yes |
| 6 | 61 | 61 | yes |
| Park | 7 | 7 | yes |
| Excluded | 3 | 3 | yes |

Derivation methods: body = 28, checkpoint = 5, excluded = 3, heading = 194, park = 7, wave = 1, wave0 = 7.

Disagreements with §16 (1):
- C153: derived `4.24` (§10 `4.24` heading (line 253)); §16 says `0`

Derivation conflicts / unowned (0):
- none

§16 cids not in INVENTORY.json: none; §16 rows: 245; inventory rows: 245.

Rows not derived from a slot heading (method ≠ heading):
- C045 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C101 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C104 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C105 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C113 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C114 → `Excluded` [excluded]: §15 Excluded row
- C119 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C120 → `Park P-2` [park]: §15
- C121 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C123 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C125 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C129 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C137 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C138 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C140 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C142 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C148 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C149 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C150 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C162 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C163 → `Excluded` [excluded]: §15 Excluded row
- C165 → `Park P-3` [park]: §15
- C167 → `R` [checkpoint]: §12 coverage scope
- C169 → `0.1` [wave0]: §6 0.1 item 7: confirm `f`, `f1`, `f2` absent at HEAD (ruling R-C169)
- C171 → `Park P-1` [park]: §15
- C172 → `Park P-1` [park]: §15
- C175 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C176 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C181 → `0.2` [wave0]: §6 0.2: delete the `command_mode` DONE filter; CHANGELOG discharges the v0.692 deferral (ruling R-C181)
- C186 → `Park P-4` [park]: §15
- C187 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C189 → `R` [checkpoint]: §12 coverage scope
- C190 → `Park P-3` [park]: §15
- C192 → `R` [checkpoint]: §12 coverage scope
- C193 → `R` [checkpoint]: §12 coverage scope
- C196 → `Park P-3` [park]: §15
- C199 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C203 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C204 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C205 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C208 → `Excluded` [excluded]: §15 Excluded row
- C215 → `R` [checkpoint]: §12 coverage scope; also named in §15 P-5 for the remainder
- C216 → `6.2` [body]: §13 `6.2` body (line 306); wave from its Owned findings
- C218 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C229 → `5 (design input, wave charter, no slot by design)` [wave]: §11 Owned findings only
- C233 → `6.3` [body]: §13 `6.3` body (line 307); wave from its Owned findings
- C238 → `0.1` [wave0]: §6 0.1 item 7: stale "latest/active" index lines refreshed (C238)
- C241 → `0.1` [wave0]: §6 0.1 item 8: C241 census + `test_oracle_version_comments.py` ratchet (D12)
- C242 → `0.3` [wave0]: §6 "Required work": the gate is red until the 0.3 tree; green gate + schema-2 attestation are the 0.3 exit criteria
- C243 → `0.1` [wave0]: §6 0.1 item 7: r23 row added to the reviews index; `test_every_review_file_is_indexed` green
- C245 → `0.1` [wave0]: §6 Owned findings: closed by D8 (pre-handoff smoke); CLAUDE.md line in 0.1 item 7
<!-- generated:part-e:end -->
