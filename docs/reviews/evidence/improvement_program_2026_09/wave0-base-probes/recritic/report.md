# Re-critique — Improvement Program 2026-09, revision 2 (`program_v2.md`)

Second pass 2026-09-06 at HEAD 6459f1a6 (v0.779.0), oracle /opt/homebrew/bin/bash = 5.3.15(1)-release. Scripts and transcripts in this directory: `own2.py` (ownership check), `probes.sh`/`probes2.sh`/`probes3.sh`/`probes4.sh` with `*.out`.

## Verdict

**NOT READY** — no blocker remains and all twelve first-pass gaps are applied (ten fully, two with a residue), but one new major was found by probe (slot 3.8 / W0-N1 pins a `read` message that bash 5.3.15 does not print) plus seven minors (release/slot/dev-day totals that no longer add up after the revision, a wrong `function.py` pointer in 1.1, a vacuous guard in 3.4b, an over-simplified `-c` rule in 2.3, a cid double-listed between 1.0 and 1.18, exit criteria missing two user-guide rows, and seven directory-less file pointers). Every fix is a text edit; one more revision pass should make it READY.

## G1–G12

| Gap | Status | Evidence in the revised text |
|---|---|---|
| G1 Wave 0 = one release | **applied** | D16 "its three slots are commit boundaries on one branch and ship as ONE release"; §5 row 0 "3 (commit boundaries) \| 1"; §6 "gated once at the 0.3 tree, with one `gate_attestation.json` FINAL commit and one tag (v0.780.0). No red-gate merge to `main` at any point"; §6 Size "ONE release (v0.780.0)"; §5 "v0.780.0 = Wave 0; Wave 1 starts at v0.781.0"; checklist 4–5 dispatch the nightly at the 0.3 tree and tag only v0.780.0. Scan of every `v0.78x`, "release", "0.1/0.2 lands/merges" mention: no sentence implies three releases or v0.781/v0.782 for Wave 0 (the only "three attested releases" phrase is the provenance line describing the critic's gap). |
| G2 stale PWD/OLDPWD | **applied** | 0.3 registers **W0-N7** with `state.py:227-228`, the three probes, "masked by design in `tests/harness/shell_oracle.py:297`", "→ slot 1.4, with a §17 user-guide row until closed"; 1.4 "Also closes **W0-N7** … pins in three modes pass `case_env={'PWD': stale}` / `{'OLDPWD': bogus}` and assert `$PWD`, `pwd`, `pwd -P`, the `cd -` target and a file placed after `cd ..`; `shell_oracle.py:297` gains a pointer"; §16 row 1 "(+ W0-N7)". Re-probed: `env PWD=/nonexistent/zz` → psh `/nonexistent/zz`, bash the real cwd. |
| G3 W0-N1 repro | **applied as specified** | 0.3: "`read` with fd 0 closed at process START (`sys.stdin is None`; `psh -c 'read x' <&-` and `psh s.sh <&-`) escapes as `ValueError … from psh/builtins/input_reader.py:124`"; 3.8 pins both modes and "must not become the 0.3 exit-126 path". Re-probed: traceback in both modes; `psh -c 'echo hi' <&-` runs. BUT the expected message the critique supplied is psh's wording, not bash 5.3.15's — see R1. |
| G4 matrix first | **applied (residue R5)** | New "1.0 Write-authority matrix (C226, half of C244; S, 1 d; lands before 1.4)" with `xfail(strict=True, reason="C0xx → slot y")` cells for 1.4/1.5/1.15/1.16/1.17/1.18/4.5/4.9 — every pair matches the owning slot's heading; 1.18 "the 1.0 matrix extended with array cells (its 1.18 xfail cells flip here)"; §17 "the C226 matrix ships before consumers migrate" is now true. Residue: 1.18's heading still lists C226 as owned. |
| G5 slot cross-refs | **applied** | 0.1 item 6 "owned by slot 4.12 (C065 job text)"; 0.3 "W0-N5 … → slot 4.9", "W0-N2 … → 4.12; W0-N3 … → 4.6; W0-N6 … → 4.7; unset-readonly-function wording → 4.7", "W0-N1 … → slot 3.8"; exit criteria "signal-death row with its 4.12 owner"; "`LEDGER.md` derives every slot id from the §7–§13 headings". Script check: each target heading exists in §7–§10 and names the N-row (4.12 = C065+W0-N2, 4.6 = W0-N3, 4.7 = W0-N6+wording, 4.9 = W0-N5, 3.8 = W0-N1, 1.4 = W0-N7, 2.3 = W0-N4 in the brief body and §8 owned findings). Every other "→ slot"/"slot N.n"/"in N.n" reference (4.24, 5.6, 5.2, 6.1, 6.3, 1.14/3.1, 4.15, 4.16, 3.3, 3.7, 1.17) also resolves. |
| G6 file-disjoint | **applied** | The word appears only negated: §5 "The two are NOT file-disjoint (2.3 and 1.4 both edit `psh/builtins/navigation.py`; …)" with the merge-train order and "rebases onto the train tip and is re-verified at the rebased SHA"; §8 "is NOT file-disjoint from it". The remaining "slots touch disjoint owners" is at symbol level and holds. |
| G7 user-guide rows | **applied (residue R6)** | Outcome 6 rewritten ("`NO_ROW_PROBES`/`PARTIAL_ROW_PROBES` guards fail the moment psh starts matching bash on a No/Partial row — a slot that converts such a row flips the row and its probe in the same diff"); rows named in 1.10 (:25), 1.11 (:934/:941), 1.14 (:990), 2.1 (:968 with the 5.3.15 RETURN-trap probe), 3.4b (:931), 4.1 (:942), 4.7 (:976), 4.13 (:953), 4.18 (:983); Wave 1 and Wave 4 exit criteria list them. All twelve line numbers re-read in `17_differences_from_bash.md` and match. Residue: Wave 2/3 exit criteria do not name :968/:931. |
| G8 index row | **applied** | 0.1 item 7: "fails on `ground_up_reappraisal_23_correctness_textbook_2026-08-09.md` — the uncommitted `docs/reviews/README.md` edit indexes only the fresh appraisal — so add the r23 row and this program's row under the 'Current appraisal' table that edit introduced, leaving the table otherwise as written". |
| G9 header provenance | **applied** | "the schema-1 file records no oracle — the oracle at gate time was bash 5.2.26 per campaign memory and CHANGELOG". |
| G10 marker/ratchet/verifier | **applied** | D5 "registered in `pytest.ini` `markers` (`xfail_strict` is on), and the 0.1 ratchet … whitelists exactly these two forms"; 0.1 item 2 "`tools/verify_gate_attestation.py` stays stdlib-only (the tag job runs it with bare `python3`) and carries its own `EXPECTED_BASH_MM` constant, pinned equal to the harness's by a test". `pytest.ini` confirmed: `xfail_strict = true`, `markers` list present, no `oracle_min` yet. |
| G11 pairing delta | **applied** | D16 "two S slots with the SAME owner file may share one release … never two clusters with different owners"; §1 cites "(D16: …)" with the same rule. |
| G12 HOME via env= | **applied** | D14 "even `export HOME=/probe-home; echo ~` still prints the login home, so in-script `export` does not help; tilde-pattern pins (1.11) pass `HOME` via `env=`, while `cd`/empty-`HOME` pins (1.4) read `$HOME` directly and may assign in-script". |

## Ownership check (`own2.py` against `inventory.json` + §16)

| Measure | Result |
|---|---|
| Inventory rows | 245 (166 live, 2 oracle_changed, 74 n/a, 2 not_reproducible, 1 fixed) |
| Distinct cids in §16 / total mentions | 245 / 245 — 0 double-owned, 0 unknown, 0 inventory cids missing |
| live + oracle_changed rows | 168, each exactly once; C153, C181 (oracle_changed) → Wave 0 |
| fixed / not_reproducible queued | none — C208, C114, C163 appear only in the Excluded row |
| Park rows | C171, C172, C120, C165, C190, C196, C186 — all `live`, each with a §15 ruling and successor |
| W0-N1…N7 | one owning slot each: N1→3.8, N2→4.12, N3→4.6, N4→2.3, N5→4.9, N6→4.7, N7→1.4; every target is a §7–§10 heading and names the N-row |

## Internal consistency of the revision

- **§5 table vs "Size:" lines.** Wave 0: 3 commit-slots / 1 release ✓. Wave 1: 20 slots enumerated (1.0–1.19) = §5 "20" = §7 "20 slots (~21 releases)" ✓. Wave 2: 4/4 ✓. Wave 5: 6/6 ✓. **Wave 3 ✗**: §5 says "10 \| 10" and §9 says "10 slots (3.4 = 3 releases)", but §9 enumerates 9 slots (3.1–3.9) → 11 releases. **Wave 4 ✗**: §5 "~26" slots vs §10 "~24 slots (~26 releases)" (24 enumerated incl. conditional 4.24). **Wave 6**: "~6 slots" vs 5 enumerated (6.1–6.5). **Total ✗**: the table sums to 74–75 releases as written (75–76 with Wave 3 corrected) against "≈ 73"; dev-days sum to 5.5+28+6.5+13+32+8+8 = 101 against "~96". §17 still says "Wave 1 scope (19 slots)".
- **Slot 1.0 xfail list vs owners** ✓: 1.4 owns C043, 1.5 C044, 1.15 C027, 1.16 C028, 1.17 C090, 1.18 C093–C096/C136/C194, 4.5 C130, 4.9 C071 — all match §7/§10 headings.
- **D16 vs §1** ✓: same rule (two S slots, same owner, one bump + one attestation; never different owners) and both name Wave 0 as the other exception.
- **Schedule**: no impossibility found. `nightly.yml` has `workflow_dispatch: {}` so checklist item 4 can run on the branch; Wave 2's merge-train constraints (2.3 < 1.4, 2.4 < 1.5/1.16/1.18, 2.1 < 1.14, 2.2 < 1.16) are satisfiable with 2.x front-loaded; 1.0 "lands before 1.4" is compatible with 1.1 being the first production slot.

## Spot-checks (pointers not verified in the first pass)

Verified as described: `input_reader.py:124` (the `ValueError`), `executor/job_control.py:626-628`, `environment.py:568` (`:<15` set -o), `builtins/job_control.py:85-98`, `test_trap_flags.py:73`, `test_error_location_prefix.py:39`, `test_shopt.py:79`, `test_shopt_set_o.py:23/29/191/207/213/249`, `golden_cases.yaml:2378/:8910`, `strategies.py:605`, `control_flow.py:91-92`, `subshell.py:82-83`, `manager.py:518` and `:955` (inside `_builtin_redirect_fd_level` at :944), `scope.py:331`, `formatter_visitor.py:120`, `words.py:143`, `operator.py:129-131`, `special_commands.py:109-113/161-165/245-249`, `source_processor.py:523` and `:233-240`, `process_launcher.py:81`, the 1.14 `int(` sites (`test_command.py:435`, `positional.py:33`, `core.py:51`, `function_support.py:1012`, `system_builtins.py:50`, `limits.py:237`, `read_builtin.py:528/562`, `mapfile_builtin.py:193/204`), `options.py:78-80`, `parameter_expansion.py:480`, `state.py:1047`, `arrays.py:88-92`, `fields.py:57-63`, `operands.py:466` and `:162-169`, `word_splitter.py:47`, `read_builtin.py:359`, `word_expander.py:580`, `pure_helpers.py:380/414/435`, `modular_lexer.py:264-271`, `registry.py:78-85`, `file_redirect.py:823/:328`, `cmdsub_scanner.py:570`, `heredoc_lexer.py:207`, `literal.py:190`, `pipeline.py:344/:439`, `enhanced_test_evaluator.py:195`, `variable_store.py:214-217`, `command.py:1102`, `control_flow.py:650`, `scope.py:113-120`, `__main__.py:279-290`, `executor/core.py:380-390`, `builtins/core.py:31-37/39-41/56-61`, `trap_manager.py:94-99`, `core/CLAUDE.md:744-757`, `environment.py:152-156/665/709/730/775`, `06_expansions.md:254`, all twelve `17_differences_from_bash.md` rows, `title.py:21` (single OSC write), `escapes.py#unicode_escape_char`. Nightly: runs 33465565622…34008477403 (09-01→09-06) all `failure`; run 34008477403 log = 22,888 + 1,116 + 1,510 = 25,514 passed, 7 failed ✓.

Refuted: `function.py:196` (no `in_pipeline` in `psh/executor/function.py`; see R2) and the `quote_aware` parameter name (see R7).

Probe-confirmed claims (bash 5.3.15 vs psh HEAD): W0-N2 with the triage shape `sleep 5 & sleep 0.1; sleep 5 & jobs` → bash `[2]+`, psh `[3]+`; W0-N3 (`braces not balanced` rc 2 vs silent rc 1); W0-N5 (`ls is ./ls` vs `/bin/ls`); W0-N6 (bash `declare -c x=hello` → `Hello`); 2.4 (`declare -i`/`local -i` on readonly → rc 1 "readonly variable", `-x` allowed); 4.2 heads (`builtin declare a=(1)` rc 2, `alias a=(1)` parses, `let`/`eval` accept, `echo a=(1)` rc 2 vs psh printing it); 4.14 (`v: ambiguous redirect` rc 1; per-command `4<&3-` closes fd 3 in the parent); 0.2 posix function names (`a.b`, `a-b` accepted by 5.3.15 in posix mode, rejected by psh); 2.2 boundary (`eval "export 1bad=x" || echo caught` continues in both; `. ./bad.sh` exits; a `set -q` in a posix EXIT-trap action exits 2); 2.1 boundary (EXIT trap: entry status everywhere incl. function body and dot; ERR/USR1: function body and dot use current `$?`, `{ }`/`eval`/`if` use entry status — psh diverges on those top-level cells); C074/C225 premises (`HOME=; cd` → bash rc 0 no-op; empty CDPATH component echoes only in psh); `psh <&-` rc 0 vs bash 126; shopt widths (`shopt NAME` and `shopt -o NAME` queries 20; bare `shopt -o` and `set -o` 15).

## Remaining gaps (ranked)

### R1 — MAJOR — 3.8 / W0-N1 pin the wrong `read` message
bash 5.3.15 prints `read: 0: read error: Bad file descriptor` (script, `-c`, stdin); psh's per-command form prints `read: read error: 0: Bad file descriptor`. The program pins psh's wording twice, so a live-oracle pin would be red on the oracle and the verifier would bounce the slot.
- §6 0.3, replace "the per-command `read x <&-` form is already handled (`read: read error: 0: Bad file descriptor`, rc 1) → slot 3.8." with "the per-command `read x <&-` form already returns rc 1 without a traceback but with psh's wording `read: read error: 0: Bad file descriptor`, where bash 5.3.15 prints `read: 0: read error: Bad file descriptor` → slot 3.8 (startup crash and wording together)."
- §9 3.8, replace "handle `sys.stdin is None` as a closed fd → `read: read error: 0: Bad file descriptor` rc 1;" with "handle `sys.stdin is None` as a closed fd → bash 5.3.15's `read: 0: read error: Bad file descriptor` rc 1, retuning psh's per-command wording in the same diff (`read -u N` on a closed fd already matches: `read: N: invalid file descriptor: Bad file descriptor`);"

### R2 — MINOR — 1.1 names a save/restore copy that does not exist
`grep -rn in_pipeline psh/executor` finds copies only in `control_flow.py:91-96` and `subshell.py:82-99`; `function.py` never touches it. The remaining readers the ratchet must migrate are `command.py:925` and `strategies.py:399/:605/:632`.
- Replace "delete the save/restore copies in `function.py:196`, `control_flow.py:91-92`, `subshell.py:82-83` and the false "nothing left to do" comment" with "delete the save/restore copies in `control_flow.py:91-96` and `subshell.py:82-99` and the false "nothing left to do" comment; migrate the remaining readers `command.py:925` and `strategies.py:399/:605/:632` to the one-shot token".

### R3 — MINOR — 2.3's `-c` rule is right for one cell family and wrong for two
Probed 5.3.15: the too-many-arguments cells (`exit 7 8`, `shift 1 2`, `return 1 2`) drop the rest of the input line, `$?`=2 on the next line, and `-c` exits 1 — as the brief says. But `break abc`/`continue abc` drop the line and `-c` exits **2**, and the numeric-argument cells (`exit abc`, `shift abc`, `return abc`) **continue on the same line** with rc 2 in every mode. Unregistered psh divergences inside the family 2.3 owns: `shift abc` → rc 1 (bash 2); `shift 1 2`/`return 1 2`/`exit 7 8` in script mode → rc 1 on the next line (bash 2); `break abc`/`continue abc` → psh exits 128 (bash 2).
- Replace "(serves exit/shift/return/break/continue: `$?`=2 on the next line, `-c` still exits 1)" with "(too-many-arguments cells of exit/shift/return: rest of the input line dropped, `$?`=2 on the next line, `-c` exits 1; bad-count `break`/`continue` drop the line but `-c` exits 2; numeric-argument cells `exit abc`/`shift abc`/`return abc` CONTINUE on the same line with rc 2 in every mode — probed 5.3.15; the brief ships the full probe matrix)" and register `shift abc` rc 1, script-mode next-line rc 1, and `break`/`continue abc` exit 128 as N-rows owned by 2.3 (D9).

### R4 — MINOR — Totals no longer add up after the revision
- §5 Wave 3 row "10 \| 10" → "9 \| 11"; §9 "Size: 10 slots (3.4 = 3 releases), ~13 dev-days." → "Size: 9 slots (3.4 = 3 releases; 11 releases), ~13 dev-days."
- §5 Wave 4 row "~26 \| ~26" → "~24 \| ~26"; §13 "Size: ~6 slots" → "Size: 5 slots".
- §5 "Estimated total ≈ 73 releases (v0.780.0 = Wave 0; Wave 1 starts at v0.781.0), ~96 dev-days." → "Estimated total ≈ 75 releases (v0.780.0 = Wave 0; Wave 1 starts at v0.781.0), ~101 dev-days."
- §17 "Wave 1 scope (19 slots) creeps" → "Wave 1 scope (20 slots) creeps".

### R5 — MINOR — C226 is owned by two slots; C244 is split without a stated owner
D10 requires one owner per cid. 1.0's heading says "(C226, half of C244…)" and 1.18's heading still lists "…C194, C226". §16 lists C244 under Wave 6 only.
- 1.18 heading: "(C093, C094, C095, C096, C136, C194, C226; M+, 3 d; …)" → "(C093, C094, C095, C096, C136, C194; extends the 1.0 matrix; M+, 3 d; …)".
- 1.0 heading: "(C226, half of C244; S, 1 d; lands before 1.4)" → "(C226; C244 instances, C244 itself stays owned by 6.5; S, 1 d; lands before 1.4)".

### R6 — MINOR — Wave 2 and Wave 3 exit criteria omit their user-guide rows
- §8 exit criteria: add "- User-guide row `17_differences_from_bash.md:968` corrected (2.1); `test_claims_have_tests.py` green."
- §9 exit criteria "§8.4/§17 corrected" → "§8.4 and `17_differences_from_bash.md:931` corrected".

### R7 — MINOR — 3.4b names a parameter that does not exist, so its guard is vacuous
`pure_helpers.py` calls the flag `track_quotes` (lines 69, 80, 101, 145, 152, 197, 231, 243, 249); `grep -rc quote_aware psh/lexer` is already 0 on base, so the stated guard proves nothing.
- Replace "(`quote_aware` parameter DELETED at all four call sites; `:152-154` docstring fixed)" with "(`track_quotes` parameter DELETED from `scan_double_paren_arithmetic`/`find_balanced_double_parentheses` and their call sites; `:152-154` docstring fixed)" and "`grep -c quote_aware psh/lexer` = 0" with "`grep -rc track_quotes psh/lexer` = 0 (9 hits on base)".

### R8 — MINOR — Directory-less pointers that do not resolve by name alone
For the ledger generator, give full paths: `control_structures.py:220` → `psh/parser/recursive_descent/parsers/control_structures.py:220` (`token_lexeme` call, verified); `word_scanners.py:102` → `psh/lexer/recognizers/word_scanners.py:102` (`find_closing_delimiter(text, pos + 2, '{', '}')`, verified); `input_sources.py:545` → `psh/scripting/input_sources.py:545` (`except OSError: block = b''`, verified); `pipelines.py:166-173` → `psh/parser/combinators/commands/pipelines.py:166-173` (verified); `printf_formatter.py` → `psh/utils/printf_formatter.py:152` (verified); `support/nested_parse.py` → `psh/parser/recursive_descent/support/nested_parse.py`; `dot_generator.py` → `psh/parser/visualization/dot_generator.py`. Also make 0.2's "15 only for `-o` listings" explicit as "bare `shopt -o`/`-so`/`-uo` listings and `set -o` stay 15; `shopt -o NAME` queries go to 20" (probed: `shopt -o errexit` prints width 20 on 5.3.15).

## Verified-true, no action
- Nightly figures in §1 (25,514 / 7; runs 33465565622 … 34008477403 all failed; 08-30 and 08-31 also failed but are not claimed).
- `psh -c 'echo hi' <&-` runs normally in both shells; `psh <&-` rc 0 vs bash 126 (0.3 retune premise).
- D14, C181, `trap -P`, `hash -d`, `${ }` funsub, `exit abc` continue — unchanged from the first pass and re-confirmed where re-probed.
- `test_plain_with_closed_fd0`, `test_dash_s_with_closed_fd0`, `bcontract_exit_bad_first_operand_exits_two`, `r18t2_builtins_history_write_to_stdout`, `test_declare_i_on_readonly_succeeds`, `test_attrs_only_add_integer_allowed`, `test_cd_too_many_arguments`, `test_exit_too_many_args_does_not_exit`, `test_declare_export_read_report_and_continue`, `test_for_and_function_rejected_by_both`, `test_dash_d_on_empty_table_silently_succeeds`, `_suppressed_c_mode`, `TestPosixSpecialBuiltinExit`, `posixexit_*` golden rows, `NO_ROW_PROBES`/`PARTIAL_ROW_PROBES` all exist; `oracle_policy.py`, `oracle_min`, `test_oracle_version_comments.py`, `test_no_internal_defects_corpus.py`, `numeric.py`, `pattern_words.py`, `command_head.py` are correctly new.
