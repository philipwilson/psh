[
 {
  "node_id": "tests/conformance/bash/test_error_prefix_conformance.py::test_multiline_line_number_matches_bash",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15 (`bash -c $'echo a\\necho b\\ntrap -x\\ncd /nonexistent_zz_99' psh`) stderr = 'psh: line 3: trap: -x: invalid option\\ntrap: usage: trap [-Plp] [[action] signal_spec ...]\\npsh: line 4: cd: /nonexistent_zz_99: No such file or directory\\n', rc 1. psh at HEAD prints byte-identical line 3 and line 4 prefixes/bodies but the usage line is 'trap: usage: trap [-lp] [[arg] signal_spec ...]'. Only the usage-line text differs (`[-lp]`->`[-Plp]`, `[arg]`->`[action]`); line-number tracking, exit status (1) and every other message still match.",
  "prior_expectation": "Live-oracle byte-for-byte stderr equality (`assert p.stderr == b.stderr`); the psh side pins bash 5.2's usage line via TrapBuiltin.synopsis = \"trap [-lp] [[arg] signal_spec ...]\" (psh/builtins/signal_handling.py:24). File docstring frames these cells as 'prefix-only (body already matches bash)' — that body premise held for 5.2.26 and no longer holds for 5.3.15.",
  "proposed_retune": "Test needs no edit. Change psh: psh/builtins/signal_handling.py:24 synopsis -> \"trap [-Plp] [[action] signal_spec ...]\". Verified in-process (scratch tmp/program-2026-09/usage-lines/mutation_check.py, property monkeypatched): stderr becomes 'psh: line 1: trap: -x: invalid option\\ntrap: usage: trap [-Plp] [[action] signal_spec ...]\\n' = bash 5.3 exactly. COLLATERAL (must land in the same change): (1) tests/unit/tooling/test_builtin_help_sync.py::test_no_unspec_flag_advertised will flip RED with {'trap': 'P'} because the synopsis regex `-([A-Za-z]+)` now yields {P,l,p} vs parse_flags spec 'lp' — so either implement -P (flags='lpP') or add a justified _ALLOWLIST entry; implementing is recommended since it is a real 5.3 option (observed semantics: `trap -P` with no operand -> 'trap: -P requires at least one signal name' rc 2; `-pP`/`-Pp` -> 'trap: cannot specify both -p and -P' rc 2; `-Pl`/`-lP` with an operand -> signal listing wins; `trap -P INT` prints the bare action one per operand (nothing if unset, empty line if action is ''), invalid spec -> 'NOPE: invalid signal specification' rc 1, `--` accepted, `-P EXIT 0` prints twice; unit help text OPTIONS block also needs a `-P` line). (2) Update psh-own pins of the old text: tests/unit/builtins/test_trap_flags.py:73 and tests/unit/builtins/test_error_location_prefix.py:39. (3) Cosmetic: docs/user_guide/04_builtin_commands.md:1107 `help` listing shows the old synopsis (not test-pinned).",
  "effort": "M",
  "user_guide_claim": "",
  "note": "Same root cause as the other three nodes: one psh change fixes all four. The pure expectation edit is S but is not self-contained because of the help-sync guard; -P implementation is ~20 lines in signal_handling.py plus a raw-action accessor on TrapManager. Not in CLAIM_TESTS; no docs/user_guide row mentions the error-location prefix. Not ENV: none of this family is sandbox-related (I did not re-run the orchestrator's five ENV candidates, which are outside this family)."
 },
 {
  "node_id": "tests/conformance/bash/test_error_prefix_conformance.py::test_prefix_matches_bash_exactly[trap -x]",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15 `bash -c 'trap -x' psh` stderr = 'psh: line 1: trap: -x: invalid option\\ntrap: usage: trap [-Plp] [[action] signal_spec ...]\\n', rc 2 (`help trap` first line confirms: 'trap: trap [-Plp] [[action] signal_spec ...]'). psh at HEAD: 'psh: line 1: trap: -x: invalid option\\ntrap: usage: trap [-lp] [[arg] signal_spec ...]\\n', rc 2. Prefix line and exit status identical; only the usage text differs.",
  "prior_expectation": "PREFIX_CELLS entry 'trap -x' (comment: 'builtin bad option (+ usage line)'), asserting exact stderr and returncode equality against live bash with $0=psh. psh side pinned to the bash 5.2 synopsis string in signal_handling.py:24.",
  "proposed_retune": "No test edit. psh: TrapBuiltin.synopsis -> \"trap [-Plp] [[action] signal_spec ...]\" (verified by mutation check to produce byte-identical stderr to bash 5.3). Same collateral as the first node: implement -P (flags='lpP' + the two 5.3 diagnostics + bare-action printing) or allowlist ('trap','P') in test_builtin_help_sync.py; update tests/unit/builtins/test_trap_flags.py:73 and tests/unit/builtins/test_error_location_prefix.py:39 to the new string; refresh docs/user_guide/04_builtin_commands.md:1107.",
  "effort": "M",
  "user_guide_claim": "",
  "note": "Shared fix with the other three nodes (effort M counted once). The other 14 PREFIX_CELLS cells in this parametrized function pass at HEAD against 5.3.15 — only the trap cell carries a usage line. Not in CLAIM_TESTS."
 },
 {
  "node_id": "tests/conformance/bash/test_error_prefix_conformance.py::test_usage_line_stays_unprefixed_like_bash",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15 `trap -x` second stderr line = 'trap: usage: trap [-Plp] [[action] signal_spec ...]' (unprefixed, as before). psh second line = 'trap: usage: trap [-lp] [[arg] signal_spec ...]' (also unprefixed). Assertion `plines[1] == blines[1]` fails on the text only; the property the test is actually about (usage line carries no `psh: line N:` prefix, first line identical) still holds in both shells — plines[0] == blines[0] == 'psh: line 1: trap: -x: invalid option' and the unprefixed check pass.",
  "prior_expectation": "Dual-line shape check: line 0 must equal 'psh: line 1: trap: -x: invalid option' in both shells (still true), line 1 must be equal in both (fails: old 5.2 text on the psh side), and line 1 must not start with 'psh: line' (still true).",
  "proposed_retune": "No test edit. psh synopsis -> \"trap [-Plp] [[action] signal_spec ...]\" makes plines[1] == blines[1] again (verified via mutation check). Optional hardening if the user wants this test to stay oracle-version-agnostic: assert only `plines[1].startswith('trap: usage: trap ')` and the no-prefix property, leaving exact-text equality to test_prefix_matches_bash_exactly[trap -x]. Same collateral set as the first node (help-sync guard, two unit pins, user-guide help listing).",
  "effort": "M",
  "user_guide_claim": "",
  "note": "Shared fix. The test's real subject (usage line unprefixed) is unaffected by the 5.3 change; it fails only through the incidental full-line equality."
 },
 {
  "node_id": "tests/conformance/bash/test_trap_signal_spec_conformance.py::TestTrapPosixNumericForms::test_single_invalid_operand_usage_error",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15 `trap NOTASIGNAL; echo rc=$?` -> stdout 'rc=2\\n', stderr 'trap: usage: trap [-Plp] [[action] signal_spec ...]\\n', exit 0; `trap 999; echo rc=$?` -> same usage line, 'rc=2'. psh at HEAD: stdout 'rc=2\\n' for both, stderr 'trap: usage: trap [-lp] [[arg] signal_spec ...]\\n', exit 0. Semantics (usage error, rc 2, no location prefix, shell continues) match; only the usage text differs.",
  "prior_expectation": "assert_identical_behavior('trap NOTASIGNAL; echo rc=$?') and ('trap 999; echo rc=$?') — full stdout/stderr/exit equality vs live bash (comment: 'Neither shell prefixes its usage message, so full comparison works'). Module docstring line 15 states 'Verified against bash 5.2.' psh's usage text is emitted from self.synopsis at signal_handling.py:136.",
  "proposed_retune": "No test edit (optionally update the docstring's 'Verified against bash 5.2' to 5.3). psh synopsis -> \"trap [-Plp] [[action] signal_spec ...]\"; mutation check confirms both `trap NOTASIGNAL` and `trap 999` then emit 'trap: usage: trap [-Plp] [[action] signal_spec ...]\\n' rc 2, identical to bash 5.3. Same collateral as the first node.",
  "effort": "M",
  "user_guide_claim": "Indirect only: the file bash/test_trap_signal_spec_conformance.py is the CLAIM_TESTS evidence for the 'DEBUG/ERR/RETURN traps' Full-support row (docs/user_guide/17_differences_from_bash.md:968) via marker \"trap 'echo RET' RETURN\" (lines 194/198/202 of the file, a different test function); this failing test is not the mapped marker. The 'trap command | Yes | Yes | Standard signals + EXIT/DEBUG/ERR/RETURN' row (line 966) is not a Full-support row and is not CLAIM_TESTS-gated.",
  "note": "Shared fix with the three error-prefix nodes. Other pins of these commands (tests/unit/builtins/test_signal_builtins.py:305-306, tests/integration/job_control/test_signal_handling.py:191) only check rc==2 and are unaffected."
 },
 {
  "node_id": "tests/conformance/bash/test_cmdsub_errexit_conformance.py::TestInheritErrexitShopt::test_shopt_query_states",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15: `shopt inherit_errexit` -> stdout 'inherit_errexit     \\toff' (name left-justified in a 20-char field: 15 chars + 5 spaces, then tab), exit 1; `shopt -s inherit_errexit; shopt inherit_errexit` -> 'inherit_errexit     \\ton', exit 0. Full `shopt` listing census: 58 names at width 20, only the 23-char no_empty_cmd_completion unpadded => fixed %-20s. Bare `shopt -o`/`-so`/`-uo` and `set -o` remain width 15.",
  "prior_expectation": "psh at HEAD emits 'inherit_errexit\\toff' (no padding at all: f\"{name:<15}\" and the name is exactly 15 chars), exit 1 (observed). Test was green at the 2026-08-09 gate vs bash 5.2.26 and psh's comment (shell_options.py:191) says 'bash left-justifies the option name in a 15-char field', so 5.2.26 printed width 15; 5.2 is not installed here to re-run (deduced, not observed). File docstring stamps 'Verified against bash 5.2.' Pytest diff: PSH stdout='inherit_errexit\\toff\\n' exit=1 vs Bash stdout='inherit_errexit     \\toff\\n' exit=1 (second assert not reached).",
  "proposed_retune": "No edit to this test (live compare). psh change in psh/builtins/shell_options.py `_print_option`: non-reusable print width 15 -> 20 for the shopt table (all `shopt NAME` and `shopt -o NAME` queries, bare `shopt`/`shopt -s`/`shopt -u` listings); keep 15 ONLY for o_mode bare listings (`shopt -o`, `-so`, `-uo`) — e.g. add `width: int = 20` param and have `_list` pass `width=15 if o_mode else 20`; rewrite the 15-char comment at lines 191-193. Leave `set -o` (psh/builtins/environment.py:568) at 15 (bash 5.3 unchanged there). Validated on a scratch copy tmp/program-2026-09/shopt-query/patched: this command and 14 other shopt/set -o forms identical to bash 5.3.15. Collateral expectation edits (width 15->20): tests/unit/builtins/test_shopt.py:79; tests/unit/builtins/test_shopt_set_o.py:23,29,191,207,213,249 (keep :64 `shopt -o` and :219 `shopt -so` at 15); tests/behavioral/golden_cases.yaml:2378 -> \"nocasematch         \\toff\\n1\\n\", :8910 -> \"extglob             \\toff\\nrc=1\\nrc=1\\n\"; tests/unit/expansion/test_braceexpand_option.py:211 (`set -o`) unchanged. Also refresh the 'Verified against bash 5.2.' docstring stamp.",
  "effort": "M",
  "user_guide_claim": "Not in CLAIM_TESTS (no hit for this file/class/command). The `| shopt options | Yes | Partial | ... inherit_errexit ... |` row (docs/user_guide/17_differences_from_bash.md:978) is a Partial row backed by PARTIAL_ROW_PROBES['shopt options']='shopt -s lastpipe ...' (test_claims_have_tests.py:146-147), not by this test.",
  "note": "Not ENV: fails identically in-sandbox with the same padding diff; the orchestrator's ENV-confirmation list (test_bg_actually_resumes etc.) is outside this family and was not run here. Semantics (exit 1/0, inherit_errexit effect on cmdsub) unchanged in 5.3. bash 5.3 CHANGES has no entry for the width change; evidence is empirical. Incidental, out of scope: `shopt expand_aliases` differs (bash off / psh on in -c mode) — pre-existing and declared at 17_differences_from_bash.md:400."
 },
 {
  "node_id": "tests/conformance/bash/test_locale_conformance.py::TestRangesAndGlobasciiranges::test_shopt_globasciiranges_query",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15 (env C): `shopt globasciiranges` -> stdout 'globasciiranges     \\ton' (15-char name padded to 20, then tab), exit 0. Option is still recognized and on by default. Fixed %-20s across the whole shopt table (only 23-char no_empty_cmd_completion unpadded).",
  "prior_expectation": "psh at HEAD emits 'globasciiranges\\ton' exit 0 (no padding: 15-char name in a 15-char field), observed. Test comment: 'bash reports it `on`; psh used to error \"invalid shell option name\"'. Green at the 2026-08-09 gate vs 5.2.26, so 5.2.26 printed width 15 (deduced from the green gate + psh's mirror comment; 5.2 not installed). Pytest diff: PSH stdout='globasciiranges\\ton\\n' exit=0 vs Bash stdout='globasciiranges     \\ton\\n' exit=0.",
  "proposed_retune": "No edit to this test (live compare). Same single psh change as the family: psh/builtins/shell_options.py `_print_option` width 15 -> 20 for shopt-table prints (queries + bare shopt/-s/-u listings), keep 15 for o_mode bare listings; leave environment.py:568 `set -o` at 15. Validated on the scratch copy: `shopt globasciiranges` identical to bash 5.3.15. Collateral edits as listed for test_shopt_query_states (test_shopt.py:79; test_shopt_set_o.py:23,29,191,207,213,249; golden_cases.yaml:2378,8910).",
  "effort": "M",
  "user_guide_claim": "Not in CLAIM_TESTS. Narrative mentions only: docs/user_guide/17_differences_from_bash.md:54 ('shopt globasciiranges # ON by default (bash 5)') and :581 ('`shopt globasciiranges` is recognized and defaults on') — not a compatibility-table row.",
  "note": "Not ENV. The locale semantics the class exists for ([a-z] ranges ASCII-bound, `shopt -s globasciiranges` rc 0) are untouched; only the query line's padding moved. The sibling test_shopt_set_globasciiranges_ok (`shopt -s globasciiranges; echo $?`) does not print the table and still passes."
 },
 {
  "node_id": "tests/conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_after_unset_exits_1",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15: `shopt -s nocasematch; shopt -u nocasematch; shopt nocasematch; echo $?` -> stdout 'nocasematch         \\toff\\n1\\n' (11-char name padded to 20), exit 0. Query exit status after unset is still 1 — the semantics this class pins are unchanged.",
  "prior_expectation": "psh at HEAD emits 'nocasematch    \\toff\\n1\\n' (padded to 15), observed. Docstring premise: query form exits 0 when set / 1 when unset (reappraisal #6 bug L6 fix) — still true in 5.3. Green at the 2026-08-09 gate vs 5.2.26, so 5.2.26 printed width 15 (deduced; 5.2 not installed). Pytest diff: PSH stdout='nocasematch    \\toff\\n1\\n' vs Bash stdout='nocasematch         \\toff\\n1\\n', both exit=0.",
  "proposed_retune": "No edit to this test (live compare). Same family psh change: shell_options.py `_print_option` width 15 -> 20 for shopt-table prints, 15 kept only for o_mode bare listings; environment.py:568 unchanged. Validated on the scratch copy: this exact command identical to bash 5.3.15. Collateral: test_shopt.py:79; test_shopt_set_o.py:23,29,191,207,213,249; golden_cases.yaml:2378 (`shopt_query_unset_exit_1`) -> \"nocasematch         \\toff\\n1\\n\" and :8910 -> \"extglob             \\toff\\nrc=1\\nrc=1\\n\".",
  "effort": "M",
  "user_guide_claim": "Not in CLAIM_TESTS. The `| shopt options | Yes | Partial | ... nocasematch ... |` row (17_differences_from_bash.md:978) is backed by PARTIAL_ROW_PROBES['shopt options'] (lastpipe probe), not by this class.",
  "note": "Not ENV. The golden case golden_cases.yaml:2377-2378 pins the width-15 stdout for the sibling command, so the --compare-bash phase will also be red against 5.3.15 until that row is retuned alongside the psh change."
 },
 {
  "node_id": "tests/conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_set_exits_0",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15: `shopt -s nocasematch; shopt nocasematch; echo $?` -> stdout 'nocasematch         \\ton\\n0\\n' (name padded to 20), exit 0. Query exit status when set is still 0.",
  "prior_expectation": "psh at HEAD emits 'nocasematch    \\ton\\n0\\n' (padded to 15), observed. Green at the 2026-08-09 gate vs 5.2.26, so 5.2.26 printed width 15 (deduced; 5.2 not installed). Pytest diff: PSH stdout='nocasematch    \\ton\\n0\\n' vs Bash stdout='nocasematch         \\ton\\n0\\n', both exit=0.",
  "proposed_retune": "No edit to this test (live compare). Same family psh change: shell_options.py `_print_option` width 15 -> 20 for shopt-table prints (keep 15 for `shopt -o`/`-so`/`-uo` bare listings; `set -o` untouched). Validated on the scratch copy: this exact command identical to bash 5.3.15. Collateral edits as listed for test_query_after_unset_exits_1.",
  "effort": "M",
  "user_guide_claim": "Not in CLAIM_TESTS; `shopt options` Partial row (17_differences_from_bash.md:978) is backed by a different probe.",
  "note": "Not ENV. Sibling test_print_form_set_exits_0 (`shopt -p nocasematch`) prints the reusable form 'shopt -u nocasematch' which is unaffected in 5.3.15 (observed identical) and still passes."
 },
 {
  "node_id": "tests/conformance/bash/test_nocasematch_conformance.py::TestShoptQueryExitCode::test_query_unset_exits_1",
  "category": "FORMAT",
  "bash53_behavior": "bash 5.3.15: `shopt nocasematch; echo $?` -> stdout 'nocasematch         \\toff\\n1\\n' (name padded to 20), exit 0. Query exit status when unset is still 1.",
  "prior_expectation": "psh at HEAD emits 'nocasematch    \\toff\\n1\\n' (padded to 15), observed. Green at the 2026-08-09 gate vs 5.2.26, so 5.2.26 printed width 15 (deduced; 5.2 not installed). Pytest diff: PSH stdout='nocasematch    \\toff\\n1\\n' vs Bash stdout='nocasematch         \\toff\\n1\\n', both exit=0. tests/behavioral/golden_cases.yaml:2377-2378 (`shopt_query_unset_exit_1`) pins this exact command with the width-15 stdout.",
  "proposed_retune": "No edit to this test (live compare). Same family psh change: shell_options.py `_print_option` width 15 -> 20 for shopt-table prints (keep 15 for o_mode bare listings; environment.py:568 `set -o` untouched). Validated on the scratch copy: this exact command identical to bash 5.3.15. Must co-land golden_cases.yaml:2378 -> stdout: \"nocasematch         \\toff\\n1\\n\" (and :8910 -> \"extglob             \\toff\\nrc=1\\nrc=1\\n\"), plus unit pins test_shopt.py:79 and test_shopt_set_o.py:23,29,191,207,213,249 (keep :64 and :219 at 15).",
  "effort": "M",
  "user_guide_claim": "Not in CLAIM_TESTS; `shopt options` Partial row (17_differences_from_bash.md:978) is backed by PARTIAL_ROW_PROBES, not this test.",
  "note": "Not ENV. Whole family is one psh one-liner plus expectation edits; no bash 5.3 semantic change involved. Scratch validation artifacts: tmp/program-2026-09/shopt-query/pytest-run.txt and tmp/program-2026-09/shopt-query/patched/ (untracked scratch only; no tracked file modified)."
 },
 {
  "node_id": "tests/system/test_stdin_startup_robustness.py::TestClosedFd0Startup::test_plain_with_closed_fd0",
  "category": "SEMANTIC",
  "bash53_behavior": "Reproduced (1 failed): `assert r.returncode == b.returncode` -> `AssertionError: assert 0 == 126`, bash side `CompletedProcess(args=['/opt/homebrew/bin/bash'], returncode=126, stdout=b'', stderr=b'/opt/homebrew/bin/bash: error creating buffered stream: Bad file descriptor\\n')`. Direct probes (fresh mktemp cwd, env -u PWD -u OLDPWD): bash 5.3.15 `bash <&-` -> stderr `/opt/homebrew/bin/bash: error creating buffered stream: Bad file descriptor`, rc=126; `bash -s <&-` -> same message, rc=126; `bash -c 'echo hi' <&-` -> `hi` rc=0; `bash ./s.sh <&-` -> `fromscript` rc=0; `bash -c 'echo $(echo hi)' <&-` -> `hi` rc=0; `bash < /dev/null` and `bash -s < /dev/null` -> rc=0 (only a CLOSED fd 0 triggers it, not an open-empty one). psh at HEAD: `psh <&-` -> rc=0 no output; `psh -s <&-` -> rc=0; `-c`/script-file -> same as bash (rc=0). None of the 15 bash-5.3 patch Bug-Descriptions (fetched bash53-001..015 from ftp.gnu.org) mention stdin/buffered streams, so this is base bash-5.3 behavior, not a 5.3.x patch; nearest CHANGES entry (5.3-rc1->rc2, line 88) is `o. Fix a potential bug that could result in a NULL buffered stream even with a valid input file descriptor.`",
  "prior_expectation": "Test docstring: \"`exec 0<&-; psh` (no -c): non-interactive, nothing to read, exit 0.\" asserting `r.returncode == 0` AND `r.returncode == b.returncode`. Module docstring (R18 T1-1): \"A closed/absent stdin is now simply non-interactive, exactly like bash (exit 0).\" psh/__main__.py comment in the SourceKind.STDIN branch: \"A closed/empty fd 0 simply yields no commands (exit 0).\" All pinned against bash 5.2 (attested 2026-08-09 gate, gate_attestation.json gated_commit 72a2da8d).",
  "proposed_retune": "psh change (psh/__main__.py, SourceKind.STDIN non-interactive branch, just before `ProgramSource.stdin_script()`): if `sys.stdin is None` (CPython sets it None when fd 0 is closed at startup) write `psh: error creating buffered stream: Bad file descriptor\\n` to stderr and `sys.exit(126)` — prefix `psh: ` matches psh's existing startup-error convention (`psh: /nonexistent/script.sh: No such file or directory` vs bash `/opt/homebrew/bin/bash: /nonexistent/script.sh: ...`). Leave the `-c`, script-file, and `< /dev/null` (open-but-empty) paths untouched (bash 5.3 still exits 0 there). Test edits: test_plain_with_closed_fd0 `assert r.returncode == 0` -> `== 126` (keep `== b.returncode`; optionally also assert `b'error creating buffered stream: Bad file descriptor' in r.stderr and in b.stderr`); test_dash_s_with_closed_fd0 `assert r.returncode == 0` -> `== 126` (bash 5.3 `-s <&-` is also 126); update the module docstring sentence to \"...simply non-interactive with no stream to read: bash 5.3 exits 126 with 'error creating buffered stream: Bad file descriptor' (bash 5.2 exited 0)\" and the __main__.py comment likewise. S-alternative if the user prefers not to imitate a low-level bash diagnostic: keep psh at exit 0, change the test to `assert r.returncode == 0` only and drop the bash comparison, and record it as a declared divergence.",
  "effort": "M",
  "user_guide_claim": "none — not in CLAIM_TESTS (tests/conformance/test_claims_have_tests.py has no entry for test_stdin_startup_robustness) and no docs/user_guide/ row references closed-fd-0 startup.",
  "note": "Sibling PSH_BUG found while probing (not one of the four node ids): `psh -c 'read x; echo rc=$?' <&-` under PSH_STRICT_ERRORS=1 raises an internal defect — `AttributeError: 'NoneType' object has no attribute 'fileno'` at psh/builtins/input_reader.py:429 (`stream.fileno()` on sys.stdin=None) then `ValueError: InputCursor needs exactly one of fd or stream` at input_reader.py:124; with strict-errors off the user sees `psh: line 1: read: InputCursor needs exactly one of fd or stream` rc=1. bash 5.3.15 prints `bash: line 1: read: 0: read error: Bad file descriptor` and read returns 1. Independent of the oracle version; worth a separate ticket. The oracle-version story is exactly as the orchestrator stated: 5.2 -> exit 0, 5.3 -> 126."
 },
 {
  "node_id": "tests/behavioral/test_golden_behavior.py::test_golden[r18t2_builtins_history_write_to_stdout]",
  "category": "ENV",
  "bash53_behavior": "PASSES in isolation from my (unsandboxed — no sandbox-exec in the process tree, AF_UNIX bind succeeds) Bash tool: `1 passed in 0.40s`. Gate-run failure text from docs/reviews/evidence/fresh_appraisal_2026_09_06/canonical-tests.txt: `AssertionError: stdout mismatch for 'r18t2_builtins_history_write_to_stdout' ... expected: 'x\\n' got: ''`. The row is `psh_only: true`, and test_golden_behavior.py:157-158 does `pytest.skip(\"case marked psh_only\")` on the bash-comparison path, so bash is never consulted; the oracle version cannot affect it. Direct psh probe (fresh cwd): `psh -c 'set -o history; history -s x; history -w /dev/stdout'` -> `x` rc=0, and with stdout redirected to a regular file (the harness's file-backed capture shape) -> out=[x] rc=0 err=[]. Reference only: bash 5.3.15 same command -> rc=1, empty stdout/stderr (why the row is psh_only). Mechanism reproduced: under a sandbox-exec profile that denies file-write on /dev, the same psh command yields `rc=1 out=[] err=[psh: line 1: history: /dev/stdout: cannot access history file]` — opening `/dev/stdout` by path (a /dev/fd/1 re-open) is a path-permission check the sandbox refuses, producing exactly the gate's `got: ''`. (A pytest-level run under that profile INTERNALERRORs earlier because pytest's logging plugin opens /dev/null: `PermissionError: [Errno 1] Operation not permitted: '/dev/null'`.)",
  "prior_expectation": "golden_cases.yaml:6678-6683: command `set -o history; history -s x; history -w /dev/stdout`, stdout `x\\n`, stderr ``, exit_code 0, psh_only: true.",
  "proposed_retune": "None — expectation is correct and psh matches it whenever the process may open /dev/stdout by path. Run the gate outside the filesystem sandbox (validation.md line 29-33 already records the unsandboxed rerun passing). If the sandbox must stay, the only in-tree option is to mark the case as requiring /dev/fd access (e.g. a `requires_dev_fd: true` key skipped when a probe open of /dev/stdout fails), but that is a harness-policy decision, not a retune of the pin.",
  "effort": "S",
  "user_guide_claim": "none — golden cases are not mapped in CLAIM_TESTS and no docs/user_guide row cites this case.",
  "note": "Confirms the orchestrator's ENV observation. The evidence file's own note says: \"Golden case `r18t2_builtins_history_write_to_stdout`: initial `/dev/stdout` output mismatch\" and that it passed on the unsandboxed rerun."
 },
 {
  "node_id": "tests/conformance/bash/test_cv_carry_characterization.py::TestExecutableSpecialFileEarlier::test_socket_earlier_bash_126_psh_runs_later",
  "category": "ENV",
  "bash53_behavior": "PASSES in isolation unsandboxed: `1 passed in 0.47s`. Gate-run failure (canonical-tests.txt:433-438): `tests/conformance/bash/test_cv_carry_characterization.py:250: in test_socket_earlier_bash_126_psh_runs_later  s.bind(\"cvs\")  PermissionError: [Errno 1] Operation not permitted` — the test's own AF_UNIX socket setup, before either shell runs. Reproduced byte-for-byte under `sandbox-exec -f` with profile `(version 1)(allow default)(deny network*)`: `E PermissionError: [Errno 1] Operation not permitted` at line 250. Underlying command re-run directly (fresh cwd, socket earlier on PATH, real script later): bash 5.3.15 -> stdout `rc=126`, stderr `/opt/homebrew/bin/bash: line 1: /var/folders/.../sock/cvs: Permission denied` (bash execve's the executable-bit socket, tier-1 access(X_OK)); psh -> stdout `LATE\\nrc=0` (psh's tier-1 requires a regular file and runs the later real executable). Both exactly as the test asserts, so the bash-5.2-verified premise still holds on 5.3.15.",
  "prior_expectation": "Docstring: \"Carry #30 (CV2 R3, DESIRABLE deviation) ... bash's tier-1 is access(X_OK) on ANY type, so it takes the special file and execve's it — a FIFO HANGS, a socket fails 126. psh's tier-1 requires a REGULAR FILE ... bash 5.2-verified.\" Asserts `\"rc=126\" in b.stdout` and `\"LATE\" in p.stdout and \"rc=0\" in p.stdout`.",
  "proposed_retune": "None to the assertions. Optional S doc touch: change \"bash 5.2-verified\" to \"bash 5.2- and 5.3.15-verified\". If the gate must run sandboxed, guard the setup: wrap `s.bind(\"cvs\")` in `try/except PermissionError: pytest.skip(\"AF_UNIX bind denied by sandbox\")` — a skip, not an expectation change.",
  "effort": "S",
  "user_guide_claim": "none — not in CLAIM_TESTS and no docs/user_guide row references this test or carry #30 by test name.",
  "note": "Pure sandbox artifact: macOS seatbelt `(deny network*)` covers AF_UNIX bind. The declared psh divergence (run the later regular executable instead of the socket) and bash's 126 are both unchanged on 5.3.15."
 },
 {
  "node_id": "tests/unit/tooling/test_shell_oracle_harness.py::test_cap_kill_reaches_a_writer_that_left_the_process_group",
  "category": "ENV",
  "bash53_behavior": "PASSES in isolation unsandboxed: `1 passed in 0.10s`. The test runs `SH = \"/bin/sh\"` (test module line 44; `/bin/sh --version` = `GNU bash, version 3.2.57(1)-release`), NOT the 5.3.15 oracle, so the oracle version is irrelevant. Gate-run failure (canonical-tests.txt:724-728): `AssertionError: writer 14280 left the shell's process group and survived the cap kill; it is still filling an unlinked capture file`. Reproduced with the identical assertion (`writer 28220 left the shell's process group and survived the cap kill...`, `1 failed in 5.09s`) under a deny-default sandbox-exec profile. Mechanism: shell_oracle.py `_descendant_pids` (line 319-353) enumerates via `subprocess.run([\"ps\", \"-eo\", \"pid=,ppid=\"])` and by design returns [] on any spawn failure (\"a cleanup helper must never raise into a test\"); under the sandbox `ps` itself is refused — observed `sandbox-exec: execvp() of 'ps' failed: Operation not permitted` — so the escaped `yes` group is never swept, the test's own final cleanup `os.kill(escaped_pid, 9)` runs, and it raises. Unsandboxed, `ps -eo pid=,ppid=` works and the sweep kills the writer within the 5 s window.",
  "prior_expectation": "Docstring: \"RED before the descendant sweep in `_killpg_sigkill`, green after.\" Expects `OutputLimitExceeded` and that the escaped `yes` (own pgid under `set -m`) is dead within 5 s of the cap kill.",
  "proposed_retune": "None to the assertion — the sweep works wherever `ps` can be exec'd. Run the gate unsandboxed (validation.md line 32 records the unsandboxed pass: \"initial process-discovery/cleanup probe failed; the test's own final cleanup kills the writer before raising\"). If sandboxed gating is required, add a module-level skip when `ps -eo pid=,ppid=` cannot be spawned (`pytest.mark.skipif` on a probe `subprocess.run([\"ps\",...])` raising OSError), or make `_descendant_pids` fall back to a `sysctl`/`os.listdir('/proc')`-free method — but on macOS there is no /proc, so a `ps`-free fallback is real work.",
  "effort": "S",
  "user_guide_claim": "none — harness self-test, not in CLAIM_TESTS, no user-guide row.",
  "note": "Confirms ENV. Note the failure mode is benign on disk: the test's own cleanup SIGKILLs the writer before raising, so a sandboxed gate run does not leave a disk-filling `yes` behind. Regarding the orchestrator's mention of `test_bg_actually_resumes...`: it is not in this family's node list and was not run here; validation.md line 34 records it as a `ps`-based precondition failure under the sandbox that passed unsandboxed, i.e. the same `ps`-denied mechanism."
 },
 {
  "node_id": "tests/conformance/bash/test_variable_projection_reads_conformance.py::TestNotFoundMessageVariableTruth::test_explicit_empty_path_is_no_such_file",
  "category": "SEMANTIC",
  "bash53_behavior": "`PATH= nosuchcmd 2>&1; echo rc=$?` -> '/opt/homebrew/bin/bash: line 1: nosuchcmd: command not found' + 'rc=127'. Same for `PATH=; nosuchcmd`, `PATH=\"\"`, `export PATH=`, `f(){ local PATH=; nosuchcmd; }`, `PATH= command -p nosuchcmd`, `PATH=; zcwdcmd | cat` (-> 'cat: command not found'). UNCHANGED from 5.2: `unset PATH; zzznope` and `f(){ local PATH; nosuchcmd; }` (declared-unset) still say 'nosuchcmd: No such file or directory' rc=127; `PATH=/nope-xyz` and `PATH=:` misses still 'command not found'. Cause: bash 5.3 CHANGES (5.3-alpha section, /opt/homebrew/Cellar/bash/5.3.15/CHANGES:848) 'p. Treat a NULL value for $PATH as equivalent to \".\".' -- an empty PATH is now a cwd search that missed (bash 5.3 runs a cwd executable under `PATH=`: 'RAN-CWD' rc=0; psh already does too). psh today: 'psh: line 1: nosuchcmd: No such file or directory' rc=127.",
  "prior_expectation": "Test asserts psh==bash via _assert_same (prefix-stripped). Under bash 5.2 both empty AND unset PATH gave 'No such file or directory'. Module docstring: 'Probed against bash 5.2 (tmp/boundary-ledgers/CV-probes/cv2_matrix.sh)'; class docstring: 'a `local PATH` shadow yields the empty-PATH wording'. psh implements the 5.2 rule in psh/executor/strategies.py:148 `empty_path = not state.get_variable('PATH')` and format_exec_failure docstring lines 104-111 ('probe-verified bash 5.2 ... the discriminator is emptiness').",
  "proposed_retune": "psh change (bash 5.3 rule): in psh/executor/strategies.py#report_exec_failure replace `empty_path = not state.get_variable('PATH')` with `unset_path = not state.scope_manager.lookup('PATH').is_set` (VariableLookup.is_set is True only for LookupStatus.VALUE, incl. the empty string; MISSING and PRESENT_UNSET/`local PATH` -> 'No such file or directory'); rename the `empty_path` kwarg of format_exec_failure to `unset_path` and rewrite its docstring lines 104-111: 'bash 5.3 treats a NULL PATH as \".\" (CHANGES 5.3-alpha p.), so an empty PATH is a cwd search that misses -> \"command not found\"; only an UNSET PATH (MISSING or declared-unset `local PATH`) is \"No such file or directory\"'. Test: rename to `test_explicit_empty_path_is_command_not_found` (the _assert_same call stays byte-identical), add a `f(){ local PATH=; nosuchcmd 2>&1; echo rc=$?; }; f` row (5.3: 'command not found'), keep test_local_unset_path_is_empty_path_message but retitle its comment 'unset/declared-unset PATH -> No such file'; module docstring 'bash 5.2' -> 'bash 5.3.15', class docstring 'empty-PATH wording' -> 'unset-PATH wording'.",
  "effort": "M",
  "user_guide_claim": "none — not in CLAIM_TESTS (tests/conformance/test_claims_have_tests.py has no key mapping this file); no compatibility-table row covers the 127 message wording.",
  "note": "Sibling tests test_local_unset_path_is_empty_path_message and test_normal_path_is_command_not_found_message PASS today (36/40 of the four classes green). Same psh fix closes test_command_resolution_r3 node below and the same-class divergences `PATH= command -p nosuchcmd` / `PATH=; cmd | cat`. Pre-existing, out-of-scope divergences seen while probing: `unset PATH; type zcwdcmd` bash prints the absolute path, psh './zcwdcmd'; `PATH= env zcwdcmd` bash 'env: command not found' (env not on the '.' path) vs psh runs its env builtin. Probe transcripts: /Users/pwilson/src/psh/tmp/program-2026-09/resolution-hash-export/probe.out, probe2.out, probe3.out."
 },
 {
  "node_id": "tests/unit/executor/test_command_resolution_r3.py::TestEmptyPathNotFoundMessage::test_empty_path_says_no_such_file",
  "category": "SEMANTIC",
  "bash53_behavior": "`PATH= zzznope 2>&1; echo rc=$?` -> '/opt/homebrew/bin/bash: line 1: zzznope: command not found' + 'rc=127' (bash 5.3 CHANGES:848 'Treat a NULL value for $PATH as equivalent to \".\"'). The three sibling rows are unchanged in 5.3 and still pass: `unset PATH; zzznope` -> 'No such file or directory'; `PATH=/nope-xyz zzznope` and `PATH=: zzznope` -> 'command not found'. psh today: 'psh: line 1: zzznope: No such file or directory' rc=127. Assertion diff: '- SH: line N: zzznope: command not found / + SH: line N: zzznope: No such file or directory'.",
  "prior_expectation": "Class docstring: 'bash reports a bare-name miss under an EMPTY or UNSET PATH as No such file or directory (rc 127), not command not found'. Test pins psh-side `assert 'No such file or directory' in p.stdout` then prefix-normalized equality with live bash (`run_bash(['--norc','--noprofile','-c', ...])`).",
  "proposed_retune": "Depends on the psh change in the node above (strategies.py:148 -> tri-state `not state.scope_manager.lookup('PATH').is_set`). Test edit: rename to `test_empty_path_says_command_not_found`; change the psh-side pin to `assert 'command not found' in p.stdout`; keep the normalized bash equality. Class docstring -> 'bash 5.3 (CHANGES 5.3-alpha p.: a NULL PATH is \".\") reports an EMPTY-PATH miss as \"command not found\" (a cwd search that missed, like PATH=:); only an UNSET PATH (incl. declared-unset `local PATH`) is \"No such file or directory\" (rc 127 both).' test_unset_path_says_no_such_file stays as is (verified: 5.3 unchanged).",
  "effort": "M",
  "user_guide_claim": "none — unit test, not in CLAIM_TESTS.",
  "note": "Must land in the same change as the conformance node above and the strategies.py discriminator flip; after the psh fix this test's own edit is S. psh/executor/strategies.py docstrings (lines 96-111, 144-148) carry the bash 5.2 premise verbatim and should be rewritten per the no-sketch rule, not left stale."
 },
 {
  "node_id": "tests/conformance/bash/test_hash_conformance.py::TestHashBuiltin::test_dash_d_deletes",
  "category": "SEMANTIC",
  "bash53_behavior": "Third sub-assertion `hash -d nosuchcmd_xyz; echo rc=$?` (EMPTY table) -> stderr '/opt/homebrew/bin/bash: line 1: hash: nosuchcmd_xyz: not found', stdout 'rc=1'. Also `hash -d ls` on an empty table -> 'hash: ls: not found' rc=1; `hash -d a b` -> two 'not found' lines, rc=1. Cause: bash 5.3 CHANGES (5.3-alpha section, CHANGES:614) 'ggggg. Fix `hash' to return 1 if -d is supplied and the hash table is empty.' psh today: no message, 'rc=0'. First two sub-assertions (`hash ls cat; hash -d ls; hash` and populated-table miss `hash ls; hash -d nosuchcmd_xyz 2>/dev/null; echo rc=$?` -> rc=1) match and pass.",
  "prior_expectation": "Test comment: '# EMPTY table: -d silently succeeds (bash quirk)'; module docstring '(bash 5.2, probed 2026-06-13)'. psh implements the quirk in psh/builtins/hash_builtin.py:80-83 (`# bash quirk (probe-verified): -d against an EMPTY table silently succeeds ... if len(table) == 0: return 0`) and documents it in the module docstring lines 15-16 ('except against an EMPTY table, which silently succeeds, rc 0').",
  "proposed_retune": "psh change: delete the `if len(table) == 0: return 0` short-circuit and its comment in psh/builtins/hash_builtin.py:80-83 (the existing loop already emits `hash: NAME: not found` and returns 1); fix the module docstring lines 15-16 to 'hash -d NAME... forgets each name (missing: `not found`, rc 1 — also against an empty table, bash 5.3 CHANGES ggggg)'. Test: the conformance framework does NOT normalize the shell-name stderr prefix, so per this file's stated convention change the third row to `'hash -d nosuchcmd_xyz 2>/dev/null; echo rc=$?'` and its comment to '# EMPTY table: bash 5.3 reports the miss too (CHANGES 5.3-alpha ggggg), rc 1'; module docstring 'bash 5.2, probed 2026-06-13' -> 'bash 5.3.15'. Collateral unit pin that will go RED after the psh fix: tests/unit/builtins/test_hash_builtin.py::TestHashBuiltin::test_dash_d_on_empty_table_silently_succeeds (lines 128-133) -> invert to `assert rc == 1` and `'hash: nosuchcmd_zz9: not found' in captured_shell.get_stderr()`, rename `test_dash_d_on_empty_table_reports_miss` (27/27 in that file pass today).",
  "effort": "M",
  "user_guide_claim": "none — no `hash` row in docs/user_guide/17_differences_from_bash.md compatibility table (only 'shopt options ... checkhash'); CLAIM_TESTS has no hash key.",
  "note": "bash 5.3 explicitly calls the 5.2 behavior a bug, so psh should follow rather than declare a divergence. `hash -d` with no names ('option requires an argument', rc 1) and `hash -t NAME` misses already match. Probe transcript: /Users/pwilson/src/psh/tmp/program-2026-09/resolution-hash-export/probe.out."
 },
 {
  "node_id": "tests/conformance/bash/test_export_env_sync_conformance.py::TestExportAttributeLifecycle::test_declare_i_on_readonly_succeeds",
  "category": "SEMANTIC",
  "bash53_behavior": "`readonly R=1; declare -i R; echo \"rc=$?\"` -> stderr '/opt/homebrew/bin/bash: line 1: declare: R: readonly variable', stdout 'rc=1'; `declare -p R` afterwards is still `declare -r R=\"1\"` (attribute NOT added). Cause: bash 5.3 CHANGES (5.3-alpha section, CHANGES:705) 'llllll. Fixed a bug that allowed attribute changes to readonly variables that changed the effects of attempted assignments.' Observed matrix on a readonly var in 5.3.15 — REFUSED (rc 1, 'declare: R: readonly variable', no change): -i, +i, -l, +l, -u, +u, -c, -a, +a, -A, -n, +n, and any combined set containing one (-ir, -ix, -xi); `typeset -i R` -> 'typeset: R: readonly variable'; `f(){ local -r x=1; local -i x; }` -> 'local: x: readonly variable' rc 1, `declare -r x=\"1\"`. STILL ALLOWED (rc 0): -x, +x, -t, +t, -r, -g, bare `declare R`, `export R` (-> `declare -rx R=\"1\"`), `export -n R`, `readonly R`, `local -x x`/`local x` on a readonly local. Multi-name `declare -i R S=2` -> R refused, S still gets -i, rc 1. psh today: rc=0 and `declare -ir R=\"1\"` for every refused form (only -a/-A/+r already refused).",
  "prior_expectation": "Test comment: '# readonly forbids changing the VALUE, not the metadata'; module docstring 'pinned to bash 5.2'. psh encodes the 5.2 premise verbatim in psh/core/scope.py#ScopeManager.apply_attribute (line 1290) docstring: 'Readonly variables ACCEPT new attributes — readonly forbids changing the value, not the metadata (bash 5.2, probe-verified: `readonly R=1; export R` and `readonly R=1; declare -i R` both succeed)'; remove_attribute at line 1363 likewise.",
  "proposed_retune": "psh change: in ScopeManager.apply_attribute / remove_attribute (psh/core/scope.py:1290/1363), after nameref resolution, raise ReadonlyVariableError(name) when the target is readonly and the added/removed set intersects `VarAttributes.INTEGER | LOWERCASE | UPPERCASE | ARRAY | ASSOC_ARRAY | NAMEREF` (psh/core/variables.py:17-22); EXPORT/TRACE/READONLY changes stay allowed. The declare loop at psh/builtins/function_support.py:299-310 already renders `declare: NAME: readonly variable` (or `typeset:`) and continues with the next name; the `local` builtin's attrs-only path must render `local: NAME: readonly variable` rc 1. Rewrite the apply_attribute docstring to the 5.3 rule (CHANGES 5.3-alpha llllll). Test: rename `test_declare_i_on_readonly_fails`, comment '# bash 5.3 (CHANGES llllll): attribute changes that alter assignment effects are refused on a readonly, rc 1; -x/-t/-r/plain still succeed'; because the framework compares raw stderr (prefix differs), use `'readonly R=1; declare -i R 2>/dev/null; echo \"rc=$?\"; declare -p R'` (5.3: 'rc=1' / `declare -r R=\"1\"`), and add the allowed-half pin `'readonly R=1; declare -x R; echo \"rc=$?\"; declare -p R'` (rc=0, `declare -rx R=\"1\"`). Module docstring 'bash 5.2' -> 'bash 5.3.15'. Collateral unit pin that goes RED after the fix: tests/unit/builtins/test_local_builtin.py::test_attrs_only_add_integer_allowed (lines 200-206, `f(){ local -r x=1; local -i x; declare -p x; }; f` expects rc 0 / `declare -ir x=\"1\"`) -> invert to rc 1, stderr 'local: x: readonly variable', stdout `declare -r x=\"1\"`, rename `test_attrs_only_add_integer_refused`.",
  "effort": "M",
  "user_guide_claim": "none mapped — CLAIM_TESTS maps only 'set -o allexport' to this file (different command). Nearest unmapped table row: docs/user_guide/17_differences_from_bash.md:927 '| Variable attributes | Yes | Yes | declare -i, -r, -x, etc. |' (not a 'Full support' row, so not policed by the meta-test).",
  "note": "tests/unit/core/test_nameref_attribute_resolution.py apply_attribute pins are unaffected (no readonly+integer combination on one target). psh rejects `declare -c` as an invalid option (pre-existing, out of scope). Sibling test_valueless_export_of_existing_readonly (`readonly R=1; export R`) still matches 5.3 and passes. Probe transcripts: /Users/pwilson/src/psh/tmp/program-2026-09/resolution-hash-export/probe.out, probe2.out, probe4.out."
 },
 {
  "node_id": "tests/conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_export_bad_identifier_survives",
  "category": "SEMANTIC",
  "bash53_behavior": "`set -o posix; export 1bad=x; echo rc=$?` -> bash 5.3.15: stdout '' , exit 1, stderr \"export: `1bad=x': not a valid identifier\" (shell EXITS; identical in -c, script-file, stdin and --posix modes). psh: stdout 'rc=1\\n', exit 0 (continues). Authority: bash 5.3 CHANGES 5.3-alpha jj/nnnnn/bbbbbb; bashref 5.3 POSIX-mode item \"Non-interactive shells exit if the export, readonly or unset builtin commands get an argument that is not a valid identifier, and they are not operating on shell functions.\" Shape of the new exit (probe-verified): SUPPRESSIBLE class — `export 1bad=x || echo caught; echo survived` -> 'caught\\nsurvived' rc 0; through a function under `if` -> 'F\\nsurvived'; passes through eval (`eval 'export 1bad=x'; echo survived` -> exit 1, no 'survived') and through a bare function call; stripped by `command export`/`builtin export` (rc=1, continues); contained by subshell (`( export 1bad=x )` -> rc=1 continues). bash stops at the FIRST bad arg (`export 1bad=x 2bad=y` prints only the 1bad message; psh prints both). `export -f 1bad` -> 'not a function' rc 1, NO exit (function operands exempt). `export 1bad` (no '=') also exits 1.",
  "prior_expectation": "Pinned to bash 5.2.26 (module docstring: 'does NOT exit on OPERAND errors (bad identifier, ...)'; docs/reviews/posix_special_builtin_exit_matrix_2026-07-07.md row 48 'bad identifier: export 1bad=x | continue | continue (NO exit)'). Test expects stdout 'rc=1\\n', exit 0 in both shells.",
  "proposed_retune": "psh follows bash 5.3: in psh/builtins/environment.py ExportBuiltin (~lines 152-156, the `not a valid identifier` branch that currently sets status=1 and `continue`s) — when invoked as the special builtin (not via command/builtin) print the diagnostic for the first bad identifier and `raise SpecialBuiltinUsageError(1, suppressible=True)` (stop processing further args, matching bash's first-arg stop); leave the function-operand (`-f`) path untouched. Conformance row moves from TestPosixSpecialBuiltinNoExit to TestPosixSpecialBuiltinExit as `test_export_bad_identifier_exits_1`: command \"set -o posix; export 1bad=x; echo survived\" (bash: stdout '', exit 1). Lockstep edits: tests/integration/test_posix_special_builtin_exit.py SURVIVING_ROWS (\"export 1bad=x\", 1) -> EXITING_ROWS (\"export 1bad=x\", 1); tests/behavioral/golden_cases.yaml `posixexit_export_badid_survives` -> stdout \"\", exit_code 1 (rename ..._exits); matrix doc row 48; docstrings in psh/core/exceptions.py SpecialBuiltinUsageError ('Operand/semantic errors (export 1bad=x ...) deliberately do NOT raise this') and psh/core/internal_errors.py special_builtin_usage_exit ('bash 5.2, probe-verified').",
  "effort": "M",
  "note": "Not ENV: fails identically in -c/file/stdin/--posix. Sibling `unset 1bad` stays rc 0 with no message in bash 5.3 (probe A1) — test_unset_bad_identifier_survives remains green. `command export 1bad=x` still continues (probe A12) so the command-strips row is unaffected. Not in CLAIM_TESTS; no user-guide compat row (the 'set -o posix / POSIXLY_CORRECT' row maps to bash/test_posixly_correct_conformance.py)."
 },
 {
  "node_id": "tests/conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_readonly_bad_identifier_survives",
  "category": "SEMANTIC",
  "bash53_behavior": "`set -o posix; readonly 1bad=x; echo rc=$?` -> bash 5.3.15: stdout '', exit 1, stderr \"readonly: `1bad=x': not a valid identifier\" (EXITS). psh: stdout 'rc=1\\n', exit 0. Same rule and same suppressible/eval/command/subshell shape as the export row (bash CHANGES 5.3-alpha bbbbbb names readonly explicitly). Also exits for `readonly 1bad` (no '=', probe F10) and `readonly é=1` under a UTF-8 locale (probe G12: stdout '', exit 1). `readonly -f é` on a defined function: bash 5.3 rc 0 (function operands exempt; psh currently rc 1 'not a valid identifier' + 'not found').",
  "prior_expectation": "Pinned to bash 5.2.26 (matrix doc row 49 'bad identifier: readonly 1bad=x | continue | continue (NO exit)'); test expects stdout 'rc=1\\n', exit 0 in both shells.",
  "proposed_retune": "psh follows bash 5.3: readonly is dispatched through psh/builtins/function_support.py DeclareBuiltin.run_as(invoked_as='readonly', special=True); the two `not a valid identifier` sites (`_declare_assignment` ~line 373 and `_declare_bare_name` ~line 516, both `self._diag(...); return 1`) must, when invoked_as == 'readonly' (special) and the operand is a VARIABLE (not `-f`), raise SpecialBuiltinUsageError(1, suppressible=True) after printing — NOT for declare/typeset (bash 5.3 `declare é=1; echo done` still prints done, probe T5a). Conformance row moves to TestPosixSpecialBuiltinExit as `test_readonly_bad_identifier_exits_1`: command \"set -o posix; readonly 1bad=x; echo survived\" (bash: stdout '', exit 1). Lockstep: integration SURVIVING_ROWS (\"readonly 1bad=x\", 1) -> EXITING_ROWS; matrix doc row 49; same docstrings as the export row.",
  "effort": "M",
  "note": "Not ENV. Shares the psh change with test_export_bad_identifier_survives; the `readonly -f é` / `export -f é` posix acceptance (bash 5.3 rc 0, psh rc 1) is a related but separate psh gap surfaced by probes F6/F7 — belongs with the function-name retune below. Not in CLAIM_TESTS; no user-guide compat row."
 },
 {
  "node_id": "tests/conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSpecialBuiltinNoExit::test_unset_readonly_survives",
  "category": "SEMANTIC",
  "bash53_behavior": "`set -o posix; readonly r=1; unset r; echo rc=$?` -> bash 5.3.15: stdout '', exit 1, stderr 'unset: r: cannot unset: readonly variable' (EXITS; same in file/stdin/--posix modes). psh: stdout 'rc=1\\n', exit 0. Suppressible class: `unset r || echo caught; echo survived` -> 'caught\\nsurvived' rc 0 (probe A10); `eval 'unset r' || echo caught` -> caught (G6). `unset r x` -> exit 1 (G10). Readonly FUNCTION too: `readonly -f f; unset -f f` -> exit 1, stderr 'unset: f: cannot unset: readonly function' (F13; psh continues, and psh's message is 'unset: f: readonly function' — wording differs). `unset 1bad` and `unset -f 1bad` remain rc 0 with no message in both shells (A1, G11).",
  "prior_expectation": "Pinned to bash 5.2.26 (matrix doc row 51 '`unset r` on a readonly r | continue | continue (NO exit)'); test expects stdout 'rc=1\\n', exit 0 in both.",
  "proposed_retune": "psh follows bash 5.3: in psh/builtins/environment.py UnsetBuiltin, the ReadonlyVariableError branch (~line 665 `self.error(f\"{var}: cannot unset: readonly variable\"); exit_code = 1`) and the array-element readonly branches (~709/730/775) and the readonly-function branch should record the failure and, after the arg loop, `raise SpecialBuiltinUsageError(1, suppressible=True)` when invoked as the special builtin (bash stops at the first readonly failure: G10 prints only the `r` message). Conformance row moves to TestPosixSpecialBuiltinExit as `test_unset_readonly_exits_1`: command \"set -o posix; readonly r=1; unset r; echo survived\" (bash: stdout '', exit 1). Lockstep: integration SURVIVING_ROWS (\"readonly r=1\\nunset r\", 1) -> EXITING_ROWS; golden `posixexit_unset_readonly_survives` -> stdout \"\", exit_code 1; matrix doc row 51; same docstrings.",
  "effort": "M",
  "note": "Not ENV. Optional FORMAT follow-up surfaced by probe F13: psh prints 'unset: f: readonly function' where bash 5.3 prints 'unset: f: cannot unset: readonly function' (stderr wording only; the conformance helper only checks stderr presence, so it does not affect this row). Not in CLAIM_TESTS; no user-guide compat row."
 },
 {
  "node_id": "tests/conformance/posix/test_posix_special_builtin_exit_conformance.py::TestPosixSuppressibleExit::test_eval_boundary_not_suppressed",
  "category": "SEMANTIC",
  "bash53_behavior": "`set -o posix; eval 'set -q' || echo caught; echo survived` -> bash 5.3.15: stdout 'caught\\nsurvived\\n', exit 0 (the guard OUTSIDE eval now suppresses the special-builtin exit; same in file/stdin/--posix). psh: stdout '', exit 2. Corroborating probes: `if eval 'set -q'; then echo T; else echo F; fi; echo survived` -> bash 'F\\nsurvived' rc 0, psh exit 2 (A13); `eval 'return' || echo caught` -> bash caught, psh exit 2 (F18); nested `eval \"eval 'set -q'\" || echo caught` -> bash caught (G15); DOT boundary is transparent too: `echo 'set -q' > d.sh; . ./d.sh || echo caught; echo survived` -> bash 'caught\\nsurvived' rc 0, psh exit 2 (G2), and `if . ./d.sh` -> 'F\\nsurvived' (G3). Unchanged: guard INSIDE eval still suppresses (G1 'in\\nrc=0' both); HARD class still exits when guarded — `eval 'if' || echo caught` exit 2 (F16), dot syntax error exit 2 (F15), `readonly r=2 || echo caught` exit 1 (F17), unguarded `eval 'set -q'; echo survived` exit 2 (A5) — all match psh today. A trap action `trap 'set -q' USR1; kill -USR1 $$ || echo caught` exits 2 in both (G4).",
  "prior_expectation": "Bounce-F1 model pinned to bash 5.2.26: suppression 'reaches through functions/brace groups/subshells but NOT across an eval/dot boundary' (`eval 'set -q' || x` still exits). Test expects stdout '', exit 2 in both shells. psh implements the boundary via ExecutionContext.special_exit_floor raised in psh/scripting/source_processor.py (~lines 233-240) for every nested run.",
  "proposed_retune": "psh follows bash 5.3: stop raising `special_exit_floor` for eval/dot nested runs in psh/scripting/source_processor.py (the block `saved_floor = executor.context.special_exit_floor; executor.context.special_exit_floor = executor.context.errexit_suppress`), so `special_exit_suppressed` sees the outer guard across eval and `.`; probe the trap-action case (`if kill -USR1 $$` with a `set -q` action) before deciding whether trap-action nested runs keep the floor. Conformance row becomes `test_eval_boundary_suppressed` with the same command, expected bash stdout 'caught\\nsurvived\\n', exit 0; add sibling rows for `if eval 'set -q'` and the guarded-dot case (G2). Lockstep: tests/integration/test_posix_special_builtin_exit.py TestSuppressionBoundaries.test_guard_outside_eval_does_not_suppress (and the guarded-dot twin that follows it) flip to survive rc 0; golden `posixexit_no_suppress_across_eval` -> stdout 'caught\\n', exit_code 0; docstrings in psh/executor/context.py (special_exit_floor comment), psh/core/internal_errors.py special_builtin_usage_exit, psh/scripting/source_processor.py, module docstring of the conformance file, and the matrix doc's 'eval/dot boundaries reset suppression' sentence.",
  "effort": "M",
  "note": "Not ENV. The HARD-class rows (test_hard_dot_missing_guarded_still_exits, eval syntax) and guard-inside-eval rows are unaffected and still match bash 5.3. Not in CLAIM_TESTS; no user-guide compat row."
 },
 {
  "node_id": "tests/conformance/bash/test_identifier_policy_conformance.py::TestPosixRestrictsUnicodeLikeBash::test_declare_export_read_report_and_continue",
  "category": "SEMANTIC",
  "bash53_behavior": "Under LC_ALL=C.UTF-8 with `set -o posix`: `declare é=1; echo done` -> bash 5.3.15 stdout 'done', exit 0, stderr \"declare: `é=1': not a valid identifier\" (unchanged; psh identical). `read é <<< hi; echo done` -> bash stdout 'done', exit 0, stderr \"read: `é': not a valid identifier\" (unchanged; psh identical). `export é=1; echo done` -> bash stdout '' , exit 1, stderr \"export: `é=1': not a valid identifier\" (EXITS — the same bash 5.3 special-builtin identifier rule as the `export 1bad=x` row; identical in file/stdin/--posix modes). psh prints 'done', exit 0. The test's assertion `psh.stdout == bash.stdout == 'done\\n'` fails only on the export iteration.",
  "prior_expectation": "Module docstring (pinned against bash 5.2): 'declare/export/read report \"not a valid identifier\" (status 1) and continue'. Test loops over the three builtins expecting stdout 'done\\n' and returncode 0 in both shells.",
  "proposed_retune": "Split the loop: keep `declare é=1` and `read é <<< hi` as report-and-continue rows (stdout 'done\\n', rc 0, 'not a valid identifier' in both stderrs); move `export é=1` (and optionally `readonly é=1`, probe G12) into a new row `test_export_readonly_unicode_exit_in_posix` expecting stdout '' and returncode 1 in both shells with 'not a valid identifier' in both stderrs — which becomes green once the export/readonly psh change from the test_export_bad_identifier_survives row lands (same SpecialBuiltinUsageError(1, suppressible=True) raise). Until then it could only be pinned as a declared divergence, which is not worth doing given the shared fix. Update the module docstring lines 15-16 and the user-guide example block (`declare é=1 # ... (status 1)` stays; add a line noting `export é=1`/`readonly é=1` exit a non-interactive posix shell, bash 5.3).",
  "effort": "M",
  "user_guide_claim": "docs/user_guide/17_differences_from_bash.md section 'Identifier (Name) Rules — Unicode Extension' (lines 504-540) — declared-divergence prose stating that in POSIX mode 'declare é=1 ... (status 1)', 'read é ... (status 1)'; this is NOT a compatibility-table row and neither test file is mapped in CLAIM_TESTS (the 'set -o posix / POSIXLY_CORRECT' table row maps to bash/test_posixly_correct_conformance.py).",
  "note": "Not ENV. The psh behavior change is the same one required by the export/readonly bad-identifier rows (test effort alone is S; counted M because it is gated on that psh change). declare and read are not special builtins and bash 5.3 still continues for them, so the psh side of those two rows needs no change."
 },
 {
  "node_id": "tests/conformance/bash/test_identifier_policy_conformance.py::TestPosixRestrictsUnicodeLikeBash::test_for_and_function_rejected_by_both",
  "category": "PREMISE",
  "bash53_behavior": "bash 5.3 CHANGES 5.3-alpha (p): 'Posix mode no longer requires function names to be valid shell identifiers.' Under LC_ALL=C.UTF-8: `set -o posix; function é { echo body; }` -> bash 5.3.15 stdout '', stderr '', exit 0 (function DEFINED; `...; é; echo rc=$?` prints 'body\\nrc=0'); `é() { echo body; }` -> same, exit 0. psh: exit 1, stderr \"psh: `é': not a valid identifier\" (and a later call gives 'command not found' 127). bash 5.3 posix also accepts `9x() {...}` and `a-b() {...}` (define and run, probes F1/F2; psh rejects both, rc 127 on call). `a/b()` defines silently in bash 5.3 but lookup gives 'No such file or directory' 127 (posix mode skips function lookup for names with '/'). `for é in a; do ...; done` STILL parse-aborts in bash 5.3 (stdout '', exit 2, stderr \"`é': not a valid identifier\"; psh exit 1 same message) and `select é` likewise exit 2 (F5). `export -f é` / `readonly -f é` on a defined function: bash 5.3 rc 0, psh rc 1 'not a valid identifier' (F6/F7). `declare -f é`: bash 5.3 rc 1 \"declare: `é': not a valid identifier\", psh rc 1 (message lacks the 'declare:' prefix). The failing assertion is `assert bash.returncode != 0` on 'function é { echo body; }' (bash returned 0).",
  "prior_expectation": "Module docstring and docs/user_guide/17_differences_from_bash.md 'Note on for/function error flow' (pinned to bash 5.2): under `set -o posix` bash treats an invalid `for`/function name as a PARSE error and aborts the whole input (exit 2), psh rejects at execution (status 1); the test asserts bash rejects (rc != 0, 'not a valid identifier' in stderr) for all three constructs.",
  "proposed_retune": "(1) Test: drop the two function constructs from the `for/function rejected by both` loop, keeping `for é in a; do echo body; done` (bash rc 2 / psh rc 1, both 'not a valid identifier', body never runs) and optionally adding `select é in a; do ...; done </dev/null`. (2) Add a new row `test_function_names_unrestricted_in_posix` pinning bash 5.3: `set -o posix; function é { echo body; }; é` and `é() { echo body; }; é` -> stdout 'body\\n', rc 0, empty stderr, in BOTH shells. (3) psh follows bash 5.3: delete the posix-mode `is_valid_name(node.name, posix_mode=True)` rejection in psh/executor/function.py (~lines 47-55, FunctionDef visitor) — psh's parser already accepts these names (default mode works today) — and stop pre-validating FUNCTION operands (`-f`) against the identifier policy in psh/builtins/function_support.py so `export -f é`/`readonly -f é` succeed under posix (bashref 5.3: 'and they are not operating on shell functions'). (4) Prose: rewrite the module docstring lines 20-27/33-37 and the user-guide 'Note on for/function error flow' paragraph (17_differences_from_bash.md ~533-540) to say the posix parse-abort applies to `for`/`select` names only and that bash 5.3+ places no identifier restriction on function names in posix mode; the earlier 'Bash restricts names to the POSIX/ASCII set in every mode' sentence should be narrowed to variable names.",
  "effort": "M",
  "user_guide_claim": "docs/user_guide/17_differences_from_bash.md section 'Identifier (Name) Rules — Unicode Extension' (lines 504-540), specifically the 'Note on for/function error flow' paragraph asserting bash parse-aborts invalid function names under `set -o posix` — declared-divergence prose, NOT a compatibility-table row; the file is not mapped in CLAIM_TESTS.",
  "note": "Not ENV. The docstring references an R21-A 'certification row' that polices the 'parses everything up front' phrase family tree-wide; I could not locate that test by phrase (grep for 'up front'/'abort-vs-continue'/'phrase family' under tests/ found nothing relevant), so re-check for it before editing the prose. `declare -f é` remains rejected by both shells (rc 1) — only the psh message prefix differs (FORMAT nit, out of scope)."
 },
 {
  "node_id": "tests/integration/job_control/test_pipeline_signal_death.py::TestPipelineLastMemberSignalDeath::test_sigterm_last_member_announced",
  "category": "FORMAT",
  "bash53_behavior": "cmd `true | sh -c \"kill -TERM \\$\\$\"; echo rc=$?` -> bash 5.3.15: rc=0, stdout 'rc=143\\n', stderr 'Terminated: 15             true | sh -c \"kill -TERM \\\\$\\\\$\"\\n' (signal description left-justified in a 27-column field, then the job's re-printed command text). psh: rc=0, stdout 'rc=143\\n', stderr 'Terminated: 15\\n'. Only line 51 `assert psh.stderr == bash.stderr` fails; stdout/rc and `psh.stderr.strip() == strsignal(SIGTERM)` still hold.",
  "prior_expectation": "Docstring lines 8-9 and 45: bash 5.2 printed the bare `Terminated: 15` for SIGTERM (\"bare form in both shells\" / \"exact parity\"), pinned by `psh.stderr == bash.stderr`.",
  "proposed_retune": "S path (declared format divergence): replace line 51 with `assert bash.stderr == signal.strsignal(signal.SIGTERM).ljust(27) + 'true | sh -c \"kill -TERM \\\\$\\\\$\"\\n'` (or the looser `assert bash.stderr.startswith(signal.strsignal(signal.SIGTERM))`), keep line 50 as psh's bare-form pin, and reword the module docstring: \"bash 5.3 announces via its job-table printer: the status text padded to 27 columns followed by the job's command text; psh emits just the bare description (documented format difference, now for SIGTERM too)\". L path (exact parity): `JobManager.report_signal_death_at` prints `message.ljust(27) + job_text` where job_text is a bash-faithful pre-expansion unparse of the job (quotes verbatim, whitespace normalized, `> /dev/null` spacing, `( ... )` for subshells) — psh currently has no such text (single commands use expanded args joined by spaces, subshells are `<subshell>`).",
  "effort": "S",
  "note": "Padding rule pinned empirically: SIGFPE's 27-char macOS description is followed by the command with NO separator (`Floating point exception: 8sh -c ...`), so it is a pure 27-column left-justified field. Same line in -c, script-file and stdin modes. Non-TERM signals still carry the 5.2-style `/opt/homebrew/bin/bash: line 1: PID ` prefix (per-process lines for pipelines). Golden rows pipeline_last_member_sigterm_announced etc. substring-match 'Terminated' and are unaffected. The psh docstring in psh/executor/job_control.py:626-628 ('exact for SIGTERM') is now stale for 5.3 and should be reworded on the S path.",
  "user_guide_claim": "none — not in CLAIM_TESTS; no docs/user_guide row mentions the signal-death diagnostic"
 },
 {
  "node_id": "tests/integration/job_control/test_pipeline_signal_death.py::TestPipelineLastMemberSignalDeath::test_sigterm_no_trailing_command",
  "category": "FORMAT",
  "bash53_behavior": "cmd `echo hi | sh -c \"kill -TERM \\$\\$\"` -> bash 5.3.15: rc=143, stdout '', stderr 'Terminated: 15             echo hi | sh -c \"kill -TERM \\\\$\\\\$\"\\n'. psh: rc=143, stdout '', stderr 'Terminated: 15\\n'. Only line 58 `assert psh.stderr == bash.stderr` fails (rc 143 matches; line 59 strsignal pin holds).",
  "prior_expectation": "Same premise as the class docstring: bash 5.2 bare `Terminated: 15` for SIGTERM, exact-parity pin.",
  "proposed_retune": "S: replace line 58 with `assert bash.stderr == signal.strsignal(signal.SIGTERM).ljust(27) + 'echo hi | sh -c \"kill -TERM \\\\$\\\\$\"\\n'` (or `bash.stderr.startswith(signal.strsignal(signal.SIGTERM))`), keep line 59 psh pin. L alternative as for test_sigterm_last_member_announced (bash-faithful job text on the announce path).",
  "effort": "S",
  "note": "Confirms the announcement is unchanged when the signal death is the shell's last command in a pipeline (no exec-optimization for pipeline members). Exit status 143 unchanged between 5.2 and 5.3.",
  "user_guide_claim": "none"
 },
 {
  "node_id": "tests/integration/job_control/test_pipeline_signal_death.py::TestPipelineNonLastMemberSignalDeath::test_pipefail_announces_status_determining_member",
  "category": "FORMAT",
  "bash53_behavior": "cmd `set -o pipefail; sh -c \"kill -TERM \\$\\$\" | cat; echo rc=$?` -> bash 5.3.15: rc=0, stdout 'rc=143\\n', stderr 'Done                       sh -c \"kill -TERM \\\\$\\\\$\" | cat\\n' — the status column shows the LAST member's label (cat exited 0 -> `Done`), not the signal; same with `| sh -c \"exit 0\"` ('Done ... | sh -c \"exit 0\"'). The announce DECISION is still pipefail-driven: without pipefail `sh -c \"kill -TERM \\$\\$\" | cat` is silent (rc=0), and `set -o pipefail; ... | grep nomatch` is silent (rc=1) in 5.3. psh: rc=0, stdout 'rc=143\\n', stderr 'Terminated: 15\\n'. Only line 110 fails.",
  "prior_expectation": "Docstring line 102-103: \"bash announces it (bare form — exact parity)\" — bash 5.2 printed the status-determining member's bare `Terminated: 15`.",
  "proposed_retune": "S: replace line 110 with `assert bash.stderr == 'Done'.ljust(27) + 'sh -c \"kill -TERM \\\\$\\\\$\" | cat\\n'` (or `assert bash.stderr.startswith('Done')`), keep line 109 (psh names the signal of the status-determining member), and reword the docstring: \"bash 5.3 announces the job through its job-table printer, whose status column is the LAST member's label (`Done` for cat) followed by the command text, even though the announced condition is the pipefail member's signal death; psh keeps naming that member's signal (declared divergence)\". M alternative (partial): print `background_completion_label(last member status)` instead of the signal text in the pipeline case — still not parity without the command text, so exact parity is L.",
  "effort": "S",
  "note": "Wording-only change but a semantically odd one: bash 5.3 prints `Done` for a pipeline whose $? is 143. Worth recording verbatim in the divergence note so nobody later 'fixes' psh to print Done without the command text. Exit status and announce/silence decisions are identical between psh and bash 5.3 across all four pipefail/no-pipefail probes.",
  "user_guide_claim": "none"
 },
 {
  "node_id": "tests/integration/job_control/test_signal_killed_diagnostic.py::TestAbnormalTerminationDiagnostic::test_reported_in_explicit_subshell",
  "category": "FORMAT",
  "bash53_behavior": "cmd `(sh -c \"kill -TERM \\$\\$\"); echo next` -> bash 5.3.15: rc=0, stdout 'next\\n', stderr 'Terminated: 15             ( sh -c \"kill -TERM \\\\$\\\\$\" )\\n' (note bash re-prints the subshell as `( ... )` with inner spaces). psh: rc=0, stdout 'next\\n', stderr 'Terminated: 15\\n'. Only line 108 fails.",
  "prior_expectation": "Docstring line 101-102: \"like bash (exact parity for SIGTERM)\" — bash 5.2 bare form.",
  "proposed_retune": "S: replace line 108 with `assert bash.stderr == signal.strsignal(signal.SIGTERM).ljust(27) + '( sh -c \"kill -TERM \\\\$\\\\$\" )\\n'` (or `bash.stderr.startswith(signal.strsignal(signal.SIGTERM))`), keep line 107. Also update the module docstring lines 18-20: bash 5.3 prefixes `bash: line N: PID` only for non-SIGTERM signals but now appends the padded command text for SIGTERM as well. L alternative: bash-faithful job text — psh's `jobs` renders a subshell job as `<subshell>` today, so parity here needs an AST unparser.",
  "effort": "S",
  "note": "psh `jobs` for `(sleep 30) &` prints `<subshell> &` where bash prints `( sleep 30 ) &`; psh's `jobs` also drops assignment prefixes/redirects (`x=1 sleep 30 > /dev/null 2>&1` -> `sleep 30`). That pre-existing job-text divergence is what makes exact parity L rather than M.",
  "user_guide_claim": "none"
 },
 {
  "node_id": "tests/integration/job_control/test_signal_killed_diagnostic.py::TestAbnormalTerminationDiagnostic::test_sigterm_prints_bare_signal_description",
  "category": "FORMAT",
  "bash53_behavior": "cmd `sh -c \"kill -TERM \\$\\$\"; echo next` -> bash 5.3.15: rc=0, stdout 'next\\n', stderr 'Terminated: 15             sh -c \"kill -TERM \\\\$\\\\$\"\\n'; identical in script-file and stdin modes. Command text is the pre-expansion AST re-print: `sh   -c   \"...\"` -> `sh -c \"...\"`, single quotes kept verbatim, `>/dev/null 2>&1` -> `> /dev/null 2>&1`, `if/for/{ }/function` wrappers stripped, unexpanded `$v` kept. psh: rc=0, stdout 'next\\n', stderr 'Terminated: 15\\n'. Only line 61 fails.",
  "prior_expectation": "Docstring line 52-53: \"For SIGTERM bash uses the same bare form, so this is exact parity.\" (bash 5.2). Module docstring line 18-20 declares the `bash: line N: PID` prefix divergence for non-SIGTERM signals only.",
  "proposed_retune": "S: rename/reword to 'psh prints the bare signal description; bash 5.3 appends the padded job text' and replace line 61 with `assert bash.stderr == signal.strsignal(signal.SIGTERM).ljust(27) + 'sh -c \"kill -TERM \\\\$\\\\$\"\\n'` (or `bash.stderr.startswith(signal.strsignal(signal.SIGTERM))`); reword module docstring lines 18-20 and psh/executor/job_control.py:626-628 ('which is exact for SIGTERM' is no longer true under 5.3). L: full parity per the other rows (`message.ljust(27) + bash-faithful job text` in `JobManager.report_signal_death_at`).",
  "effort": "S",
  "note": "Non-TERM signals under 5.3 still use the 5.2 shape, e.g. `/opt/homebrew/bin/bash: line 1: 28769 Segmentation fault: 11     sh -c \"kill -SEGV \\$\\$\"`, so test_diagnostic_names_the_signal (substring check) still passes. The 5.3 NEWS/CHANGES in /opt/homebrew/Cellar/bash/5.3.15/share/doc/bash do not name this change explicitly (closest: job-notification entries ii/qq/rr and CHANGES 'g. Fixed a problem with jobs run from a trap ... printing the status of terminated foreground jobs'); classification rests on the observed output.",
  "user_guide_claim": "none"
 },
 {
  "node_id": "tests/integration/job_control/test_bg_resume_refreshes_state.py::test_bg_actually_resumes_a_job_stopped_behind_the_shells_back",
  "category": "ENV",
  "bash53_behavior": "Not an oracle test (psh-only subprocess; no bash run). On this host, unsandboxed: PASSED 6/6 consecutive runs ('1 passed in 0.16s'). Reproduced the gate failure under `sandbox-exec -p '(version 1)(allow default)(deny process-info* (with no-log))'`: `AssertionError: harness precondition failed ... stdout='before_bg=\\n[1]+ sleep 30 &\\nbg_rc=0\\nafter_bg=RESUMED\\n'` — `ps -o stat= -p $pid` returns nothing when process-info is denied, so the `before_bg=T` precondition fires, while psh's `bg` itself still resumed the job.",
  "prior_expectation": "Test asserts (line 78) that `ps` reports the externally STOPped child as `T` before `bg`, then (82-91) that `bg` resumes it, prints `sleep 30 &`, rc 0. Independent of bash version.",
  "proposed_retune": "None required for the 5.3 oracle move. Optional hardening (S): at the top of the test, `pytest.skip('ps cannot read process state here (sandboxed)')` when a probe `ps -o stat= -p <own pid>` returns empty, or run the gate for this file outside the process sandbox. Matches the orchestrator's observation that it passes on the unsandboxed host.",
  "effort": "S",
  "note": "Confirmed ENV: failure is a seatbelt process-info denial affecting `ps`, exactly the wording in docs/reviews/evidence/fresh_appraisal_2026_09_06/validation.md:40 ('ps-based precondition reported no state'). No psh defect: even under the sandbox the resume path ran (`[1]+ sleep 30 &`, `after_bg=RESUMED`). I did not run the other four orchestrator-listed ENV tests (socket_earlier, cap_kill_reaches, r18t2_builtins_history_write_to_stdout) — outside this family.",
  "user_guide_claim": "none — not in CLAIM_TESTS; the user-guide 'fg/bg commands' compatibility row is not mapped to this test"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_cd_options_conformance.py::TestCdOptions::test_cd_too_many_arguments",
  "category": "SEMANTIC",
  "bash53_behavior": "bash 5.3.15 `-c 'cd a b; echo rc=$?'` -> stderr `/opt/homebrew/bin/bash: line 1: cd: too many arguments`, stdout `rc=2`, process rc 0 (same rc=2 with `set -o posix` and in script mode; `cd a b && echo tail-ran` -> tail not run, rc=2). psh today: same stderr wording, stdout `rc=1`. Assertion diff: `assert 'rc=1\\n' == 'rc=2\\n'` (line 69, the middle operand is bash's stdout).",
  "prior_expectation": "Module docstring line 12: \"`cd a b` is \\\"too many arguments\\\" (status 1, no chdir)\"; test asserts `psh.stdout == bash.stdout == 'rc=1\\n'`. Green at the 2026-08-09 gate against bash 5.2.26 (5.2 returned 1).",
  "proposed_retune": "psh change (follow bash 5.3): psh/builtins/navigation.py:103 `return 1` -> `return 2` after `self.error(\"too many arguments\", shell)`. Test edit: line 69 `'rc=1\\n'` -> `'rc=2\\n'`; docstring line 12 `(status 1, no chdir)` -> `(status 2, no chdir)`. tests/unit/builtins/test_navigation.py:423 only checks the message, no rc edit needed there.",
  "effort": "M",
  "prior_expectation_note": "",
  "note": "No bash 5.3 CHANGES/NEWS line names cd's status; the shift 1->2 is observed directly (usage errors now return 2 uniformly: cd/exit/return/shift/break/continue all print `rc=2` in bash 5.3 script mode, psh prints 1 for all of them). Probe transcript: tmp/program-2026-09/exit-cd-trapstatus/probe_toomany.out. Not in CLAIM_TESTS; no user-guide compat row.",
  "user_guide_claim": "none (not in CLAIM_TESTS; no compatibility-table row for cd)"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_cd_options_conformance.py::TestExitStatus::test_exit_too_many_args_does_not_exit",
  "category": "SEMANTIC",
  "bash53_behavior": "Script file `exit 1 2 3\\necho after=$?` in bash 5.3.15 -> stderr `s.sh: line 1: exit: too many arguments`, stdout `after=2`, process rc 0 (shell keeps running, as before, but $? is now 2 not 1; same under `set -o posix`). psh today: same stderr, stdout `after=1`, rc 0. Assertion diff: `assert 'after=1\\n' == 'after=2\\n'` (line 55). Under `-c 'exit 7 8; echo survived'` bash 5.3 still abandons the string with PROCESS rc 1 and psh matches (rc 1) — only the continue-on-next-line status changed.",
  "prior_expectation": "Docstring line 8: \"too many arguments errors and does NOT exit (status 1, shell continues)\"; comment lines 45-48 and assertion `psh.stdout == bash.stdout == 'after=1\\n'` plus `returncode == 0`. Verified against bash 5.2 (psh/builtins/core.py:56-61 comment says \"rc 1\", \"probe-verified against bash 5.2\").",
  "proposed_retune": "psh change: in psh/builtins/core.py:61 the usage discard must leave $? = 2 for the next input line while the `-c` abandonment keeps process status 1 — i.e. psh/core/internal_errors.py#special_builtin_usage_discard should `raise SystemExit(1)` in command_mode but `raise TopLevelAbort(2, errexit_immune=True, contain_nested=False)` otherwise (bash 5.3 observed: -c rc=1, script next-line $?=2, for exit/shift/return/break/continue alike). Test edit: line 55 `'after=1\\n'` -> `'after=2\\n'`; docstring line 8 `(status 1, shell continues)` -> `(status 2, shell continues)`; core.py:59-60 comment `rc 1` -> `$? 2 (rc 1 under -c)`.",
  "effort": "M",
  "note": "Same helper serves shift/return/break/continue (psh/builtins/positional.py:38, function_support.py:1000, loop_control.py:82), all of which bash 5.3 now leaves at $?=2 in script mode (probe_toomany.out) — fixing the helper once retunes them together; their `-c`-mode pins (golden bcontract_exit_too_many_discards exit_code 1, test_bcontract_argument_policy.py:78/105 rc==1, test_break_continue_levels.py:149) stay valid because bash 5.3 -c still exits 1. Adjacent NEW divergence not in this family: bash 5.3 `exit abc; echo rc=$?` prints `rc=2` and CONTINUES (script and -c) whereas psh exits 2 — golden pin bcontract_exit_bad_first_operand_exits_two (`exit abc 7; echo survived` exit_code 2) is now wrong vs 5.3. Not in CLAIM_TESTS.",
  "user_guide_claim": "none (not in CLAIM_TESTS; no compatibility-table row for exit)"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_trap_status_precedence_conformance.py::test_bare_exit_in_a_signal_trap_still_uses_current_status",
  "category": "SEMANTIC",
  "bash53_behavior": "bash 5.3.15 `-c \"trap 'echo entry=$?; false; exit' USR1\\nkill -USR1 $$\\nsleep 0.2\\nexit 3\"` -> stdout `entry=0`, rc 0 (bare exit resolves to the trap ENTRY status, as the EXIT trap already did). psh -> `entry=0`, rc 1 (current $? from `false`). Assertion diff: `assert ('entry=0\\n', 1) == ('entry=0\\n', 0)` at line 123; line 125 `assert psh.returncode == 1` would fail next. Documented change: bash 5.3 NEWS item uu / CHANGES 5.3-alpha item q: \"If `exit' is run in a trap and not supplied an exit status argument, it uses the value of $? from before the trap only if it's run at the trap's `top level' and would cause the trap to end (that is, not in a subshell). This is from Posix interp 1602.\"",
  "prior_expectation": "Docstring: \"MUST-HOLD ... The saved-status rule is EXIT-trap-SPECIFIC. In a SIGNAL trap bash resolves a bare `exit` from the CURRENT `$?`\" (slot 4A.2 ruling R4); asserts `psh.returncode == 1  # current $?, NOT the entry status 0`. psh/core/trap_manager.py:94-99 docstring pins the same premise \"probe-verified against bash 5.2.26\"; psh/core/CLAUDE.md:749-752 states it as an invariant (\"EXIT-ONLY by design\").",
  "proposed_retune": "psh change (M): generalize TrapManager's entry-status mechanism from EXIT-only to every trap that bash 5.3 covers, with bash 5.3's `top level` boundary. Observed boundary (probe_trap.out, probe_trap2.out, all 3 input modes): entry status is used when the bare `exit` is in the action's own command text incl. `if`/`{ }`/`for`/`while` bodies, `||`/`&&` lists, `eval`, and a signal trap nested inside the EXIT action; CURRENT `$?` is used when the bare `exit` is inside a function body or a `source`d file invoked from the action; the depth is RELATIVE to trap entry (a USR1 trap that fires while a function/sourced file is running still uses the entry status, rc=0 / rc=5 cases). Applies to signal traps and ERR (`trap '{ true; exit; }' ERR; false` -> rc 1 in bash, 0 in psh); DEBUG keeps current `$?` (bash rc=1, psh agrees); EXIT stays unconditional (in-function bare exit in an EXIT action still yields 3 in bash 5.3, psh agrees). Concretely: trap_manager.py:449-451 record `(saved_exit_code, len(state.function_stack), state.source_depth)` for EXIT, signal and ERR traps instead of `saved_exit_code if signal_name == 'EXIT' else None`; builtins/core.py:39-41 apply it when the trap is EXIT, or when function-stack depth and source_depth equal the recorded entry values. Test edit: rename to `test_bare_exit_in_a_signal_trap_uses_entry_status`, docstring: \"bash 5.3 (POSIX interp 1602) resolves a bare `exit` at a trap's top level from the status at trap ENTRY for every trap, not only EXIT\"; line 125 `assert psh.returncode == 0  # entry status 0, NOT the current $? 1`; add the new must-hold for the boundary: `f() { false; exit; }; trap f USR1; kill -USR1 $$; sleep 0.2; exit 3` -> rc 1 (function body is not top level). Also update prose in psh/core/CLAUDE.md:744-757 and trap_manager.py:94-99 / core.py:31-37 (\"EXIT-ONLY\" -> \"top level of any trap, bash 5.3\").",
  "effort": "M",
  "note": "Same underlying change as the three parametrized cells below. bash 5.3 source could not be fetched (savannah cgit 404, GitHub raw 404, git ls-remote empty from the sandbox) so the boundary is stated from 30+ probes, not from exit.def. Not in CLAIM_TESTS; the only trap compat row (\"DEBUG/ERR/RETURN traps\", 17_differences_from_bash.md:968) is backed by test_trap_signal_spec_conformance.py, not this file.",
  "user_guide_claim": "none (not in CLAIM_TESTS; 17_differences_from_bash.md:968 'DEBUG/ERR/RETURN traps' row maps to bash/test_trap_signal_spec_conformance.py, not this test)"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-command]",
  "category": "SEMANTIC",
  "bash53_behavior": "Cell script `trap 'echo entry=$?; false; exit' USR1\\nkill -USR1 $$\\nsleep 0.2\\nexit 3` via `-c`: bash 5.3.15 -> `entry=0` rc 0; psh -> `entry=0` rc 1. Assertion message: \"disc-signal-trap-uses-current-status [command] DISCRIMINATING ... bash: 'entry=0\\n' rc=0 / psh: 'entry=0\\n' rc=1\". Cause: bash 5.3 NEWS uu (POSIX interp 1602) — bare `exit` at a trap's top level now takes the pre-trap $? for all traps.",
  "prior_expectation": "CELLS comment lines 60-62: \"The SIGNAL-trap counterpart ... so the EXIT-only boundary is pinned at the same width as the rule itself\"; cell id `disc-signal-trap-uses-current-status` expects both shells to exit 1 (current $?).",
  "proposed_retune": "psh change as for the must-hold above (generalize entry-status to signal/ERR traps with the relative function/source-depth top-level guard). Test edit: rename cell id to `disc-signal-trap-uses-entry-status` and its comment to \"bash 5.3 (interp 1602): a bare exit at the top level of ANY trap resolves to the entry status\"; the cell then agrees at rc 0. Extend CELLS with the bash-5.3 boundary rows (all 3 modes, values observed): `trap 'true; exit' USR1\\n(kill -USR1 $$; exit 5)\\nsleep 0.2\\nexit 3` -> rc 5; `f() { false; exit; }\\ntrap f USR1\\nkill -USR1 $$\\nsleep 0.2\\nexit 3` -> rc 1 (function body not top level); `trap 'if true; then false; exit; fi' USR1 ...` -> rc 0; `trap 'eval \"false; exit\"' USR1 ...` -> rc 0; `trap '{ true; exit; }' ERR\\nfalse\\necho nr` -> rc 1; `trap '. ./s.sh' USR1` with s.sh=`false; exit` -> rc 1.",
  "effort": "M",
  "note": "36 other cells x modes in this file still pass on bash 5.3 (EXIT-trap rule unchanged, incl. disc-bare-exit-in-called-function -> 3). Not in CLAIM_TESTS.",
  "user_guide_claim": "none (not in CLAIM_TESTS; no compatibility-table row)"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-script]",
  "category": "SEMANTIC",
  "bash53_behavior": "Same cell as a script file: bash 5.3.15 -> `entry=0` rc 0; psh -> `entry=0` rc 1 (assertion message \"disc-signal-trap-uses-current-status [script] DISCRIMINATING ... bash rc=0 / psh rc=1\"). Independently re-run outside pytest: probe_trap.out `baseline script bash=[entry=0] rc=0 | psh=[entry=0] rc=1 DIFF`.",
  "prior_expectation": "As the command-mode cell: bash 5.2 resolved a bare `exit` in a signal trap from the current `$?` (rc 1 in both shells); pinned as the EXIT-only boundary.",
  "proposed_retune": "Same psh change and same cell rename/extension as the command-mode cell; script mode needs no mode-specific handling (bash 5.3 behaves identically in command/script/stdin for every probed shape).",
  "effort": "M",
  "note": "Mode-agnostic: all three modes show bash rc 0 / psh rc 1. Not in CLAIM_TESTS.",
  "user_guide_claim": "none (not in CLAIM_TESTS; no compatibility-table row)"
 },
 {
  "node_id": "tests/conformance/bash/test_exit_trap_status_precedence_conformance.py::test_exit_trap_status_matches_bash[disc-signal-trap-uses-current-status-stdin]",
  "category": "SEMANTIC",
  "bash53_behavior": "Same cell piped on stdin: bash 5.3.15 -> `entry=0` rc 0; psh -> `entry=0` rc 1 (assertion message \"disc-signal-trap-uses-current-status [stdin] DISCRIMINATING ... bash rc=0 / psh rc=1\"). Re-run outside pytest: probe_trap.out `baseline stdin bash=[entry=0] rc=0 | psh=[entry=0] rc=1 DIFF`.",
  "prior_expectation": "As the command-mode cell (bash 5.2: current `$?`, rc 1 in both shells).",
  "proposed_retune": "Same psh change and same cell rename/extension as the command-mode cell; stdin mode needs no mode-specific handling.",
  "effort": "M",
  "note": "Mode-agnostic failure. Not in CLAIM_TESTS. Probe scripts/outputs kept under /Users/pwilson/src/psh/tmp/program-2026-09/exit-cd-trapstatus/ (probe_trap.sh/.out, probe_trap2.sh/.out, probe_toomany.sh/.out); no tracked file modified.",
  "user_guide_claim": "none (not in CLAIM_TESTS; no compatibility-table row)"
 },
 {
  "node_id": "tests/conformance/bash/test_bash_compatibility.py::TestBashJobControl::test_job_control_commands",
  "category": "FORMAT",
  "bash53_behavior": "`sleep 1 & jobs` in -c mode -> stdout '[1]+  Running                    sleep 1 &\\n' (status field padded to 27 chars: 'Running' + 20 spaces), stderr '', rc 0. Same 27-wide field observed in script/stdin modes, in `jobs -l` ('[1]+ PID Running                    sleep 1 &'), for Stopped/Done/Exit N/'Terminated: 15', and in the interactive async notice via PTY ('[1]+  Done                       sleep 0.1'). No minimum separator: a 27-char label prints 'Floating point exception: 8sleep 5'. First assertion (`jobs` with no jobs -> '') still passes.",
  "prior_expectation": "Test compares live bash byte-for-byte (assert_identical_behavior); at the 2026-08-09 gate bash 5.2.26 padded the field to 24 ('Running' + 17 spaces), which is what psh emits today: '[1]+  Running                 sleep 1 &\\n'.",
  "proposed_retune": "psh change, no test edit (test is live-oracle): psh/executor/job_control.py:279 and :280 `{state_str:<24}` -> `{state_str:<27}`, and :683 `{label:<24}` -> `{label:<27}` (async notice, PTY-verified 27 in bash 5.3); refresh the illustrative comment at :276. Validated: scratch driver tmp/program-2026-09/jobs-listing/psh_retune_both.py with the 27 patch matches bash 5.3 stdout/stderr/rc exactly for this command in -c, script and stdin modes.",
  "effort": "M",
  "note": "Pure presentation drift; psh already mirrors bash's no-separator overflow rule at 24, so `:<27` reproduces bash 5.3 exactly (and fixes the current 25-char-label divergence 'User defined signal 1: 30sleep 5' vs bash '...: 30  sleep 5'). Blast radius: no .py test pins the 17-space width (grep), tests/unit/builtins/test_job_control_builtins.py::test_jobs_l_pid_column uses \\s+, no test_doc_snippets.py drift-lock touches job_control.py; psh/interactive/CLAUDE.md:617 and docs/user_guide/15_job_control.md samples are illustrative with already-inconsistent widths.",
  "user_guide_claim": "Not mapped in CLAIM_TESTS. Nearest rows in docs/user_guide/17_differences_from_bash.md 17.5: 'jobs command | Yes | Yes | Interactive only' and 'Background jobs | Yes | Yes | Interactive only' — not 'Full support' rows, so the claims meta-test requires no mapping."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_done_label_script",
  "category": "FORMAT",
  "bash53_behavior": "Script file `true & sleep 0.3; echo A:; jobs; echo B:` -> stdout 'A:\\n[1]+  Done                       true\\nB:\\n' ('Done' + 23 spaces = 27-wide field), stderr '', rc 0. Listed-once semantics unchanged from 5.2.",
  "prior_expectation": "psh stdout must equal live bash stdout and contain 'Done'. psh today: 'A:\\n[1]+  Done                    true\\nB:\\n' (24-wide field); diff is the 3 extra pad spaces only.",
  "proposed_retune": "Same psh width edit as the conformance test (psh/executor/job_control.py:279/:280/:683 24->27); no test edit. Validated byte-for-byte via the scratch driver in script mode.",
  "effort": "M",
  "note": "Docstring premise ('Verified vs bash 5.2 ... script-file: completed job LISTED once') still holds on 5.3; only padding changed. The 'Done' in out assertion is unaffected.",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row (17.5 'jobs command' is 'Interactive only')."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_listed_once_script",
  "category": "FORMAT",
  "bash53_behavior": "Script file `false & sleep 0.3; echo A:; jobs; echo B:` -> stdout 'A:\\n[1]+  Exit 1                     false\\nB:\\n' ('Exit 1' + 21 spaces = 27), stderr '', rc 0.",
  "prior_expectation": "psh stdout == live bash stdout; psh today 'A:\\n[1]+  Exit 1                  false\\nB:\\n' (24-wide). Pytest diff shows only the '---' three-space padding delta.",
  "proposed_retune": "psh width edit 24->27 at psh/executor/job_control.py:279/:280/:683; no test edit. Scratch-driver validation: MATCH in script mode.",
  "effort": "M",
  "note": "Sibling test test_argument_less_builtin_has_no_trailing_space_script in the same module already passes (asserts endswith('false'), width-agnostic).",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_listed_once_stdin",
  "category": "FORMAT",
  "bash53_behavior": "Piped stdin `false & sleep 0.3; echo A:; jobs; echo B:` -> stdout 'A:\\n[1]+  Exit 1                     false\\nB:\\n', stderr '', rc 0 (27-wide field; listed once as before).",
  "prior_expectation": "psh stdout == live bash stdout; psh today 'A:\\n[1]+  Exit 1                  false\\nB:\\n'.",
  "proposed_retune": "psh width edit 24->27 (psh/executor/job_control.py:279/:280/:683); no test edit. Scratch-driver validation: MATCH in stdin mode.",
  "effort": "M",
  "note": "stdin-mode semantics unchanged between 5.2 and 5.3; padding only.",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_builtin_suppressed_c_mode",
  "category": "SEMANTIC",
  "bash53_behavior": "`bash -c 'false & sleep 0.3; echo A:; jobs; echo B:'` -> stdout 'A:\\n[1]+  Exit 1                     false\\nB:\\n', stderr '', rc 0 — the completed job IS now listed (once) in -c mode. Also observed: with `set -m` prefix identical stdout and NO stderr notice; `jobs; jobs` lists it exactly once; after `wait` it is still listed once; `jobs -n` lists a finished job once in -c. bash 5.3 CHANGES (5.3-alpha section): 'd. Implement the POSIX requirement that running the `jobs' builtin removes jobs from the jobs list.' and 'bbbb. Fixes to removing jobs from the jobs table when the user isn't notified'.",
  "prior_expectation": "Module docstring lines 8-9 / test lines 104-113: in -c mode bash 5.2 reaped the finished job eagerly so `jobs` stdout was empty ('A:\\nB:\\n'); test asserts psh == bash AND 'Exit' not in out and 'Done' not in out. psh today prints 'A:\\nB:\\n' because psh/builtins/job_control.py:96-98 filters DONE jobs under options['command_mode'].",
  "proposed_retune": "psh change + test rewrite (follow bash 5.3): delete the command_mode filter at psh/builtins/job_control.py:96-98 and rewrite the comment block :85-95 ('A completed job is listed by `jobs` exactly ONCE, then reaped, in EVERY non-interactive read path — bash 5.3 dropped the -c eager reap; verified vs bash 5.3.15'); in the test module update docstring lines 8-9 (-c: completed job LISTED once, no stderr notice), rename test_completed_builtin_suppressed_c_mode -> test_completed_builtin_listed_once_c_mode, keep `out == _bash_c(FALSE)` and flip line 109 to `assert 'Exit 1' in out`; retitle the section comment at line 104. Add a CHANGELOG entry noting the 5.3 flip (historical entry at CHANGELOG.md:1204 stays as history). Also needs the 24->27 width edit to be byte-identical. Validated: scratch driver with filter neutralised + 27 width -> MATCH for -c FALSE/TRUE/EXTERNAL/JOBS_N and `set -m` variant.",
  "effort": "M",
  "note": "The removal/remembered-status loop after the listing (job_control.py:120-127) already runs unconditionally, so `(exit 7)& p=$!; sleep .3; jobs; wait $p` still yields rc=7 with the filter gone (probed). Incidental pre-existing divergence seen in that probe, independent of oracle version: psh lists a subshell bg job as '<subshell>' where bash prints '( exit 7 )'. Incidental PSH_BUG outside this family: a foreground external command consumes a job number — script `sleep 5 & sleep 0.1; sleep 5 & jobs` gives bash '[1]-'/'[2]+' but psh '[1]-'/'[3]+' (JobManager.create_job only resets next_job_id when the table is empty; foreground jobs pass through the table).",
  "user_guide_claim": "Not mapped in CLAIM_TESTS. No user-guide prose claims the -c suppression (grep of docs/user_guide for reap/-c/completed-job found none); 17.5 'jobs command' row is 'Interactive only'."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_listed_once_script",
  "category": "FORMAT",
  "bash53_behavior": "Script file `sleep 0.1 & sleep 0.3; echo A:; jobs; echo B:` -> stdout 'A:\\n[1]+  Done                       sleep 0.1\\nB:\\n', stderr '', rc 0.",
  "prior_expectation": "psh stdout == live bash stdout; psh today 'A:\\n[1]+  Done                    sleep 0.1\\nB:\\n' (24-wide).",
  "proposed_retune": "psh width edit 24->27 (psh/executor/job_control.py:279/:280/:683); no test edit. Scratch-driver validation: MATCH in script mode.",
  "effort": "M",
  "note": "Padding only; external-job listed-once semantics unchanged.",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_listed_once_stdin",
  "category": "FORMAT",
  "bash53_behavior": "Piped stdin `sleep 0.1 & sleep 0.3; echo A:; jobs; echo B:` -> stdout 'A:\\n[1]+  Done                       sleep 0.1\\nB:\\n', stderr '', rc 0.",
  "prior_expectation": "psh stdout == live bash stdout; psh today 'A:\\n[1]+  Done                    sleep 0.1\\nB:\\n'.",
  "proposed_retune": "psh width edit 24->27 (psh/executor/job_control.py:279/:280/:683); no test edit. Scratch-driver validation: MATCH in stdin mode.",
  "effort": "M",
  "note": "Padding only.",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_completed_external_suppressed_c_mode",
  "category": "SEMANTIC",
  "bash53_behavior": "`bash -c 'sleep 0.1 & sleep 0.3; echo A:; jobs; echo B:'` -> stdout 'A:\\n[1]+  Done                       sleep 0.1\\nB:\\n', stderr '', rc 0 — completed external job listed once in -c mode (bash 5.2 printed nothing). Same CHANGES basis as the builtin case.",
  "prior_expectation": "Test asserts psh -c stdout == bash -c stdout, which at the 2026-08-09 gate was 'A:\\nB:\\n' (eager reap). psh today still prints 'A:\\nB:\\n' via the command_mode filter at psh/builtins/job_control.py:96-98.",
  "proposed_retune": "Same psh change as the builtin -c test: remove the command_mode filter (psh/builtins/job_control.py:96-98) and rewrite comment :85-95; rename test to test_completed_external_listed_once_c_mode (assertion `_psh_c(EXTERNAL) == _bash_c(EXTERNAL)` can stay verbatim; consider adding `assert 'Done' in out`); update module docstring lines 8-9 and section comment line 104; plus the 24->27 width edit for byte identity. Validated MATCH via scratch driver in -c mode.",
  "effort": "M",
  "note": "With the filter removed psh's -c behaviour becomes identical to its script/stdin behaviour, which is exactly what bash 5.3 now does; the 'deferred -c+monitor boundary notice' mentioned in the psh comment and CHANGELOG.md:1205 no longer exists on 5.3 (probed `set -m; sleep 0.1 & sleep 0.3; :` -> empty stderr on both shells).",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no user-guide claim about -c-mode jobs suppression."
 },
 {
  "node_id": "tests/integration/job_control/test_jobs_completed_listing_modes.py::test_jobs_n_completion_listed_once_script",
  "category": "FORMAT",
  "bash53_behavior": "Script `sleep 5 & sleep 0.2 & echo A:; jobs -n; sleep 0.4; echo B:; jobs -n; echo C:; kill %1 2>/dev/null` -> stdout 'A:\\n[1]-  Running                    sleep 5 &\\n[2]+  Running                    sleep 0.2 &\\nB:\\n[2]+  Done                       sleep 0.2\\nC:\\n', stderr '', rc 0. jobs -n semantics (both listed first, only the completed one re-shown) unchanged; every line is 27-wide.",
  "prior_expectation": "psh stdout == live bash stdout; psh today emits the same four job lines with 24-wide fields ('Running' + 17 spaces, 'Done' + 20 spaces).",
  "proposed_retune": "psh width edit 24->27 (psh/executor/job_control.py:279/:280/:683); no test edit. Scratch-driver validation: MATCH in script (and -c, stdin) mode.",
  "effort": "M",
  "note": "The -n notified-flag logic is untouched by either retune (verified: `jobs -n; jobs -n` in -c mode after removing the filter matches bash 5.3: listed once, then empty).",
  "user_guide_claim": "Not mapped in CLAIM_TESTS; no Full-support user-guide row."
 },
 {
  "node_id": "tests/conformance/bash/test_bad_substitution_conformance.py::TestBadSubstitutionRejected::test_rejected_cases_match_bash",
  "category": "SEMANTIC",
  "bash53_behavior": "Fails on the 2nd BAD_CASE: `echo ${ }` -> bash 5.3.15 rc=0 stdout='\\n' stderr='' (assertion: `bash unexpected for 'echo ${ }'` / `assert 0 == 1`). bash 5.3 NEWS item s: new `${ command; }` / `${|command;}` function substitution, so `${ }` is an EMPTY funsub. Also changed: `echo ${ :-x}` -> rc=2 stderr \"/opt/homebrew/bin/bash: -c: line 2: unexpected EOF while looking for matching `}'\" (`${ ` opens a funsub that `:-x}` never closes). The other 9 BAD_CASES (`${}`, `${1abc}`, `${.foo}`, `${a.b}`, `${:-x}`, `${1abc:-x}`, `${!.foo}`, `${!1abc}`, `${! }`) still rc=1 'bad substitution' with message tails identical to psh (verified). psh today: `${ }` -> rc=1 'psh: line 1: ${ }: bad substitution'; `echo ${ echo fs; }` -> psh rc=1 bad substitution where bash prints 'fs'; `echo ${| REPLY=rv; }` -> psh rc=1 where bash prints 'rv'. psh has no funsub support (grep of psh/ finds none).",
  "prior_expectation": "All 11 BAD_CASES exit 1 with 'bad substitution' (module docstring: 'All expectations verified against bash 5.2'; lists `${ }` among rejected forms).",
  "proposed_retune": "Interim (S): remove \"echo ${ }\" and \"echo ${ :-x}\" from BAD_CASES; rewrite the module docstring to say bash 5.3's `${ cmd; }`/`${|cmd; }` function substitution makes `${ }` an empty funsub (rc 0, expands to nothing) and `${ :-x}` an unclosed-funsub parse error (rc 2); add a both-sides declared-divergence pin: bash `echo ${ }` -> ('\\n',0), `echo ${ echo fs; }` -> ('fs\\n',0), `echo ${| REPLY=rv; }` -> ('rv\\n',0) vs psh rc 1 'bad substitution'. Real retune to follow bash 5.3 (L): implement `${ cmd; }` / `${| cmd; }` function substitution in lexer/parser/expansion, then move those rows to parity pins.",
  "effort": "L",
  "user_guide_claim": "none (not in CLAIM_TESTS; user guide 18_troubleshooting.md:56-67 shows 'bad substitution' only for ${var[0]}/${var:bad})",
  "note": "Orchestrator context confirmed. Effort L is for matching bash 5.3 (funsub is real feature work); the expectation-only edit that greens the test honestly is S. Sibling tests test_reported_at_runtime_not_parse / test_not_taken_branch_does_not_error use `${}` (not `${ }`) and are unaffected."
 },
 {
  "node_id": "tests/conformance/bash/test_subscript_keying_conformance.py::test_divergence_procsub_compound_render_residual",
  "category": "FORMAT",
  "bash53_behavior": "Only the `case` param fails. `declare -A a; a[<(case x in y) echo n;; esac)]=v; for k in \"${!a[@]}\"; do printf \"%s\" \"$k\"; done` -> bash 5.3.15 stdout '<(case x in y)\\n        echo n\\n    ;;\\nesac)' (pattern `y)` now on the `case x in` line, no trailing space / own line). if/for/while params unchanged and pass ('<(if true; then\\n    echo x;\\nfi)', '<(for i in 1 2;\\ndo\\n    echo x;\\ndone)', '<(while false; do\\n    :;\\ndone)'). psh (rd and combinator): '<(case x in y) echo n;; esac)' raw spelling, unchanged. Note bash's `declare -f` printer still puts the pattern on its own line ('case x in \\n        y)\\n            echo n\\n        ;;\\n    esac'); only the procsub-in-subscript (make_command_string) render moved.",
  "prior_expectation": "bash_key for the case param: '<(case x in \\n    y)\\n        echo n\\n    ;;\\nesac)' (bash 5.2 layout with `case`'s trailing space and the pattern on its own line).",
  "proposed_retune": "Change the 4th parametrize tuple to ('case x in y) echo n;; esac', '<(case x in y)\\n        echo n\\n    ;;\\nesac)') and drop the docstring phrase about `case`'s trailing space (verified: bash 5.3.15 emits exactly that string; psh side unchanged, both parsers).",
  "effort": "S",
  "user_guide_claim": "none (not in CLAIM_TESTS)",
  "note": "Cross-ref docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md:31 lists this pin as a render residual that 'flips only with a faithful re-render' — this is a bash printer layout change, not a psh flip."
 },
 {
  "node_id": "tests/conformance/bash/test_subscript_keying_conformance.py::test_divergence_sq_in_dq_readback_outcome",
  "category": "PREMISE",
  "bash53_behavior": "`declare -A h; h['$(if)']=v; echo \"read=${h['$(if)']}\"; echo rc=$?` -> bash 5.3.15 rc=0 stdout 'read=v\\nrc=0\\n' stderr '' — IDENTICAL to psh rd and combinator ('read=v\\nrc=0\\n', rc 0). bash no longer re-expands the dq-context subscript at read time (bash 5.3 CHANGES k: 'Bash attempts to expand indexed and associative array subscripts only once when executing shell constructs and word expansions'). The divergence is CLOSED on the oracle side. Undeclared-target sibling test_sq_inside_dq_subscript_runtime_stage_parity (line 978) still passes: bash rc 1 \"command substitution: line 2: syntax error near unexpected token `)'\". test_divergence_dq_ansi_bracket_read still holds on 5.3 (bash rc 1: \"bad substitution: no closing `}' in \\\"read=${a[[]}\\\"\" and 'read=${a[]x]}: bad substitution').",
  "prior_expectation": "b.returncode == 1 and b.stdout == '' and 'syntax error' in b.stderr (bash attempts the $(if) cmdsub at read time); psh rc 0 'read=v\\nrc=0\\n'.",
  "proposed_retune": "Convert to a parity pin: `assert p.returncode == b.returncode == 0 and p.stdout == b.stdout == 'read=v\\nrc=0\\n' and b.stderr == ''` plus the existing `_psh_comb(cmd).stdout == p.stdout`; rename (e.g. test_sq_in_dq_readback_round_trips) and rewrite the docstring: R1-6 divergence closed by bash 5.3's expand-once change (bash 5.2.26 failed rc 1 attempting the cmdsub). Record the oracle-side flip against FLIP-PINS.md:47 (2.3 KEEP-ruled v0.758.0) and 2.3-rescue/slot-ledger.md:710/768/848.",
  "effort": "S",
  "user_guide_claim": "none (not in CLAIM_TESTS)",
  "note": "Verified with the harness in retune-check.txt: parity holds for rd + combinator."
 },
 {
  "node_id": "tests/conformance/bash/test_subscript_keying_conformance.py::test_unlexable_subscript_route_audit[let_arith]",
  "category": "PREMISE",
  "bash53_behavior": "`declare -A a; let 'a[\"]=7'; echo rc=$?; declare -p a` -> bash 5.3.15 rc=0 stdout 'rc=0\\ndeclare -A a\\n' stderr \"/opt/homebrew/bin/bash: line 1: let: `a[\\\"]': not a valid identifier\". `let` no longer fails on the invalid identifier: the assignment is dropped with a diagnostic, nothing is stored, the line continues, and let's status is the expression value's truth (`let 'a[\"]=0'` -> rc=1, verified; `let 'a[\"]'` read form -> rc=1 with two 'bad array subscript' lines). psh (rd + combinator): rc=1 stdout '' stderr 'psh: line 1: [\"]: bad array subscript' (typed abort of the line) — unchanged. The other 7 route rows (testv/unset/indirection/dparen/nameref/read_into/printf_v) still pass on 5.3.",
  "prior_expectation": "declared_typed branch: `b.returncode == 0 and 'rc=1' in b.stdout`; psh rc 1, stdout '', 'bad array subscript' in stderr.",
  "proposed_retune": "In the `declared_typed` branch replace the bash assertion with `assert b.returncode == 0 and 'rc=0' in b.stdout and 'declare -A a\\n' in b.stdout and 'not a valid identifier' in b.stderr` (bash 5.3: diagnostic, nothing stored, line continues, status = truth of the expression value 7); keep the psh assertions. Update the _UNLEXABLE_ROUTE_ROWS comment ('MEASURED post-R4-2' -> re-measured at bash 5.3.15) and the disposition comment for let_arith.",
  "effort": "S",
  "user_guide_claim": "none (not in CLAIM_TESTS)",
  "note": "No bash 5.2 binary on the host (/opt/homebrew/Cellar/bash has only 5.3.15; /bin/bash is 3.2), so 5.2's exact wording/status was not observed — only the test's pinned 'rc=1'. psh's disposition is unchanged; the divergence widened on the bash side (rc 1 -> rc 0 from let)."
 },
 {
  "node_id": "tests/conformance/bash/test_subscript_keying_conformance.py::TestCompositeQuoting::test_tilde_expands_in_key",
  "category": "ENV",
  "bash53_behavior": "`HOME=/probe-home; declare -A a; a[~]=v; echo \"${!a[@]}\"` -> bash 5.3.15 '/Users/pwilson\\n' (the STARTUP environment HOME); psh '/probe-home\\n'. This is an oracle-BINARY artifact, not bash 5.3 semantics: the Homebrew bottle is built `--with-installed-readline` (brew cat bash line 127; otool -L shows /opt/homebrew/opt/readline/lib/libreadline.8.dylib; `nm -m` shows `_tilde_expand` UNDEFINED, resolved from libreadline while bash's own `_sh_get_env_value`/`_get_string_value` are defined in the executable). Tilde expansion therefore runs inside libreadline, whose getenv-based sh_get_env_value wins under the two-level namespace. Consequence on this host: EVERY unquoted `~` (echo ~, ~/x, x=~, assoc subscripts, eval, newline-separated, script file, --posix) resolves from the process's startup HOME and ignores in-shell reassignment and `unset HOME`: `env HOME=/env-home bash -c 'HOME=/tmp; cd && echo $PWD; echo ~'` -> '/tmp' then '/env-home' (cd uses the shell variable; ~ does not); with HOME absent from the env -> passwd home. Deterministic (30/30 runs; single-env-var bisection found no flipping variable). With HOME supplied through the ENVIRONMENT the row is identical in both shells: env HOME=/probe-home + `declare -A a; a[~]=v; echo \"${!a[@]}\"` -> '/probe-home\\n' rc 0 in bash and psh.",
  "prior_expectation": "assert_identical_behavior -> both print '/probe-home\\n' (comment block lines 30-51: 5.2.24+ prints /probe-home; skip guard _OLD_BASH_NO_SUBSCRIPT_TILDE does not skip 5.3.15).",
  "proposed_retune": "Supply HOME via the environment instead of an in-script assignment: `self.assert_identical_behavior('declare -A a; a[~]=v; echo \"${!a[@]}\"', env={'HOME': '/probe-home'})` (conformance_framework.py:383 already accepts env=; verified passing via run_bash(env=hermetic_shell_env({'HOME':'/probe-home'})) / run_psh(env={'HOME':'/probe-home'})). Extend the comment block to record that the Homebrew 5.3.15 bottle (installed-readline link) resolves `~` from the startup environment, so the row must not reassign HOME in-script. Do NOT make psh copy this: it contradicts the bash manual (HOME shell variable) and bash's own `cd`, and psh's behaviour matches GNU bash with bundled readline.",
  "effort": "S",
  "user_guide_claim": "none (CLAIM_TESTS 'Tilde expansion' maps posix/test_posix_compliance.py 'echo ~/test', a different test)",
  "note": "Only this module combines in-script HOME= with ~ (grep of tests/conformance), so no other conformance row is exposed today, but any future probe that reassigns HOME then expands ~ will diverge on this oracle. Cross-refs: 1.4-rescue/slot-ledger.md rows C4/16, nightly-status.md:32. Evidence: tmp/program-2026-09/expansion-subscript-divergences/tilde-matrix.txt, tilde-envbisect.txt, tilde-determinism.txt."
 },
 {
  "node_id": "tests/conformance/bash/test_syntax_template_timing_conformance.py::test_redirect_procsub_suppression_is_a_declared_divergence",
  "category": "ENV",
  "bash53_behavior": "Could not reproduce the failure. PASSED 3/3 in isolation ('1 passed in ~5.6s'), PASSED in the whole module (186 passed in 117.52s), and PASSED under `-n 8` xdist alongside test_subscript_keying_conformance.py (429 passed; the only 4 failures were the subscript rows). Underlying rows match the test's expectations exactly on bash 5.3.15 vs psh: read rows -> bash 'F:1\\nEND\\n' / psh 'got:A\\nEND\\n' (both the plain and eval forms); write row -> bash 'ok\\nNOWRITE\\nEND\\n' / psh 'ok\\nWROTE\\nEND\\n'. This confirms it as the 'possibly one more' ENV entry in the orchestrator's list.",
  "prior_expectation": "Same as observed: bash kills the redirect-spelled procsub child at `false` under set -e; psh's child runs on (declared divergence, both channels).",
  "proposed_retune": "No expectation change. If the gate log shows the psh write row as 'ok\\nNOWRITE\\nEND\\n', the mechanism is environmental: the row does `d=$(mktemp -d)` and a `> >(...)` child writing $d/w.txt with a 10x0.1s bounded poll — a sandbox denying TMPDIR writes/FIFO creation or heavy load exceeding 1s would turn WROTE into NOWRITE. Mitigation if it recurs: lengthen the poll (e.g. 30x0.1s) or run that gate phase unsandboxed. Hypothesis only; not observed here.",
  "effort": "S",
  "user_guide_claim": "none (not in CLAIM_TESTS; psh/core/internal_errors.py:280 and FLIP-PINS.md:20 reference the test name in prose only)",
  "note": "Not oracle-version related: bash 5.3.15 output equals the pinned 5.2 expectations for all read/write/control rows."
 },
 {
  "node_id": "tests/conformance/bash/test_typed_expansion_errors_conformance.py::TestDeclaredDivergences::test_invalid_regex_diagnostic_is_psh_only",
  "category": "PREMISE",
  "bash53_behavior": "`[[ x =~ [ ]]; echo rc=$?` -> bash 5.3.15 rc=0 stdout 'rc=2\\n' stderr \"/opt/homebrew/bin/bash: line 1: [[: invalid regular expression `[': brackets ([ ]) not balanced\" (bash 5.3 NEWS item g: 'Print an error message if a regular expression used with [[ fails to compile'). Assertion diff: `assert '/opt/homebre...ot balanced\\n' == ''`. psh: rc=0 'rc=2\\n' stderr 'psh: [[: invalid regex: unterminated character set at position 0'. Status parity (2) holds; sibling test_invalid_regex_is_a_typed_user_error_not_a_masked_defect passes. Same shape for `re=\"[\"; [[ x =~ $re ]]`.",
  "prior_expectation": "b.stderr == '' (bash silent) while psh prints 'invalid regex'; both status 2.",
  "proposed_retune": "Rename (e.g. test_invalid_regex_diagnostic_wording_differs) and assert `b.returncode == p.returncode`, `b.stdout == p.stdout == 'rc=2\\n'`, `'invalid regular expression' in b.stderr`, `'invalid regex' in p.stderr`; docstring: both shells now diagnose, wording differs (bash: regerror() text; psh: Python re message). Optional M follow-up: reshape psh's message at psh/executor/enhanced_test_evaluator.py:195 to bash's frame \"[[: invalid regular expression `<re>': <reason>\" with the error_location_prefix (`line 1:`), accepting that the <reason> text cannot match libc regerror strings without a mapping table. Update LEDGER.md:413 (D-3.5-s5) and FLIP-PINS.md:87.",
  "effort": "S",
  "user_guide_claim": "none (not in CLAIM_TESTS; 'Regex matching =~' in 17_differences_from_bash.md:973 is a Yes|Yes row not mapped to this test)",
  "note": "Side finding outside this node: `[[ x =~ a{1 ]]` -> bash 5.3 rc=2 + \"invalid regular expression `a{1': braces not balanced\"; psh rc=1 silent (Python re accepts `a{1` as a literal) — a pre-existing psh divergence now visible because bash diagnoses; not covered by any of the seven nodes."
 }
]