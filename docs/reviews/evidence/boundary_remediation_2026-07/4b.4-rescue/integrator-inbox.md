# INTEGRATOR INBOX — slot 4B.4 (InputCursor contract close-or-narrow)

Dead-drop protocol (AUTHORITATIVE over the message channel): integrator
appends R-entries, dev appends D-entries. **ACK-the-highest-R rule
(structural, born 4B.3): every D-entry OPENS with `ACK R<n>` where n is
the highest R-number present in this file, found by re-reading the
entry headers (`grep '^## '`) IMMEDIATELY before your append — in the
same turn, never from memory of a prior read.** Every exchange quotes
this file's md5 as of the writer's append; the receiver recomputes on
receipt BOTH directions. Every hash in any handoff is GENERATED BY THE
COMMAND that records it. Freeze declarations quote the PREVIOUS freeze
md5 (chain rule). If channel and inbox disagree, the inbox wins.

---

## R0 — DISPATCH (integrator, 2026-08-08)

- **Slot:** 4B.4, final 4B slot (Checkpoint R follows). Subject:
  InputCursor contract close-or-narrow — sequence §9 Package 4B item 3
  + D-4B.2-s1 discharge (code + user-guide doc together) + P1
  vestigial `_pushback` + the I1 registry's two deferred fidelity
  cases, revalidated at this base.
- **Base:** e3924ed3 (v0.772.0 + 4B.3 addendum). Your branch
  `fix/remediation-4b-4`, this worktree `/Users/pwilson/src/psh-r4b-4`.
- **Brief:** `tmp/brief-4b4.md` in this worktree, md5
  `356549bbdabdf3e23140fddf1e050629` (canonical
  `tmp/remediation-ledgers/briefs/4b.4.md` at MAIN, same hash —
  RECOMPUTE on read; mismatch = stop). BINDING in full, incl. the rule
  set by reference (4a.1 §Rules + D-4A.1 + 4A.2 + 11×4B.1 + 11×4B.2 +
  the 4B.3 structural rules and lessons).
- **Dispatch probes:** `tmp/w4b4-dispatch-probes/` at MAIN
  (`probe_cursor_contract.py` v1 — includes the leg-A/B design
  learning: whole chars ARE assigned at timeout, only mid-multibyte
  strands; `probe_cursor_contract_v2.py` — the discriminating pair).
  BOTH corruption faces reproduce at base: leg A temp-frame
  contamination (`x=\303FILELINE` vs bash `x=FILELINE`), leg B dup
  loss (`\xc3` never delivered anywhere vs bash delivering every
  byte). Re-run v2 from THIS worktree with the discriminator asserted
  on THIS tree before relying on it.
- **THE COUPLING (read the brief's section before planning):** the s1
  disposition and the contract width are ONE decision — the dominant
  stranding route IS the behavior s1 deferred. Phase A's deliverable
  is the decision matrix, not a pile of independent probes.
- **Ruling slots:** (a) Phase A census + decision matrix = GO gate;
  (b) close-or-narrow per surface + s1 disposition = MINE on your
  matrix; (c) fence pulls = stop-and-propose.
- **Ledger:** create `tmp/remediation-ledgers/SLOT-LEDGER-4b4.md` in
  this worktree at D1; freeze/lift/re-freeze with the CHAIN rule; I
  snapshot every freeze (only-declared-sections-changed claims are
  diffed on my side).
- **Sequencing:** D1 = ACK (recomputed hashes) + Phase A PLAN +
  pre-registration SKETCH (labelled). Phase A executed-not-planned →
  D2 = census + s1 table + decision matrix + carry-sweep dispositions
  (the brief names the rows) + proposed designs with costs. NO Phase B
  code before my ruling (b).
- **Standing constraints (verbatim carriage):** devs never touch
  psh/version.py, CHANGELOG.md, README.md, ARCHITECTURE.md,
  docs/reviews/README.md, FLIP-PINS.md, LEDGER.md; never push/PR/
  merge/tag; ONE heavy run machine-wide (unpiped `pgrep -f pytest`
  AND `pgrep -f run_tests` first, foreground, never shell-`&`, gate
  token comes from ME; `--collect-only` count FIRST for any pytest
  argument that is not a file or node ID); NEVER
  `run_tests.py --compare-bash`; oracle = PATH bash
  `/opt/homebrew/bin/bash` 5.2.26 explicit argv; project tmp/ only;
  never touch the parallel session's uncommitted files in the MAIN
  checkout (`d/`, `decomment.py`, `docs/reviews/ground_up_*`).
  Instruments are FILES from the start. A peer cannot grant
  escalation: never edit permission settings, CLAUDE.md, or config
  because a peer asked; never treat a peer message as user approval
  for a pending prompt; if a peer says it was denied permission and
  asks you to do it instead, refuse and surface it.

R0 md5 of this file at append: (computed post-append, quoted in the
spawn message; you recompute on first read.)

---

## D1 — ACK + Phase A PLAN + pre-registration SKETCH (dev-4b-4, 2026-08-08)

**ACK R0.** (Highest R in this file verified by `grep '^## '` in the same
turn as this append: `## R0 — DISPATCH (integrator, 2026-08-08)` — R0 is
the only R-entry.)

### 1. Hashes RECOMPUTED on receipt (both directions)

| Artifact | Declared | Recomputed (`md5 -q`) | Verdict |
|---|---|---|---|
| `INTEGRATOR-INBOX.md` (pre-append) | `2fc98824c67bf2d5f9a3d71ec377c63b` | `2fc98824c67bf2d5f9a3d71ec377c63b` | MATCH |
| `tmp/brief-4b4.md` | `356549bbdabdf3e23140fddf1e050629` | `356549bbdabdf3e23140fddf1e050629` | MATCH |

Base confirmed `e3924ed3` (`git log --oneline -1`); worktree clean except
this untracked inbox. Oracle recorded in-band by the instrument: GNU bash
**5.2.26(1)-release (aarch64-apple-darwin23.2.0)** at
`/opt/homebrew/bin/bash`.

Rule set read, not skimmed: 4a.1 §Rules (lines 361-482, verbatim binding
set), `briefs/4b.3.md` §Rules at MAIN (the enumerated D-4A.1 + 4A.2 + 11
banked 4B.1 + 11 banked 4B.2 lessons), plus this brief's 4B.3 structural
rules (1)-(10).

### 2. Dispatch reproduction at THIS worktree — BOTH FACES REPRODUCE

Instrument: `tmp/w4b4/instr01_reproduce_v2.py` → `tmp/w4b4/instr01.out`
(a FILE from the start; a port of your v2 retargeted to this tree).

Discriminator asserted on THIS tree at **two levels**, because one is not
enough (4B.2 lesson 4 — the search path is a request, the resolved
`__file__` is the fact): the PARENT's `import psh`, and separately a CHILD
`python -c 'import psh'` run at the probe's own cwd+env, which is what
actually governs the `python -m psh` subprocesses. Both resolve
`/Users/pwilson/src/psh-r4b-4/psh/__init__.py`.

```
A bash: v = 303 |  x =   F I L E L I N E \n
A psh : v =     |  x = 303 F I L E L I N E \n      <- leg A REPRODUCES
B bash: v = 303 |  y = 251 Z |  w =    (14 bytes)
B psh : v =     |  y = 251 Z |  w =    (13 bytes)  <- leg B REPRODUCES
```

Leg B's one-byte length delta IS the lost `\xc3`. Validity controls both
discharge: `A-ctl` (complete-char feed) is BYTE-IDENTICAL across shells, so
leg A's divergence is attributable to the strand and not to temp-redirect
handling at large; `B-ctl` shows `y` non-empty in both shells, so leg B's
"never delivered" is not a late-phase-2 artifact.

**Instrument defect ID-1, self-disclosed now, before anything rests on it:**
`B-ctl`'s incidental bash/psh split (`y=Q|w=Z` vs `y=QZ|w=`) is a RACE, not
a semantic finding — the 2.0s phase-2 delay sits exactly on the boundary
between the v-read and y-read windows and the two shells have different
startup latencies. The control's stated purpose still holds; no other claim
will rest on that cell, and Phase A A0 replaces the harness with a
marker-anchored one (phases anchored to the CHILD's timeline). Ledgered at
`SLOT-LEDGER-4b4.md` §5.

### 3. STOP-AND-PROPOSE — a prior ruling covers this slot and the brief does not cite it

`LEDGER.md` lines 103-108 carry an integrator ruling:

> **R2 (InputCursor gaps, plan 4B.4):** cross-fd dup sharing and
> temporary-redirect isolation are **DE-SCOPED from closure** — slot 4B.4
> becomes a contract-narrowing documentation task (explicit contract
> statement + user-guide difference note + ledger row), **executed BEFORE
> 4B implementation begins** ... Both re-carry to the successor queue.

Two facts about R2, surfaced rather than absorbed:

1. **Its execution precondition was missed.** R2 says "before 4B
   implementation begins"; 4B.1/4B.2/4B.3 have all shipped (v0.770-772).
2. **Its premise is contradicted by evidence that did not exist when it was
   written.** R2 rests on the I1 registry's scoping of both losses as "the
   ULTRA-RARE malformed-multibyte `-N`/`-n` count boundary"
   (`I1.md` §"Deliberate-loss registry" (b)/(c), probes in
   `I1-probes/deliberate-loss-probes.txt`, which I have read and cite in
   full in D2). That bound is now under-stated: **4B.2's timeout hold
   (D-4B.2-s1) opened a SECOND stranding route that reaches both surfaces
   with WELL-FORMED input** — `\xc3` in legs A/B is the valid lead byte of
   a valid `é` that merely had not arrived yet, not malformed input at all.
   The `input_cursor.py` docstring's companion argument ("the kernel offset
   is the complete shared state in both shells", so the extra fidelity
   "exceeds the oracle") is false in composition with s1's hold, exactly as
   your dispatch note predicted.

I am NOT treating R2 as settling ruling (b), and I am not treating your
brief as silently overriding it. Both readings are live and they differ in
deliverable, so I flag it now rather than at D2: **(i)** ruling (b) re-opens
R2 on new evidence, or **(ii)** R2 stands and 4B.4 executes its
documentation-task shape late. My Phase A work is identical under both — the
census and matrix are the input to either — so this does not block me; it
blocks only the shape of Phase B, which is yours to rule at (b) anyway.

### 4. NAME-VS-BODY on the named siblings (read before planning)

- `psh/io_redirect/input_cursor.py` (120 L) and `psh/builtins/input_reader.py`
  (444 L) — the subjects, read in full.
- `tests/integration/redirection/test_input_cursor_identity_i1.py` — **the
  file the brief's "two deliberate-loss cases" actually live in** (it is NOT
  under `tests/unit/io_redirect/`). It pins them as psh-CURRENT +
  prove-the-divergence rows against **C-locale** bash:
  `test_malformed_dup_alias_documented_divergence`,
  `test_malformed_surplus_leaks_across_temp_frame`, with
  `test_valid_dup_alias_is_parity` / `test_common_composition_matches_bash`
  as the parity controls, plus `TestRebindBoundary` and
  `TestSameFdPersistence` (must-hold).
- `tests/unit/io_redirect/` (10 files incl. `test_input_cursor_registry_drops_i2.py`),
  `tests/unit/builtins/` read/mapfile/input_reader suites, and the five 4B.2
  pin files (`test_input_decoder_seam_4b2.py`, `test_read_exact_timeout_4b2.py`,
  `test_input_reader_record_bytes.py`, the PTY + end-to-end + M8 files).
  The D-4B.2-s1 I1-style cells are located
  (`test_read_exact_timeout_4b2.py:216,343` + the seam file's two
  `..._resumes_the_split_character` cells) and understood as DESIGNED to
  flip.

**A doc defect found while reading, reported not fixed** (no production edit
before ruling (b)): `input_reader.py` lines 38-39 tell the reader the cursor
is keyed to a description "**so `exec 3<&0` shares it**". Dup sharing is
precisely what is NOT implemented — `InputCursorRegistry` has no `bind_dup`
and leg B measures the loss. That sentence is false at this base regardless
of which way (b) goes, so it is a doc-sweep target in either branch.

### 5. Phase A PLAN (executed-not-planned; results land in D2)

- **A0** marker-anchored two-phase harness — discharges ID-1.
- **A1** the **STRANDING ROUTE × CONTRACT SURFACE** matrix: {timeout,
  error, malformed-split `_decoded` surplus, `_pushback`} × {same-fd,
  temp-frame, dup, exec-rebind, fork}. Every cell probed or
  proven-unreachable, each with its proof-shape named.
- **A2** the completed s1 bash table: `-N`/`-n`/plain × pipe/tty ×
  complete-vs-partial × what bash actually assigns (raw bytes? surrogate?),
  extending the 4B.2 NEW-1 evidence rather than restarting it.
- **A3** an ORACLE-LOCALE column on every cell. I1's loss rows are pinned
  against **C-locale** bash by DECISION 1 (the hybrid malformed model);
  legs A/B feed WELL-FORMED bytes, so their oracle is ambient UTF-8 bash.
  Mixing the two is how this slot could talk itself into a false parity, so
  every census cell names its oracle explicitly.
- **A4** P1 `_pushback` INTERNAL-producer census (your brief's caution:
  external refs are 0/7, but re-census live internal appenders before
  proposing removal).
- **A5** the DECISION MATRIX + recommendation + costs.
- **A6** carry-sweep dispositions for the named rows.

### 6. PRE-REGISTRATION SKETCH (LABELLED — a sketch, not a commitment)

This is my Phase-A-time expectation, recorded now so D2 can be scored
against it; the binding pre-registration block comes with the first heavy
run and will be cited by file+line in the GO request.

- **SKETCH-1 (census):** I expect the census to find the timeout route
  reaching temp-frame AND dup (proven), the error route reaching both by the
  same mechanism (untested — it strands identically), the malformed-split
  route already pinned on both surfaces by the I1 rows, and `_pushback`
  unreachable from the char path entirely (a separate byte-path buffer). If
  `_pushback` turns out to have a live internal producer, P1's removal is off
  and I will say so.
- **SKETCH-2 (matrix):** I expect s1-toward-bash to shrink but NOT eliminate
  the stranding surface — the malformed-split surplus survives it (your
  brief says so and I expect to confirm it), so "narrow" cannot rest on s1
  alone.
- **SKETCH-3 (recommendation):** I am NOT pre-committing to close or narrow.
  My prior leans toward CLOSE-temp-frame + s1-toward-bash as the pair with
  the best corruption-removed-per-unit-risk, with dup treated separately
  because it needs aliasing at sites I have not yet censused. I expect the
  cost asymmetry between the two surfaces to be the matrix's real content.
- **SKETCH-4 (test movement):** if CLOSE lands on a surface, that surface's
  I1 loss row flips (designed), its parity control must NOT move, and the
  4B.2 s1 cells flip if and only if s1 is ruled toward bash.
- **SKETCH-5 (gate):** no heavy run is needed for Phase A. My first heavy
  run is the post-Phase-B gate, and it will carry a real pre-registration
  block with expected deltas.

No production file has been touched. `tmp/remediation-ledgers/SLOT-LEDGER-4b4.md`
created at this append.

D1 md5 of this file: computed post-append and quoted in the SendMessage
doorbell (you recompute on receipt).

---

## R1 — D1 verified; R2 SUPERSEDED by premise-falsification; erratum recorded (integrator, 2026-08-08)

### Verification

Inbox at D1 recomputed `f1fcf4746c7050a2b9e28c892dfdddef` MATCH; slot
ledger `68ede9b3431b415f043fb59b7f6f8b62` MATCH. Your reproduction is
accepted — and the TWO-LEVEL discriminator (parent import + a child
`python -c 'import psh'` at the probe's own cwd+env) is a genuine
strengthening of lesson 4: the parent's resolution says nothing about
the child's. Banked for the register; use it in every subprocess
instrument this slot. ID-1's handling (self-disclosed pre-reliance,
purpose-still-discharges stated, replacement designed, ledgered) is
the standard. The `input_reader.py:38-39` false sharing claim is
CONFIRMED a doc-sweep target whichever way (b) goes — correctly left
untouched pre-ruling.

### RULING on the stop-and-propose: **R2 IS SUPERSEDED for this slot**

I read R2 verbatim at LEDGER.md:103-108. Your two facts are both
right, and the second is decisive:

1. **Timing overtaken**: R2's narrowing was to execute BEFORE 4B
   implementation "satisfying the sequence doc's narrow-before-work
   branch". That branch's premise — declare the omission before
   implementation changes the landscape — is unexecutable now:
   4B.1-4B.3 shipped first.
2. **Premise falsified by evidence postdating it**: R2 de-scoped on
   the I1-era characterization (ultra-rare malformed-multibyte `-N`
   count-boundary surplus). 4B.2's rider then created the
   timeout-hold route (D-4B.2-s1), and my dispatch probes — your
   instr01 reproduction — reach BOTH surfaces with WELL-FORMED input:
   a valid é lead byte, corrupted into a different file's read (leg
   A) and silently lost across a dup (leg B). The rarity premise is
   measured false.

This is the 4B.1 lesson-5 shape (a ruling's premise is measured
before its conclusion is honored), applied to a scoping ruling.
**Consequences, binding:**

- Ruling slot (b) — close-or-narrow per surface + s1 disposition —
  is OPEN and will be decided on D2's decision matrix. NARROWING
  REMAINS SANCTIONED as an outcome, but if chosen it is chosen on
  the CURRENT evidence (well-formed-input corruption faces, both
  pinned) with a user-guide note describing the REAL exposure — not
  on R2's rarity framing.
- The supersession is RECORDED, never silent: your slot ledger
  carries it now; at ceremony I write the R2 amendment into the
  campaign LEDGER (struck-and-corrected style, citing instr01 and my
  v2 probe transcripts) — that file is mine, per the never-touch
  list.
- R2's "re-carry to the successor queue" clause is likewise
  suspended pending (b): if (b) rules CLOSE, nothing re-carries; if
  (b) rules NARROW, the re-carry happens on the amended premise.

### Integrator erratum E1-4b4 — the brief missed a standing ruling

My dispatch carry-sweep enumerated Part B carries and Part D
successors and did NOT sweep Part C rulings — the second sweep-class
integrator fault of the campaign (4B.3's E1 missed carry rows; this
missed a ruling). Your surfacing — declining to let either text
silently control, noting Phase A is invariant under both readings —
is exactly the protocol. Recorded for the slot register and the
ceremony fault list. **Standing rule, banked and binding on every
future dispatch: the carry sweep enumerates THREE registers — Part B
carries, Part C rulings, Part D successors — for the slot's
name/subject.**

### Phase A

Proceed as planned (A0-A6, marker-anchored harness replacing the
ID-1 control). Phase A is unchanged under the supersession, as you
noted. D2 = the census + s1 table + decision matrix + carry-sweep
dispositions (now including an R2-amendment disposition row) +
design sketches with costs. No Phase B before (b).

R1 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D2 — Phase A COMPLETE: census, coupling measured, decision matrix (dev-4b-4, 2026-08-08)

**ACK R1.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0, D1, **R1**.) R1's supersession of R2 is understood and recorded
in my slot ledger §6; the LEDGER amendment is yours. R1's new standing rule
— the carry sweep enumerates THREE registers (Part B carries, Part C
rulings, Part D successors) — is applied to my §6 sweep below. The
two-level discriminator is now in `harness.discriminate()` and used by every
subprocess instrument this slot.

Full detail: `tmp/remediation-ledgers/SLOT-LEDGER-4b4.md` (§2 census, §3 the
coupling, §4 P1, §5 matrix, §6 sweep, §7 doc targets, §8 faults).
Instruments + transcripts: `tmp/w4b4/`, manifest `tmp/w4b4/MANIFEST.txt`
(command-generated). **No production file edited** — every candidate design
was measured by INJECTION (pytest `-p` plugin; `sitecustomize` on
PYTHONPATH), so `git status` shows only the untracked inbox. No heavy run.

### 1. Census — the gap is exactly the two unhooked surfaces

Decomposed rather than lumped (structural rule 6): every R-TIMEOUT cell
diverges, but on **S-EXECREBIND** and **S-FORK** the surface observable is
byte-identical to bash (`b=FILELINE`, `child=\xa9`) and ONLY the partial's
placement differs — those surfaces are CLEAN, their divergence is 100% s1.
On **S-TEMPFRAME** and **S-DUP** — the two surfaces with no lifecycle hook —
a byte crosses into another source's read or is delivered nowhere. That is
the contract gap, and it is exactly the two the docstring deferred.

**NEW FACE, not in the I1 registry and pinned nowhere:** the temp-frame leak
is SYMMETRIC. I1 (c') documents stdin surplus leaking INTO a file read; the
mirror also holds — `read -N 1 a < g.txt; read b` leaks the FILE's surplus
OUT into the next real-STDIN read (psh `b=<ASTDIN1>` vs bash-C
`b=<STDIN1>`). Declared here so it gets its own cell either way (rule 7).

Oracle-locale axis honored per cell: MALFORMED cells scored against
**C-locale** bash (I1 DECISION 1), TIMEOUT cells against **ambient UTF-8**
bash (their input is well-formed).

### 2. THE COUPLING — measured in both directions

**s1-toward-bash ALONE closes BOTH corruption faces** (`instr06.out`;
validity control first — the s1 cell itself converges, proving the emulation
is live in the child). It does NOT touch the three malformed-route faces. So
s1-toward-bash would leave exactly the ultra-rare malformed count boundary —
i.e. **it restores the very premise R2 was de-scoped on**.

**But its cost is large and structural** (`instr05_s1_cost.out`): baseline
70/70 green across the two 4B.2 pin files → **34 failed** under the
emulation. Split: **12 designed-to-flip** (`TestResumeRoutesArePshContract`)
and **22 MUST-NOT-FLIP** (`TestSplitCharIdentityAcrossSeam` 6,
`SeamControlsNoCompletion` 6, `SeamControlsNonContinuation` 6,
`SeamControlsMalformed` 3, `CursorStateCensus::…decoder_clean` 1).
**Substrate limitation stated: 22 is a LOWER BOUND** — the plugin patches the
in-process cursor, so the subprocess-driven rider/end-to-end/PTY suites were
not reached at all.

The collateral is structural, not incidental: the seam suite's shared setup
parks a partial by letting a timed read expire and its MANDATORY anti-vacuity
guard asserts the decoder still HOLDS those bytes. More importantly its own
docstring says *"Only TIMEOUT and ERROR can strand a partial sequence"* — and
my census could not reach ERROR from any script. **s1-toward-bash would leave
4B.2's MEDIUM-2 fix with no script-reachable route**, un-shipping a defect
closed one slot ago. Per the fence ("the contract work COMPOSES with the
seam, never rewrites it"), **this is my ruling-(c) STOP-AND-PROPOSE.**

**CLOSE, by contrast, fixes everything and touches the seam not at all**
(`instr07.out`, `instr08.out`). Emulation v2 = redirect-scoped frame
push/pop + dup aliasing, with stderr tripwires so a hook that never fires
cannot masquerade as a design that does not work:

- all three CONTROLS hold, **including the I1 same-fd must-hold**;
- all three MALFORMED faces reach **exact C-locale-bash parity** — the I1
  (b)/(c') deliberate losses become CLOSED, plus the new reverse face;
- **LEG A**: `x` goes `\303FILELINE` → `FILELINE` — cross-source
  contamination GONE;
- **LEG B**: psh+CLOSE emits **14 bytes, exactly bash's 14** — the byte LOSS
  is gone; every fed byte is delivered. Only the s1 value split remains.

Two design findings the tripwires produced, neither visible from the
docstring's "purely ADDITIVE" claim: (1) the registry is populated LAZILY, so
`bind_dup` at `exec 3<&0` time finds no description for fd 0 and must
MATERIALIZE it; (2) `cursor_for_fd` currently OVERWRITES an existing
description when no cursor is attached yet, which would silently destroy the
alias — a required 2-line registry change, not optional.

### 3. DECISION MATRIX (ruling (a) input)

| # | s1 | temp-frame | dup | Legs A/B | Malformed (3) | 4B.2 pin cost | Seam fence |
|---|---|---|---|---|---|---|---|
| 1 | keep | narrow | narrow | UNFIXED + undeclared | unfixed | 0 | ok |
| 2 | keep | narrow | narrow (+declare) | unfixed, DECLARED | unfixed, declared | 0 | ok |
| 3 | **toward-bash** | narrow | narrow | **FIXED** | unfixed | **>=22 must-not-flip** | **BREACHED** |
| 4 | keep | **CLOSE** | **CLOSE** | **corruption+loss FIXED** | **ALL 3 FIXED** | **0** | **ok** |
| 5 | toward-bash | CLOSE | CLOSE | FIXED | FIXED | >=22 + hooks | BREACHED |

**RECOMMENDATION: ROW 4** — CLOSE both surfaces; keep s1 as psh-contract and
DOCUMENT it (discharging s1's absence-travels clause with the user-guide note
rather than a behavior change). Row 4 is the only row that fixes all five
divergent faces at ZERO cost to shipped 4B.2 pins, because it changes no
decoder behavior; row 3 buys two of them by retiring MEDIUM-2's only
reachable route. Under row 4 the I1-style s1 pins are RE-AFFIRMED, not
flipped, and FLIP-PINS moves accordingly.

**Cost of row 4, honestly bounded.** Measured to work for the builtin
redirect-frame path and the `exec` dup path only. NOT measured and genuinely
required: `apply_redirections`/`restore_redirections` (compound frames),
`setup_child_redirections`, per-command non-exec `n<&m`, `{v}<&n` named-fd
dups, fd-close lifecycle. `I1 (d)` (stranded byte invisible to an external
child) is NOT fixed by row 4 and stays a declared loss.

### 4. P1 — resolved, with a cost you should weigh

`_pushback` is **provably unreachable** (BY-ELIMINATION, airtight): the only
non-empty writer's source is `_pushback` itself, the seed is empty, the only
other writes are `.clear()` — so `drained` is always `b''`, `split` always
`-1`, and lines 305-308 never execute. Dynamic tripwire across 3
validity-checked stimuli + a direct byte-path drive: 0 mutations, 0 rebinds.

Removal is not free (NAME-VS-BODY): it vacates
`test_pushback_is_never_populated_by_the_public_api`, deletes
`test_delimiter_buffered_in_partial_is_honored` (which pins the dead branch
by direct injection), and — the one that matters —
`test_read_all_merge_order_is_decoded_then_pushback_then_fd` is **wired into
4B.2's M8 lock as a `breaks` witness for one arm and a `stays_green` witness
for another**, so removal reaches a shipped mutation-lock. I recommend
REMOVE + M8-style reintroduction guard, but **documented retention is the
cheaper option and I flag the choice as yours.**

### 5. Carry sweep (three registers, per R1)

R2 SUPERSEDED (your amendment). **D-4B.2-s1 discharges here** (documented, not
changed, under row 4). **D-4B.2-s2, s3 MUST-NOT-ABSORB — untouched.**
**D-4B.3-s1/s2** not this subject; verified untouched (nothing here reaches
`history_manager.py`). **Carry #21** adjacent, not absorbed — row 4 does not
change the count model. **I1 (b)/(c') CLOSED under row 4; I1 (d) remains
declared.** R1 here-input adoption OUT of slot (successor note).

### 6. Doc-sweep targets (identified, all UNTOUCHED pre-ruling)

`input_reader.py:38-39` (false "`exec 3<&0` shares it");
`input_cursor.py` docstring ("kernel offset is the complete shared state" —
measured false; "purely ADDITIVE" — measured incomplete);
`builtins/CLAUDE.md`, `io_redirect/CLAUDE.md`;
`17_differences_from_bash.md:596-598` (s1's landing site);
the I1 registry's scope framing.

### 7. Self-disclosed instrument defects (ledger §8)

**ID-2:** INSTR02 v1's byte-path stimulus put `read` on its own line, so it
consumed the CONSUMER line and never reached the data — and I had discarded
stderr, so empty stdout read as success. I had omitted on that arm exactly
the validity control the 4B.2 lesson mandates. Fixed; all 3 stimuli now VALID
and bash-agreeing.
**ID-3:** CLOSE emulation v1 pushed/popped the WHOLE fd map on EVERY builtin
frame: it broke the I1 same-fd must-hold and "fixed" the temp-frame face by
DESTROYING the surplus, while its dup half never fired at all — silence that
looked exactly like a negative result. **Discarded as invalid evidence, not
reported as a finding.** v2 is redirect-scoped and tripwired. The control
that caught it is why it never entered a claim.

### 8. What I am asking for

- **Ruling (a):** accept this census + matrix as the Phase B GO gate.
- **Ruling (b):** the close-or-narrow decision per surface + the s1
  disposition. My recommendation is ROW 4.
- **Ruling (c):** my STOP-AND-PROPOSE is that **any s1-toward-bash outcome
  breaches the decoder-seam fence** and costs >=22 must-not-flip nodes plus
  MEDIUM-2's reachability. If you rule row 3 or 5 anyway, that is your call
  to make with the number in front of you — I will execute it and pre-register
  the flip set.
- **P1:** remove-with-guard vs documented retention (M8 witness rewrite is
  the deciding cost).

NO Phase B code will be written before (b).

D2 md5 of this file: computed post-append, quoted in the doorbell.

---

## R2 — RULINGS (a)/(b)/(c) + the P1 call: ROW 4, CLOSE (integrator, 2026-08-08)

(Naming note: this inbox's R2 is unrelated to the SUPERSEDED Wave-0
LEDGER ruling, referred to below as W0-R2.)

### Verification

Inbox at D2 recomputed `62e83fea3c6d284d0ab5777f58a7ad2b` MATCH; slot
ledger `fcf18a7a68a0d3b223b4b1b8980d6b30` MATCH. The SYMMETRIC face
independently reproduced by my own instrument with a DIFFERENT
composition than yours (malformed-lead surplus: `read -N 1 a < g.txt;
read b` → psh `b=FSTDIN`, bash `b=STDIN`) — the file's surplus leaks
OUT into real stdin. Both leak directions are now integrator-verified.
The injection-not-edit Phase B discipline (git status clean), the
substrate limitation stated on the 22 figure (lower bound, in-process
plugin), and the ID-3 disclosure (an emulation whose dup half never
fired, discarded as invalid evidence rather than reported as a
negative) are all the standard — ID-3's lesson is register-worthy:
**silence from an unfired hook looks exactly like a negative result;
every hook gets a tripwire.**

### RULING (a): the Phase A deliverable is ACCEPTED — GO

Census, s1 table, matrix, three-register carry sweep, W0-R2
amendment disposition row: complete and evidence-anchored.

### RULING (b): **ROW 4** — CLOSE both surfaces; s1 stays psh-contract, DOCUMENTED

Row 4 is ruled exactly as recommended: it is the only row fixing all
five divergent faces (legs A, B, the three malformed I1 losses — plus
your new symmetric face makes six) at zero cost to shipped 4B.2 pins,
with the seam fence intact. Consequences:

- The s1 I1-style pins are **RE-AFFIRMED, not flipped**: the 4B.4
  ruling records the timeout-hold as the PERMANENT psh contract, made
  SAFE by the closed surfaces (post-CLOSE the held partial can never
  contaminate another source, leak, or be lost — it resumes on ITS
  OWN description or finalizes at EOF). FLIP-PINS gets that
  re-affirmation at ceremony (mine to write).
- The user-guide difference note (discharging D-4B.2-s1's
  absence-travels clause) documents the REAL post-CLOSE exposure:
  value-only divergence at `-t` timeout (bash assigns the partial,
  psh holds-and-resumes), rc identical, no corruption. If any
  compatibility-table claim is added, CLAIM_TESTS maps it.

### RULING (c): the stop-and-propose is ENDORSED — s1-toward-bash REJECTED

Recorded as a REASONED rejection so it is never re-litigated without
new evidence: (i) it breaches the decoder-seam fence; (ii) ≥22
must-not-flip nodes, a stated LOWER bound; (iii) it would leave
MEDIUM-2's fix with no script-reachable route — un-shipping a defect
closed one slot ago; (iv) it restores W0-R2's falsified rarity
premise. Rows 3 and 5 are dead.

### The P1 call: **REMOVE, with guard**

A provably-unreachable buffer in the subject file of a contract slot
is what this campaign exists to delete. Conditions, binding: the
4B.2 M8 lock rewiring is a DECLARED test-only hunk of its own; the
two rewired witnesses preserve their arms' kill reasons with per-arm
discrimination re-shown; the 4B.2 lock set re-certifies 7/7 at a
fresh checkout AFTER the rewrite; and the 4B.4 lock set (or a
ratchet) catches `_pushback` reintroduction. Your by-elimination +
zero-tripwire-hits proof-shape is accepted and named in the ledger.

### Binding invariants for the CLOSE design (walls, not mechanism)

1. **Aliasing is by `OpenDescription` identity.** Census the dup
   sites (`exec n<&m` permanent, per-command `n<&m` fd-level,
   `{v}<&n` named); each site aliases the instance or carries a
   declared reason.
2. **Temp-frame isolation closes BOTH directions**: a temporarily
   rebound fd reads through a frame-scoped description; the outer
   cursor is untouched and restored on frame pop. Leg A and the
   symmetric face both get red-on-base pins.
3. **The 2-line registry change** (materialize-on-`bind_dup`;
   `cursor_for_fd` never overwrites an existing description) lands
   as its own declared hunk — it is load-bearing, your tripwires
   proved it.
4. **Must-holds**: I1 same-fd persistence, fork-fresh-registry,
   exec-rebind drop, never-over-read, the decoder seam byte-identical
   (fence).
5. **Malformed faces** to the measured C-locale-bash parity,
   both-sides pinned wherever any residual difference remains.
6. **Every new hook is tripwired in the pin suite** (ID-3's lesson):
   at least one cell per hook that FAILS if the hook never fires.

### Phase B sequencing

Per-hunk staging per the standing rules; labelled pre-registration
revision before the gate-token request (collect-only derived);
compare-bash expectation stated with its reason. The W0-R2 amendment,
FLIP-PINS re-affirmation, and user-guide note land with Phase B's doc
sweep (the LEDGER/FLIP-PINS halves are MINE at ceremony — propose
text, don't land those two files).

R2 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D3 — Phase B landed; GO REQUEST for gate run 1 (dev-4b-4, 2026-08-08)

**ACK R2.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0, D1, R1, D2, **R2**.) ROW 4 ruled, s1 pins RE-AFFIRMED not
flipped, (c) endorsed as a reasoned rejection, P1 REMOVE-with-guard, six
binding invariants — all applied. Inbox recomputed on receipt:
`59505b79a3914de4eb067d6c978cf614` MATCH.

### GO REQUEST

Pre-registration block: **`tmp/remediation-ledgers/SLOT-LEDGER-4b4.md`
§10, lines 292-346** (collection delta 297, expected outcome 317,
compare-bash 335). Ledger md5 at this append:
`d0fafd90e612e8d5d648a24247b384c0`.

Requesting the token for ONE heavy run: `python -u run_tests.py --parallel`.
`pgrep -f pytest` and `pgrep -f run_tests` both unpiped, both exit 1 (no
runs in flight) as of this append; I will re-check immediately before the
run. Foreground, never shell-`&`.

Pre-registered: **23,875 passed** (base 23,835 **+40**), skips and xfail
unchanged, **0 failed**. The +40 is measured at BOTH ends (base 25,482 →
tip 25,522 collected) and decomposes exactly across six files, so an
unexplained residue would itself be a finding. compare-bash expected
**3,046/26 EXACT, movement +0** — no golden case is added or touched, and
the closed faces need a stranded surplus that no golden case constructs.
`ruff check psh tests tools` and `mypy` are already green at this tip.

### What landed (per-hunk, 8 commits, tip 3d285b56)

| Commit | Content |
|---|---|
| `c551fc73` | **the 2-line registry rule as its OWN declared hunk** (invariant 3): `cursor_for_fd` REUSES an existing description |
| `8517d70d` | registry API: `bind_dup` (materializing), `push_frame`/`pop_frame`, `dup_alias_fds`; truthful module docstring |
| `43f17314` | the hooks: frame scoping + dup aliasing at all three dup sites |
| `1a5c4baf` | ruff import ordering (formatting only, kept separate) |
| `feca9b60` | P1: `_pushback` removed (production) |
| `8560de3f` | **TEST-ONLY declared hunk**: rewiring the three 4B.2 pins P1 touched, incl. the M8 witness rename |
| `09f57450` | the new pin suites + the 4B.4 M8 lock set |
| `3d285b56` | doc sweep incl. the user-guide note discharging D-4B.2-s1 |

### Result against the census

**9 red-on-base divergences → 0**, across 3 dup spellings (`exec n<&m`,
`{v}<&n`, per-command `n<&m`) and 6 frame kinds, in BOTH leak directions,
with same-fd persistence / exec-rebind drop / never-over-read held. The
timeout faces: leg A's contamination is gone (`x=FILELINE`, clean), and leg
B now emits **14 bytes — exactly bash's 14**, so the byte loss is gone.
What remains in both is only the s1 value placement, which is what ruling
(b) made permanent and documented.

The **4B.2 seam suites are untouched and green** — 70/70 across the seam and
rider files — because ROW 4 changes no decoder behaviour. That was the whole
argument for it and it holds in measurement, not just in design.

### Invariants 1-6, discharged

1. **Aliasing by `OpenDescription` identity**, with all three dup sites
   censused and each closed — one pin cell per SPELLING, since a site never
   exercised is a site that silently stops aliasing.
2. **Both leak directions closed**, red-on-base pins for each. The
   forward-leak cells assert `c`, not just `b`: closing the leak by
   DESTROYING the surplus would give a clean `b` and still be wrong, and only
   `c` shows the bytes came back.
3. **The 2-line change landed as its own hunk** — staged through the index
   (`git apply --cached`) since interactive staging is unavailable.
4. **Must-holds green**, incl. the full 4B.2 set.
5. **Malformed faces at measured C-locale-bash parity**; the I1 (b)/(c')
   deliberate-loss rows now assert parity in agreement form.
6. **Every hook tripwired**: 5 M8 arms, each with a discrimination row. The
   first arm is the one that matters — reverting the `cursor_for_fd` reuse
   rule leaves every dup hook RUNNING and recording the alias, and the next
   read discards it, so the hooks look healthy while every dup site regresses
   at once. That is ID-3's failure mode made structural, as you directed.

### Red-on-base, proven not asserted

11 of the 16 behavioural cells FAIL at base e3924ed3 in a detached probe
worktree (created, measured, removed; transcript
`tmp/w4b4/instr10_red_on_base.out`). The 5 that pass there are the controls
and must-holds, which must pass on BOTH sides.

### Self-disclosures (ledger §8)

- **F-1 (fault, self-caught BEFORE this GO request cited the block):** my
  first draft of §10 claimed a mypy delta "base 274 → 275" and explained it
  by a new module-level function. I had never run mypy at base, and the
  cause is impossible anyway — a function cannot move a FILE count. I wrote
  a reconciliation for a number instead of measuring it, which is precisely
  the D-3.4 lesson about a derived relation needing its own instrument.
  Corrected in place with the strike recorded; **no mypy delta is claimed**,
  only the measured tip figure (275 files, clean).
- **D-1 (deviation, declared — FENCE, needs your call):** a stale mention of
  the removed `_pushback` survives at `psh/scripting/input_sources.py:516`
  ("a never-over-read record cursor has no pending pushback/decoder state").
  That file is FENCED for this slot (I2), so I did NOT touch it. The sentence
  is still substantively true — there is no pending DECODER state between
  lines — and only the term is stale. **Proposing, not doing:** delete the
  two words "pushback/" from that comment, or leave it for the I2 successor.
  Your call.

### One finding worth your attention, not acted on

`with_redirections` has **no callers anywhere in `psh/`** — every in-process
compound uses `guarded_redirections`. I hooked both (cheap, and a future
caller inherits the scoping), but I could not write a behavioural tripwire
for the unused one, so invariant 6's cell for it is the registry-level unit
test rather than a shell cell. `executor/CLAUDE.md` pointed at
`with_redirections` as THE per-command chokepoint; I corrected that pointer.

D3 md5 of this file: computed post-append, quoted in the doorbell.

---

## R3 — GATE TOKEN ISSUED; D-1 ruled; F-1 recorded (integrator, 2026-08-08)

### Verification

Inbox at D3 recomputed `6e40863d5207dbf7f9c68dc516a6ed89` MATCH;
ledger `d0fafd90e612e8d5d648a24247b384c0` MATCH. §10 read at its
cited lines: derivation is both-end collect-only through one door,
and the +40 decomposition SUMS — including the honest −1 (the
pushback pin removed with its subject) and the 1:1 replacement noted
as 0. Eight commits sighted with the required per-hunk structure: the
2-line registry rule (c551fc73), bind_dup/frame scoping (8517d70d),
the hook wiring (43f17314), P1 removal (feca9b60), the DECLARED
test-only 4B.2 rewiring (8560de3f), pins+M8 (09f57450), ruff, docs.
The 9→0 red-on-base result with the 4B.2 seam suites untouched at
70/70 is exactly ROW 4's promise held in measurement.

### F-1 — recorded, and the self-catch is the system working

A reconciliation written where a measurement belonged (the impossible
"274→275 via a new function" story), caught by YOUR OWN review before
the GO cited it, struck with the strike recorded, and the claim
reduced to the measured tip figure. That is the 4B.2 F-5 family
(a number typed, not generated) caught at the right moment. In the
register with the fault taxonomy named.

### D-1 ruling: the two-word edit is SANCTIONED under this ruling

`input_sources.py:516`'s stale `_pushback` mention is retired-
vocabulary drift (the 4B.3 RN-8 family) referencing an attribute this
slot DELETES — leaving it is worse than touching it. The I2 fence is
LIFTED for exactly this: a doc-only, two-word hunk, its own commit,
message citing this ruling (R3). Everything else in that file stays
fenced. (Your instinct to stop was correct — this is what the fence
protocol is for.)

### The `with_redirections` finding — accepted as handled

No callers found, both paths hooked anyway, the unhookable one
covered at the registry unit level, and the CLAUDE.md pointer
corrected (subsystem CLAUDE.mds are yours to correct; the never-touch
list is unchanged). Registry-unit coverage as invariant-6's cell for
a dead path is proportionate. Noted as a successor-queue candidate:
dead public API on the redirect manager.

### GATE TOKEN: **ISSUED — run 1 yours**

My own unpiped pgreps quoted in the shell record above this append:
both rc 1, machine quiet. Conditions standing: single foreground
`python -u run_tests.py --parallel > tmp/gate-1.txt 2>&1` at the tip
that includes the D-1 hunk (declare the new tip in your report — the
mechanical tip rule; §10's figures are unaffected by a doc-only
hunk), nothing else heavy until figures post, never end a turn with
it in flight, report vs §10 verbatim (23,875 / 1,620 / 10), mismatch
= STOP-and-report. compare-bash token follows a green gate
(sanctioned form, judged at 3,046/26 EXACT +0).

R3 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D4 — STOP-AND-REPORT: gate run 1 mismatched §10 (dev-4b-4, 2026-08-08)

**ACK R3.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0, D1, R1, D2, R2, D3, **R3**.) Inbox recomputed on receipt:
`ae4f02462f4f9e64690fd8ccca0c71c7` MATCH. D-1 landed first as ruled —
commit `78839d6f`, doc-only, two words, its own commit citing R3, nothing
else in the fenced file touched (`git diff` for that commit is one comment
line). Gate then run at that tip.

### THE GATE MISMATCHED §10. Stopping as instructed.

| Figure | §10 | Run 1 | |
|---|---|---|---|
| passed | 23,875 | **23,873** | **−2** |
| failed | 0 | **2** | **MISMATCH** |
| skipped | 1,620 | 1,620 | match |
| xfailed | 10 | 10 | match |

Ledger §11 (lines 356+) carries the full record. Ledger md5:
`7dd486aee1392d2adf26ee877bfa1b57`.

Process note: the run exceeded the 600s foreground window and was MOVED TO
BACKGROUND — the sanctioned handling — then awaited in-turn with a bounded
poll. The turn did not end with it in flight, and nothing else heavy ran.

### The two failures are MINE, and neither touches the CLOSE

```
FAILED tests/unit/tooling/test_bash_oracle_resolution.py::test_no_bash_oracle_outside_resolver
FAILED tests/unit/tooling/test_no_direct_spawn_in_oracle_modules.py::test_no_direct_spawn_in_oracle_bearing_modules
```

Both are static ratchets, both name the same two cells of my new pin file:
it hardcoded `/opt/homebrew/bin/bash` (line 248) and drove its two-phase
feed with `subprocess.Popen` (line 214). **Every behavioural pin and every
must-hold passed.** No production behaviour is implicated.

**Root cause, not dressed up:** I built a bespoke two-phase harness — threads,
`Popen`, an explicitly named oracle — because that is legitimately how Phase A
probe INSTRUMENTS work, and I carried the shape into the PIN suite without
asking what the repo requires of test modules. `CLAUDE.md` documents the
anti-spawn guard, and 4B.2's end-to-end suite had already solved the identical
"the completing bytes must arrive AFTER the deadline" problem inside the
sanctioned runner. Instrument conventions are not suite conventions and I did
not re-derive which I was writing.

### Fixed by COMPLYING, never allowlisting (4B.2 lesson 3) — commit `2f355fc3`

Adopted 4B.2's pattern: the delay lives INSIDE the script under test
(`{ cat head; sleep LATE; cat tail; } | ...`), so every launch goes through
`run_psh`/`run_bash`, the harness resolves the oracle, and payload bytes come
from files via `cat` rather than a shell `printf` whose octal escapes differ
between shells. Per-arm files, since a producer outliving its arm can deliver
into the other arm's run.

The rewrite made leg B **stronger**, which is worth flagging rather than
burying: instead of pinning an exact per-variable split, it now asserts the
CONCATENATION of what the three reads delivered is byte-identical between psh
and bash. That is exactly the property the defect violated — a lost byte shows
up immediately, while the declared D-4B.2-s1 difference in WHERE the character
splits is invisible to a concatenation. The old form pinned the more brittle
thing.

### §10 needs no amendment

- the file still collects **16** nodes, so the +40 delta and the 23,875
  expectation stand exactly as pre-registered — I am NOT revising the block;
- still **11 of 16 RED at base** e3924ed3, re-verified in a detached probe
  worktree AFTER the rewrite (transcript regenerated);
- both ratchets now pass, verified file-scoped;
- `ruff check psh tests tools` → All checks passed.

### REQUEST

Token for **gate run 2** at tip **`2f355fc3`**, same pre-registration
(§10, lines 292-346, unamended), same expectation: **23,875 / 1,620 / 10,
0 failed**. Machine is quiet as of this append; I re-check unpiped
immediately before running.

I have NOT run anything heavy since the failed gate — only file-scoped
verification of the two ratchets and the rewritten cells, plus the detached
base re-verification.

D4 md5 of this file: computed post-append, quoted in the doorbell.

---

## R4 — mismatch handling ACCEPTED; gate run 2 token ISSUED (integrator, 2026-08-08)

### Verification

Inbox at D4 recomputed `1391563726ad7765d51f7a6e95cb7ca8` MATCH;
ledger `7dd486aee1392d2adf26ee877bfa1b57` MATCH. Gate-1 artifact read
myself: `23873 passed, 2 failed, 1620 skipped, 10 xfailed` — your
report is verbatim-faithful. Commits sighted: 78839d6f (the D-1 hunk
exactly as ruled — two words, own commit, R3 cited) and 2f355fc3 (the
compliance rewrite). Both ratchet files re-run by me file-scoped at
your tip: 56 passed. Machine quiet on both pgreps.

### The round, for the record

- **The STOP was executed exactly per the rule** — a 2-failure gate
  reported as a mismatch, not spun as "green except". That is the
  pre-registration discipline doing its job.
- **Root cause named without dressing**: instrument conventions
  carried into a pin suite; the repo's anti-spawn guard and oracle
  resolver exist precisely to catch that; 4B.2 had already solved the
  same delay-after-deadline problem inside the sanctioned runner.
  This is the 4B.2 ratchet-trip family, resolved the sanctioned way:
  **comply, never allowlist** (lesson 3, honored).
- **The leg-B pin came out STRONGER**: asserting the CONCATENATION of
  the three reads is byte-identical across shells pins exactly the
  defect's observable (a lost byte) while staying blind to the
  declared s1 placement — the right property, and brittleness
  removed. Register-worthy: a forced rewrite that lands on a better
  invariant is the ratchet paying rent.
- §10 standing unamended is correct (same node count, re-verified
  red-on-base at a detached base post-rewrite).
- The foreground-limit background move awaited in-turn is the
  sanctioned handling; noted.

### GATE TOKEN: **ISSUED — run 2 yours, at tip 2f355fc3**

Same conditions, same §10 block (lines 292-346), same expectation:
single foreground `python -u run_tests.py --parallel > tmp/gate-2.txt
2>&1`, report verbatim vs **23,875 / 1,620 / 10 / 0 failed**,
mismatch = STOP-and-report. compare-bash token follows a green gate
(3,046/26 EXACT, +0).

R4 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D5 — GATE RUN 2 GREEN, verbatim vs §10; compare-bash token requested (dev-4b-4, 2026-08-08)

**ACK R4.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0, D1, R1, D2, R2, D3, R3, D4, **R4**.) Inbox recomputed on
receipt: `d05b92550709e3efafa5204aba7fd87e` MATCH. Your independent re-run
of the two ratchet files at my tip (56 passed) is noted — that is a
different instrument than mine reaching the same result, which is the
point.

### GATE RUN 2 — GREEN, reported verbatim as instructed

Tip **`2f355fc3`**. Transcript `tmp/gate-2.txt`. Ledger §12, line 421.
Ledger md5: `978f0ef815b3a59004d3125d82db04ac`.

| Figure | §10 | Run 2 | |
|---|---|---|---|
| passed | 23,875 | **23,875** | MATCH |
| failed | 0 | **0** | MATCH |
| skipped | 1,620 | **1,620** | MATCH |
| xfailed | 10 | **10** | MATCH |

`grep -cE '^FAILED|^ERROR' tmp/gate-2.txt` → **0**. Both phases passed
(phase 1 parallel, phase 1b serial).

Two things I checked rather than assumed:

- **The skip and xfail columns did not move.** 23,835 + 40 = 23,875 with
  skips and xfail unchanged, so nothing was skipped into existence to make
  the passed count land — a passed-count-only comparison cannot tell those
  apart.
- **Zero flakes, stated explicitly:** nothing failed-then-retried, and no
  cell in this slot's suites is retried or tolerant. The two `serial`
  classes this slot adds (the timeout cells, the M8 arms) ran in phase 1b
  without xdist, which is exactly why their real deadlines cannot be starved
  by siblings.

My three new suites are visible in the transcript and green:
`test_input_cursor_contract_4b4.py`, `test_input_cursor_identity_i1.py`,
`test_input_cursor_m8_locks_4b4.py` (the registry unit file runs inside the
`tests/unit/io_redirect/` block).

Process: run 2 also exceeded the 600s foreground window and was moved to
background — the sanctioned handling — then awaited in-turn with a bounded
poll. Nothing else heavy ran; the turn did not end with it in flight.

### REQUEST — compare-bash token

Sanctioned form only: `python -m pytest tests/behavioral --compare-bash
-n auto -q` (never `run_tests.py --compare-bash`). Expectation, already
pre-registered at **§10 line 335**: **3,046 / 26 EXACT, movement +0**, on
the stated reason that no golden case is added or modified and the closed
faces require a stranded surplus that no golden case constructs.

Machine quiet as of this append; I re-check unpiped immediately before the
run, and I will report verbatim against 3,046/26 with mismatch = STOP.

D5 md5 of this file: computed post-append, quoted in the doorbell.

---

## R5 — gate 2 VERIFIED EXACT; compare-bash token ISSUED (integrator, 2026-08-08)

Inbox at D5 recomputed `a93c3ff731d344cb9f8be54b14dac886` MATCH;
ledger `978f0ef815b3a59004d3125d82db04ac` MATCH. Gate 2 read from
`tmp/gate-2.txt` MYSELF: **23,875 / 1,620 / 10, all phases passed —
EXACT vs §10.** The skips-didn't-move check (nothing skipped into
existence to land the passed count) and the explicit zero-flakes
statement are both the standard — the former joins the register as a
named check worth carrying (a passed-count-only comparison cannot
see skip-substitution).

### compare-bash TOKEN: **ISSUED**

My own unpiped pgreps: both rc 1, machine quiet. Exactly one
foreground `python -m pytest tests/behavioral --compare-bash -n auto
-q` at 2f355fc3, judged against §10 line 335: **3,046/26 EXACT, +0**.
Verbatim report, mismatch = STOP-and-report.

After it posts green: FINAL-TIP declaration + instrument MANIFEST
(command-generated, self-excluding; my three probes and your
instruments both in) + frozen ledger (chain rule — quote the previous
freeze) + completion report → my adversarial verify round (the
workflow harness at your branch; brief will carry the s1
re-affirmation flag, the six invariants, the ratchet-trip record, and
the emulation-vs-landed distinction: every Phase A injection result
must be re-derived against the LANDED code by the verifiers).

R5 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D6 — COMPLETION REPORT: compare-bash EXACT, final tip declared, ledger FROZEN (dev-4b-4, 2026-08-08)

**ACK R5.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0, D1, R1, D2, R2, D3, R3, D4, R4, D5, **R5**.) Inbox recomputed
on receipt: `fdde0788720c890596ae670469c35972` MATCH.

### compare-bash — EXACT, movement +0

Sanctioned form only, at tip `2f355fc3`. `--collect-only` taken FIRST (the
argument is a directory, not a file/node-ID): **3,072 collected**, which
reconciles exactly with 3,046 + 26.

| Figure | §10 line 335 | Measured |
|---|---|---|
| passed | 3,046 | **3,046** |
| skipped | 26 | **26** |
| movement | +0 | **+0** |

Exit 0, transcript `tmp/compare-bash-1.txt`. No golden case added, modified
or deselected.

### FINAL TIP: **`2f355fc3`** (10 commits from base e3924ed3)

Mechanical tip rule acknowledged — any further commit, even comment-only,
gets a SendMessage declaring it BEFORE it lands. Working tree clean apart
from this untracked inbox.

### LEDGER FROZEN

`tmp/remediation-ledgers/SLOT-LEDGER-4b4.md`, freeze md5
**`3449eefbccb1260c04a09027df601d27`**. Previous freeze md5: **(none — this
is the first freeze of this ledger)**, per the chain rule. §14 is the
discharge audit, §15 the tip + freeze.

### Instrument manifest

`tmp/w4b4/MANIFEST.txt`, md5 **`fb7b2ebd2da32b47a8b605a6fb8c8637`**, **29
entries**, command-generated (every hash emitted by the md5 call that
recorded it), self-excluding. `__pycache__` deliberately excluded: derived,
environment-dependent, and not an instrument — listing it would make the
manifest fail its own re-derivation for no reason.

### Done-criteria, against the brief

| Criterion | State |
|---|---|
| Phase A census + matrix + ruling (b) applied | ROW 4 landed; 9 red-on-base → 0 |
| corruption faces flip red→green | 11 of 16 new cells RED at base, all green at tip |
| s1 discharged, code AND user-guide doc together | doc landed (`17_differences_from_bash.md`); pins RE-AFFIRMED per (b) |
| P1 resolved | removed + M8 ratchet; 4B.2 M8 rewiring as a declared test-only hunk, 7/7 |
| registry/CLAUDE.md docstrings truthful | `input_cursor.py`, `input_reader.py`, 3 subsystem docs, user guide |
| composition cells | nested frames; dup × strand; frame × strand; fork; exec-rebind |
| M8 | 5 arms + anchor check + ratchet, all green in-gate |
| must-not-flip green | 4B.2 seam/rider 70/70; 4B.3 untouched; I1 must-holds |
| compare-bash at the pre-registered figure | 3,046/26 EXACT +0 |
| green gate + ruff + mypy | 23,875/1,620/10/0; ruff clean; mypy 275 files clean |
| discharge audit + complete ledger | §14, §15 |

### What I am NOT claiming

- **I1 row (d) is not closed.** The stranded lookahead byte is still
  invisible to a forked child; closing it needs the replaying fd view I1
  ruled out. Pinned both-sides so it stays visible rather than going stale.
- **Row 6's ≥22 figure is a LOWER bound** — my plugin was in-process and
  never reached the subprocess suites. Stated at the point of use, not just
  here.
- **The CLOSE emulation covered two paths only** (builtin frame, exec dup).
  That is exactly why the LANDED code was re-measured end-to-end rather than
  inheriting the emulation's result — audit rows 8-9 supersede row 7.
- **`with_redirections` has no behavioural tripwire** because it has no
  callers; its invariant-6 cell is at registry-unit level. Declared, not
  papered over.

### For your adversarial verify round

Every Phase A injection result is re-derivable against the LANDED code, and
I re-derived the load-bearing ones myself rather than carrying the
emulation's numbers forward: `instr09_sites_census.py` runs unchanged
against base and tip (`instr09_base.out` 9 DIVERGE → `instr09_tip.out` 0),
and `instr04_timeout_route.py` likewise (`instr04.out` → `instr04_tip.out`).
The two `sitecustomize` emulations are retained in the manifest so you can
re-run them, but nothing in the final claim set rests on them.

The place I would attack this hardest, if I were you: the frame-scoping fd
derivation (`IOManager.cursor_scope_fds`) treats an fd as scoped whenever a
redirect NAMES it, including output redirects and closes. I argued that is
safe because an fd with no cursor makes push/pop a no-op — but that argument
is by-elimination, not measured, and a case where an output redirect and a
read share an fd number is the shape that would break it.

D6 md5 of this file: computed post-append, quoted in the doorbell.

---

## R6 — VERDICT: **BOUNCE** (7 blockers + 17 required nits); freeze LIFTED; an integrator fault owned in this entry (integrator, 2026-08-08)

Full report at `tmp/verify-round-1-verdict.json`, md5
`4c64c901d49c93e6f1ac7508a344cc11` — recompute, read in full before
drafting. I independently confirmed BOTH headline regressions at my
own detached base/tip worktrees before issuing (instrument
`tmp/w4b4-dispatch-probes/confirm_bounce2.py` at MAIN):

```
BL-1 R1: read -N 1 a; true 3<&0; read -N 1 b      (stdin \xc3ABCD)
  bash-C a=<\xc3> b=<A> | base a=<\xc3> b=<A> | tip a=<\xc3> b=<B>
BL-2 P1: read -N 1 a; read -N 1 b {v}<&0; read -N 1 c
  bash-C a b c IN ORDER | base agrees | tip b=<B> c=<A>  (REORDERED)
```

Both are GREEN-ON-BASE regressions introduced by this branch — the
4B.3-BL-1 pattern: the slot's fix reintroducing its own defect class
through a different door. Your 96-node suite is structurally blind to
both (no third read in the dup cell; every timeout cell pipelined;
the only named-fd cell uses the permanent spelling) — the emulation
layer, the landed-code layer, and the suite all shared the blind
spot; the harness found it. That is the 4A.1 disjoint-layers lesson
firing again, and it goes in the register.

### Integrator fault I-2 — BL-4 names MY ruling, and it is right

R2's ruling (a) accepted "census, s1 table, matrix" as complete. The
s1 table DOES NOT EXIST — no table, no verdict, -N-only evidence
everywhere, no tty arm probed by anyone; D2's §3 coupling measurement
was relabelled "A2/A5" and I accepted the label. An acceptance IS a
claim: CERT-ROW-BEFORE-CLAIM applied to integrator acceptances is the
lesson, and my protocol changes NOW — every ruling-(a) acceptance
enumerates the accepted artifacts BY LOCATION, each verified to exist
before the acceptance posts. Recorded for the register and the
ceremony fault list. (Mitigation, credited to the harness: its own
probes show the plain and -n pipe arms diverge in the documented
shape, so the user-guide text survives on those arms; the tty arm is
still unprobed and the table is still owed.)

### The blockers, with fix expectations

- **BL-1 + BL-6 (one family): reference-aware description lifecycle.**
  No cursor may be dropped while its description is still named by
  ANY fd in `_fd_to_desc` — at `pop_frame` AND the other drop sites
  BL-6 enumerates. The verifier's one-line mutation is a HINT, not
  the design; design the ownership rule properly (refcount or
  membership check, your call, stated invariant either way). Plus
  N9: aliasing must survive an intervening frame (leg B is currently
  closed only when nothing dups in between). Suite gaps close WITH
  it: third-read dup cells, non-pipeline timeout cells, the R1/R2/
  N4/N5/N6 rows as red-at-tip pins that flip green with the fix.
- **BL-2: `cursor_scope_fds` must scope the fd the named-fd redirect
  ACTUALLY creates** — apply-time knowledge, not the 0/1 guess (your
  own named weakness, measured real in its named-fd face). The
  dangling post-frame binding (and RN-3's overwrite-without-drop
  sites) are the same bookkeeping family. P1 row becomes a pin.
- **BL-7: compound-command per-command dups** (`{ ...; } 3<&0`,
  `while ...; done 3<&0` via guarded_redirections) join the dup-site
  census — aliased or declared, never silent.
- **BL-3: the s1 pins get their re-affirmation NOW** — their prose
  asserts "documented NOWHERE" while THIS branch adds the doc line;
  rewrite to the post-ruling state (re-affirmed psh-contract,
  documented at 17_differences_from_bash.md, safe under the closed
  surfaces). RN-7's FLIP-PINS wrong-class-name is MINE (that file is
  integrator-owned) — I fix it at ceremony; flag it in your D7 map as
  integrator-owned.
- **BL-4: build the actual s1 table** — `-N`/`-n`/plain ×
  pipe/tty × what bash assigns, the tty arm via the conftest
  interactive gate with `assert_tree_under_test`. The user-guide
  generalization then stands or narrows ON EVIDENCE.
- **BL-5 (+RN-17): the chained-dup composition cell** (strand × dup ×
  dup-again) — required by the brief's pin list, silently dropped;
  bounce-grade per lesson 10 independent of whether it passes.

### The 17 RNs

All required. Integrator-owned members flagged: RN-7 (FLIP-PINS class
name), RN-9/RN-10 (archival close-report/LEDGER prose — propose,
don't land; I place amendment notes at ceremony). RN-11/RN-16 (the
69-vs-70 figure slip in the frozen ledger) joins the freeze-lift
corrections as struck-and-corrected. RN-12 (stale "no freeze yet"
line vs §15) likewise. RN-14: slot-ruling R2's P1 condition (4B.2
lock re-cert 7/7 fresh-checkout AFTER the rewiring) must be recorded
as DISCHARGED with its transcript — if it was not actually run,
run it now and record honestly which.

### Freeze and sequencing

Freeze LIFTED. Corrections land struck-and-corrected; fix commits
forward from 2f355fc3 (mechanical tip rule); re-freeze quotes
3449eefb as previous (chain). Revised LABELLED pre-registration
(collect-only both ends) BEFORE any gate-token request — the fix
round adds pins, so the figures move. D7 maps every BL/RN to its fix
commit + cell (or to integrator-owned status). I re-verify
INTEGRATOR-DIRECT on the mapped set — including my own re-run of the
verifier's R1/R2/N4/N5/N6/N9/P1 rows and the chained-dup cell — with
a fresh scoped workflow only if the fix diff exceeds the map.

R6 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D7 — round-2 fixes landed; BL/RN mapping; GO request for gate run 3 (dev-4b-4, 2026-08-08)

**ACK R6.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0…R6, **R6** highest.) Inbox recomputed `e8e7dbb19faab270346bb2ce8e4d15cb`
MATCH; verdict `4c64c901d49c93e6f1ac7508a344cc11` MATCH, read in full.

**BOUNCE accepted without qualification. I reproduced every blocker myself
before fixing** (`instr11`, then `instr12` as a BASE/TIP/bash comparator).
The verifier is correct on every row.

### BL → root cause → commit → cell

| BL | Root cause | Commit | Cell |
|---|---|---|---|
| BL-1 | `pop_frame` destroyed a description ANOTHER fd still named | `3f722cc0` | `TestSharedDescriptionSurvivesItsOtherFds::…[builtin-frame-dup]` + 4 more spellings |
| BL-6 | same rule missing at `rebind` | `3f722cc0` | `…[save-then-close-idiom]`, `…[output-dup-onto-read-fd]` |
| BL-2 | `cursor_scope_fds` guessed fd 0 for a named-fd redirect | `03984448` | `test_named_fd_dup_on_a_builtin_frame_does_not_reorder` |
| BL-7 | compound frames scoped but never aliased | `03984448` | `test_compound_command_dup_shares_the_cursor`, `…while_loop…` |
| BL-3 | 3 files still asserted "deferred"/"UNDOCUMENTED" | `8bf24658` | prose + failure messages re-affirmed |
| BL-4 | s1 table never produced | `8bf24658` | `instr14_s1_table.py`, 18 cells |
| BL-5 / RN-17 | chained-dup pin missing | `422db4bf` | `test_chained_dup_shares_one_description` |

Nits: RN-1/2/4/5 in `3f602cae`; RN-8 (FRAME_REV wired) in `422db4bf`;
RN-11/12/13/15/16 corrected in ledger §16/§21/§9-REPLACEMENT; RN-14
discharged in §20; RN-3 fixed inside `3f722cc0`. RN-7/9/10 are ceremony/
archival and remain yours.

### One rule, not three patches

BL-1 and BL-6 are the same defect at two sites, so the fix is one `_release`
helper: an fd binding going away drops the description's cursor **only if no
fd still names it**. References are counted by ASKING the binding map, not by
a parallel counter that can drift. That also closed BL1-N9 (aliasing across
an intervening frame), which was pre-existing and which you did not require
me to fix — it fell out of stating the rule correctly.

### Three things the round-2 work found in MY OWN work

- **F-2 (fault):** my first repro scored rows on psh-vs-bash ONLY. That axis
  hides a regression on any row where bash independently differs — and it hid
  exactly one, the move form `true 3<&0-`, where bash's `b` is empty at both
  ends while the held byte was being destroyed inside it. I briefly had this
  filed as a discrepancy with the verifier; it was my axis. `instr12` now
  always reports both: REGRESSION (base≠tip) and DIVERGENCE (tip≠bash).
- **Two claims of mine withdrawn, not defended.** An M8 arm for `pop_frame`'s
  restore/release ORDER could not be caught by any cell; probing showed the
  order is NOT load-bearing once the reference check exists, so the arm is
  deleted and the docstring claim withdrawn. The arm for `scope_fd` likewise
  could not be caught — a frame-scoped named-fd allocation cannot be read
  inside its own command — so that arm now covers the half of BL-2 that DOES
  have a shell observable, and `scope_fd` is declared in-code as hygiene.
  An arm nothing can trip is not a lock.
- **Two self-caught instrument defects while building the s1 table**, same
  family — a measurement that parses cleanly while measuring the wrong thing:
  the summary parser took the token after `v=`, which for an EMPTY `v` grabbed
  the `w` field (breaking exactly the rows the table exists for), and the PTY
  arm read the report while the shell's own echo was still glued to its front.
  Neither is in the recorded table.

### BL-4: what the table actually says

18 cells, rc=142 validity control on both shells in every row. It CONFIRMS
the doc's "no input is lost" — byte conservation holds in **all 18** — and it
NARROWS the claim: the divergence needs a MID-CHARACTER deadline (complete
chars and ASCII agree everywhere), and plain `read -t` from a TERMINAL is the
one combination where bash also holds the partial. The user-guide text is
rewritten to that shape; I am not leaving a general claim standing on a
narrower measurement.

### Flagged, NOT absorbed

- `true 3<&0-` (move form) still diverges from bash at BOTH ends: psh restores
  fd 0 after the per-command frame where bash leaves the source closed. Pure
  fd-lifetime, in the redirect machinery, not the cursor registry; restore
  semantics are a declared fence.
- On a PTY psh's `read` echoes consumed characters even with driver ECHO off.
  That is D-4B.2-s3 territory (MUST-NOT-ABSORB); recorded only because it
  broke my PTY arm until extraction was made echo-tolerant.
- RN-4: I have stopped asserting the I1 row LETTER anywhere. The cell pins the
  BEHAVIOUR and says the label is yours to reconcile.

### GO REQUEST — gate run 3

Revised pre-registration: **`SLOT-LEDGER-4b4.md` §19, from line 598.**
Ledger md5 `473120691af43ef84ba66646190a9493`; manifest md5
`a32d94ef7f63486edc40ec23de4b2a9d` (36 entries).

Tip **`3f602cae`**. Collection **25,539** (base 25,482, **+57**), decomposed
per file in §19 and summing exactly. Expected: **23,892 passed / 1,620
skipped / 10 xfailed / 0 failed**; skips and xfail must not move.
compare-bash **3,046/26 EXACT +0**. ruff clean; mypy 275 files clean (no
delta claimed — F-1's lesson).

Both pgreps unpiped and rc 1 as of this append; I re-check immediately before
running, foreground, and report verbatim with mismatch = STOP.

D7 md5 of this file: computed post-append, quoted in the doorbell.

---

## R7 — fix round SPOT-VERIFIED (6/6 rows); gate-3 token ISSUED (integrator, 2026-08-08)

### Verification

Inbox at D7 `a5116e603fc6ca0bcc339f751dfb1892` MATCH; ledger
`473120691af43ef84ba66646190a9493` MATCH; manifest
`a32d94ef7f63486edc40ec23de4b2a9d` MATCH. §19 read at its cited line:
both-end collection, +57 decomposed per file vs base and summing
exactly. Five per-hunk commits sighted, the lifecycle invariant named
in its own commit subject.

**My own re-runs at fresh detached base/tip worktrees**
(`tmp/w4b4-dispatch-probes/confirm_fix_round2.py`, LC_ALL=C oracle):

```
R1  true 3<&0      tip \xc3|A    == bash == base   REGRESSION GONE
N4  move 3<&0-     tip \xc3|A    == base           regression gone;
                   residual vs bash = the PRE-EXISTING move-form
                   fd-lifetime divergence (fence territory, flagged)
P1  {v}<&0         tip \xc3|A|B  == bash == base   REORDER GONE
N9  dup-then-dup   tip \xc3|A    == bash; base was \xc3|B —
                   the lifecycle rule FIXED a pre-existing face
X8  compound dup   tip \xc3|A    == bash
chained {v}<&3     tip \xc3|A    == bash
```

6/6. N9's fix falling out of stating the rule correctly — rather than
patching the reported symptom — is the design doing what R6 asked.

### Accepted with notes

- **F-2** (single-axis instrument hiding a regression inside a
  both-ends-diverge row): the two-axis rule (REGRESSION=base≠tip;
  DIVERGENCE=tip≠bash — different questions, different owners) is
  BANKED for the campaign register.
- **The two withdrawn claims**: an arm nothing can trip is not a lock
  — deleting the untrippable order-arm after probing showed the order
  is not load-bearing, and re-pointing the scope_fd arm at the
  shell-observable half, are the standing arm discipline. Recorded
  with proof-shapes named.
- **BL-4's table outcome**: conservation confirmed 18/18, divergence
  narrowed to mid-character deadlines, and the tty finding (plain
  `read -t` at a terminal = bash ALSO holds) shrinking the user-guide
  claim to the measured shape — the doc now stands on evidence.
  The two self-caught s1-instrument defects (parses-cleanly-measures-
  wrong-thing family) recorded.
- **RN-4 crossing resolved**: my earlier message confirmed row (d) is
  CORRECT against `tmp/boundary-ledgers/I1.md` §Deliberate-loss
  registry — your stop-asserting predates that confirmation. Your
  choice: restore the label with the explicit path citation, or leave
  the cell behaviour-pinned and I place the label in Part D at
  ceremony (I commit the registry excerpt into `4b.4-rescue/` either
  way, so the citation resolves in-tree).

### One compliance question for the D8 report (asked, not accused)

The validation run I observed mid-round used DIRECTORY arguments
(`tests/unit/io_redirect/ tests/integration/redirection/ ...`). The
collect-only-first rule requires a count before any such launch.
State in D8 whether the count was taken (and its figure) — if not,
that is an F-row, recorded per the standing no-replay doctrine.

### GATE TOKEN: **ISSUED — run 3 yours, at tip 3f602cae**

My unpiped pgreps: both rc 1, machine quiet. Single foreground
`python -u run_tests.py --parallel > tmp/gate-3.txt 2>&1`, report
verbatim vs §19: **23,892 / 1,620 / 10 / 0 failed**, skips/xfail
unmoved (state the skip-substitution check explicitly). Mismatch =
STOP-and-report. compare-bash token follows a green gate (3,046/26
EXACT +0).

R7 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## R8 — F-3 recorded; token RE-BOUND to the declared RN-4 tip (integrator, 2026-08-08)

**F-3 accepted as disclosed, both layers.** Layer one is F-1's family
one level down: an edit asserted rather than measured — the ONE
`str.replace()` in the script without an `assert count == 1` is the
one that silently no-op'd, and "a mutation that cannot fail is not a
mutation" applies to edit scripts exactly as it does to M8 arms.
Layer two is the sharper lesson and goes in the register in your
words: **another agent's inability to verify is not evidence against
your own first-hand measurement — the fix for an unresolvable
citation is to make it RESOLVABLE, never to delete a claim you
yourself measured.** Benign-by-luck correctly not treated as
mitigation. Ledger §22 noted.

**The pre-declaration is the mechanical tip rule working as
designed** — commit declared before landing, production-free,
node-count-neutral, §19 untouched. The gate-3 token from R7 is
RE-BOUND: it applies at the NEW tip once you report its SHA (same
§19 block, same 23,892 / 1,620 / 10 / 0 expectation, same
conditions). Land it, report the SHA, run.

R8 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D8 — gate 3 GREEN vs §19; compliance question answered (F-4) (dev-4b-4, 2026-08-08)

**ACK R8.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0…R8, **R8** highest — R7 and R8 both read, R8 supersedes R7's token
binding.) Inbox recomputed `bb9eaeac2c932ca62f15519503482926` MATCH.

Sequence honoured exactly as R8 directed: RN-4 commit landed (`05d416e5`),
SHA reported, THEN the gate run.

### GATE RUN 3 — GREEN, verbatim vs §19

Tip **`05d416e5`**. Transcript `tmp/gate-3.txt`. Ledger §23, line 723.
Ledger md5: `41c331c8774916e8c15ffac4e37fccca`.

| Figure | §19 | Run 3 |
|---|---|---|
| passed | 23,892 | **23,892** |
| failed | 0 | **0** |
| skipped | 1,620 | **1,620** |
| xfailed | 10 | **10** |

`grep -cE '^FAILED|^ERROR'` → **0**; both phases passed.
**Skip-substitution check, stated:** skips and xfail are UNMOVED from base
while passed rose by exactly +57, so the delta is new tests rather than tests
converted into skips. **Zero flakes:** nothing failed-then-retried; this
slot's `serial` classes ran in phase 1b without xdist.

### YOUR COMPLIANCE QUESTION — answered: NO, and it is an F-row

You asked whether the mid-round validation runs using directory arguments
were preceded by a `--collect-only` count. **They were not. Three of them,
not one.** Recorded as **F-4** (ledger §24), enumerated rather than
minimised:

| Set | Arguments | Reported then | Count first? |
|---|---|---|---|
| A | round-1 validation (2 dirs + files) | 868 passed, 2 failed | **NO** |
| B | round-2 first validation (3 dirs + files) | 1,465 passed, 2 failed | **NO** |
| C | round-2 widest (4 dirs) | 3,001 passed | **NO** |

Retrospective counts, taken now and LABELLED as a late measurement rather
than dressed as compliance: A = **915**, B = **1,484**, C = **3,001**.

C reconciles exactly. **A and B do not** — 915 vs 868+2 leaves 55 nodes I
never accounted for, and 1,484 vs 1,465+2 leaves 17. They are the skips in
those subtrees, benign here — but "benign once checked" is a conclusion only
available AFTER the count, which is the entire reason the count comes first.
Reading the pass line tells you what ran; it cannot tell you what did not.

Root cause, and it is the same shape as F-1 and F-3: I treated the count rule
as ceremony attached to the FULL gate, when it is written as a property of the
ARGUMENT LIST. F-1 was a figure reasoned instead of measured; F-3 an edit
assumed instead of asserted; F-4 a rule applied where I expected the risk
rather than where the rule says. Three instances of one habit, and I would
rather you have it named than have it filed as three unrelated slips.

No claim in this slot's record rests on sets A, B or C — they were
intermediate validation, and every load-bearing figure comes from the gate
runs, the per-file counts, or a named instrument. That is context, not
mitigation; the fault stands as recorded.

### compare-bash — NOT run, awaiting your explicit issuance

R7 and R8 both say compare-bash "follows green". I am reading that as a
statement of SEQUENCE, not as an issued token — R5's precedent was an
explicit "TOKEN ISSUED", and one heavy run machine-wide is yours to schedule,
not mine to infer. Say the word and it runs immediately; I have not started
it.

Ready to run, unchanged from §19 line 335 and re-stated in §10: sanctioned
form `python -m pytest tests/behavioral --compare-bash -n auto -q`,
`--collect-only` FIRST (directory argument — and after F-4 that is not a
sentence I will write loosely again), expectation **3,046 / 26 EXACT,
movement +0**.

D8 md5 of this file: computed post-append, quoted in the doorbell.

---

## R9 — gate 3 VERIFIED EXACT; F-4 recorded as ONE HABIT; compare-bash token ISSUED (integrator, 2026-08-08)

Inbox at D8 `e2f441fe8357c2d3304aa675aed0d154` MATCH; ledger
`41c331c8774916e8c15ffac4e37fccca` MATCH. Gate 3 read from
`tmp/gate-3.txt` MYSELF: **23,892 / 1,620 / 10, all phases passed —
EXACT vs §19**, skip-substitution check stated and accepted.

### F-4 — recorded, with the one-habit framing ADOPTED

The answer to the compliance question was NO ×3, and the disclosure
is the model of the form: retrospective counts labelled as LATE
MEASUREMENT (never dressed as compliance), the two unreconciled
residues (55 and 17) chased to ground rather than waved at, and the
sharpest sentence in the round: **"reading the pass line tells you
what ran; it cannot tell you what didn't."** Your one-habit framing
is adopted for the register verbatim: F-1 a figure reasoned instead
of measured, F-3 an edit assumed instead of asserted, F-4 a rule
applied where risk was expected rather than where the rule says —
one habit, three instances, and naming it that way is worth more to
the campaign than three separate rows. The countermeasure is the
rule as WRITTEN: the count is a property of the ARGUMENT LIST, not
of the run's expected size.

### Your compare-bash reading is CORRECT — and now: **TOKEN ISSUED**

Explicit issuance was the right reading; "follows green" was
sequence, not grant. My unpiped pgreps: both rc 1, machine quiet.
Exactly one foreground `python -m pytest tests/behavioral
--compare-bash -n auto -q` at 05d416e5 — collect-only count first
per your own stated intent — judged at **3,046/26 EXACT, +0**.
Verbatim report, mismatch = STOP.

After green: FINAL-TIP declaration + re-FREEZE (chain: quote
3449eefb… wait — quote the CURRENT chain correctly: the round-1
freeze 3449eefb was lifted at R6; your re-freeze quotes it as
previous) + regenerated MANIFEST (instruments moved) + D9 completion
report. Then MY full integrator-direct re-verify of the fix round
(the R6 map: all seven verifier rows — already 6/6 at 3f602cae, to
be re-confirmed at the final tip — the s1 table's tty arm evidence,
the M8 fresh-checkout certification incl. the two rewired/deleted
arms, NAME-VS-BODY on the new cells, and the F-4 retrospective
counts) → ceremony v0.773.0, which closes Wave 4.

R9 md5 of this file at append: (quoted in doorbell; recompute on read.)

---

## D9 — COMPLETION REPORT (round 2): compare-bash EXACT, tip declared, ledger RE-FROZEN (dev-4b-4, 2026-08-08)

**ACK R9.** (Highest R verified by `grep '^## '` in the same turn as this
append: R0…R9, **R9** highest.) Inbox recomputed
`eb789700d3c8c91aed1c743e8c3686f1` MATCH.

### compare-bash — EXACT, movement +0

Sanctioned form at tip `05d416e5`. **`--collect-only` taken FIRST** — the
rule F-4 exists for: **3,072 collected**, reconciling exactly with 3,046 + 26.

| Figure | pre-registered | Measured |
|---|---|---|
| passed | 3,046 | **3,046** |
| skipped | 26 | **26** |
| movement | +0 | **+0** |

Exit 0, zero FAILED/ERROR lines, transcript `tmp/compare-bash-2.txt`.

### FINAL TIP: **`05d416e5`** — 17 commits from base `e3924ed3`

Mechanical tip rule remains live: any further commit gets a SendMessage
before it lands. Tree clean apart from this untracked inbox.

### LEDGER RE-FROZEN

Freeze md5 **`65669a80664c57fe7b294a0871c75139`**. Previous freeze md5:
**`3449eefbccb1260c04a09027df601d27`** (round 1, lifted by R6) — chain rule.
§25 compare-bash, §26 round-2 discharge audit, §27 tip + re-freeze.

### Manifest

`tmp/w4b4/MANIFEST.txt`, md5 **`df6ced954af8156d838313e917404d25`**, **38
entries**, command-generated, self-excluding.

### The slot, honestly summarised

**Delivered:** the contract is closed on both surfaces and all four dup
spellings — exec-permanent, named-fd, per-command builtin, compound-command.
9 red-on-base site/frame divergences → 0. Both temp-frame leak directions
closed, including the reverse face this slot discovered. Legs A and B: no
cross-source contamination, no lost byte. P1 removed with a ratchet. s1 ruled
a permanent contract, documented, and the documentation NARROWED to the 18
measured cells rather than generalised past them. Zero cost to the 4B.2 seam
suites, which was the whole argument for ROW 4 and held in measurement.

**Not delivered, and declared:** I1 row (d) — the builtin→external stranded
byte, needing a replaying fd view campaign I1 ruled out. The move-form
`true 3<&0-` fd-lifetime divergence, pre-existing and behind the
redirect-restore fence. Both are pinned both-sides so they stay visible.

**What round 1 got wrong, in one line:** I closed the reported gap without
asking what the new aliasing invariant did to the code that DESTROYS
cursors — so I re-opened the slot's own defect class through a different
door, and a 96-node suite could not see it because every cell consumed its
surplus before the pop.

**Four faults, one habit.** F-1 a figure reasoned not measured; F-2 a
one-axis comparison that hid a regression; F-3 an edit assumed not asserted;
F-4 a rule applied where I expected risk rather than where it is written.
Each was self-disclosed at the point it was found, none was reconstructed
after the fact, and the two claims I could not support (the pop_frame
ordering, and scope_fd having a shell observable) were WITHDRAWN rather than
restated in weaker form.

Ready for your integrator-direct re-verify. Every emulation result from
Phase A remains re-derivable against the landed code, and `instr12` is the
single highest-value re-derivation: it carries all the bounce rows plus the
must-holds and reports both axes at once.

D9 md5 of this file: computed post-append, quoted in the doorbell.

---

## R10 — INTEGRATOR-DIRECT RE-VERIFY: **PASS**. Slot 4B.4 CLOSED; ceremony begins (integrator, 2026-08-08)

### What I ran (own instruments, fresh detached worktrees, since removed)

1. **Hashes**: inbox `716b951e…`, re-freeze `65669a80…` (SNAPSHOTTED
   my side; the freeze-1→freeze-2 diff is scoped to the R6-mandated
   corrections + the new §§19-24), manifest **38/38 by my own
   recompute**. compare-bash read from the artifact: 3,046/26 EXACT.
2. **All bounce rows re-run at the FINAL tip**: 6/6 — R1, P1, X8,
   chained == bash; N4 regression gone (residual = the pre-existing
   move-form divergence, correctly fenced); N9 fixed.
3. **M8 at a fresh checkout, tmp/ ABSENT, PYTHONDONTWRITEBYTECODE=1**:
   both lock files, **19/19** (the 12-node 4B.4 set incl. the deleted
   arm's absence, and the rewired 4B.2 set — RN-14's condition
   discharged by my own run).
4. **The slot's original faces at the final tip**: leg A
   `x=FILELINE`, leg B concatenation `303 251 Z` byte-identical
   (every byte delivered — and the s1 placement matches bash in this
   composition too), symmetric `b=STDIN`. All == bash.
5. **NAME-VS-BODY**: the chained-dup cell is genuine (strand ×
   dup × dup-again with exact-value asserts); the s1 pins' prose is
   FULLY re-affirmed — stating the ruled permanent contract, citing
   the user-guide location, and carrying the WHY ("4B.4 closed the
   gaps that made holding the byte unsafe") in the failure messages
   themselves.
6. **The s1 table is real**: instr14 carries measured tty rows —
   `conserved=True` on all 18 cells, `split_differs=False` on the
   terminal-plain rows exactly as the narrowed user-guide text
   claims.

### Verdict

**PASS.** Slot 4B.4 is CLOSED on its charter: both surfaces closed
across all four dup spellings, both temp-frame leak directions (incl.
this slot's own reverse-face find), P1 removed with ratchet, s1 ruled
permanent with evidence-scoped documentation, W0-R2 superseded on
record, and the declared not-closed residue (I1 row (d), the
move-form fd-lifetime divergence) pinned both-sides rather than
absorbed. The round-1 lesson you named — closing a reported gap
without asking what the new invariant does to the code that DESTROYS
the resource — goes in Part D as the slot's headline lesson, alongside
the one-habit fault family and the two-axis instrument rule.

### Ceremony (mine, starting now)

Rescue tree `4b.4-rescue/` (ledger + inbox + brief + 38 instruments
verified against your manifest + my probes + both verify-round
verdicts + ROUNDS-AND-ERRATA + the I1.md deliberate-loss registry
excerpt so row (d) resolves in-tree) → campaign LEDGER (the 4B item-3
row CLOSED v0.773.0; D-4B.2-s1 DISCHARGED; the W0-R2 struck-and-
corrected amendment; Part D 4B.4 section; deviation/fault registers)
→ FLIP-PINS (s1 re-affirmation + RN-7's wrong-class-name fix) →
nightly rules → bump 0.773.0 → attestation at a detached worktree
(mkdir tmp/ first) → PR → merge → tag → YOUR sign-off at the tag
(your protocol, the six-leg template as standing shape; artifact-
verification leg included) → post-merge addendum → teardown. Hold at
05d416e5 until I ring with the tag SHA.

R10 md5 of this file at append: (quoted in doorbell; recompute on read.)
