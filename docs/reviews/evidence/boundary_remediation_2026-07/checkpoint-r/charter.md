# Checkpoint R charter — whole-tree boundary reappraisal (sequence §10)

**Dispatched 2026-08-08 by the campaign integrator on explicit user GO.**

## Position

- Waves 0–4 of the Boundary Remediation Campaign are COMPLETE: v0.751.0–v0.773.0,
  22 slots, all shipped. The tree under appraisal is **main = origin/main =
  `ae871a16` (v0.773.0)**. This exact SHA is the ONLY measurement subject.
- Governing documents (read in this order; they are the record you verify against):
  1. `docs/reviews/boundary_remediation_campaign_sequence_2026-07-21.md` — §10 is
     THIS checkpoint's definition (five questions + exit criteria); §11 is the
     Wave 5 you are re-scoping; §13 is the finding ownership map.
  2. `docs/reviews/evidence/boundary_remediation_2026-07/LEDGER.md` — THE registry.
     Part A = #22 findings with closure claims; Part B = 35 predecessor carries;
     Part C = rulings (incl. R1: Wave 5 = 5B+5C, 5A OUT; W0-R2 superseded);
     Part D = new facts + per-slot registrations incl. successor rows and the
     banked lesson sets.
  3. `docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md` — every
     DECLARED divergence/deviation. **A declared deviation behaving as declared is
     NOT a finding. A finding is behavior that contradicts the record.**
  4. Per-slot evidence: `docs/reviews/evidence/boundary_remediation_2026-07/
     <slot>-rescue/` (slot-ledger, ROUNDS-AND-ERRATA, instruments).

## What Checkpoint R is — and is not

Sequence §10: "a short independent whole-repository appraisal. This is a
checkpoint, not another open-ended review campaign." It is VERIFICATION-ONLY:

- **No implementation.** You never edit `psh/`, `tests/`, `tools/`, or any
  committed file. You never commit. Findings are reported, not fixed.
- **You write ONLY under (a) your own scratch worktree area and (b) your
  assigned instruments directory `tmp/ckr-probes/<scope>/` in the MAIN repo.**
  Nothing else in the main tree is writable to you.
- Output = evidence with named proof shapes. The integrator composes the
  independent report, runs attack rounds to zero, and owns all rulings.

## The five questions (§10 verbatim)

1. Do all original #22 HIGH and user-visible MEDIUM discriminators now pass?
2. Did any adapter, raw-string fallback, visitor override, subprocess helper,
   or service-locator path recreate a deleted boundary?
3. Are the new representations transitively immutable and authority-timed?
4. Did the work introduce new dependency cycles, deferred-import caps, broad
   owner parameters, complexity cliffs, process leaks, or flaky tests?
5. Is Wave 5 still the right architecture backlog, or did Waves 1-4 remove or
   reshape some of it?

Exit criteria: an independent report records confirmed closures, new blockers,
and an updated Wave 5 scope; **no unresolved correctness or evidence-trust
blocker is deferred into textbook cleanup**; Wave 5 begins from a freshly
verified consumer/import/typing/complexity census rather than the historical
v0.749.0 counts.

## Environment discipline (every scope, binding)

- **Own detached worktree** (rule B71): create it with
  `git worktree add --detach <your-scratch>/wt ae871a16` from
  `/Users/pwilson/src/psh`; your scratch root is a fresh directory you create
  under the session scratchpad
  `/private/tmp/claude-501/-Users-pwilson-src-psh/05736dde-f3cd-4b98-98df-9708e107bca4/scratchpad/ckr/<scope>/`.
  All measurement happens IN the worktree (cwd inside it). At the end,
  `git worktree remove --force <path>` and report success/failure of the removal.
- **Discriminator asserted before any measurement**: from the worktree, run
  python with `cwd` = worktree and assert BOTH
  `psh.__file__` resolves under the worktree path AND
  `psh.version.__version__ == "0.773.0"`. `python -m psh -c` prepends CWD to
  sys.path — cwd discipline IS the mechanism. The search path is a request;
  the resolved `__file__` is the fact.
- **Bash oracle**: `/opt/homebrew/bin/bash` (5.2.26), explicit argv, NEVER
  `/bin/bash`. Verify the version once and record it.
- **Instruments are FILES from the start**: every probe is a script saved under
  `tmp/ckr-probes/<scope>/` (main repo) before it runs; transcripts are saved
  next to it. No inline-heredoc probes. Any hash/count in your report is
  generated by the command that records it.
- **NO heavy runs.** Never `run_tests.py`, never a tree-wide pytest, never
  `-n auto` over `tests/`, never `run_tests.py --compare-bash`. Single-module or
  node-ID pytest runs only. For any pytest argument that is not a file or node
  ID: `--collect-only -q` count FIRST. The collect-only count is a property of
  the ARGUMENT LIST. If a needed check would be heavy, report it as
  INTEGRATOR-RUN-REQUESTED instead of running it.
- **Project `tmp/` only** for anything in the main repo; never system `/tmp`
  (your scratchpad area above is sanctioned).
- No glob-deletes outside your own scratch. Never `git checkout`/`git clean` in
  the MAIN tree.

## Evidence discipline (every scope, binding)

Condensed from the campaign's banked registers — the full sets are binding by
reference: `4a.1-rescue/brief.md` §Rules; the 11 banked 4B.1 + 11 banked 4B.2
lessons (enumerated in `tmp/remediation-ledgers/briefs/4b.3.md` §Rules); the
D-4B.3-lessons ten and D-4B.4-lessons ten (LEDGER Part D). The ones you will
actually need:

1. **Proof-shape NAMED on every claim**: revert-proven / mutation-proven /
   by-elimination / characterization. A claim without a named shape is a draft.
2. **Two-axis instruments**: REGRESSION (base≠tip) and DIVERGENCE (tip≠bash)
   are different questions; state which axis each cell measures. Base for this
   checkpoint = the slot-close claims in the LEDGER, not v0.750.0 re-derivation
   (except where a row's evidence says red-on-base — then the wave0 probe is
   the base instrument).
3. **Instrument-mirror family**: a cell consistent with two hypotheses is
   evidence for neither — make each candidate mechanism's execution VISIBLE.
   Assume your first probe composition has this flaw and design against it.
4. **A mutation that cannot fail is not a mutation**: when you test a guard or
   ratchet, produce the synthetic offender and show the guard FAILING on it
   (in your worktree). A guard you only saw pass is unverified. This extends
   to edit scripts (assert match counts) — though you should not be editing.
5. **NAME-VS-BODY**: read the body of any test you cite as coverage; a pin
   whose name states the property does not necessarily assert it.
6. **Acceptances are claims**: every artifact you cite is cited BY LOCATION and
   you verified it exists at `ae871a16`.
7. **Instrument discipline binds both directions**: your instrument can fail
   toward "claim wrong" as easily as toward "all clear". `grep -F` for finding
   counts; byte-exact probe files (`od -c` verified) for `[[ ]]`/`$'...'`
   spellings, never via `-c` string munging through extra quoting layers.
8. **"Reading the pass line tells you what ran; it cannot tell you what
   didn't"**: for any coverage claim, show the collected set, not the summary.
9. Timestamps/durations: no wall-clock-dependent verdicts; deadlines in probes
   get generous margins and deterministic constructions where possible.
10. **Severity taxonomy** (report every finding with one):
    - **BLOCKER**: a CLOSED row's claim fails to reproduce at ae871a16; a
      guard/ratchet does not bite; a correctness or evidence-trust defect that
      must not defer into Wave 5 textbook cleanup.
    - **REQUIRED-NIT**: record-integrity defect (stale/wrong/unresolvable
      citation, count mismatch, hollow pin) that must be corrected in the
      report/LEDGER but implies no behavior defect.
    - **NOTE**: observation for the successor queue or Wave 5 scoping.
11. Zero findings is a legitimate result — state it plainly with what you ran.
    Never manufacture a finding; the campaign's standing score is zero false
    findings in either direction, and a false BLOCKER costs a full round.

## Known-deviation awareness (do NOT re-report these as findings)

The record already declares (spot-check them where your scope says so, i.e.
verify they behave AS DECLARED): the b1–b5 history `-a` deviation family
(4B.3); D-4B.2-s1 timeout-partial hold = PERMANENT documented contract (18-cell
table, incl. tty), s2 `-N` count model, s3 `read -s -N` echo; D-3.4-s4
RANDOM-masked-read on LAYER/SEED route; bare-`$@`/IFS + case-pattern-first-field
operand exclusions (3.3); carry #21 `read -N` hybrid model; move-form `n<&m-`
fd-lifetime (D-4B.4-s2); I1 row (d) builtin→external stranded lookahead
(D-4B.4-s1); empty-arith-subscript (#3); combinator arrays.py word-builder seam
+ top-level line stamps (2.2 carries); lexer word-extent family (2.3 carry);
`[[ ]]` operand execution divergences + double-quoted operand flattening (2.1
carries); locale warn wider surface (1.4 carry); `bg`-on-RUNNING silent (1.4
carry); sub-13 fd parking envelope (4A.1); PRESENT/deliberate special-file
execution (#30). FLIP-PINS.md is the authoritative enumeration.

## Scopes

### Q1-discriminators — do the #22 HIGH + user-visible MEDIUM discriminators pass?

For every Part A row HIGH-1..HIGH-10 and MEDIUM-1..MEDIUM-13 (MEDIUM-14/15/16
are Wave 5's and belong to Q5; still confirm their base facts remain accurate
in one line each): re-execute the row's DISCRIMINATOR at ae871a16 and verify
the CLOSED claim's headline behavior. Sources:
- `docs/reviews/evidence/boundary_remediation_2026-07/wave0-base-probes/` —
  the committed base probes. **HAZARD (D-4B.3-note)**: `claim_a_psh.py` and
  `claim_a_s.py` are version-pinned `assert __version__ == '0.750.0'` and MUST
  abort at tip. NEVER edit or loosen committed probes — write FRESH equivalents
  in your instruments dir targeting the current model.
- The Part A row text itself — each "Base result" column states the exact
  discriminating command(s).
- LOW rows with CLOSED status (STD_FDS lease, skip-on-failure) get one
  spot-check each; LOW printf `%a` stays OPEN (Wave 5 rider) — confirm still
  reproduces, one line.
Expected outcome per row: CONFIRMED (fixed behavior reproduces / declared
deviation behaves as declared) or FAILED (with transcript). Where the closure
was internal-integrity (4B.1, MEDIUM-6), the discriminator is the loud-rejection
probe, not a bash comparison. Prioritize end-to-end shell-observable
discriminators over unit internals. Every row gets a verdict; batch trivial
ones. Also verify `gate_attestation.json` at ae871a16 reconciles (version
0.773.0, gated commit an ancestor).

### Q2-resurrection — did anything recreate a deleted boundary?

The campaign DELETED these authorities; sweep for their resurrection in ANY
form (adapter, raw-string fallback, visitor override, subprocess helper,
service-locator path):
- direct-subprocess psh/bash spawns in oracle-bearing test modules (HIGH-1;
  guard: `tests/unit/tooling/test_no_direct_spawn_in_oracle_modules.py`)
- per-visitor child enumeration overrides bypassing `TotalTraversalVisitor`
  (HIGH-2; generated sentinel battery + anti-bypass guard)
- facade `_ParserWrapper` / `support/utils.py` context-dropper (HIGH-5) —
  any new wrapper discarding ParseInputs
- regex/raw-string heredoc detection outside the lexer-event authority
  (MEDIUM-3); raw completeness scanners
- `arg_types`/`quote_types`-style parallel string metadata (Word AST is sole)
- `_file_synced_len` (retired for the pending set, 4B.3), `_pushback`
  (removed w/ ratchet, 4B.4), `with_redirections` resurrection with callers
  (D-4B.4-s3 says dead — confirm still dead)
- broad `except Exception` re-growth at the closed sites (subscript.py, the
  3.5 expansion/arith sites; ratchets: `test_subscript_no_broad_except.py` +
  the 3.5-grown ratchet)
- scalar projection of OperandValue outside the ruled terminal-consumer set
  (3.3; guard: `test_operand_projection_guard.py`); second ExpandedField
  producers beyond the two named
- string-surgery outside sanctioned sites (2.6 guard); env materialization
  outside `_materialize_env_name`; command resolution outside `CommandResolver`
Method: for each, (a) locate the guard/ratchet at ae871a16, (b) read its body
(NAME-VS-BODY), (c) run it scoped, (d) prove it BITES with a synthetic offender
in YOUR WORKTREE (mutation-proven), then revert your offender. Also do a fresh
independent sweep (grep/AST over `psh/` in the worktree) for each pattern —
the guard proving itself ≠ no unguarded siblings. Report per boundary:
INTACT / RESURRECTED (where) / GUARD-DOES-NOT-BITE.

### Q3-representations — transitively immutable and authority-timed?

The new representations, each with its slot's claim:
- Token/TokenPart/Position frozen graph + parts tuples (2.5; transitive census
  guard exists — run it, then attempt mutations at depth ≥2 from live objects)
- pattern AST nodes + `CompiledPattern` frozen, routing bits precomputed (3.2/
  MEDIUM-6; the 7 poisoning demos are raise-assertions — spot-run 2)
- `VariableLookup` read-only slots + MISSING/PRESENT_UNSET frozen singletons
  (4B.1; all three surfaces reject set/del)
- `OperandValue`/`ExpandedField` field IR — `__str__` raises, `.as_scalar()`
  only at ruled consumers (3.3)
- `OpenDescription`/`InputCursorRegistry` (4B.2/4B.4): lifecycle rule "a
  description outlives any ONE of the fds naming it" — probe dup + temp-frame
  cells from `4b.4-rescue/` instruments (copy into your dir, never edit)
- history pending set positional invariants (4B.3 ruling O3's five invariants;
  `_sync_owed` strict=True drift-loudness)
- `HeredocRedirect` required kw-only body; `NonExecutableRedirectError` arms
  (MEDIUM-10)
- AnalysisSession derived-state guard (2.6, both directions)
Authority-timing spot checks: exactly ONE `resolve_command` from
post-side-effect state (3.4's pinned count); seed-at-COMMIT; analysis drives
execution's own unit chunking (shared `iter_command_units`); refuse-before-
evaluate on readonly prefixes. Method: run the committed guards scoped +
fresh mutation attempts from OUTSIDE the tested paths (a new probe, not the
suite's own) — immutability that only the suite's own cells prove is
NAME-VS-BODY territory. Report per representation: HOLDS / HOLE (transcript).

### Q4-new-debt — did the campaign itself introduce debt?

- **Dependency cycles**: import-graph acyclicity over `psh/` at ae871a16
  (write a stdlib-only import walker; runtime imports, then note deferred
  imports separately). The import-layering lock from r19 exists — find it,
  run it, then check the campaign's new modules (procsub_render, input_cursor,
  child_policy, analysis session, operands, pattern engine files) obey it.
- **Deferred-import caps**: locate the cap/ratchet inventories; compare current
  counts vs the committed cap values — did waves 1-4 grow them? Any new
  function-body imports in wave-touched files?
- **Broad owner parameters**: did wave code add new full-`Shell`/`ShellState`
  parameters (the MEDIUM-14 anti-pattern) instead of narrow capabilities?
  Census new signatures in wave-touched modules.
- **Complexity cliffs**: fresh fn-length census (`≥100 lines`, exact count vs
  the 54 baseline at v0.749.0); which of the top offenders are NEW or GREW
  during the campaign (git log -L or diff vs 0215279c for the top rows)?
- **Process leaks**: orphan/zombie discipline — targeted probes: procsub reap,
  background job reap on shutdown, lease orphan sweep (4A.1). Small scoped
  runs only. Check `pgrep` before/after your own probe batches.
- **Flaky tests**: from RECORDED evidence, not fresh heavy runs: `gh run list
  --workflow=nightly.yml` recent results + `nightly-status.md` reading rules;
  the flake-watch rows (1.4 carries, 1.2-era queue, D-4A.2-s1 exit-trap
  family). Targeted re-runs allowed ONLY for specific small node IDs (≤20
  iterations, report iteration count). Verify the two un-quarantined exit-trap
  sentinel tests' recent behavior from run logs if reachable.
Report each axis: count/status + NEW-DEBT findings with evidence.

### Q5-wave5-census — is Wave 5 (5B+5C) still the right backlog?

Fresh census at ae871a16 (the §10 exit criterion: Wave 5 starts from THIS, not
v0.749.0 counts). Produce NUMBERS with the instrument that generated them:
- **MEDIUM-14 protocols**: every `Protocol` in `psh/` — defined-and-used /
  defined-unused; the two name collisions (`ExpansionContext`, `LocaleContext`)
  still live? full-`Shell`-typed members/params in protocols; `Any`-typed
  members. Consumers taking full `Shell`/`ShellState` vs narrow capabilities
  (count + top sites); the core-to-expansion private import — still present?
- **MEDIUM-16 signatures**: incomplete boundary signature magnitude at tip
  (state your methodology; compare to the 510–623-by-methodology base note);
  mypy is on with `check_untyped_defs` — what would `disallow_untyped_defs`
  flag at the boundary seams (producers/adapters/protocols)? Sample-based is
  fine if the method is stated.
- **MEDIUM-15 hubs**: fn-length + file-size census (share with Q4); map top
  hubs to §11 5C's named transactions (state construction, command
  preparation/dispatch, job lifecycle, history expansion, input execution,
  redirect acquisition) — does the named-transaction list still match the
  actual hubs?
- **Q2-debt ledgers**: broad-except ratchet inventory count (which entries
  remain, which carry deadness arguments — e.g. let_builtin.py:52); MEDIUM-12
  5C residue enumerated.
- **What waves 1-4 already removed/reshaped**: diff the §11 package text
  against reality (e.g. 5B says "move shared POSIX tables to a neutral owner"
  — still needed? "remove the core-to-expansion private import" — done or
  not?). The LOW printf %a Wave-5 rider — restate current repro.
Output: per-package (5B/5C) a PROPOSED amended scope: keep / drop-as-done /
reshape (with the evidence line for each), plus anything Waves 1-4 exposed
that Wave 5 should ABSORB (cross-check Part D successor rows marked "5C
candidate", e.g. D-4B.4-s3 dead API).

### QR-queue — Checkpoint-R-queued rows + no-defer audit

The LEDGER rows explicitly queued to this checkpoint; for each, produce a
re-evaluation with fresh evidence and a RECOMMENDATION (quick-rider now /
Wave 5 absorb / successor campaign / record-only):
1. **EXIT-trap output misdirected on fatal-signal death** (Part D row, OPEN
   after v0.754.0; re-eval owed "w/ %P rider"): do NOT attempt the fix; verify
   the current characterization stands at tip (file-mode + stdin-mode rates —
   bounded sampling ≤200 runs total, foreground, report exact counts), restate
   the successor ENTRY REQUIREMENT (non-perturbing observation), and check the
   two un-quarantined sentinel tests' recent gate/nightly behavior.
2. **TIMEFORMAT %P absurd values** (CANDIDATE QUICK RIDER): reproduce at tip
   (rate over ≤60 `time true` runs), confirm the mechanism note (10ms tick /
   tiny elapsed), scope the fix's blast radius (which file/function computes
   %P; is a bash-parity fix one-file?), and recommend rider-vs-defer.
3. **Benchmark tier baselines** (1.4 carry, "Checkpoint R / Ceremony C"):
   locate the 3 non-gating absolute-threshold rows + the workflow-comment
   visibility conditions; check recent nightly benchmark artifacts for the
   measured runner baselines the row says are owed; recommend the discharge
   path.
4. **D-4A.2-s1 exit-trap flake mechanism** (successor "1.3b-adjacent or
   Checkpoint R"): restate; check whether the 4B.4-era redirect work changed
   the residual-window premise; recommend.
5. **No-defer audit** (§10 exit criterion): sweep Part B RE-CARRIED rows +
   Part D successor-queue rows and flag ANY that is actually an unresolved
   CORRECTNESS or EVIDENCE-TRUST blocker being deferred into textbook cleanup
   — those must surface as Checkpoint R blockers, not stay queued. State
   explicitly if none qualify.
Also verify the standing open items' record accuracy: tags v0.766/767 unminted
(user decision pending), v0.770-773 minted; nightly reading rules present
through v0.773.0 in `nightly-status.md`.

## Report format (every scope)

Structured output per the dispatch schema: scope; ≤1-page prose summary;
per-item verdict table (CONFIRMED/FAILED/PARTIAL/NOT-CHECKED + one-line
evidence w/ instrument path); findings list (id `<scope>-F<n>`, severity,
claim, evidence, proof shape, instrument); census numbers where the scope
produces them; recommendations; instruments dir; worktree-removed flag.
State everything you did NOT check (NOT-CHECKED is honest; silence is not).
