# Boundary Remediation Campaign — Unified Ledger

- **Created:** 2026-07-24 (Wave 0 launch). Maintained by the campaign integrator.
- **Launch base:** `0215279c` (v0.750.0).
- **This is THE registry** (integrator plan amendment A2): it merges reappraisal
  #22's findings with the predecessor campaign's 35-row carry register. A row
  leaves this ledger only as CLOSED (probe + flipped/added pin + guard),
  DE-SCOPED (ruled out in writing before implementation), or RE-CARRIED
  (explicit reason + successor owner). "Partial" is not a terminal state.
- Base results reference the integrator's verification of 2026-07-21 (every #22
  HIGH/MEDIUM discriminator reproduced at `0215279c`; probe scripts in
  `wave0-base-probes/`) and the Wave 0 baseline legs (`wave0-legs/` summaries).

## Part A — Reappraisal #22 findings

Status codes: OPEN (awaiting its wave), CLOSED, DE-SCOPED.
All rows OPEN at Wave 0 close unless marked otherwise.

| Finding | Wave/slot | Base result (2026-07-21 verification) | Intended closure evidence |
|---|---|---|---|
| HIGH-1 oracle false-green + bypass | 1.1 + 1.2 | CONFIRMED: `yes` → both rc −9, 8 MB truncated, verdict IDENTICAL; ~30 direct-subprocess differential modules | **CLOSED** v0.751.0 (typed outcomes; `is_comparable` sole authority; forgery-proof frozen+slots) + v0.752.0 (92-module runner migration; AST guard w/ site budgets + PTY registry + offenders; committed replayable audits). Evidence: `1.1-rescue/`, `1.2-rescue/`, `tests/harness/oracle_migration_census.md` |
| HIGH-2 traversal misses executable syntax | 2.1 | CONFIRMED: 4/4 probes report "No security issues found!" (redirect-only, redirect target, for/case subjects) | **CLOSED v0.756.0**: `TotalTraversalVisitor` — framework-owned total child enumeration (mechanical from AST schema, one authority, exactly-once per node incl. the S3 template-carrier fields the old S5 exception never descended into); all 5 analysis visitors migrated; generated sentinel-child battery (node×edge×visitor) + anti-bypass guard proven vs synthetic offenders; the 4 probes flip to reporting findings, promoted to committed tests. Security gains a no-false-clean-claim policy (UNANALYZED_REGION for backtick/heredoc/flat-`[[`-operand executable regions, keyed to psh execution). 4 verification rounds (double-visit exponential regression, backtick coverage loss, prefixed-assignment double-read, B11 scanner contract — each a seam the new reach exposed, zero framework regressions); executable 99-cell reference-coverage suite pins base⊆tip. Evidence: `2.1-rescue/`. |
| HIGH-3 resolution before prefix side effects | 3.4 | CONFIRMED: bash BUILTIN / psh FN, both `$((POSIXLY_CORRECT=1))` and `${POSIXLY_CORRECT:=1}` | **CLOSED v0.766.0** (slot 3.4): two-phase prefix transaction — `expand_prefix` stages left-to-right into an enumeration-invisible `is_staging` temp-env scope (option (F); FIVE sanctioned scope.py edits + prose-only state.py), each value's in-process side effects visible to later values AND to resolution; exactly ONE `resolve_command` from post-side-effect authoritative state; `commit_prefix` installs LIVE staged values (no snapshot, no second expansion — cmd-sub side-effect count pinned at exactly 1) routed per target kind. REFUSE-BEFORE-EVALUATE on readonly prefixes (bash-measured: a refused value is NEVER evaluated — closes the fatal-expansion, `:=`/cmd-sub/xtrace consequence classes, all declared + red-on-base pinned via a GENERATED side-effect-KIND family, 44 = 26 refused + 18 control). RO1 taken in-slot (declared-unset readonly refused on the function route, bash skip-and-continue; nameref spelling pinned presence-equality + wording both-sides). Both signature cells + own-flip persistence = bash; A8 matrix (88 base cells + generated family) green vs bash 5.2.26; 233-row battery (100 red-on-base) + 11-test ordering ratchet (self-tested offenders incl. REORDER; apply_prefix ABSENT from dispatch). SEVEN verification rounds (6 bounces + integrator-direct pass-with-correction): trajectory 7-7-5-4-3-1-1, **28 blocker-class findings, 28 real, 0 false**; three semantics regressions all at COMPOSITION cells, one further caught by the slot's own shipped pins in-flight. Residual A8 DIFF set across all rounds = {Q1,Q3,R1,R1b,R4,X1} (the two out-of-charter confounders only). Evidence: `3.4-rescue/` |
| HIGH-4 subscript procsub identity/timing | 2.3 | CONFIRMED: psh keys `/dev/fd/…` for `a[<(printf x)]=v`; `a[<(if)]` runs where bash rejects the unit | **CLOSED v0.758.0 with declared compound-render normalization residual**: THREE-RENDER-TIER model discovered by probe (source write/read RE-RENDER from the parse — incl. recursive inner frames, backgrounded bodies, `\|&`→`2>&1 \|` canonicalization; ARITH routes hold spelling RAW; RUNTIME STRINGS never render/parse — bash never parses a procsub body at keying time); seam-owned `procsub_render.py` (never-for-declare-f) + parse-time splice; invalid bodies degrade literal with $-forms live; zero-side-effect + read-time-rejection parity pinned; per-route × validity matrix (70 cells) + generated 570-cell render matrix; 4 verification rounds. Evidence: `2.3-rescue/` |
| HIGH-5 combinator drops ParseInputs | 2.2 | CONFIRMED: nested `@(a\|b)` in `$()` rejected by combinator only; facade wrapper discards source/options. AMENDED in-slot: the RD heredoc path ALSO dropped source_text/line_offset (round-1 find; the original "combinator-only" framing was corrected in the slot ledger) | **CLOSED** v0.757.0: ONE `parse_with_inputs(tokens, inputs)` entry threads the whole ParseInputs into BOTH parsers (facade `_ParserWrapper` deleted; orphaned `support/utils.py` context-dropper deleted; `parse`/`parse_with_heredocs`/`create_parser` = documented thin adapters; `parse_tree` builtin's lexer_options drop fixed via the public adapter); 82-param lockstep structure+location parity corpus (extglob ON/OFF, heredoc path, backtick control); HIGH-5 case flipped to 3-way parity; co-landed RD heredoc line-number improvement (base misreported line 1; now = bash) declared + pinned red-at-base across file/-c/stdin × rd+combinator. 3 verification rounds. Evidence: `2.2-rescue/` |
| HIGH-6 operand `$@` flatten | 3.3 | CONFIRMED: `<a b>` vs bash `<a><b>` | **CLOSED v0.765.0** (slot 3.3): opaque `OperandValue` field-vector IR — value operands expand to `ExpandedField`/`FieldRun` (the walker's OWN currency, no third field model) through `:-`/`:+`/`:=`/`:?` and non-colon twins; scalar projection ONLY at the RULED terminal consumers via named `.as_scalar()` (`__str__` raises TypeError so silent str-consumption fails LOUDLY under strict-errors; `test_operand_projection_guard.py` asserts the CLOSED ruled set with self-tested scanners — and caught a re-flatten in the slot's own first draft). Signature cell `"${x:-"$@"}"` → `<a><b>` = bash; 1,970-row matrix vs bash 5.2.26: base 416 divergent → 21 substantive + 1 prefix-only probe artifact; raw-pair sweep over 2,146 cells: 386 changed, toward-bash 385, **away-from-bash 0**. Flip pin FLIPPED 4→21 equality rows (`test_operand_at_preserves_fields`) + 7 combinator rows; 88-row conformance battery; FIVE M8 mutation locks own-reason (flatten / := / empty-collapse / view-branch / redirect-arity). Closed IN-SLOT beyond the charter cell: untriggered-conditional H6 (`a=(""); "${a[@]:+X}"` — untriggered returns the parameter's OWN quoted expansion) and the redirect-target consumer (multi-field target = `ambiguous redirect` rc=1, prefix-normalized byte-identical to bash). Declared exclusions, both-sides pinned, successor-owned: bare-`$@`/IFS family (`test_operand_bare_at_ifs_divergence`, 3 rows) + case-pattern first-field (`test_case_pattern_multifield_operand_divergence`, 2 rows). ExpandedField gained a NAMED second producer (operands.py) — R4.1-confirmed re-cut of the single-producer guard + `test_operand_walker_stays_field_level` anti-drift pin. 3 rounds (BOUNCE 6/13 → BOUNCE 2/15 → integrator-direct PASS): 7/7 real blockers, 0 false, ZERO semantics defects — every blocker was record integrity (pins/prose), not the fix. Three consecutive EXACT pre-registered gate predictions. Evidence: `3.3-rescue/` |
| HIGH-7 pattern negation/nullable + quadratic | 3.1 (semantics), 3.2 (perf/immutability) | CONFIRMED: 3/3 `[[` rows diverge; `matching_starts` 4×/doubling (1.45 s @ N=8000) | **SEMANTICS HALF CLOSED v0.763.0** (3.2 perf/immutability half remains): the engine's star∘extglob composition is the MEASURED bash mechanism — the glibc star-jump (inter-star segments committed at their LEFTMOST match, never retried; the committed entry position feeds the end-of-subject negation special — fires only for a non-`enclosed` group — and the nullable-group skip branches), ported from bash-5.2 `sm_loop.c` and validated cell-exact against live bash 5.2.26 (437,811 corpus cells run / 427,586 distinct, 0 mismatches; verifier-independent assaults: round-2 ~1.8M cells + round-3 fresh grammars, 0 mismatches outside classes then fixed). All three H7 `[[` rows + per-consumer propagation pins created red-on-base and flipped; KNOWN_DIVERGENCES set (q4_sub1/q4_sub2/q4_sub3/neg7_sub3) CLOSED via the measured substitution consumer layer (bash `pat_subst`/`match_upattern`/`match_pattern_char` mechanics incl. the raw-char both-ends wrap guard + paren pun; backslash/escaped-metachar axis corpus 2,016 cells 0 divergent). Path-A eligibility-gated fast path restores linear substitution for non-quirk patterns (equivalence proof 341,836 comparisons 0 disagreements, cache-clear forcing mutation-proven M6). A9-declared residual for 3.2: quirk-flagged relations pay up to ~O(nodes·n³) (full_match `**(a)b` 85× at N=400; baselines in the 3.1 handoff table) — 3.2's all-start rewrite restores poly bounds; `matching_starts` quadratic untouched. Battery: `test_pattern_bash_composition_differential.py` (18 tests, default-run). 3 verification rounds (BOUNCE/BOUNCE/PASS), 5 blockers + 43 nits total, integrator fault 1 + dev faults 4 (all self-caught). Evidence: `3.1-rescue/` **PERF HALF CLOSED v0.764.0 — HIGH-7 FULLY CLOSED** (slot 3.2): one-pass backward all-start relation (`_Matcher._starts`) + memoized ok-table (`_BashMatcher._ok_table`) replace per-start forward DP and the explicit `_closure` set — `matching_starts` quadratic→LINEAR (1.38s→0.0002s at N=8000), `full_match('**(a)b')` cubic→quadratic (36.2s→0.011s at N=800), `${v%%*+(a)}` cubic→LINEAR, quartic `matching_ends`/`span_at` (measured n^3.95, an A9-envelope correction) →~n^2.9; the 3.1 D-2 handoff obligation (ineligible-class substitution) discharged LINEAR on BOTH subject shapes (×1535 consecutive after the dev's own in-slot pre-filter regression was caught by round-1 verification and gated on `has_extglob`; ×1861 word-spaced); eligible Path-A controls at base parity. Semantics BIT-IDENTICAL: full-universe equivalence proof 428,144 cells × 27 relations/operators = 11,559,888 comparisons 0 disagreements, process/tree-forced, re-proved at the fix tip. Deterministic `count_transitions` + `operation_transitions` instruments (the shipped `count_states` pin was accidentally green — memo-miss counter quadratic while evaluation cubic — found by dev-3-2 Phase A, its bound tightened (n+2)²→4·(n+2) as a declared pin change); linearity pins scoped to extglob-free patterns, extglob-bearing rows pinned at ACHIEVED bounds on shaped subjects; M8 mutation class re-introduces the round-1 blocker permanently. `!`-group relations stay quadratic (declared floor: the relation has O(n²) cells). 2 rounds (BOUNCE 5 blockers/19 nits → PASS 0/16), 0 false blockers; dev fault 1 (verifier-caught stale live-worktree perf cells → per-table-provenance rule, B71 extended to devs). Evidence: `3.2-rescue/` |
| HIGH-8 lease rollback incomplete | 4A.1 | CONFIRMED by fault injection + AMPLIFIED: orphan lease poisons later unrelated shells (spurious competing-owner LeaseError); `release_owner` never sweeps | **CLOSED v0.768.0**: component-depth checkpoint at BOTH grant windows (newly acquired components unwound LIFO before owner-metadata rollback); per-lease `owner_ref` discrimination at ALL `_components` consumers — incl. `find_component`, the THIRD undiscriminated site found in Phase A (A-18, integrator-reproduced) — with scripted consumer-census completeness; orphan sweep; restore failures collected + failing leases QUARANTINED (observable: `is_clean`/`quarantine_report`/`clear_quarantine`, the only public additions) + one aggregate `LeaseRestoreError` (LeaseError-derived, never ExceptionGroup); GC-handover fixed where fixable (`_StdStreamBaseline` holds state WEAKLY; PINS True→False measured) and demoted to nicety — the deterministic sweep is the guarantee. A5 battery COMMITTED (first-owner must-hold, transfer-rollback, drop-no-close per kind, release_owner sweep, quarantine re-activation) + full fault-injection battery + composition cells X-1/X-2/X-3 + M8 16 arms/15 locks/0 shared kill sets/1 disclosed equivalent. 2 verification rounds (harness BOUNCE 5 distinct/5 real/0 false → integrator-direct PASS w/ VF-1 condition discharged). Evidence: `4a.1-rescue/` |
| HIGH-9 substitution-origin fact unconsumed | 2.4 | CONFIRMED: rc 2 vs 127; `AFTER` runs after `eval 'echo $(if)'` | **CLOSED v0.760.0 FOR THE PRODUCER-TAGGED CLASSES** (the scanner-balancing six-form class — `case x in a) :;`, bare `case x in`, bare `( :`, `$(( 1 +`, two unterminated-quote forms — reaches neither producer typing nor consumers and is CARRIED to the r18 lexer successor; end-to-end pins in-slot): one typed `SubstitutionSyntaxAbort(BaseException)` raised at ONE consumer helper, consumed at the top level (EXIT trap runs), the exit builtin, teardown (report-and-change-nothing swallow in `trap_manager.execute_exit_trap`) and every fork boundary; `-c`=127 / reader-parse=2 / nested-frame=1 per the status policy; child status honours EFFECTIVE errexit (stamp-at-raise from the fork-site+in-child depth) with bash's severing rule at every route (simple-command members and bg bare-simples sever; compound bodies and directly-invoked functions carry; expansion-time substitution children keep the PRE-SEVER depth via one shared helper — spelling-split at redirect procsub declared); 3 consumer-side anti-bypass guards that BITE; interactive REPL exempt (PTY-pinned); 10 verification rounds, 2 devs, discharge-audit 124/124 + bounced-rows 99/99 at tip. Flipped: `test_c_mode_exit_code_is_127_like_bash` (6 params), `test_eval_source_frame_fatality_matches_bash`, `test_eval_source_procsub_joined_family_matches_bash`, golden `heredoc_nested_error_reports_absolute_line` exit_code 2→127. Evidence: `2.4-rescue/` |
| HIGH-10 closure artifact defects | 0, 1, C | CONFIRMED (all seven particulars at v0.750.0) | Wave 0: A4 corrections + governing docs committed + evidence tree (this commit); Wave 1: durable oracle evidence; C: self-contained close |
| MEDIUM-1 EXIT-trap bypasses shutdown work | 4A.2 | CONFIRMED structurally + measured live (trap-exit: recorded shutdown steps NONE vs both on control; latch made skips permanent) | **CLOSED v0.769.0**: mandatory phases in `Shell.shutdown` — trap phase's terminal exception HELD, history/dispose/reap all run, `close()` unconditional, re-raise with specified precedence (close-error > trap SystemExit > phase failure, `__context__` chaining); order unchanged (= bash's own exit_shell). Two declared toward-bash deltas: history saves under trap-exit on saving routes (route still owns policy); bare `exit` in an EXIT trap resolves to TRAP-ENTRY status (pre-existing divergence found by the round-1 HARNESS after the dev battery's vacuous cells certified parity — fixed in-slot per R4 scope extension, EXIT-only, USR1 must-hold; 39/39 discriminating battery). Exit-criterion widths stated exactly: HUP+history PTY-proven (pexpect+tmux); **reap leg UNIT-proven** (shutdown-path reap structurally unobservable end-to-end — reparent-to-init makes both worlds zombie-free); lease leg = direct ownership-release cell + 4A.1 composition for downstream restores. 63 committed tests; 2 rounds (harness BOUNCE 1/1/0 false → integrator-direct PASS); fresh-checkout 58/58. Evidence: `4a.2-rescue/` |
| MEDIUM-2 UTF-8 decoder seam | 4B.2 | CONFIRMED end-to-end: split é → two surrogates (FIFO + `read -t` timeout repro) | **CLOSED v0.771.0**: ONE incremental decoder across the cursor/bulk seam — `read_all` feeds its tail through the decoder already holding the pending bytes (merge order unchanged; clean-decoder branch measured behavior-preserving); census proved exactly TWO stranding routes (TIMEOUT, ERROR) and `_pushback` producerless (P1 → 4B.4 report). Split matrix 2/3/4-byte × every split × every route: char identity FLIPPED red→green; byte round-trip was NEVER broken (0/36 at base — held as labelled controls). Malformed-byte surrogateescape matrix must-hold. **A5 rider co-closed**: `read -N` honors `-t` (bash rc mapping DATA→0/EOF→1/TIMEOUT→142, partial assigned; the tty arm was genuinely broken at base — PTY pins after the F-4 wrong-tree probe was self-caught). THREE divergences declared + FLIP-PINS-registered, none absorbed: D-4B.2-s1 (timeout-partial assignment, deferred to 4B.4 with I1-style pins; UNDOCUMENTED in the user guide — that absence travels with s1), s2 (`-N` count model, rc-reaching), s3 (`read -s -N` echo, pre-existing, report-only). Carry #21 RE-CARRIED with fresh 24-cell split + first-ever pin + EMPTY base-vs-tip diff; carry #33 declined-with-reason. 92 nodes (22 defect-red incl. 3 PTY + labelled controls/characterization); M8 7/7 certified at a FRESH checkout (BL-1); golden promotion compare-bash 3,042→3,046/26 (count increase, ZERO flips, deselection-proven). Perf: no measurable cost expected; not measured. 2 rounds (harness BOUNCE 3 blockers + 8 RNs, all real, 0 false → integrator-direct PASS); dev self-report register 3 deviations + 5 faults + 5 instrument defects, ALL pre-verdict. Evidence: `4b.2-rescue/` |
| MEDIUM-3 escaped `\<<` heredoc misdetect | 2.5 | CONFIRMED — SCOPING CORRECTED at close (2.5 B70): the interactive-only/latency claim holds for the RD parser ONLY; under the COMBINATOR the misdetection also moved non-interactive diagnostic LINE NUMBERS on all 3 channels (base merged swallowed lines into one buffer; the original claim was accurate on rd, over-claimed for combinator — original text: "INTERACTIVE-ONLY (latent in `-c`: flush re-lexes correctly)"). psh dropped to PS2, swallowed next line | **CLOSED v0.761.0**: session pending-heredocs derived from LEXER EVENTS (one grammar; `heredoc_detection.py` keeps only the shared delimiter/terminator algebra + declared out-of-oracle consumers); PTY pin red-on-base per A5 + 48-row combinator diag pins per channel/parser (DERIVED membership: a family moves iff base regex mis-detected AND lexer says complete — 3 moving families incl. `set -o posix`, 5 literal-line controls); 731-case lexer/session equivalence corpus (internal-consistency oracle, declared as such). 9 verification rounds, 2 devs (handover at round 6), 29/29 blockers real. Evidence: `2.5-rescue/` |
| MEDIUM-4 raw bracket extent / locations | 2.3 | CONFIRMED — prose STRUCK-AND-CORRECTED v0.758.0 (replayed by two verifiers): base behavior was ACCEPTED-WITH-WRONG-KEY `"` (truncated extent + broad-catch literal degradation), NOT rejected | **CLOSED v0.758.0 (parser+expansion halves)**: ONE quote-aware `find_subscript_end` across all four consumer surfaces + whole-string extent validation at `split_subscript` (fixed the destructive `a[]]` runtime-arg alias); `a["]"]`/`a["a]b"]`/nested/cmdsub-embedded/ANSI-C all = bash; absolute SourceSpan + projection pins (origin dropped on parse-time-rewritten specs, documented); the LEXER half (element word-split at space-after-quoted-section + $-form/procsub-body widenings) = CARRY with suite-visible pins |
| MEDIUM-5 mutable VariableLookup/_MISSING | 4B.1 | CONFIRMED both parts: readonly bypass via `.binding.value`; `_MISSING` poisoning | **CLOSED v0.770.0**: read-only properties over private `__slots__` (design D, R1(a)) — every public name rejects assignment/deletion on all three surfaces (fresh VALUE / MISSING singleton / PRESENT_UNSET singleton); `binding` **OMITTED per ruling R1(b)** — the planned "immutable view" was NOT shipped (census: ZERO production readers, double-derived AST+grep) and the exit criterion's "bindings reject mutation" leg is **met BY ELIMINATION**, ruled deliberately (no path from a lookup result to a live cell exists to guard; future view = D-4B.1-s1, never silent resurrection); MISSING **and** PRESENT_UNSET are shared FROZEN singletons (R1(c); PRESENT_UNSET stops allocating — 0.936x at the detached base-vs-tip certification, 0.956x in the Phase A in-process arm). The documented non-frozen perf ruling was overturned by MEASUREMENT: its premise ("lookup() sits on the shell's hottest read path") is false at base — 0 constructions on realistic workloads and at startup (P5, integrator-reproduced; colon operators never reach `_param_is_set`) — while its "roughly triples" cost figure was CONFIRMED for the routes it described; the chosen design costs 1.000x, so the read authority stays hot-path-usable and the `get_variable`/`lookup` projection split is never perf-locked. Threat model = honest-caller accident with an OPEN-CLASS deliberate-circumvention boundary (c-1, widened after verification found routes outside the first closed enumeration) — DEMONSTRATED by a labelled control cell, not guarded (D-4B.1-s2: no available representation closes the class). Internal-integrity slot: ZERO shell-observable delta (compare-bash 3,042/26 EXACT both ends). 58 pin cells (32 defect-evidencing red + 1 incidental-red control + 25 declared green-at-base); M8 5/5 (kill counts 37/1/10/1/2, identical across FOUR independent runs); 2 rounds (harness BOUNCE 1 blocker + 5 required nits, all real, 0 false → integrator-direct PASS). Evidence: `4b.1-rescue/` |
| MEDIUM-6 mutable pattern cache | 3.2 | CONFIRMED: cache poisoning makes `'a'` match `'b'`, incl. through live `case` (7 poisoning-surface demos at base incl. end-to-end `${v//abc/HIT}` flip through a live Shell) | **CLOSED v0.764.0**: all six node classes + `CompiledPattern` are `@dataclass(frozen=True, eq=False, slots=True)` (identity semantics + id-keyed memos retained); the three routing bits (`has_extglob`/`bash_quirk`/`sub_fast`, + new `nullable`) PRECOMPUTED non-recursively at construction (`init=False` — no longer constructor parameters; eager-vs-lazy equivalence guarded per-cell through the corpus proof); both lru caches keep keying/size unchanged by ruling (incl. the two-slot call-shape fact: `compile_cached(p)` vs `compile_cached(p, True)` are distinct entries — recorded, out of scope). THREAT MODEL (ruled, two clauses): pins prove HONEST-CALLER ACCIDENT (all 7 demos are raise-assertions, 12-red at base / green at tip), `object.__setattr__`/module rebinding declared out of scope; the `INSTRUMENTATION` counter global is the declared exempt surface (test scaffolding, `record` defaults False). Evidence: `3.2-rescue/` |
| MEDIUM-7 history cursor conflation | 4B.3 | CONFIRMED: `-d` decrements `_file_read_len` → `-n` duplicates C; `-s` bypasses HISTSIZE | **CLOSED v0.772.0**: read cursor independent of memory ops (`-d`/`-c` no longer move it — legs A/C, bash parity); `history -s` routes through the ONE recording pipeline — FULL recording policy (ignorespace/ignoredups/erasedups/HISTIGNORE-matched-against-the-STORED-text) + HISTSIZE cap, all measured bash (the base `store_entry` docstring claiming "no filtering — bash keeps an explicit -s entry" was measured FALSE); `-r`/`-n` now cap-trim (base: 10+ lines into a HISTSIZE=4 list). `_file_synced_len` RETIRED for an explicit owed-per-POSITION pending set (ruling O3, 5 binding invariants; pending ⊆ memory structurally, covering even the CV3 strip's direct tail delete with the fence untouched — byte-identical base-to-tip). TWO data-integrity fixes beyond the charter: `-w NAMED` no longer drops the session's pending commands at exit; read paths no longer swallow pending typed entries (R2-F2, integrator-found). bash's `-a` MEASURED as a POSITIONAL TAIL WINDOW (executable model: N += recorded/`-s`/`-n`-read lines, not `-r`; −= per `-d`; zeroed by `-c`/`-a`; `-w` untouched) — bash both LOSES typed commands and LEAKS read lines on interleaved compositions, so the exit criterion's "match Bash" and "without duplicate file lines" are jointly unsatisfiable there; RULED deviation family **b1(i)/b2/b3/b4(TWO faces — forward corruption + the R12 mirror suppression, offset-not-blanket proven by control)/b5**, every face both-sides pinned; b1(ii) swallow = DEFECT fixed. Carry #32 CLOSED (leg C); carry #25 rider CLOSED at its RATIFIED true scope (bash fixed internal order — `-ps`≡`-sp`; AT MOST ONE action per invocation with OPERAND-SENSITIVE `-c` suppression (`-cw` suppressed / `-cw FILE` runs); `-d` argument-taking (`-da`=`-d a`); `-c` suppresses `-d`; `-s` short-circuits; `--` honored; separate option words; the `cannot use more than one of -anrw` diagnostic, channel-pinned) — the row's "trivial option-scan" framing did not survive contact. 122 cells / 135 nodes (count history labelled sketch→84→90→106→120→122; behavioural red-on-base case = 13 unit + 35 conformance, the 21 API-absent reds disclosed separately by the dev); M8 14 arms, fresh-checkout-certified (tmp/ absent). Gates 23,817 → 23,833 / 1,620 / 10 EXACT per pre-registration ×2; compare-bash 3,046/26 EXACT, +0 both rounds. TWO verify rounds: workflow harness **BOUNCE — 4 blockers + 15 RNs, ALL real, 0 false** (BL-1 a slot-introduced REGRESSION: text-resolved pending resurrected `-d`'d entries as duplicate file lines where base was CORRECT, its naming pin hollow — NAME-VS-BODY inside the dev's own suite) → fix round (which OUT-MODELED the bounce: operand-sensitivity was the dev's discovery) → integrator-direct PASS + 1 mirror-face finding, pinned with its control. Fault register F-1..F-9 gap-free (8 dev-disclosed, F-5 integrator-found) + 2 integrator brief errata (E1 carry-sweep, E2 wrong pin pointer, both dev-found). Evidence: `4b.3-rescue/` |
| MEDIUM-8 signal dispositions outlive close | 4A.1 | CONFIRMED (audit code refs verified in HIGH-8 injection work) | **CLOSED v0.768.0**: distinct `ComponentKind.MANAGED_SIGNALS` (shared-kind folding measured to LOSE one family's restore — D-04, both orders; ruling (b)), acquired ONLY when the shell already owns the process — mode setup NEVER transfers ownership (the BL-2 harness lesson; the R3-accepted setup-takes-ownership widening was RETRACTED in R9 as the defect's cause) — plus an UNCONDITIONAL drain in `Shell.close()` ordered so a raising release still completes teardown (EN-1). 7 script / 10 interactive leaked dispositions → restore-exact-prior, pinned across mode × teardown-order × double-setup × re-use-after-close × two-sequential-shells × composed-with-trap (X-3 both orders). Evidence: `4a.1-rescue/` |
| MEDIUM-9 analysis under initial options | 2.6 | CONFIRMED: extglob script executes but fails `--validate` | **CLOSED v0.762.0**: state-aware incremental `AnalysisSession` — analysis drives EXECUTION'S OWN unit chunking (shared `iter_command_units`; one heredoc-aware lex per unit feeds BOTH the parse and the state absorption — a body is never lexed as command text), threading the DERIVED parse-relevant state universe (extglob, posix, expand_aliases + alias table, active_parser; derivation guard traces the real pipeline, both directions, mutation-proven). Per-option transition rule measured-then-encoded: monotone enables for extglob/posix (incl. function bodies; 29-row truth table; monotone-enable safety property pinned over a 29,984-cell subset-lattice hunt corpus w/ inverted-direction mutation proof), ORDERED last-wins for expand_aliases (declared cost = unreached conditional disable narrows analysis, pinned in the divergent direction); declared residuals: eval-string/source-file directives, expansion heads. Directive recognizer mirrors the builtin's own argument loop (first-operand stop, contradictory-cluster refusal, quote-aware head via lexer part-context, command/builtin/\-escape/assignment prefixes) — 6 behavior-anchored routing cells, per-cell mutation-proven; alias absorption RUNS `AliasManager.expand_aliases` (position discipline inherited, not re-derived). MEDIUM-9(b): mode composition RULED reject-at-invocation (usage 2 naming ALL flags, before Shell construction; help/version SUPPRESS the conflict check — the measured mechanism, which falsified the integrator's own unmeasured precedence chain, R22-A); single honest `analysis_mode` representation, ambiguous state unrepresentable on both construction paths. Per-unit line diagnostics = execution's renderer (stderr gained `file:N:`, strict improvement, pinned). Co-lands (each declared + red-on-base pinned): rd post-heredoc word-corruption fix (F7, AST-level), `--format` posix render, `psh -n`/`--validate` two-surface split (`bash -n` conformance module = claim-proving CONTROL ROW, green-on-base by design), string-surgery sanctioned-sites guard + tree-wide missing-underscore collection backstop. SEVEN verify rounds, TWO devs (handover r5 per R13-D standing agreement), 19 defects + 1 integrator-chain falsification, 0 false. Evidence: `2.6-rescue/` |
| MEDIUM-10 shallowly-valid heredoc/lexed values | 2.5 | CONFIRMED: `heredoc_content: Optional` with documented-representable invalid state; RD `:156` constructs None | **CLOSED v0.761.0**: `HeredocRedirect(Redirect)` w/ REQUIRED kw-only body = the only executable form (Shape A; plain Redirect w/ heredoc type = structurally-heredoc parse state; typed `NonExecutableRedirectError` arms at BOTH backends + the var_fd route + direct-call primitives, offender-proven per fd-kind none/digit/named — the shape IS live-reachable via ALIAS expansion, declared + pinned per family); COMPLETE lexical value graph frozen (TokenPart + parts tuple + Position; transitive census guard over 8-source corpus w/ stated universe); co-landed: named-fd heredoc/here-string completion `{v}<</<<-/<<<` = bash (improvement beyond base — base parse-errored; two-direction table-parity guard w/ in-guard exclusions), degenerate operand-less forms complete-with-error (= base = bash; plain/digit fenced as pre-existing divergence pin). Evidence: `2.5-rescue/` |
| MEDIUM-11 parser lifecycle vs docs | 2.2 | CONFIRMED: second `.parse()` returns empty program | **CLOSED** v0.757.0: single-use ENFORCED (ruling (a)) — second `.parse()`/`.parse_outcome()` on an RD Parser instance and on any `create_parser` handle raises a loud RuntimeError (the strict-errors internal-defect class; the parser's own PshError taxonomy is the swallowed class — approved deviation, msg ae50a337); failed parse also consumes; combinator GRAMMAR instance stays reusable; classifier pinned not to soften the error to Invalid; docs (`parse_inputs.py`, `context.py`, parser CLAUDE.md) state the contract. Caller census (7 true base sites, all fresh+parse-once; census instrument corrected in-slot after a round-2 verifier find) |
| MEDIUM-12 broad exception nets | 2.3 (subscript) **CLOSED v0.758.0**, 3.5 (expansion/arith), 5C (rest) | CONFIRMED: `except Exception` at subscript.py:129,144 (v0.750.0) — the broad catch was the K1 mis-key MASKING mechanism | Subscript half CLOSED v0.758.0: both catches deleted; typed `SubscriptSyntaxError` narrowed to the tokenize-level unclosed-quote class (8-route audit: 6/8 bash-parity measured, 2 declared builtin-wording faces); granted builtin translations (unset loud / -v quiet = bash). NOTE (amended intent): "Q2 masker ledger shrinks" was IMPOSSIBLE — no registry ever counted these sites (detector keys VT names); the slot BIRTHED the ratchet instead (`test_subscript_no_broad_except.py`, offender-proven). **Expansion/arith half CLOSED v0.767.0 (slot 3.5)**: the `arithmetic_expansion_value` VT net DELETED (VE leg forcing-proven dead; TE leg the masker); PS4 fallback `Exception`→`PshError` (observable byte-identical); four-site dead-VE class narrowed; substring failure typed at detection; `[[ ]]` net → `(TestExpressionError, OSError)` w/ can't-happen branches → RuntimeError; ratchet GROWN by manager.py + arithmetic/evaluator.py; Q2 ledger genuinely SHRUNK by 3 entries (two carried FALSE reasons, corrected in place). Only 5C (builtins/scripting rest — incl. the named let_builtin.py:52 residual, deadness argument transfers) remains open |
| MEDIUM-13 race-dependent background test | 1.3 | CONFIRMED structurally (pass-without-assertion path; also 1 flake in #22's fresh run) | **CLOSED** v0.753.0: subprocess route + job-API `wait` + unconditional asserts (existence, exact bash bytes, clean stdout); vacuity replayed at base; 2 discriminating mutations; 50 seq + 25 shuffled green; state-guarded-assert census 1 → 0 tree-wide. Evidence: `1.3-rescue/` |
| MEDIUM-14 protocol boundaries incomplete | 5B | CONFIRMED: 2 migrated, 3 defined-unused; both name collisions live | per Checkpoint R re-scope — **DONE 2026-08-08 (CR-R1)**: 5B backlog verified 100% intact at ae871a16 (both collisions live at recorded file:line; core-to-expansion private import live; 0 consumers for the 3 unused protocols; full-Shell surface GREW 251→255 Shell + 14→19 ShellState params); 3 reshapes incl. FIRST-deliverable ratchet-scope extension (q5-F2, mutation-proven blind to campaign-created modules). Fresh census = `checkpoint-r/report.md` |
| MEDIUM-15 complexity/hubs; O(k²) session | 5C (hubs); 5A DE-SCOPED (see ruling R1) | CONFIRMED: 54 fns ≥100 lines (exact) | hub decomposition per named transactions. **Checkpoint R census (CR-R1)**: tip = 60 fns ≥100 (base 54 reproduced EXACTLY, replayed by round 2); the six campaign growers explicitly in 5C scope; §11's named-transaction list still matches the hub census |
| MEDIUM-16 incomplete boundary signatures | 5C | CONFIRMED magnitude (510–623 by methodology) | boundary seams annotated first, then per-package. **Checkpoint R census (CR-R1)**: 648 Method A / 488 Method B at tip (grew 625→648 during the campaign; both bases replayed exactly by round 2); the 2 campaign-created modules join the disallow-untyped ratchet in 5C |
| LOW printf `%a/%A` precision + `#` flag | Wave 5 rider (declared here per sequence §13) | CONFIRMED: `%.2a` full-precision vs bash `0x1.92p+1` | float-format conformance rows |
| LOW STD_FDS lease retained on failed exec | 4A.1 | CONFIRMED by injection (+ same poisoning consequence) | **CLOSED v0.768.0**: failing `exec` releases exactly the STD_FDS state it itself acquired (earlier legitimate lease untouched — discrimination cell must-hold); baseline-dup `None` encoding EBADF-only (the B-13a family — failed dup recorded as "closed at baseline", `close()` then closing HOST fds 0/1/2 — dead, pinned); ADAPTIVE parking base `min(_PARKING_BASE=63, max(_PARKING_FLOOR=10, soft − _PARKING_SLOTS(3) − _PARKING_SPARE(3)))` — i.e. `soft−6` in the adaptive regime (CORRECTED at sign-off, D-4A.1-a1: the ceremony record quoted the pre-spare `soft−3` proposal, exactly the version the relocation composition cell proved insufficient) — restores bash parity at every measured threshold (24/40/50/63/64/70/256; base+tip+bash three-way, harness BL-1 + integrator reproduction) with `_PARKING_SPARE=3` found by the relocation composition cell; genuine exhaustion still aborts transactionally (B-13b). Sub-13 envelope = declared divergence (FLIP-PINS row). Evidence: `4a.1-rescue/` |
| LOW skip-on-failure tests | 1.3 | CONFIRMED verbatim in both cited files | **CLOSED** v0.753.0: both named sites + census (14 true instances fixed incl. a 100%-dead NameError-swallowing test; 13 env gates + 1 corpus filter stay, per-hit table); 9 silent-skip oracle modules loud (base: exit-0 w/ 191 differentials skipped); mutation-proven conversions. Evidence: `1.3-rescue/` |
| LOW deferred-import/Q2 debt ledgers | 5B/5C | CONFIRMED (ratchets are inventories, not closures) | caps materially shrink |

## Part B — Predecessor carry register (35 rows): dispositions

| # | Carry | Wave 0 disposition |
|---|---|---|
| 1 | Q2 retained oracles | Already CLOSED (Q3). No action. |
| 2 | F9 git-range self-check | Already CLOSED (Q3). No action. |
| 3 | empty-arith-subscript | Register wording CORRECTED at Wave 0 (A4). Divergence itself RE-CARRIED (deliberate, both-sides-pinned, psh-cleaner ruling stands); slot 2.3 MAY revisit if subscript rework makes parity cheap — revisit is optional, not owed. |
| 4 | operand-`$@` flatten | **CLOSED v0.765.0** via slot 3.3 (= HIGH-6): flip pin flipped to `test_operand_at_preserves_fields` (21 rows) + `_combinator` (7 rows). See the HIGH-6 Part A row. |
| 5 | F6.6 definition-rejection | RE-CARRIED (orthogonal to #22; successor queue). |
| 6 | exec-builtin message wording | RE-CARRIED (cosmetic wording). |
| 7 | RANDOM-in-prefix | **CLOSED-WITH-SCOPE v0.766.0** (slot 3.4). Closed: interleave reads (`RANDOM=1 b=$RANDOM cmd` -> b=1 = bash at ALL THREE target kinds), function-target masking, seed-at-COMMIT. NOT closed, pinned both-sides as documented divergence: the command's OWN read of a masked dynamic special on the LAYER/SEED route (`RANDOM=1 eval 'echo $RANDOM'` -> bash 1 / psh generates; `declare -p` shows `-ix`). "A later prefix and the command itself are DIFFERENT READERS; the slot fixed the first." Successor row D-3.4-s4; Option (A) (D-3.4-s5) would close it. |
| 8 | timeformat `%P` flake | **CLOSED** v0.753.0 (root-caused: shape helper pinned %P digit WIDTH vs a 10 ms accounting tick; base 4/60 → tip 0/60+0/25). The %P VALUE defect is a separate Part D row below. |
| 9 | plain-expansion echo stream | RE-CARRIED (orthogonal). |
| 10 | history `-p` failed-arg wording | RE-CARRIED (message wording only, both rc 1). |
| 11 | trailing-redirect-at-EOF | RE-CARRIED by 2.5 (assessed per R1-D, not assumed: byte-exact PTY probe at base AND tip, both parsers — redirect-TARGET completeness family, unreached by the heredoc-detection fix; base-identical; 2.5's instrument-defect fix REVEALED its rows had been misread as timeouts — conclusion stands, evidence corrected, R13-B). |
| 12 | general async reaper (H19 residual) | RE-CARRIED to successor (with #16); NOT owed by 4A (which owes EXIT-trap/lease paths only). **Checkpoint R attaches CR-D1** (bg-job reap-timing characterization + repro, previously unregistered — see Part D Checkpoint R registrations): the characterization now lives with this mechanism row. |
| 13 | stopped-fg-subshell not recorded | RE-CARRIED. |
| 14 | procsub-`$!`-wait | RE-CARRIED. |
| 15 | tcsetattr-drain probe note | NOTE only (probe construction), remains recorded. |
| 16 | RESUMABLE-PARSER (H15) | RE-CARRIED as SUCCESSOR CAMPAIGN per ruling R1 (5A OUT). |
| 17 | J1 Linux-nightly watch (MUST) | **RECLASSIFIED — CANNOT be discharged as green: the nightly has been RED since 2026-07-02** (23 consecutive failures; last green `b314064c` 2026-07-01, first red `a0e99959` 2026-07-02). Latest census (2026-07-24, at `0215279c`): full-suite job 24 failed / 21,746 passed (16 parallel + 8 serial) incl. the J1-family signal/trap/procsub-reap/pipeline-death tests #17 was watching, plus history-outcome[6], locale-provenance, protocol-freeze, PTY tab-completion; conformance job 54 failed / 2,600 passed dominated by syntax-template `-file`-channel arith rows (oracle = Linux bash 5.2.21 vs local 5.2.26). Multiple accretion layers (several failing tests postdate 2026-07-02). Full record: `nightly-status.md`. **CLOSED v0.755.0 (slot 1.4): nightly GREEN — first since 2026-07-02, three consecutive green dispatch runs (30171120171, 30172534890, 30175067149 @ cdff0704); conformance 54→0 (2,671 passed), parallel 26→0 (21,873 passed). Census: 30 rows classified (i)=46-conformance+PTY-xfail-family /(ii)=13 /(iii)=1 /(iv)=13 /(v)=2 — every J1-family row root-caused, none quarantined. The J1 watch's real defects: `bg` stale-state SIGCONT gap (fg-mirror refresh, deterministic red-on-base pin) + locale reactive over-warn (LC_ALL reset path silenced per bash's per-trigger rule, both-direction mutation pins) + the harness cap/timeout kill missing escaped process groups — a runaway `yes` filling unlinked capture files at ~480MB/s that killed BOTH the Linux nightly ([Errno 28]) and this macOS host's gates (the months-old "external consumer"). Descendants-first sweep, gated to live-leader kills, synthetic escaped-offender pin. Evidence: `1.4-rescue/` (slot ledger 1,226 lines + integrator inbox + probes). Standing rule: first SCHEDULED nightly post-merge must be verified green (wave-close checklist).** |
| 18 | posix special-builtin redirect fatality | RE-CARRIED (both-sides-pinned). |
| 19 | `$'\xNN'` byte model | RE-CARRIED (pinned). |
| 20 | key_decoder replace-decode | REMAINS sanctioned terminal-UI non-goal. |
| 21 | `read -N` mixed valid+malformed hybrid | **RE-RULED at 4B.2 (v0.771.0): RE-CARRY, not close** — fresh 24-cell probe (6 payloads × `-N 1..4` × psh/bash-UTF8/bash-C): psh matches both 1 / UTF-8 9 / C 6 / NEITHER 8 — the hybrid is psh's own documented model and closing would adopt one libc's quirks. Obligations discharged: fresh split on record (`4b.2-rescue/`), FIRST test pin added (6 both-sides characterization cells in `test_cv_carry_characterization.py`), no-silent-change PROVEN (24/24 byte-identical base-vs-tip, dev + verifier + clean-checkout replays). Still open for a future ruling. |
| 22 | S3→I3 substitution-origin not consumed | **CLOSED v0.760.0** via slot 2.4 (= HIGH-9), qualified to the producer-tagged classes (scanner-balancing six-form class carried to the r18 lexer successor). Consumer chain: `scripting/source_processor.py#SourceProcessor._substitution_syntax_abort` → `SubstitutionSyntaxAbort` → `core/internal_errors.py#substitution_abort_status` (main) / `executor/child_policy.py#map_child_exception` (children). |
| 23 | nested-quote arith carriers | RE-CARRIED (B1 model limit; #31 contract-reshape family). |
| 24 | two-tier introspection + 126 wording | RE-CARRIED (resolver candidate-model flag; not in #22 scope). |
| 25 | `history -ps` clustered flag | **CLOSED v0.772.0 (slot 4B.3 rider, scope RATIFIED at ruling RN-4)** — the "trivial option-scan" framing did not survive contact with bash: clustered flags apply in a FIXED INTERNAL ORDER (`-ps`≡`-sp`, `-s` wins and CONSUMES the line-scoped strip flag), AT MOST ONE action runs per invocation with OPERAND-SENSITIVE `-c` suppression, `-d` is argument-taking (`-da`=`-d a`), `-c` suppresses `-d` (`-cd 9` rc 0 silent), `-s` short-circuits file ops, `--` honored, separate option words parse as options, and two-of-`-anrw` gets bash's `cannot use more than one of -anrw` diagnostic (channel-pinned). 13/13 rc rows + file/memory observables. See the MEDIUM-7 row + `4b.3-rescue/`. |
| 26 | cmdsub-with-`;` in arith subscript | NOTE only (no divergence). |
| 27 | sticky-hash of non-exec lose-on | RE-CARRIED (deliberate, risk-ruled, pinned). |
| 28 | nested-subscript assignment extractor | ATTACHED to slot 2.3: the subscript/extent rework covers this shape — close or re-carry with evidence. |
| 29 | heredoc history trailing newline | RE-CARRIED (cosmetic). |
| 30 | executable special-file earlier (FIFO/SOCKET) | REMAINS sanctioned DESIRABLE deviation (do not "fix" back). |
| 31 | `[[` operand arith provenance (+ composed read/write) | RE-CARRIED to successor campaign (whole-contract reshape, with #16). |
| 32 | `history -a/-c/-n` counter model | **CLOSED v0.772.0 (slot 4B.3)** — root cause was `clear_history` resetting the read cursor (an entry `-a`'d then `-c`'d re-materialized on `-n`); the cursor no longer moves on memory ops, and bash's actual `-a` mechanism was measured as a positional tail window (executable model in `4b.3-rescue/`). The leg-C composition is pinned end-to-end. |
| 33 | CRLF in piped `-i` | RE-CARRIED; slot 4B.2 optional revisit (input-layer adjacency). **4B.2 DECLINED the optional revisit (v0.771.0)**: different input layer, no shared code path per the i2 census, slot already carried two defects + a deferred contract question. Stays carried. |
| 34 | PROMPT_COMMAND piped `-i` only | REMAINS recorded piped-harness artifact (PTY-proved working). |
| 35 | eval'd outer-single `history -p "!!"` | RE-CARRIED (expansion-engine reconstruction; pinned). |

## Part C — Wave 0 rulings

**R1 (5A / H15, plan A7):** Package 5A (resumable lexer/parser transaction) is
**OUT of this campaign's closure scope**, recorded as the successor
RESUMABLE-PARSER CAMPAIGN together with carries #16, #31, #12, and the other
Part B re-carries. Reason: campaign-scale by prior ruling (I3 Option B); under
sequence success criteria 1+6 a late deferral would make this campaign
permanently unclosable. Wave 5 here = 5B + 5C (+ Checkpoint R amendments).
MEDIUM-15's O(k²) `ParseSession` element follows 5A out; its hub-decomposition
element stays (5C).

**R2 (InputCursor gaps, plan 4B.4):** ~~cross-fd dup sharing and
temporary-redirect isolation are DE-SCOPED from closure — slot 4B.4 becomes
a contract-narrowing documentation task, executed BEFORE 4B implementation
begins. Both re-carry to the successor queue.~~ **SUPERSEDED at slot 4B.4
dispatch (2026-08-08, ruling 4B.4-R1) on PREMISE FALSIFICATION**: the
de-scope rested on the I1-era rarity characterization (ultra-rare
malformed-multibyte count boundary), but 4B.2's timeout-hold (D-4B.2-s1,
postdating this ruling) opened WELL-FORMED-input routes to both surfaces —
integrator + dev probes at e3924ed3 showed temp-frame contamination
(x=\303FILELINE) and dup byte LOSS with a valid é lead. The timing clause
("before 4B implementation begins") was also unexecutable by then. Slot 4B.4
re-opened close-or-narrow on current evidence and RULED CLOSE (both surfaces;
4B.4-R2 row 4); nothing re-carries. Struck-and-corrected per rule 11 —
the original text preserved above; evidence in `4b.4-rescue/`.

**R3 (nightly, plan amendment AM-1):** Wave 1 gains slot **1.4 — Linux nightly
recovery** (see `nightly-status.md`). Rationale: the nightly is the Linux
observation channel; Wave 1's charter is evidence trust, and a 23-day-red
backstop is an evidence-trust failure exactly like HIGH-1 (nobody looked at
run RESULTS — the repo's own CI-green lesson). Exit for 1.4: nightly green on
Linux, or every red row explicitly classified (Linux-genuine → fixed or
wave-assigned; oracle-version → handled by the Wave 1 oracle policy;
env/infra → fixed) with the run link recorded. Carry #17's J1 rows are
discharged INSIDE 1.4.

**R4 (LOW printf):** declared a Wave 5 rider now (satisfying sequence §13's
"declared at Wave 0"), not a follow-on: it is user-visible float formatting
with a one-file blast radius.

### Checkpoint R rulings (2026-08-08, integrator; evidence `checkpoint-r/`)

Sequence §10 executed at `ae871a16` (v0.773.0): two rounds (6 scoped
verifiers + 3 adversarial attack scopes), **clean bill certified under
attack** — zero BLOCKERs / zero REQUIRED-NITs both rounds, 23/23
discriminators pass, zero boundary resurrections, all representations hold,
no-defer audit PASS. Full record: `checkpoint-r/report.md`.

**CR-R1 (Wave 5 re-scope amendment):** Wave 5 = 5B (KEEP ENTIRE — backlog
verified 100% intact — plus 3 reshapes: ratchet-scope extension as FIRST
deliverable; the 12 campaign-added owner params in the migration set; caps
criterion measured from 179/200) + 5C (KEEP with the fresh census: 60 hubs,
648/488 signatures, MEDIUM-12 residue enumerated; absorbs D-3.5-s2 +
D-4B.4-s3; MEDIUM-12's subscript + expansion/arith halves DROPPED-AS-DONE).
Baseline for all Wave 5 exit measurement = the Checkpoint R census,
superseding v0.749.0 counts. printf %a/%A stays the declared rider.

**CR-R2 (%P):** TIMEFORMAT %P QUICK RIDER APPROVED (mechanism confirmed
live: os.times() 10ms quantum; zero-face 60/60 + independently 20/20;
one-file blast radius + un-blinding the magnitude-blind test). Runs as a
standalone micro-slot on user GO.

**CR-R3 (exit-trap misdirection):** characterization STANDS (0/200
validity-controlled foreground; sentinels green local + Linux; recurrence
tally 2); stays successor queue with the non-perturbing-observation ENTRY
REQUIREMENT intact.

**CR-R4 (benchmark baselines):** the owed runner baselines NOW EXIST
(harvested from 3 consecutive nightly artifacts); discharge at Ceremony C by
retuning the three constants to runner-measured envelopes and re-gating.

**CR-R5 (D-4A.2-s1):** premise UNCHANGED by 4B.4 (zero signal/trap/job files
changed v0.769.0..tip); stays with the successor row.

**CR-R6 (no-defer audit):** PASS — no Part B / Part D row is a correctness
or evidence-trust blocker deferred into Wave 5 textbook cleanup; the
2.3-carry lexer no-progress crash re-affirmed PRIORITY at the head of the
r18-lexer successor queue with a named owner.

**CR-R7:** the bg-job reap-timing divergence (previously unregistered) is
registered as CR-D1, attached to Part B carry #12.

## Part D — New facts registered at Wave 0

| Item | Detail | Owner |
|---|---|---|
| Nightly red 23 days | See carry #17 row + `nightly-status.md` — **CLOSED v0.755.0 (slot 1.4)**: green since 2026-07-25, 3 consecutive dispatch runs; first-SCHEDULED-green check owed at Wave 1 exit | 1.4 ✅ |
| `read -t X -N n` hangs | `-N` path passes no deadline to `read_limited` (found 2026-07-21, verified end-to-end) | 4B.2 rider |
| Lease-poisoning amplification | Orphaned component lease breaks LATER shells' activation; `release_owner` early-return never sweeps | 4A.1 (exit criteria include it) |
| gh account-switch hazard | Active gh account found switched to a READ-only account 2026-07-24; ceremony pre-flight now includes `gh auth status` | ceremony checklist |
| **psh EXIT-trap output MISDIRECTED on fatal-signal death (top-level shell)** | **OPEN after v0.754.0** — the rider closed THREE provable windows (drain-before-trap; pop-after-restore w/ stream-level `streams_restored`; mid-SETUP fd-0 ambiguity → explicit `fd0_was_closed`), four mutation replays pin them, and the rate did NOT separably move: base ~1.08% (5/464) → post-A ~0.14% (1/700) → post-B ~0.60% (3/500) — statistically inseparable (dev retracted its own '~10x' read). CORRECTED MECHANISM (proven by sentinel-file capture): output is not lost — it is written INTO the live per-command redirect target. Mechanism misdiagnosed TWICE (dead-binding story; teardown-sliver hypothesis FALSIFIED by (B) at N=500). CHARACTERIZATION: fires in file-mode 1/25 and stdin-mode 1/25, -c 0/25 (verifier, tip); face (b) wrong-exit observed 1/100 independently. OBSERVATION LIMIT: every fd-write trace hides the window (0/400 traced vs ~3/500 untraced). **ENTRY REQUIREMENT for successor: non-perturbing observation (pre-allocated in-process ring buffer sampled at death, or external record) as FIRST deliverable.** GATE CLASSIFICATION: sentinel tests `test_exit_in_exit_trap_matches_bash_sigterm` (face a) and `test_exit0_in_exit_trap_command_mode_dies_by_signal` (face b) stay un-quarantined by ruling — classify their rare failures against THIS row. The interactive SIGHUP death path DELIBERATELY retains pre-1.3b behavior (no drain, old swallow — exposure exists there; successor inherits). RULINGS RECORDED (2026-07-25, integrator): slot retitled 'signal-window hardening'; in-tree statistical oracle WAIVED (would permanently flake the gate); ONE-tripwire rule. | successor queue — Checkpoint R re-eval DONE 2026-08-08 (CR-R3): characterization stands, entry requirement intact |
| Redirect-frame teardown atomicity (CLASS) | 1.3b closed the EXIT-trap instance + pop-order instance + mid-SETUP fd-0 instance; the CLASS question (any writer landing in a frame window inherits a dying/foreign binding) remains — audit = successor row. Also: doc-pointer guard validates only the PATH half of file#symbol cites (tooling carry, found 1.3b round-2) | successor queue |
| psh `TIMEFORMAT %P` absurd values | `time true`: psh `R=0.000 U=0.010 S=0.000 P=11934.31` vs bash `P=2.02` — 10 ms accounting tick / 84 µs elapsed ≈ 11930 (mechanism verified arithmetically); 2/30 idle-host rate; CLI-reachable. 1.3's test fix makes the suite deliberately magnitude-blind, so nothing in the tree notices this until fixed. | QUICK RIDER APPROVED at Checkpoint R (CR-R2), awaiting GO |
| Backgrounded-subshell redirect bypassed when `sys.stdout` ≠ fd 1 | In-process harness only (file created EMPTY, output to captured stdout); foreground twin, bg simple command, and subprocess psh all CORRECT → internal inconsistency in the bg child-runner path (`psh/executor/subshell.py#_execute_background_subshell` shares streams :254-256 as does fg :144-146 — divergence is in the surrounding runner). CLI-unreachable. Characterization pin added v0.753.0 (both capture regimes). 5-row evidence matrix in `1.3-rescue/`. | successor queue; discharge trigger: any slot touching `_execute_background_subshell`/`run_background_shell_child` |
| `try_resolve_bash` dead inventory | After 1.2's 11 + 1.3's 9 loudness conversions, zero consumers outside its own definition/self-test/gen_census pattern string — the exact referenced-only-by-its-own-test shape F2 outlawed for the catalog | successor queue (delete or re-justify) |
| F1: documented-difference matching is BEHAVIOR-BLIND | Found by 1.2's FLIP-PINS audit, verified twice: `_is_documented_difference` is `command in catalog` with both observed results unused — a forged nonsense psh stdout for `echo $$` still classifies DOCUMENTED_DIFFERENCE, so those pins cannot fail for the right reason (HIGH-1 family). Closure = typed expected-shape per catalog entry + shape-validating classifier + forged-output discriminator red-on-base | **1.3 — CLOSED** v0.753.0: per-side expected shapes + validating classifier; forged stdout AND exit-status discriminators red-on-base (4 failed/3 passed); right-reason by mutation (PSH_BUG at tip, no-change at base); vacuous-expected hole closed BOTH halves (runtime + meta-test) with offender. |
| F2: difference catalog rot | 4 of 7 entries (HELP_BUILTIN, PUSHD_BEHAVIOR, PUSHD_CWD_DIFFERENCE, POPD_BEHAVIOR) referenced by ZERO tests; PUSHD_CWD_DIFFERENCE documents a HARNESS artifact as a shell difference. Closure = every entry test-referenced or deleted + zero-dead-entries meta-test | **1.3 — CLOSED** v0.753.0: 4 dead entries deleted w/ live probes (pushd family CONTRADICTED by test_bash_compatibility identity assertion; harness artifact confirmed); 1 REAL divergence registered shape-carrying (BUILTIN_LONG_HELP_OPTION); zero-dead-entries meta-test, resurrection-proven. |
| 1.2-era flake queue additions | `test_complex_pipeline_background` (1x under load); `TestExitTrapOnFatalSignal::test_command_mode_fires_exit_trap` (1x under load, 3/3 isolated); `timeformat %P` recurrence (= carry #8); malformed-bytes `mapfile_read_all` (1-of-9) | **1.3 — DISPOSED** v0.753.0: pipeline + exit-trap-command-mode NOT REPRODUCED (80-90 runs each incl. load), hardened not quarantined; %P root-caused (carry #8 row); mapfile not reproduced (80 runs), hardened w/ absolute expected-bytes pins; exit-trap family root-caused to the REAL race (row below → 1.3b) |
| Runner default-timeout exposure | 1.2's migration gave 296 runner call sites in 53 migrated modules the 10s default where base was untimed (loud Timeout → non-comparable, never false-green); fresh flake surface under load — per-case explicit timeouts, not a global bump. 1.3: tested and ruled OUT as mechanism for every queued flake | 1.3 → standing watch |
| 9 pre-existing silent-skip oracle modules | `try_resolve_bash`+skipif modules predating 1.2 (enumeration in 1.2-rescue/slot-ledger) silently skip w/o bash oracle; loudness ruling applies | 1.3 |
| 1.4 carry: locale warn wider surface | Seven psh-vs-bash divergence rows OUTSIDE the fixed LC_ALL reset path (probe matrix in `1.4-rescue/slot-ledger.md`): bash SILENT / psh WARNS on `LANG=<bad>` (assign, assign+unset, temp-env prefix, startup) and startup `LC_COLLATE=<bad>`; REVERSE direction on `unset` of already-bogus LC_CTYPE (bash warns, psh silent) — **a blanket silencing pass would make this worse, forbidden**; plus warn SHAPE (bash 1 line naming LC_ALL, psh per-category lines). Fix seam: `psh/core/state.py` (NOT locale_service.py — the v0.755.0 fix landed there; ruling #1's file name was wrong) | successor queue |
| 1.4 carry: `bg` on RUNNING job silent | psh returns 0 silently where bash prints "bg: job N already in background"; pre-existing, out of 1.4's minimal-diff ruling | successor queue |
| 1.4 carry: benchmark tier baselines | 3 absolute-threshold rows (~24% over on shared runners, pass locally) made non-gating per ruling #4 (workflow comment + artifact trail + this row = the visibility conditions); owed: measured runner baselines | Ceremony C (baselines harvested at Checkpoint R, CR-R4) |
| 1.4 carry: second-sweep blindness | Timeout path's second `_killpg_sigkill` cannot see descendants (leader reaped ⇒ children reparented to init, pid/ppid links gone); first call's enumerate-before-kill does the real work; §7(1) producer bound covers the cap-disabled residual | recorded limit |
| 1.4 carry: bg PTY manifestation unpinned | Ruling #2's deterministic pin construction is -c/file/stdin-mode; the load-dependent interactive-PTY manifestation stays covered only by pre-existing `test_pty_smoke` row (sanctioned) | flake watch |
| 1.4 carry: nightly instrumentation expiry | Sampler + fd snapshots + PSH_DISK_WATCH + core_pattern normalization stay as relapse watch; REMOVAL CRITERION (in workflow comment): several consecutive green SCHEDULED nightlies, zero ENOSPC, no trips | Wave 2+ close checks |
| 1.4 carry: StopIteration one-off | `test_argument_less_builtin_has_no_trailing_space_script` StopIteration in 30143337081, never recurred; now reports the listing on failure. Procsub zombie rows: fixed `sleep 0.2` settle accepted; bounded reap-poll PRE-RULED APPROVED if either row flakes on a scheduled run | flake watch |
| 2.1 carry: `[[ ]]` operand execution divergences | THREE psh-vs-bash execution divergences found while building the security scanner, all in `[[ ]]` flat-text operand quoting (bash 5.2.26 oracle, marker-file verified): (1) escaped backtick `[[ "\`cmd\`" == x ]]` — psh EXECUTES, bash treats literal; (2) `[[ "\\$(cmd)" == x ]]` — bash EXECUTES, psh does not (lexer collapses `\\$(`→`\$(`); (3) `[[ $'$(cmd)' == x ]]` — psh expands the substitution in `$'...'`, bash does not. Scanner flags per PSH's own execution ("opener live unless immediately preceded by a backslash"); the DIVERGENCES themselves (a parser/lexer question) are for a successor slot. Exact spellings + root causes in `2.1-rescue/slot-ledger.md` §11. | successor queue |
| 2.1 carry: `[[ ]]` double-quoted operand flattening | A double-quoted `[[ ]]` operand parses to a Word with a bare LiteralPart (no ExpansionPart), so a live `$()` inside it has no structural node — the security scanner's UNANALYZED_REGION flag is the only coverage. Real fix is a parser change (give the operand real expansion parts), out of 2.1's visitor scope. | successor / parser slot |
| 2.1 lesson (cross-slot): generate over the SPACE | The slot's most transferable output. A hand-built 19-shape corpus reported "zero losses" and was wrong; the adversarial harness's 105 generated scripts found 8; the dev's COMBINATORIAL 81-shape matrix found 12 (two whole families the harness missed); the integrator's user-facing gate found 1 more (the `$@` advisory no count-level check could see). Each layer caught what the others could not. Durable form: an executable coverage-SPACE suite (`test_reference_coverage_space.py`) whose claim is "generated over {families}×{positions}, domain stated." Ask of any base⊆tip claim: generated over what space, and where is the domain stated? | banked |
| 2.1 lesson (cross-slot): instrument discipline binds BOTH directions | 10 instrument faults surfaced (7 dev, 3 integrator). A verifier's instrument fails toward "dev claim wrong" as easily as a dev's fails toward "all clear" — the integrator's broken `grep -c "...'$y'"` ($ = regex anchor → false 0) and byte-mangled `printf` each nearly recorded a CORRECT dev claim as false. Rule: state the instrument, show its output, change the instrument on challenge; for finding counts use `grep -F`; write `[[ ]]`/`$'...'` probes byte-exact from files (`od -c` verified), never via `-c`/printf. | banked |
| 2.2 carry: combinator arrays.py word-builder seam (FULL reach) | The combinator's STATIC `WordBuilder` bypass at `combinators/arrays.py#parse_word_as_word` (L51) + `build_subscript_spec` (L254 area) does not thread ParseInputs — and its reach is WIDER than array literals: it also serves REDIRECT TARGETS (via `RedirectionMixin._parse_word_as_word`) and array ELEMENT ASSIGNMENTS. So `a=($(echo @(a\|b)))`, `a[0]=$(echo @(a\|b))` AND `echo hi > $(echo @(a\|b))` still diverge (RD+bash accept; combinator rejects) — pre-existing, byte-identical at base and v0.757.0, partly documented in-source as an intentional educational-combinator residual. Closing it = swapping the static builder for the shared ExpansionParsers path (behavior-change risk: subscript read-time validation). TWO divergence flip-pins land with 2.2: `test_CARRY_array_init_nested_substitution_still_diverges_on_combinator` + the redirect-target twin (both co-flip only if the WHOLE seam is threaded at once). | successor queue |
| 2.2 carry: combinator ignores line_offset for TOP-LEVEL statements | The combinator never stamps top-level statement `.line` (base probe: `[None, None]` vs RD `[1, 2]`), so `--parser combinator` misreports absolute parse-error line numbers for top-level errors even though 2.2 now threads line_offset (NESTED bodies are correct on both parsers, = bash). Pre-existing at base, NEWLY DOCUMENTED by 2.2 (no earlier documentation existed); declared out of 2.2's scope in the corpus domain statement. Closing it = combinator stamps statement lines from the threaded context. | successor / parser slot |
| 2.2 notes (record-only) | (1) compare-bash baseline CORRECTED: the campaign's recorded "2,986" was stale — true base at db6dfb13 = **2,985 passed / 25 skipped** (dev measurement CONFIRMED by round-1 T3 independent replay); v0.757.0 tip = 2,986/26 by NEW composition (+1 golden psh-side pass, +1 psh_only skip). (2) `architecture_feature_review_2026-06-09.md` line-176 OPEN row's subject (`support/utils.py`) was DELETED by 2.2 — dated docs stay snapshots; this row is the closure record. (3) `create_parser` return identity changed (base: Parser \| _ParserWrapper; now: uniform single-use `_DeferredParse` handle) — zero in-tree consumers depended on the old identity (verified round-2). (4) The 2.2 golden `heredoc_nested_error_reports_absolute_line` pins psh `-c` exit 2 on a nested-substitution syntax error — the SAME family slot 2.4 is chartered to change to 127; registered as a 2.4 co-flip in FLIP-PINS. (5) Verifier-integrity tally: round-1 T3 endorsed the (false) 3-site caller census "all TRUE" — first verifier-side fault of the campaign; round-2 ledgerCheck caught it. | recorded |
| 2.3 carry: LEXER word-extent family (WIDENED) | The lexer splits element words at a space following a quoted section in brackets (`a['x=1'a b+=]=v` → rc 127; bash keys `x=1a b+=`), and the same extent blindness blocks $-forms/`$()`/procsub bodies with spaces in ELEMENT words (write path parse-errors; READ path works, pinned green) — 150 of the 570 render-matrix cells are lexer-blocked, attributed in-test. Pre-existing, base-identical, out of 2.3's parser/expansion scope. Pins: `test_divergence_lexer_splits_quoted_space_subscript`, `test_divergence_procsub_compound_dollar_body_lexer_blocked`, doubled-open-unclosed families. | successor (lexer slot) |
| 2.3 carry: LEXER NO-PROGRESS CRASH (**PRIORITY**) | `a["x`echo "]"`"]=v` crashes psh's lexer outright — raw RuntimeError "lexer made no progress at position 26", CLI-REACHABLE, both parsers, base-identical (bash keys `x]` fine). The strict-errors INTERNAL-DEFECT class live in production. Deliberately NOT pinned (a crash is never a pin target); probe r18 in `2.3-rescue/`. | successor — PRIORITY candidate post-pause |
| 2.3 carry: unset/test -v non-bracket-arg sibling | `unset -v 'a["]"'` (arg NOT ending `]`): psh silent rc-0 no-op vs bash loud invalid-identifier. The DESTRUCTIVE `a[]]` half was FIXED in-slot (whole-string extent validation at `split_subscript`, six-leg bash parity); this non-destructive sibling stays carried. Pin: `test_divergence_unset_nonbracket_arg_silent`. | successor queue |
| 2.3 carries: rendering residuals (declared, pinned) | (1) tilde-leading assoc keys: `declare -p` prints `[~k]=` vs bash `["~k"]=` (utils/escapes.py, outside slot); (2) EMPTY assoc: `declare -A a` (bash) vs `declare -A a=()` (psh); (3) assignment-prefix element runs: `foo=1 bar[0]=2` → psh rc-127 vs bash keys it (pin: `test_divergence_assignment_prefix_element_split`, stdin channel); (4) procsub key-render residuals: separated-subshell + compound bodies keep RAW spelling (bash re-renders multiline byte-layout), `\|&` kept raw (bash CANONICALIZES to `2>&1 \|`), `#`-comment bodies keyed literally (bash rc-2 continuation) — all both-sides pinned. | successor queue |
| 2.3 declared divergences (KEEP-ruled / must-NOT-flip) | (1) g6/g8 dq-outer ANSI-C-bracket reads: psh round-trips its own writes; bash rc-1 chokes on its own early textual `$'…'` decode (its stderr shows mangled `${a[[]}`) — KEEP psh-cleaner (precedent B#3); (2) sq-in-dq READBACK: psh reads back its own sq-spelling key (`h['$(if)']`) rc-0; bash rc-1 cannot — same bash-cannot-read-own-writes family, KEEP; (3) e2 `$((h['x]))`: psh typed rc-1 vs bash rc-2 lexer-reject (base printed 0 on a junk key); (4) builtin-route wording faces (printf/read/let/nameref + printf-raw rc-2-vs-1): bash per-builtin wording + continue vs psh typed discard-line — declared, pinned both-sides, fixing = builtin surgery out of scope. | recorded; FLIP-PINS must-NOT-flip |
| 2.3 carry: I3 family WIDENED (2.4 inherits) | 2.3's typed-path routing joined the PROCSUB spelling to the pre-existing cmdsub I3 family at eval/source frames (`eval 'a[<(if)]=1'` continues where bash aborts; base's match was ACCIDENTAL — runtime arith error, same observables, different machinery). Deferred-execution model for invalid $() bodies makes every route attempt execution like bash; per-route fatality deltas pinned into I3/s2. Pin `test_divergence_eval_source_procsub_joined_i3` co-flips with 2.4's `test_divergence_eval_source_fatality_is_i3`. ALSO for 2.4: the -c 2-vs-127 residual rides the same SubstitutionSyntaxError path — 2.4's fix covers both spellings; the 2.2 golden co-flip registration stands. | **2.4 ✅ discharged v0.760.0** (both pins flipped to equality; procsub spelling covered by the same consumer) |
| 2.3 successor rows (novel-row attack finds) | (1) assoc list-assign class: `a[k]=(v)` / `a[k]+=(v)` — psh rc-2 parse error vs bash rc-1 runtime "cannot assign list to array member" (pre-existing, base-identical); (2) `#symbol` DOC-GUARD GAP: test_doc_pointers validates only the PATH half of `file.py#symbol` cites — the symbol half is unguarded (this slot's own dangling pointer escaped it; tooling successor). **2.4 SECOND OFFENDER (merged here, 1.3b carries a third copy): `executor/function.py#FunctionExecutor._function_frame` — a class that exists nowhere under psh/ — passed every guard and was caught only by a verifier reading it. Fix = the guard checks the identifier exists in the named file.** | successor queue |
| 2.3 notes (record-only) | (1) THREE-RENDER-TIER bash model (source re-render / arith raw / runtime never) + BIMODAL subshells (glued raw-preserved, separated declare-f-style) + backgrounded bodies RENDERED (`&` kept as separator) — the slot's transferable bash-semantics discoveries, full probe matrices in `2.3-rescue/`. (2) FINDINGS-INTEGRITY: the sq-inside-dq must-flip pin's basis ("psh rejects at parse time") was STALE AT ENUMERATION (0215279c) — both shells defer; caught by 2.3's discriminating probe; row closes as never-a-live-divergence. (3) TWO false ledger claims struck-and-corrected in-slot (MEDIUM-4 "rejected" prose; empty-key bash column) — both caught by verification, record-integrity treatment applied. (4) FOUR campaign ratchets disciplined this slot's own new code (Q2 field-access, README stats, import-layering, declared-field). (5) Base bonus bugs fixed: `a[2>x]` mis-key; empty-assoc-key storage; bracket-name recursion loop. | recorded |
| Nightly write-side procsub race (integrator, resume) | 07-27 + 07-30 nightlies red on `test_write_side_procsub_closed_fds_matches_bash` at 1b271d77 (green 07-28/29 — intermittent). Mechanism probed: NEITHER shell waits for `>(...)` at command end; the harness post-exit killpg sweep races the un-waited child; bash loses under runner load, psh was shielded only by interpreter-teardown latency (psh-side delivery pins latently flaky too). Fixed **v0.759.0** (test-only): shell-neutral completion-barrier `_WRITE_BODY` (flag file + bounded spin), forced-slow probes green on bash 5.2.26 + both psh parsers, 20/20 soak. Full narrative in `nightly-status.md`. | **CLOSED v0.759.0** |
| psh bare `wait` does not reach procsub children | Probe 2026-07-30 (by-catch of the nightly fix): `exec 9>f; echo data > >(sleep 1; cat >&9); wait` — bash 5.2.26 waits 1.02 s and delivers; psh returns in 0.12 s, empty (both parsers). Bash 5.x's bare `wait` covers the last process substitution; psh's ignores procsubs entirely (they are not jobs by design — `psh/expansion/command_sub.py` / `psh/io_redirect/process_sub.py` fork directly). Fix = wait/job-manager surgery, its own slot; interacts with the ProcessLauncher not-a-job design decision. | successor queue |
| 2.4 declared divergences (KEEP-ruled / pinned both-sides) | (1) O3 mid-script trap-action own-parse-error status: file/stdin bash 2 vs psh 1 (-c 127==127, stdout matches), UNIFORM across USR1/TERM/DEBUG/ERR/RETURN and joined by the FORK axis (subshell trap-action bash 2 vs psh 1; composition fork×trap×EFFECTIVE-errexit = 2==2); wording surgery out of scope. (2) O4 in-trap `$?`=257: bash leaks its pre-truncation internal; psh shows the real status; process statuses EQUAL at 1 (B#3 psh-cleaner family). (3) Function-member -c channel: bash leaks 127 into a child whose command resolves DIRECTLY to a function; psh child stays 1 — the slot MOVED this row twice (base no-abort → r4 2 → 1) toward the coherent child model, movement told straight in the pin chain; the three function-member rows are the TEST CASES for any future channel-rule unification. (4) Redirect-spelled procsub carries an ignored `set -e` in psh where bash runs those children errexit-EFFECTIVE (argument spelling carries in BOTH shells — spelling-split); base-identical; fix = procsub child seeding, procsub/fork family. (5) Interactive-channel family: per-shell `is_script_mode` gate ⇒ children of interactive shells keep legacy statuses (3 PTY rows + `-i -c` fork shape, base-identical, PTY-pinned in the default-run module). (6) `--validate` stays 2 while `-n` moved to 127=bash (pinned) — psh's two static checks disagree; CLI-surface question. | recorded; FLIP-PINS |
| 2.4 successor: PIPESTATUS collapses inside brace groups | `{ eval 'echo $(if)' \| cat; } \|\| echo GOT; echo ${PIPESTATUS[*]}` → bash `2 0`, psh `0` (three-member form: bash `0 2 0`, psh `0`) — `executor/pipeline.py`'s single-command path re-stamps `state.pipestatus` for the enclosing group. Base-identical; bookkeeping, outside HIGH-9. 2.4's own non-final-member pin deliberately observes PIPESTATUS in a TOP-LEVEL pipeline so this defect cannot contaminate it. | successor queue (pipeline/job family) |
| 2.4 successor: a pipeline MEMBER never runs its own EXIT trap | `{ true \| { trap 'echo BYE' EXIT; …; }; }` → bash prints BYE, psh does not — statuses match, trap output missing; NOT abort-specific (ordinary failure and CLEAN members lose it too; a subshell runs it and matches). Round-3 teardown declaration CHECKED: zero pipeline rows in its corpus ⇒ fresh row, not a citation. Base-identical. | successor queue (teardown/trap family) |
| 2.4 successor: anti-spawn ratchet's PTY membership pin cannot refuse growth | `test_no_direct_spawn_in_oracle_modules.py` sets `_EXPECTED_PTY_REGISTRY = frozenset(PTY_REGISTRY)` — DERIVED from the dict it freezes — while the docstring promises a frozen two-place pin; 2.4's own PTY entry passed without the two-place edit (offender demonstration). Slot 1.2's ratchet; fix = a literal membership set. | successor queue (tooling) |
| 2.4 successor: interactive UNTERMINATED substitution swallows the follow-up | At a PTY, `echo $(if)` puts psh into line continuation where bash reports the error and returns to PS1 with the next command runnable; psh's follow-up never runs until the construct closes. Base-identical, no crash; belongs to the multi-line CONTINUATION family (neighbours the r18 lexer successor). Supersedes/absorbs the round-1 S2 note (same finding, PTY-verified). | successor queue (interactive/parser) |
| 2.4 successor: external-command body diverges under a severed member | `g(){ /usr/bin/false; echo A; }; set -e; { true \| g; } \|\| …` → bash A / psh GOT rc=1 at base AND tip (builtin-`false` twin matches everywhere). Co-movement census domain scoped to its four SHELL bodies accordingly. RELATED fifth-body find (round-10 verifier): eval'd `return 1` in a severed member — the severing half moved to bash correctly; the residual 2-vs-1 is pre-existing (unsuppressed twin proves it). ALSO one-line: `set -o posix; set -e; { true \| eval 'set -q'; }` → bash GOT rc=1 / psh 2 both ends (eval'd neighbour of the special-builtin-floor family; direct `{ true \| set -q; }` matches). | successor queue (errexit/external + special-builtin-floor) |
| 2.4 note: `set -o posix` + relative `source` (S1) | POSIX `.` searches only $PATH: bash refuses `source sub.sh` (rc 1, file not found); psh sources it. Pre-existing. IN-SLOT VALUE MOVE (declared+pinned): because psh DOES source the file, the substitution-abort inside it now yields -c 127 (was 2), bash 1 — pinned with a valid-file control proving the dot-lookup root cause; the underlying divergence stays this row's. | successor queue |
| 2.4 note: embedding-API contract | `Shell.run_command()` deliberately lets `SubstitutionSyntaxAbort` (a BaseException) escape to embedding callers when `state.is_script_mode` — the typed outcome must reach the frame owner. DECLARED + pinned both-sides in-slot (incl. an ordinary-syntax-error control that fails if the abort ever widens past its origin fact); documented in the API docstring + core CLAUDE.md. | recorded |
| 2.4 notes (record-only) | (1) MAIN-SHELL suppressed-errexit family carry WITHDRAWN — closed in-slot by the R5-A stamp extension; the R4-E carry text's "base-identical" claim was measured FALSE and struck; history preserved in the flipped equality pin. (2) RECORD-INTEGRITY: rounds 4–7 each produced false-record items (unverified checklist ticks, claims citing instruments not run, corpus-scoped universals) — every one struck-and-corrected in the slot ledger; the cures instituted mid-slot are the DISCHARGE AUDIT (instrument-file anchors, header-SHA anti-staleness, count derived from the script) and the BOUNCED-ROWS REPLAY (99 rows at close — every behavioural blocker from all rounds, permanent). (3) THE AXIS PATTERN, five instances (rounds 5–9): suppressed×final / member's-own-frame / spelling / route / OPTION — "probed is not probed over the space the words claim"; round 9's exception was already written in the very file the claim was about. Bank: vary the axis the sentence QUANTIFIES OVER, incl. fixture axes and option axes. (4) FIVE campaign ratchets caught this slot's own code (child-exit taxonomy, Q1 shell-consumer ×2, function-import cap, anti-spawn PTY registry) — ALL resolved by tightening, zero allowlist entries. (5) SEVEN green-on-base pins are DECLARED CONTROLS (table in the slot ledger §R10-D: declared-divergence pins, a parser-agreement invariant, and the -n/--validate comparison pair — green-at-base is correct for each). (6) Process lessons banked for all future slots: dead-drop rulings file polled at turn start (5 consecutive message-channel misses), foreground-600s + pgrep-before-heavy (3-concurrent-pytest incident, transcript preserved), trial-in-throwaway-worktree for design decisions, audit anchors must be single-line (3 occurrences). | recorded |
| 2.5 declared deltas (all toward bash or base-identical; pinned per channel/parser in-slot) | (1) COMBINATOR diagnostic line numbers on lines a phantom heredoc had swallowed: 3 moving families by DERIVED membership (escaped `\<<`, escaped × `set -o posix`, subscript arithmetic-shift), 5 literal-line controls; rd byte-identical. (2) heredoc + unclosed-quote precedence (`cat <<EOF "abc` stays incomplete = bash; all channels). (3) substitution-bearing delimiter cooked whole (`<<$(x)` = bash) + EOF warning. (4) degenerate operand-less named-fd forms complete-with-error (= base = bash; `{v}<<-` bare was PS2-forever at base — improvement). (5) `a[<<]=1` errors-and-continues = bash (was PS2-phantom). (6) named-fd heredoc/here-string family `{v}<</<<-/<<<` EXECUTES = bash incl. fd>=10 semantics (improvement BEYOND base — base parse-errored; never called "restored"). (7) alias-introduced heredoc: family behavior UNIFORM (pre-existing for plain/digit — body lines execute; var_fd converged to family policy from base's parse-error artifact); messages user-comprehensible. (8) `--debug-ast` label/field-order for executable heredocs (HeredocRedirect). All in the declared-delta pin files with red-on-base decompositions proving each classification. | recorded |
| 2.5 successor rows (each with in-slot evidence) | (a) alias-heredoc BODY COLLECTION at expansion time (bash collects; psh cannot — divergence pin `test_divergence_alias_heredoc_body_is_not_collected`). (b) null-command named-fd fd retention (`exec {v}<<EOF` shape: bash performs-then-undoes leaving var unset; psh keeps descriptor — pre-existing semantics newly reachable; pin `test_divergence_null_command_named_fd_keeps_the_descriptor`). (c) strict-errors classification of `NonExecutableRedirectError` now the route is user-reachable (2 arms propagate / 2 caught under PSH_STRICT_ERRORS — declared, not churned at endgame). (d) plain/digit degenerate dangling-heredoc forms = pre-existing PS2 divergence (pin `test_divergence_plain_and_digit_degenerate_forms`, asserted in the DIVERGENT direction so closing it is a visible flip). (e) digit-fd operator table lacks `>|` (`2>|f` parse-errors vs bash accept — surfaced by the two-direction parity guard; in-guard exclusion carries the pointer). (f) history-expansion mirror scanner (still misdetects `\<<`; composite probe proved NO observable at tip — state resets with accumulator; discharging evidence in 2.5-rescue). (g) `exec {v}<<EOF` spurious fd-0 rebind (probe: unobservable on the data-losing path). (h) committed PTY module shares the old detector's contextual-prompt limitation (LATENT — no case opens a construct). (i) 2.2 combinator top-level `.line` carry re-carried, re-verified untouched (no-heredoc control mis-stamps at BOTH SHAs). | successor queue |
| 2.5 record-integrity note | The slot's second half was a RECORD war: three false discharges of the same two rows (silent `str.replace` no-ops printed "done"; certification rows greped the dev's own edits), cured in stages — instrument-generated discharge certification → SINCE-SHA both-ends rows → POST-STATE predicates (proven against history: RED at both dissolved tips the old instrument certified, GREEN at HEAD). HANDOVER at round 6 (dev-2-5 → dev-2-5b, 2.4 precedent): the stand-down note itself contained a wrong claim the successor refuted by tree-check — "a stand-down note is a claim like any other and inherits the reliability of its author's last several claims; the tree does not." INTEGRATOR faults this slot: 6 tallied (two premise relays without tree-verification, one instrument-universe citation, one dead-drop mis-ordering, one stale symbol in brief, one ruling collision R1-B/R14-B-N10 — adjudicated as supersession) — consistent lesson: never relay a claim into a ruling without tree-verifying it. Verification: NINE rounds, 29/29 blockers real, 0 false; round 7 delivered the campaign's first fully independent reproduction of both heavy runs. | recorded |
| 2.5 lessons (bank for all successors) | (1) ORACLE-NAMING: every coverage claim names its oracle — bash-differential / base-identity / internal-consistency / mutation; an internal-consistency instrument can never certify external correctness (731-pass vs 8+12-fail demonstration). (2) DELETED-DECIDER RULE: when a fix deletes a decider, the decider's input space IS the claim's universe; when it ADDS a decider arm, the sibling table is the universe; when it CREATES a shape, every guard universe grows with it (the `{v}<<` regression → `<<<` gap → degenerate corner → alias route chain = one lesson four times). (3) TREE-PROPERTY + POST-STATE evidence: a claim's evidence is a property of the tree, and its instrument asserts the POST-STATE the ruling ordered, never the edit that happened to be made. (4) CHANNEL DEFAULT INVERTED: a delta is all-channel until the identity instrument proves otherwise — "interactive-only is a conclusion, never a starting point." (5) OBSERVABILITY AXIS: vary what the probe's output can REVEAL, not just what the input looks like (a marker that prints the same thing wherever it runs observes nothing about where it ran). (6) AXIS CATALOGUE at slot close: spelling, channel, parser, option, fd-kind, operand-presence, observability, oracle, ALIAS. | recorded |
| 2.6 declared deltas (all toward bash, base-identical, or corrections of base defects; pinned in-slot) | (1) State-aware analysis: scripts enabling extglob/posix/aliases mid-file now VALIDATE as they EXECUTE (the chartered fix; 30-cell red-on-base class). (2) Mode combinations REJECTED at invocation (was silent fixed-priority winner; declared flip of `test_analysis_modes_ordered`); help/version SUPPRESS the conflict check (measured mechanism, pinned whole-order). (3) Analysis stderr gained execution's `file:N:` prefix + correct per-unit lines (base was wrong on 5 of 9 shapes). (4) F7: rd whole-file analysis CORRUPTED words after heredoc bodies (AST-level, `--validate`/`--format`/`--metrics` faces) — fixed by per-unit parsing; examples/text_stats.sh deltas are this class, all corrections. (5) `--format` posix render (`$äö` stays literal under `set -o posix`). (6) `--debug-exec` no longer leaks execution's trace or terminal-detection stderr into analysis (carrier constructs with debug options cleared). (7) alias-heredoc body shape: analysis accepts what execution rc-127s (permissive envelope, declared). All with three-point red-at-dissolved-tip decompositions where they arose mid-slot. | recorded |
| 2.6 successor rows (each with in-slot evidence) | (a) ONE shared flag-loop decider factored from `ShoptBuiltin.execute` — `_shopt_split` MIRRORS it today (6 behavior cells guard routing; M2 finding: the analysis corpus is the repo's ONLY guard on `--`-ends-flags, so the factored decider must CARRY that coverage). (b) `AliasManager` public analysis-overlay seam (R5-C; `command alias`/`builtin alias`/quoted-head/cmdsub-head spellings named — asymmetry pinned in the divergent direction; 5th class = quoted heads). (c) `_MODE_RUNNERS` set-equality guard vs `ANALYSIS_MODES` (the one unguarded sibling table). (d) silent no-op `shell.lint_only = True` post-construction assignment (attribute deleted, kwarg kept; no in-tree consumer). (e) alias-TABLE absorption is isolation-blind + reachability-blind (base-faithful, pinned divergent). (f) analysis perf 2.2–3.3x on large scripts (two instruments, bases recorded) — optimize only if wanted. (g) r18 lexer no-progress crash remains PRIORITY (untouched by this slot). | successor queue |
| 2.6 record-integrity note | The slot repeated 2.5's late-round arc AND its cure, then went further: 3 false discharges (round 4) → R13-C cert-row-before-claim; 2nd degradation round (round 5: blind rename de-collecting ANOTHER slot's pin; cited mechanism that did not exist as described — "intent narrated as implementation") → HANDOVER per the dev's OWN R13-D standing agreement ("that is your signal and I will not argue with it"). dev-2-6's stand-down note named five claims it would not stand behind + a RANKED distrust list; dev-2-6b tree-verified all of it (2 inherited claims killed: "ratchet down" true for 1 cap of 3; spelling count 10 not 11) and discharged the distrust list (19/8/2 re-derived WITHOUT the hand-modelled table; census → guard; perf with basis; execution-untouched got its structural reason). Certification evolved AGAIN: `collected` row kind (suite post-state — commit-content rows were blind to de-collection), `phrase_family_absent` kind, superseded_by chains, 12 mutation classes. INTEGRATOR faults: 2 tallied (R5 count; R21-C unmeasured precedence chain — falsified by the dev per the pin's-claim-must-match-reality instruction, R22-A). Verification: SEVEN rounds, 19+1 findings, 0 false. Harness hardened mid-slot: verifiers now measure ONLY in own detached worktrees (B71, dev-spotted). | recorded |
| 2.6 lessons (bank for all successors) | (1) THE NAME-VS-BODY CLASS, five instances in one slot (body-blind lex, position-blind walk, quote-blind normalize, blind rename, cluster-rule-one-spelling): reusing the NAME of a mechanism while re-deriving its BODY loses the guard that lived in the re-derived part — reuse binds the decider's WHOLE input space, guards included; the deciding spec is often ALREADY PINNED in the repo (3 times here). Cure = the sanctioned-sites guard pattern: enumerate, justify with tagged forms, occurrence-count, reject stale allowances. (2) CERT-ROW-BEFORE-CLAIM (R13-C): a discharge claim without a post-state row is incomplete by definition; where an item has code+pin halves BOTH get rows (the easier-half certification fault). (3) Agreement-form assertions beat fixed-status: "analysis agrees with execution" detects mirror drift by construction. (4) A pin's claim must match reality even when the integrator ordered otherwise — measure the chain, not just the cells (R21-C/R22-A). (5) The instrument-kind must match the claim's SUBSTRATE: suite facts need suite-reading rows (`collected`), idea-recurrence needs phrase families (with their fixed-list limit stated). (6) Handover works: substance survives context exhaustion when the record is durable and the successor treats ALL of it as claims (both stand-down notes are now model exits). | recorded |
| 3.1 declared deltas (all toward bash or A9-declared; pinned in-slot) | (1) The chartered fix: star∘extglob composition = measured bash mechanism across all five consumers (three H7 `[[` rows + removal/substitution/glob/case propagation, red-on-base → equality). (2) KNOWN_DIVERGENCES empty-subject set CLOSED (4 keys) via the measured substitution consumer layer; the two `?()`-on-empty characterization rows in the (renamed) empty-match pin file flipped to live-bash values — their own docstrings pinned old-psh-behavior-as-characterization. (3) Escaped-metachar wrap-guard: `${v/*\*/Z}`-family (7 cells incl. the paren pun `${v/%(a)/Z}`) fixed to bash; backslash axis added to the battery (2,016 cells, 0 divergent at tip). (4) A9-DECLARED perf envelope: quirk-flagged patterns pay up to ~O(nodes·n³) on full_match/matching_ends relations (85× at N=400 on `**(a)b`) — measured, tabled, handed to 3.2; Path-A fast path restores base-linear substitution for non-quirk patterns (equivalence-proven). (5) Regex-oracle agreement corpus narrowed by the derived flag predicate (3,624→3,357 compared cells, −7.4%, counts derived; excluded shapes covered by the stronger bash-corpus battery). (6) `psh -m recursion`: flagged-pattern evaluation recurses per star-run/group dispatch — pattern-structure-bounded, never subject-length; limit-relative RecursionError contract pinned (psh raises the process limit to 40k at activation — recorded fact). (7) User-guide corrections: `[[` extglob support example (old text claimed "not supported" — false vs the compat table and conformance tests) + guide-17 ¶71 scoped to globbing/case. | recorded |
| 3.1 successor rows (each with in-slot evidence) | (a) LEXER-SEAM quoted-chars-in-extglob-group family (`!("a")` etc.): lexer embeds quote chars in the group's raw text; engine (`compile_protected`) is CORRECT — `RESIDUAL_DIVERGENCES` rows lex_q1/lex_q3/lex_case_q1 pinned divergent; scope NARROWED to the `[[`/word seam (`${}` operand path closed by the engine fix, q7). Joins the r18/2.3 lexer neighborhood. (b) OPERAND-EXTENT family: unquoted `/` inside an extglob group in `${v/pat/repl}` — bash terminates the pattern at the first `/`, psh balances parens (opx_slash row, pre-existing, round-3 verifier find). (c) ~~3.2 handoff (in `3.1-rescue/slot-ledger.md` D-2/E-1)~~ **DISCHARGED v0.764.0 by slot 3.2** (all named baselines beaten or declared at achieved bounds; freeze covers both caches + the routing bits + `Extglob.enclosed`; unification RULED AGAINST in-slot — `_Matcher`/`_BashMatcher` stay separate evaluators, candidates note stays live for successors; one envelope CORRECTION: the A9 "~n³" quirk envelope understated `matching_ends`/`span_at` at measured n^3.95). (d) `matching_spans` + `_contains_negation` = labelled test-pinned permanent oracles (censused, no production callers; labels verified intact at the 3.2 tip). | successor queue |
| 3.1 record-integrity note | Three rounds: BOUNCE (3 blockers — corpus context-grammar blind spot: regression + unclosed chartered family + false A9 declaration) → BOUNCE (2 blockers — unmeasured backslash axis; FALSE integrator-authored mitigation sentence) → PASS. INTEGRATOR FAULTS: 1 tallied (R7's "tip still beats live bash on the idiom" generalized a shape-specific measurement — R21-C class, falsified by round 2; errata row carries the attribution) + 2 procedural notes (R12 wrong stall-point, corrected by dev provenance; R13 cited in channel before landing in the dead-drop, dev flagged per protocol). DEV FAULTS: 4, all self-caught with instruments hardened (R4-C poll slip; two git-checkout-over-uncommitted-work slips → cp/patch-only restore posture made BINDING, third slip = stop-and-talk; VOID equivalence proof — lru cache laundered the fast arm, caught because mutation M6 did NOT fire → cache-clear forcing + M6 permanent prover-proving class). Gate false-red round 1: same-second same-size mutation revert invisible to .pyc mtime+size validation → campaign rule: drop __pycache__ after every revert. Verification: 3 rounds, 5 blockers + 43 nits, 0 false blockers. Evidence: `3.1-rescue/` (ledger AS-IS + inbox R0–R15 + instruments + ROUNDS-AND-ERRATA). | recorded |
| 3.1 lessons (bank for all successors) | (1) A generated corpus's CONTEXT GRAMMAR is an axis: PRE/POST context lists must be ARGUED for coverage, never assumed — dev battery + full gate + integrator spot-check all shared the round-1 blind spot; only the independent-corpus verifier rule caught it. Round-2/3 verifiers therefore attack with fresh grammars BY DEFAULT. (2) Subject SHAPE is an axis: no measurement sentence without its shape scope ("the idiom" is not a measurement unit) — the slot's one tallied integrator fault. (3) BACKSLASH/escaping is an axis. (4) A proof that cannot fail is not a proof: provers get FORCING, and the forcing gets its own mutation class (M6 pattern — the mutation-that-does-not-fire is the detector). (5) Restores go through cp/patch instruments only; two checkout slips in one slot proved `git checkout` over uncommitted work is a pattern, not bad luck. (6) Mechanism-not-cell-fitting pays: the star-jump account came from reading `sm_loop.c`, and the model then survived 1.8M+ independent cells at first validation. | recorded |
| 3.2 declared deltas (perf-only; semantics bit-identical by proof) | (1) One-pass backward all-start relation + memoized ok-table (memo 33,154→258 keys at N=256); every headline bound in the HIGH-7 closure row. (2) The `spanner` all-start pre-filter is GATED to extglob-free, non-pathname patterns (the in-slot eager form was the round-1 blocker; M8 replays it permanently); the "no-match scan is linear" guarantee is SCOPED accordingly, extglob-bearing scans pinned at achieved bounds. (3) `Sequence` routing bits are no longer constructor parameters (`init=False`, precomputed; API-surface note — zero surviving callers, pinned by `init_fields == ['elements']`); `slots=True` removes `__weakref__` from engine nodes (no in-tree weakref consumer; deepcopy/pickle round-trip verified). (4) Declared pin change inside the 3.1 battery: `test_bash_matcher_states_stay_polynomial` bound TIGHTENED (n+2)²→4·(n+2) + n=256 (red-on-base verified; `count_states` logic AST-identical, docstring now states what it does NOT guard). (5) New deterministic instruments: `count_transitions` (DP work incl. `_element_ends`/`_alt_closure`), `operation_transitions` (end-to-end consumer work — the scan relation walks every position, the consumer jumps matches), `INSTRUMENTATION.matchers` (construction counting for sharing properties); tight count bounds declared safe ONLY because counts are deterministic integers. (6) Small constant-factor cost on match-at-position-0 substitution (spanner built before the pre-test short-circuits; both arms linear) — declared, successor row below. | recorded |
| 3.2 successor rows | (a) `[a-C]`-family: bracket ranges with a mixed-case endpoint under `nocasematch` diverge from bash across substitution/`[[`/`case` (220 cells, e.g. `v='a'; "${v/[a-C]*/Z}"` → bash `Z`, psh `a`) — PRE-EXISTING, byte-identical at base and tip, round-2-verifier find (3.2 was chartered not to touch bracket classes). (b) Quirk-route dispatch duplication: `reachable_ends`/`fullmatch`/`match_at` keep verbatim copies of the `_seq_bash_quirk` route the `_relation_*` helpers centralize — single-sourcing is a safe cleanup for a successor. (c) Match-at-position-0 constant factor (delta row 6): lazy spanner construction inside `_any_match` would recover it; not worth post-PASS churn in-slot. (d) `!`-group relations at the declared quadratic floor and `*`-group `matching_ends`/`span_at` at ~n^2.9: any future sub-quadratic attempt starts from the 3.2 transition-count tables. | successor queue |
| 3.2 record-integrity note | Two rounds: BOUNCE (5 blockers, 7 reports, 19 nits — eligible-control perf regression via the eager pre-filter; FALSE ledger tip cells from stale live-worktree measurements; vacuous extglob transition pins + counter blind spot; stale module narrative; missing Linux reasoning) → PASS (0/16). 0 false blockers. DEV FAULT 1, VERIFIER-CAUGHT (the campaign's first not-self-caught dev fault): stale-not-wrong perf cells — both exhibit files preserved in `3.2-rescue/instruments/`; NEW BINDING RULE: perf certification rows are measured at a DETACHED checkout of the declared tip (B71 extended to devs), per-TABLE provenance. Dev self-caught in-phase: 3 of 7 mutation classes initially vacuous (counter observed a re-derived path — same class as the accidentally-green pin the dev found in Phase A), fixed in the ENGINE. Integrator: 0 faults; R5(3) superseded by R6 on better evidence (the dev's stop-and-propose under the ruling's own clause; both sides' independent instruments agreed 0.0089s/0.0093s). Round-1 verifier `!(a)b` cell settled in round 2: partly harness-shape (per-position spanner construction), but the SCAN relation genuinely quadratic in wall AND count — verifiers' half better-founded, dev conceded on the record, both figure sets in the slot ledger. Evidence: `3.2-rescue/` (ledger AS-IS + inbox R0–R11 + 43 instruments + ROUNDS-AND-ERRATA). | recorded |
| 3.2 lessons (bank for all successors) | (1) A counter that observes a re-derived path certifies nothing: count at the one door every strategy passes through, make relations RETURN the matcher they used, and count constructions where the property is sharing. (2) Stale-not-wrong is its own fault class: per-TABLE provenance; every perf certification row at a detached checkout of the declared tip. (3) The pin-row subject-shape axis: a fixed `'a'*n` subject short-circuits before the extglob group — vacuity hides exactly where the defect lives (3.1 fault-#1 class recurring one level down). (4) Instruments that call symbols the change deletes turn retirements into fake semantic disagreements — prover arms read whichever form their tree provides. (5) A regression the suite cannot see needs its own mutation class: M8 re-introduces the round-1 blocker and the gate pin catches it at construction. (6) The instrument-substrate distinction extends to relations-vs-consumers: the scan RELATION walks every position, the CONSUMER jumps past matches — certifying the consumer requires an end-to-end instrument (`operation_transitions`). | recorded |
| 3.3 declared deltas (all toward bash; raw-pair-sweep certified away-from-bash 0) | (1) Field vector through value operands — every consumer that receives fields where base received a joined scalar: command/function argv, for/select lists, array initializers, `[`/test builtin argv, `printf -v` (joins per printf %s semantics), `[[ ]]` operands and case patterns via named restore-base projections. (2) Untriggered conditionals return the parameter's own quoted expansion (H6 family, 184-cell matrix). (3) Multi-field redirect target = `ambiguous redirect` rc=1 (was: silent single file named with the joined text). (4) Zero-fields vs one-empty-field distinction (`set --` unquoted → 0 fields; explicit `""` → 1 empty field). All pinned red-on-base; equality vs bash 5.2.26. | recorded |
| 3.3 successor rows | (a) bare-`$@`/IFS-in-operand family (3 both-sides rows: parameter CONTENT resists the IFS char the joining space splits on; mechanism un-modeled — do NOT guess). (b) case-pattern first-field model (2 both-sides rows: bash matches FIRST field of a multi-field pattern operand; psh joins = declared policy preserving base). (c) `${@:}` acceptance: psh accepts the empty slice INCLUDING `$0`, bash rejects `bad substitution` rc=1 (both-sides pinned `test_positional_slice_empty_operand_divergence`; psh-side shape changed 3.3, declared). (d) fatal-expansion subshell exit status: bash 1 / psh 127 (A10.1) — **CLOSED v0.767.0 (slot 3.5, absorbed at brief time)**: children exit 1 like bash via the `fatal_expansion_channel` stamp through `map_child_exception`; flat-1 child model (no errexit branch — probed, unlike the substitution-abort sibling); the ruled-in errexit `-c` family (bash exits 1 under `set -e` even in -c; FLAG-not-effectiveness, 5 suppression shapes pinned must-hold) landed with it. (e) bad-subscript arithmetic error wording (A10.3). (f) `[[ =~ ]]` RHS: quoted region inside an unquoted value operand loses regex-literalness (round-1 verifier find, base-identical). (g) `"${!a[@]:+X}"` on `a=("")`: bash `invalid variable name` rc=1 / psh accepts (round-2 verifier find, pre-existing). (h) `"${x:-"$"}"`: bash keeps the lone `$` literal, psh drops it (round-2 verifier find, pre-existing). | queued |
| 3.3 record-integrity note | Three rounds: BOUNCE (6 blockers/5 distinct, 13 nits — redirect-target behavior change unpinned + ledger asserting the OPPOSITE post-state, found independently by two verifiers; array-view family unpinned though the dev's own ledger NAMED the axis (mutation-proven: all 3,145 relevant tests green with the branch disabled); false `_psh_comb` coverage claim ORIGINATING IN THE BRIEF (integrator drafting fault #2, after the operators.py scope omission); two silent brief items) → BOUNCE (2 blockers, 15 nits — C13 count TYPED with no instrument on precisely the commit being declared; the R2.1 case-pattern correction missing from BOTH durable doc statements while present in ledger + inline comment = the reappraisal-#19 failure mode) → integrator-direct PASS (all nine numstat figures re-derived exact; both texts corrected; 7 probe cells red-on-base/bash-equal-at-tip; 357 pin rows green at detached tip). 7/7 real blockers, 0 false. Integrator instrument fault self-caught + dev-corroborated: `pgrep -fl \| head -4` truncated by a multi-line wrapper entry → false "no pytest running" during a live gate (macOS pgrep also lacks `-c`; use unpiped exit-status). | recorded |
| 3.3 lessons (bank for all successors) | (1) **The bounded-instrument class** (five instances + one over-correction in ONE slot: tag-vs-raw sweep; `-maxdepth` find reported unbounded; a count TYPED with no instrument; summed-vs-range cumulative; `..HEAD` moving-reference label; then annotating a CORRECT figure as faulty by ASSUMING the class): every number entering the durable record is produced by a command shown beside it; every range is named by an explicit SHA; a number without a visible instrument is an estimate — label it or derive it. A limitation disclosure is NOT a substitute for an exhaustive instrument (it makes a false claim MORE credible); fixing the instance instead of the class is the same mistake one level up; an audit that manufactures findings is no more trustworthy than one that misses them. (2) Mechanical base-vs-tip sweeps diff RAW OUTPUT PAIRS, never verdict tags — a DIFF→DIFF cell with changed content is structurally invisible to a tag sweep, and the raw form buys the stronger "away-from-bash 0" statement. (3) Doc sweeps propagate ruling corrections to EVERY durable statement (grep the corrected claim's old wording tree-wide), or the subsystem doc teaches what the code's own pin refutes. (4) Claim-made-true beats claim-retracted where cheap (false `_psh_comb` sentence → real 7-row combinator pin). (5) A loud opaque IR pays twice: the raising `__str__` FOUND a consumer the probe matrix missed, and the projection guard caught a re-flatten in the fix's own first draft. (6) An unpinned behavior change toward bash is still a BLOCKER — beneficial and correct are not substitutes for declared and pinned. (7) Evidence must not outlive its instrument: capture a derived record (diffstat) of any throwaway artifact a ruling rests on BEFORE deleting it. | recorded |


### Part D — slot 3.4 registrations (v0.766.0, ceremony 2026-08-06)

| item | fact | disposition |
|---|---|---|
| D-3.4-deltas | Declared behavior deltas, all toward bash, all red-on-base pinned: signature family (all store kinds x positions x target kinds); own-flip special-builtin persistence; refuse-before-evaluate consequence classes (zero-side-effect incl. `:=`/cmd-sub/xtrace; FATAL-EXPANSION: a script that stopped now continues); RO1 + control-flow observables (`set -e`/posix abort, function route) + nameref-spelling presence; carry-#7 interleave family; SEM-2 nameref-to-element no-write (bash-correct; ruling (b) AMENDED from measurement); live-read split-brain family; F-family enumeration-invisibility incl. the FUNCTION route | shipped, pinned |
| D-3.4-s1 | Diagnostic-wording class: readonly refusal names the TARGET where bash names the NAMEREF (pre-existing, both routes, pinned both-sides) | successor |
| D-3.4-s2 | Posix hook couples on nameref WRITE-THROUGH where bash does not; newly reaches dispatch via the value-side path (`declare -n npc=POSIXLY_CORRECT; A=$((npc=1)) eval` bash FN / psh BP; pinned both-sides + name-level bounding control) | successor (core hook, out of 3.4 charter) |
| D-3.4-s3 | rc 1-vs-127 + diagnostic-leg shape on posix special-builtin readonly abort (pre-existing, newly reachable, pinned both-sides) | successor |
| D-3.4-s4 | Carry-#7 residue: command's OWN read on the LAYER/SEED route (see Part B row 7) | successor |
| D-3.4-s5 | **Option (A)**: LAYER-as-`temporary_env` masking model — retires the dynamic-special SEED route; property table in `3.4-rescue/slot-ledger.md`; would close s4 and likely s8 | successor candidate (own A8-style matrix required) |
| D-3.4-s6 | Prefix-value arith store to a prefix-bound name: bash persists to the REAL variable, psh loses it with the temp binding (`A=1 B=$((A=9)) cmd; echo $A` -> bash 9 / psh UNSET, both ends; pinned both-sides) | successor |
| D-3.4-s7 | `${!PREFIX*}` name enumeration sees staged bindings during the staging window (fourth enumeration surface, pre-existing, pinned both-sides) | successor |
| D-3.4-s8 | Function-target nameref-to-element visibility (`declare -n r=a[0]; r=NEW f` body read: bash NEW / psh stale; pre-existing, pinned both-sides) | successor |
| D-3.4-x | Out-of-charter confounders, pinned both-sides: X1 posix function-name validation (bash rejects defining a function named for a special builtin, rc 2); R4 posix special-builtin redirection-error fatality (bash exits, psh continues) | successor |
| D-3.4-lessons | The 13-lesson set (rescue `ROUNDS-AND-ERRATA.md`): instruments-weakest-part; axis-you-add; FIXES-COMPOSE (matrix the composition cells of any two in-slot changes); action-vs-PROPERTY; relation-instrument ("an estimate wearing two citations"); compliance-claims-need-instruments; forcing-on-the-real-path; vacuous-probe labels; negative-after-the-cell; closure-must-not-outrun-evidence ("different readers"); provenance = WHEN written, not whether right; pre-approval slots read narrowly; self-referential instruments. PROCESS now property-bound: ledger FREEZE between tip declaration and verdict; sweep-paste = last edit; per-hunk staging; SHA paste-from-instrument + scripted sweep; RN-Cdoc standing slot; **from 3.5 the heavy-run GO requires the pre-registration block cited file+line in the request (binds the INTEGRATOR)** | banked, binding forward |

### Part D — slot 3.5 registrations (v0.767.0, ceremony 2026-08-06)

| item | fact | disposition |
|---|---|---|
| D-3.5-deltas | Declared behavior deltas, all toward bash or strict-errors-mandated, all pinned: A10.1 subshell/cmdsub child fatal-expansion status 127→1 (`-c` channel; 19 fork rows red-on-base); the errexit `-c` family — under `set -e` a fatal expansion exits 1 even in `-c`, keyed on the FLAG not effectiveness (19 override rows + 5 flag-vs-effective must-hold rows incl. suppression-still-1 and flag-off-127); default-mode injected-internal-defect consequence classes (arith: discard→continue-with-generic-report; PS4: silent-fallback→per-command abort; `[[`: rc 2→1) — injection-only, no user-reachable route (two independent 78-row corpora), declared + 3 border pins incl. the shell-reason counter-pin | shipped, pinned |
| D-3.5-s1 | `test_doc_pointers.py` has NO rule for the `file.py#symbol` form — the exact form the campaign's doc rule mandates; round-1 B1 escaped a green gate through this hole. Add rule R7 resolving `<file>#<symbol>` against the named file | successor (tooling) |
| D-3.5-s2 | let_builtin.py:52 `(ValueError, ArithmeticError)` leg around `evaluate_arithmetic` — 5C's half; the 3.5 deadness argument transfers (named in expansion/CLAUDE.md §error-typing as the residual) | successor (5C, pointed) |
| D-3.5-s3 | Builtin-`ArithmeticError`-vs-module-alias breadth: core.py:517 + control_flow.py's three legs catch the BUILTIN (covers ZeroDivision/Overflow/FloatingPoint), operators.py catches the alias (= ShellArithmeticError); unproven as masking, not measured in-slot (lesson-12 refusal) | successor |
| D-3.5-s4 | PS4 + bad-subscript: `TopLevelAbort` (BaseException) escapes the narrowed PS4 net — bash falls back + continues rc 0 / psh aborts rc 1 (pinned both-sides `test_ps4_bad_subscript_aborts_in_psh_but_not_bash`) | successor |
| D-3.5-s5 | `[[` invalid-regex diagnostic: psh prints `psh: [[: invalid regex:` rc 2 where bash is SILENT rc 2 (pinned both-sides `test_invalid_regex_diagnostic_is_psh_only`; wording class, s1-family) | successor |
| D-3.5-s6 | Redirect-target fatal expansion (`( cat < ${x?boom} )`): double-printed diagnostic + direct rc 1 vs bash 127 — pre-existing, byte-identical base/tip (harness fresh-probe find; outside the slot's word-position matrix spec) | successor |
| D-3.5-x | Out-of-charter probe finds, report-only: `(( 1<<-1 ))` bash rc 0 / psh rc 1 (3 contexts); `${a[]}` bash error rc 1 / psh `X1Y` rc 0; `a[]=9` bash rc 1 / psh rc 0 | successor |
| D-3.5-record | Round 1 harness BOUNCE: 8 reported / 7 distinct / 7 REAL / 0 false — ALL doc-content or record-integrity class (dangling `#_expand_ps4` pointer; FALSE UNIVERSAL falsified by the un-scanned builtins tree, two agents converged; Linux/parser-interactive/transclusion record gaps; §4e registry count false — BOTH SIDES' instruments failed identically, dev `\| head` display + integrator R2b truncated-grep verification; undeclared default-mode delta). Round 2 integrator-direct PASS (delta = 2 doc + 1 test file, zero production code). Behavioral core NEVER bounced: 78/78 novel rows tip==bash, resurrection ZERO, every fence held. Fault register: dev 5 / integrator 2, ZERO code faults either side | recorded |
| D-3.5-lessons | (1) Twice-confirmed: every fault lives in instruments/records, never code — and the dev's corollary is the design property: every fault was caught by an instrument that did NOT share the method of the claim it checked. (2) **The instrument-mirror lesson (joint, co-signed): a verification instrument that mirrors the claim's method cannot find the claim's error.** (3) Channel claims get subprocess probes on the real entry path — a hand-built in-process state object reproduces whichever route its construction configures, then confirms the wrong belief with real output. (4) A universal quantifier in PROSE needs an instrument covering its whole range (the false-universal blocker = AXIS-QUANTIFICATION as documentation). (5) Sibling models are measured, not analogized (flat-1 child status; FLAG-keyed errexit — both opposite to the substitution-abort sibling). (6) A pin pair needs its COUNTER-PIN or it rewards deleting the feature. (7) A revised pre-registration keeps both figure sets labelled; GO citations re-derive line numbers at request time. (8) A sweep whose only history is PASS teaches nothing — record when it bites | banked, binding forward |

### Part D — slot 4A.1 registrations (v0.768.0, ceremony 2026-08-07)

| item | fact | disposition |
|---|---|---|
| D-4A.1-deltas | Declared behavior deltas, net effect toward-bash or embedding-correctness, all pinned: managed signal dispositions restore exactly on `close()` (7 script / 10 interactive leaked at base); dropped-without-close shells no longer poison later shells (STD_FDS/LOCALE recover; next shell RUNS); failing `exec` leaves nothing acquired; low-RLIMIT permanent redirects now PARK ADAPTIVELY — bash parity held at every measured threshold where the mid-slot errno split had briefly broken it (harness BL-1, fixed in-slot). The R3-accepted setup-takes-ownership widening was RETRACTED in R9 (it caused BL-2): NO ownership change ships from mode setup; the inverted pin documents the retraction | shipped, pinned |
| D-4A.1-s1 | `SignalRegistry` retains every registration forever (`psh/utils/signal_utils.py` `_history`): any shell that ever installed handlers stays process-reachable regardless of leases — defeats GC for the SIGNALS families; sharpens B-03; measured (clearing `_history` lets the state collect) | successor |
| D-4A.1-s2 | B-03 documented limitation: drop-without-close holding a LIVE user trap stays REJECTED (`close()` is the contract; accurate attribution in the rejection) — permanent pin `test_dropped_shell_holding_a_trap_lease_still_rejects_the_next` | documented limitation, pinned |
| D-4A.1-s3 | Managed-family drop-without-close: next shell RUNS, handlers leak exactly as base did ("no worse than base" = the measured bar; recoverable only if s1 is ever fixed) | documented limitation, pinned |
| D-4A.1-s4 | Sub-13 RLIMIT envelope: bash succeeds at soft ≤ 12 where psh declines cleanly (diagnostic, rc≠0, nothing half-acquired); parity measured ≥ 13 | declared divergence, FLIP-PINS row |
| D-4A.1-s5 | Stale doc pointer `environment.py#_export_existing` (pre-existing, d9796e24; harness R-2) — FIXED at this ceremony; the structural guard remains D-3.5-s1's R7 rule | fixed at ceremony |
| D-4A.1-s6 | REWRITTEN at sign-off (D-4A.1-a1) — the ceremony version recorded the grant "proceeding over quarantined state"; MEASURED at 9e2c3c0c (dev at sign-off, integrator-reproduced): a new owner's activation over a dead owner's failing restore is REFUSED — attempt 1 `LeaseRestoreError` (quarantine set), attempt 2 the quarantine `LeaseError` — the grant never proceeds. The genuinely open policy question: is refuse-FOREVER absent an explicit `clear_quarantine` the right embedder contract? | successor (reframed) |
| D-4A.1-record | Round 1 harness BOUNCE: 6 reported / 5 distinct / **5 REAL / 0 false** — the slot's first CODE blockers, BOTH consequences of the slot's own fixes (BL-1 errno-split bash-parity regression under low ulimit; BL-2 poisoning REINTRODUCED on the slot's own new kind at dropped-no-close × MANAGED_SIGNALS — the slot's own new axis), 3 record-class. Round 2 integrator-direct PASS with ONE condition: VF-1, the bash-oracle cells failing at a FRESH checkout (`{TREE}/tmp` assumed; both shells fail equally so the PARITY assert passes and the success assert trips) — found only by the fresh-checkout replay, discharged by a declared test-only commit + integrator re-verify (14/14, no tmp/). Final register: 3 dev process faults (incl. the shell-`&` gate-2 invalidation, artifact story verified 3-leg), 2 code blockers, 1 test-portability, 12 self-caught corrections, 0 false findings both rounds. Gate 3 legs green EXACT at declared tip; compare-bash 3,042/26 EXACT throughout | recorded |
| D-4A.1-lessons | (1) **The three review layers caught DISJOINT classes** (dev-stated, integrator-endorsed): the dev's 64 cells + 16 arms + 77 pins found every failure INSIDE the design frame; the harness found the two fixes correct-in-their-own-terms that regressed OUTSIDE the frame; the fresh-checkout replay found what both sides' instruments were structurally blind to (every instrument ran in a tree where the scratch dir already existed by hand). Each layer's blind spot was the next layer's finding. (2) A mutation lock that passes under the very mutation it was written to catch is a LABEL, not a lock — kill-set-collision detection is what makes it visible. (3) A parity cell that reports parity on a broken environment then fails for an unrelated reason is worse than erroring — scratch dependencies are created by the test, never inherited. (4) Axis-you-contribute part two: BL-2 sat at a cell of the axis this slot registered. (5) Errno families are claims about CAUSES: EMFILE at a too-high parking base = EINVAL's configuration fact, not exhaustion. (6) Harness backgrounding ≠ shell-`&` (measured: `run_in_background` keeps `default_int_handler`; `nohup &` inherits SIG_IGN and corrupts the bash ORACLE side — 31 spurious fails wearing psh's name). (7) State a green's limits BEFORE it arrives (the run-8 pre-registration declared itself unable to prove VF-1). (8) Retracting an accepted delta is the process working — the record shows both the acceptance and the retraction. PROCESS: verifier agents never glob-delete outside their own mktemp scratch (harness diffAudit incident, main-tmp `v*` loss) | banked, binding forward |
| D-4A.1-a1 | SIGN-OFF CORRECTIONS (2026-08-07, dev-found at sign-off verification, integrator-verified before landing): (1) adaptive-parking formula was misquoted in LEDGER + ROUNDS-AND-ERRATA as the pre-spare `soft−3` proposal; shipped is `soft−6` (constants verified at 9e2c3c0c). (2) D-4A.1-s6's premise refuted by measurement — the grant REFUSES over quarantine (row rewritten). (3) "77 pins ALL red-on-base except must-holds" overstated: measured split at the shipped files = **57 red / 20 green** (activation 28/8, failed-exec 7/7, managed-signal 22/5); green-at-base has THREE distinct reasons — must-holds by design, documented-limitation pins, and REGRESSION pins against mid-slot defects (the three bash-parity cells: base "passed" parity BY ACCIDENT via the corrupt-baseline silent success; the corruption catcher itself is red-on-base). BANKED: red-on-base counts are re-derived at the declared tip, never carried forward (two independent stale-ratio instances this slot); "all red except X" is a shape claim — state it as a measured split | recorded |

### Part D — slot 4A.2 registrations (v0.769.0, ceremony 2026-08-07)

| item | fact | disposition |
|---|---|---|
| D-4A.2-deltas | Two declared toward-bash deltas, both pinned: history-under-trap-exit saves on saving routes (PTY-proven vs bash; `main-exit` still never saves — must-hold); bare-`exit`-in-EXIT-trap resolves to trap-entry status (`trap 'cleanup; exit' EXIT; exit 3` → 3 = bash; 39-row conformance table; EXIT-only — non-EXIT traps measurably use current `$?` in BOTH shells, pinned) | shipped, pinned |
| D-4A.2-s1 | Exit-trap flake MECHANISM (dev Phase A investigation A-8): 1.3b redirect-restore residual window — the trap's output lands inside the harness's own `: > "$ready"` sentinel (a per-command redirect still in effect at trap time); 2/750 density; negatives published (busy-poll 0/100, randomized delay 0/300); bash 0/120. Fenced from 4A.2; the recurrence watch now carries a cause hypothesis | successor (1.3b-adjacent) — Checkpoint R re-eval DONE 2026-08-08 (CR-R5): premise unchanged by 4B.4 |
| D-4A.2-s2 | `_run_shutdown_phases` holds ANY BaseException (KeyboardInterrupt branch pinned); whether a KeyboardInterrupt during shutdown SHOULD complete all phases is recorded deliberate-and-open | successor |
| D-4A.2-record | Round 1 harness BOUNCE: **1 blocker / 1 REAL / 0 false** — frozen A-1 certified "no divergent cell" through two VACUOUS bare-exit rows while the discriminating composition diverged (the lesson-8 shape inside the battery built to satisfy lesson 8); behavioral core of the phase split took ZERO findings across both rounds (that phrasing applies ONLY to the core — the slot score is one bounce, one real blocker, the dev's). Round 2 integrator-direct PASS. 5 dev self-corrections in place; freeze-md5-in-declaration rule adopted mid-slot; gates 23,502 → 23,546 EXACT both rounds; compare-bash 3,042/26 EXACT throughout | recorded |
| D-4A.2-lessons | (1) A battery cannot license "no divergence exists" over a rule it is structurally blind to — figure-true/inference-false; the misleading cells recommitted as LABELLED CONTROLS. (2) Pre-register claim BOUNDARIES before the verdict AND where the record could flatter you (the dev's three ceremony wording constraints). (3) A doubly-defended invariant's mutation is spelled both-halves WITH the reason. (4) Kill-counts are artifacts before numbers (parametrized-id collapse). (5) A split asserted is not a split shown (the tail-truncated transcript). (6) The flake-investigation pattern: recorded-density reproduction, deciding observable, published negatives, fence what you cannot touch | banked, binding forward |

### Part D — slot 4B.1 registrations (v0.770.0, ceremony 2026-08-07)

| item | fact | disposition |
|---|---|---|
| D-4B.1-deltas | NONE shell-observable (internal-integrity slot; compare-bash EXACT both ends). Declared representation details, each pinned: lookup equality = status+value (all PRESENT_UNSET results now mutually equal — licensed by the measured fact that NO whole-object lookup comparison exists tree-wide, dev-derived + integrator-verified); `repr` drops the binding term; hashability preserved-as-unhashable (a decision, not an accident) | shipped, pinned |
| D-4B.1-s1 | A future consumer wanting attributes-without-a-second-lookup (the omitted binding's original use case — `${x@a}`/`declare -p` style) files as a NEW immutable-view design task with aliasing semantics specified up front; never a silent field resurrection (R1(b) condition 3). Note the P7 fact for that design: at base, `.binding` was never uniformly live anyway (computed specials handed out per-read throwaways) | successor |
| D-4B.1-s2 | The (c-1) open circumvention class has NO closing representation: raising `__setattr__` (measured 1.081x) misses `__init__` re-invocation; frozen dataclass (1.13x) admits it too (dataclass-generated `__init__` uses `object.__setattr__`). The boundary is DEMONSTRATED (labelled control cell exercising assignment/`__init__`/`delattr` on fresh instances), never guarded; narrowing later deliberately flips the control | recorded boundary / successor |
| D-4B.1-record | Round 1 harness BOUNCE: **1 blocker + 5 required nits, ALL real, 0 false.** BL-1 = doc-sweep miss (a self-contradicting docstring in a NAME-VS-BODY-named sibling; root cause: the propagation grep searched one syntactic form, not the field NAME). Two dev pre-registration deviations SELF-REPORTED despite a cancelling clean total (opposite-sign per-class errors summing to the registered 33/25); M8-5 vacuous equality cell self-caught by the dev's own locks BEFORE the gate; F2 ruff-red intermediate commit (piped exit status) self-caught, fixed as a VISIBLE commit (tip-clean invariant, per-commit cleanliness never claimed). RN-2 five-vs-six cell dispute settled by integrator MEASUREMENT siding with the DEV (batched-vs-isolated collateral). Round 2 integrator-direct PASS at the integrator's own detached checkout. Gate 23,546 → 23,604 (+58 exact, cross-derived via the serial phase's deselected count); the integrator's first M8 replay omitted the companion plugin and the instrument's own CONTROL refused to certify — recorded as the control earning its keep | recorded |
| D-4B.1-lessons | (1) A doc sweep for a REMOVED field searches the field NAME, not one syntactic form of it. (2) Red-on-base is well-defined ONLY per-cell — two live demonstrations in one slot (base collateral poisons later cells in a shared interpreter: 12 red isolated vs 17 batched). (3) UNPIPED rule extended to EVERY exit-status-bearing check (ruff/mypy/pytest subsets): run unpiped or branch on the command's own exit status; never pipe a gating check through a filter on the same command line (adopted R4-b mid-slot, binding 4B.2+). (4) Threat-model boundaries are OPEN CLASSES, demonstrated not enumerated — a closed three-item list went stale against two live routes within a single slot. (5) A perf ruling's PREMISE is measured before its figure is honored — the "hottest path" premise was false while the "roughly triples" figure was true, and only measuring BOTH exposes the difference. (6) Opposite-sign registration errors can cancel into a clean headline total — per-class splits are the integrity check, and a matching total is not confirmation | banked, binding forward |
| D-4B.1-a1 | Post-merge addendum (sign-off record): dev three-leg sign-off PASS at gated tip AND at origin/main; the committed dev manifest was a self-false draft caught by the dev's sign-off pre-run (fixed pre-attestation, 29c13396 — manifests exclude themselves; handoff by declaration); D-4B.1-s2's frozen-dataclass claim now INSTRUMENT-BACKED (`4b.1-rescue/instruments/dev/out_signoff_frozen_dc_init.txt`) and SCOPED to the two priced alternatives the row names (D8); NEW STANDING RELEASE LEG for 4B.2+: artifact verification at the tag (MEDIUM-5's four legs: 4/4 at base → 0/4 in v0.770.0) | recorded |

### Part D — slot 4B.2 registrations (v0.771.0, ceremony 2026-08-07)

| item | fact | disposition |
|---|---|---|
| D-4B.2-deltas | Two declared behavior deltas, both toward-bash/toward-correctness, both red-on-base pinned: (1) a multibyte char split across the cursor/bulk drain seam now keeps its identity (was two surrogates; byte round-trip never broke); (2) `read -N` honors `-t` on BOTH the pipe and tty arms (was: ignored — blocked to EOF, unbounded without EOF; base tty arm HUNG, proven after the F-4 probe correction). Golden promotion: 2 rider cases, compare-bash 3,042 → 3,046/26 (count increase, zero flips, deselection-control-proven) | shipped, pinned |
| D-4B.2-s1 | Timeout-partial ASSIGNMENT disposition (NEW-1): bash assigns stranded partial-multibyte bytes at `-t` timeout; psh holds them on the cursor for the next read (rc identical everywhere — value-only). DEFERRED to 4B.4 (contract width, I1 territory). Pinned as I1-style PSH-CONTRACT cells (assert psh's value AND assert bash differs → flips loudly when 4B.4 rules): `test_next_read_record_resumes_the_split_character` + `test_next_read_limited_resumes_the_split_character` (seam file) + `TestRiderRcParityWithDeclaredNew1Residue` (rider file). The divergence is DOCUMENTED NOWHERE in the user guide — that absence travels with this row; 4B.4 moves doc and code together (adjacent char-model prose: `17_differences_from_bash.md:596-598`) | **DISCHARGED at 4B.4 (v0.773.0)**: RULED psh's PERMANENT contract (4B.4-R2 row 4) — safe because 4B.4 closed the dup/temp-frame surfaces; pins RE-AFFIRMED (not flipped) w/ the ruling in their failure messages; user-guide doc landed at 17_differences_from_bash.md, NARROWED to the measured 18-cell table (incl. tty arm: bash ALSO holds at a terminal with plain read -t); conservation 18/18 |
| D-4B.2-s2 | `-N` COUNT MODEL divergence (NEW-2, upgraded per R4): bash counts POST-escape characters, psh counts raw — and it REACHES THE EXIT STATUS (psh 0 / bash 142: a different count stops in a different place). Independent of the rider fix (green-on-base; neither caused nor cured). Pinned in measured shape demanding reclassification if fixed: `test_backslash_under_deadline_diverges_in_rc_and_value` | successor |
| D-4B.2-s3 | `read -s -N` at a TTY echoes the secret (bash suppresses): `_read_exact` passes `echo=True` unconditionally where `_read_special` passes `echo=not silent`. Pre-existing, base-identical, in the exact function the rider edits; the `-s -t -N` composition is newly reachable. Report-only, UNPINNED by design (verifier PTY probe on record in `4b.2-rescue/`) | successor |
| D-4B.2-record | Round 1 harness BOUNCE: **3 distinct blockers + 8 required nits, ALL real, 0 false** — BL-1 (M8 driver unrunnable on fresh checkouts, MASKED by the canonical gate creating tmp/ first; found by three verifiers independently + integrator reproduction; certified post-fix in the mandated environment), BL-2 (s1 cited a doc line that documents the char MODEL, not the divergence — 4 sites reworded, the 4th found by the dev), BL-3 (TTY leg's binding probe-or-declare commitment discharged by neither — resolved into 3 PTY pins after F-4 turned the premise from coverage-gap into genuine base defect). Round 2 integrator-direct PASS at a fresh detached checkout. Dev self-report register: DEV-1/2/3 + F-1..F-5 + 5 instrument defects, all pre-verdict; DEV-3 JOINTLY owned (integrator confirmation-without-reconciliation); the untranscluded carries and the C.3 hang over-generalization are INTEGRATOR brief errata. F-5 (two FABRICATED md5s in a handoff declaration, self-caught and corrected before any copy) is named in the record as the only fault that attacked the trust model rather than the measurement | recorded |
| D-4B.2-lessons | (1) An A/B probe must not let either side under test generate the stimulus. (2) Stimulus scripts get a validity control before their verdicts count. (3) PYTHONDONTWRITEBYTECODE=1 REQUIRED for mutation-lock drivers; M8 drivers diagnose EVERY precondition loudly incl. their own scratch parent, and their certification runs in the fresh-checkout environment. (4) A test-local tag string can trip a static ratchet that cannot tell it from the real thing: rename, never allowlist, state the constraint in-file. (5) The search path is a request; the resolved `__file__` is the fact — every A/B probe resolves and asserts the module it measures (F-4: cwd outranks PYTHONPATH under `python -m`). (6) Any hash/count/SHA in a handoff or certification is GENERATED BY THE COMMAND that records it, and the receiver recomputes on receipt (F-5). (7) Reconcile every sourced number against every figure it bears on (DEV-3, jointly earned). (8) A LEDGER carry sweep for the slot's own name is a standing dispatch-checklist item (the untranscluded-carries erratum). (9) The brief's timing-hygiene floor applies to MEASURED deadlines; setup timeouts are exempt only when determinism is BY CONSTRUCTION and stated | banked, binding forward |
| D-4B.2-a1 | Post-merge addendum (sign-off record): dev six-leg sign-off PASS at TAG v0.771.0 — discriminator-first (F-4), 22/0 defect legs per-cell, 209 must-hold, carry-#21 diff EMPTY at the tag, M8 7/7 with `tmp/` ABSENT at creation (BL-1's environment), and a FALSIFICATION leg proving the defect legs can still fail (hunks reverted → exit 1). Zero flakes, stated explicitly. The six-leg discriminator-first/falsification-last shape = STANDING sign-off template for behavior-changing slots. Integrator record fault noted: R10's attestation-SHA line garbled, restated in R11 (FINAL `d5a4f30a`, gated `ef5b5e7d`); the dev's independent tag-SHA verification (receiver-recomputes on the integrator's own declaration) is the control that covered it | recorded |
| D-4B.3-deltas | Declared behavior deltas, all toward-bash or toward-no-data-loss, all pinned: read cursor survives `-d`/`-c` (legs A/C — `-n` no longer duplicates/re-materializes); `-s` under the FULL recording policy + cap; `-r`/`-n` cap-trimmed; `-w NAMED` keeps the session's pending commands; read paths no longer swallow pending typed entries; cluster dispatch at the rider's ratified true scope incl. the `-anrw` diagnostic (base: rc 2 invalid-option for every cluster). Deviation family b1(i)/b2/b3/b4-two-faces/b5 = deliberate KEEP-psh, both-sides pinned, FLIP-PINS-registered | shipped, pinned |
| D-4B.3-s1 | Retroactive HISTSIZE trim: bash trims the in-memory list when HISTSIZE is LOWERED; psh trims only at the next producer event. A variable-assignment hook in `core/state.py`, OUTSIDE the history state machine — deliberately UNPINNED so the successor is not entrenched against (probe citation in `4b.3-rescue/`, ledger §1) | successor |
| D-4B.3-s2 | `_sync_owed` outside-editor contract, documented limitation: outside edits are limited to the CV3 strip's TAIL delete (repaired structurally); an outside MIDDLE-delete would misalign the positional owed flags — no live route exists, outside APPEND is treated not-owed (safe direction), and `strict=True` makes representation drift loud. Docstring states the contract; discharge trigger = any new outside editor of `state.history` | documented limitation |
| D-4B.3-note | Wave-0 evidence probes `wave0-base-probes/claim_a_psh.py` (:25,:31 reads) and `claim_a_s.py` (:24 ASSIGNS) reference the retired `_file_synced_len`. Both are frozen archival records version-pinned by `assert __version__ == '0.750.0'` in their headers (integrator-verified), so they abort before the attribute and stay BYTE-IDENTICAL per rule 11. Hazard noted (dev RN-2/6 analysis): the `claim_a_s.py` line is an ASSIGNMENT — if the version pin were ever loosened, Python would mint a dead attribute and the probe would read GREEN against a model it no longer describes | recorded |
| D-4B.3-record | Phase A round: integrator BOUNCED two D2 rows with probe transcripts (R2-F1 the `-a` marker-identity model — every dev cell was consistent with BOTH hypotheses; R2-F2 the read-path pending swallow) → dev re-derived an EXECUTABLE positional-tail model that survived the integrator's adversarial MIX cell, and the exit-criterion conflict it exposed (bash loses AND leaks) drove ruling O3. Verify round 1 (workflow harness): **BOUNCE, 4 blockers + 15 RNs, ALL real, 0 false** — headline BL-1 was a slot-INTRODUCED regression (text-resolved pending; base correct; naming pin hollow). Fix round: all fixed with pins; the dev OUT-MODELED the bounce (operand-sensitive `-c` suppression, found by refusing to pattern-match the harness's simpler rule). Integrator-direct re-verify: PASS + 1 finding (b4's mirror face), closed at D13 with the offset-not-suppression control. Gates EXACT ×2 per derived pre-registrations; compare-bash +0 FIRM both rounds with the reason stated. Fault register F-1..F-9 GAP-FREE (F-5 integrator-found from file order + `ps`; F-9's data honestly reused as pre-run evidence) + integrator errata E1/E2 (dev-found, dispatch-time) + the integrator's own R10 snapshot gap (closed in-slot). ZERO false findings in either direction across the whole slot | recorded |
| D-4B.3-lessons | (1) **ACK-the-highest-R**: every dead-drop entry opens by ACKing the highest counterpart entry found by re-reading the file IN THE SAME TURN — a skipped read becomes structurally visible (born of F-4/F-5: two consecutive drafts written from a mental model of a file just written). (2) **Freeze-chain** (dev-proposed): every ledger freeze declaration quotes the PREVIOUS freeze md5 — the chain is self-describing from the inbox alone. (3) **The integrator snapshots every frozen ledger at freeze time** — first live firing mechanically verified D13's "only these sections changed" claim. (4) **`--collect-only` count FIRST** for any pytest argument that is not a file or node ID (F-3/F-9: "test paths I recognise" is not "a small run"). (5) **Instruments are files from the start** — six inline-heredoc probes left transcripts with no committed instrument; regenerated transcripts read at the FIXED tip and prove nothing about base (caveat declared and archived). (6) **The instrument-mirror family** (F-1 trim arithmetic, R2-F1 two-hypothesis blindness, F-8 named-empty `-cw` confound — three occurrences in ONE slot): every probe composition must make each candidate mechanism's execution VISIBLE; a cell consistent with two models is evidence for neither. (7) **A deviation face that lives in one probe is one probe away from silence** (b5 at R4, then b4's mirror at R12) — declaration debt is bounce-grade even when behavior is correct. (8) **NAME-VS-BODY applies to one's own suite** (BL-1's hollow pin named the exact property whose violation shipped). (9) **Text can never identify an entry** — identical command strings are ordinary; debts/flags over a history list must be POSITIONAL. (10) A view-carries-the-invariant design makes eager maintenance hygiene rather than correctness — and an M8 arm that "fails" to kill there is telling you the invariant is structural (two arms re-pointed at mechanisms rather than weakened to pass) | banked, binding forward |
| D-4B.3-a1 | Post-merge addendum (sign-off record): dev six-leg sign-off PASS at TAG v0.772.0 (13a5271a; tag SHA recomputed by the dev; artifact-verification leg 0 = committed instruments hash to the declared manifest 65/65). Legs 1–5 pass (A1′ 5-match/6-deviation stable across rounds; must-hold 148; no-silent-change 121 w/ deviation classes 10/10; M8 14/14 fresh-checkout; ruff+mypy clean at the tag). **Leg 6 limitation, dev-led**: three BL-3 conformance cells SURVIVE full revert (base rejected all clusters — "suppressed" and "refused" indistinguishable there), so they are MUTATION-PROVEN (the `file-op-not-suppressed-after-clear-or-delete` arm), not revert-proven — the proof-shape must be NAMED, not assumed (family with 4B.1's met-BY-ELIMINATION). Zero flakes stated. Integrator ceremony faults recorded in the addendum: fresh-worktree no-tmp/ gate launch; the R10 snapshot gap (closed in-slot, first firing verified D13). Final inbox 0f4fccae, snapshot-boundary held | recorded |
| D-4B.4-close | **The InputCursor contract is CLOSED v0.773.0 (sequence §9 Package 4B item 3 — ruled 4B.4-R2 "row 4" after W0-R2's supersession)**: temp-frame isolation BOTH directions (incl. the reverse face this slot found) + dup sharing across ALL FOUR spellings (`exec n<&m`, per-command `n<&m`, `{v}<&n` named, compound-command frames) via OpenDescription aliasing with a reference-aware lifecycle rule ("a description outlives any ONE of the fds naming it" — the round-2 rule that ALSO fixed pre-existing dup-then-dup N9); apply-time fd scoping for named-fd redirects; `_pushback` REMOVED (P1, by-elimination + zero tripwire hits) with a reintroduction ratchet; the three I1 malformed-route faces at C-locale-bash parity. 9 red-on-base divergences → 0; legs A/B byte-exact vs bash; 4B.2 seam suites at zero cost (the ROW-4 argument, held in measurement). s1 = D-4B.2-s1 DISCHARGED as psh's PERMANENT contract, documented at the measured 18-cell scope | shipped, pinned |
| D-4B.4-s1 | NOT closed, declared + both-sides pinned: I1 deliberate-loss row (d) — builtin→external stranded lookahead (a forked child cannot see a byte held in the parent's cursor); registry excerpt committed in `4b.4-rescue/` so the citation resolves in-tree | successor (documented loss) |
| D-4B.4-s2 | NOT closed, declared + pinned: the move-form (`n<&m-`) fd-lifetime divergence (bash's move closes the source; psh's does not) — PRE-EXISTING at base, behind the redirect-restore fence; surfaced by the round-2 N4 axis split | successor (fence territory) |
| D-4B.4-s3 | Dead public API: `IOManager.with_redirections` has NO callers (every in-process compound uses `guarded_redirections`); hooks wired defensively, invariant covered at registry-unit level | successor-queue candidate (5C) |
| D-4B.4-record | TWO verify rounds. Round 1 (workflow): **BOUNCE, 7 blockers + 17 RNs, ALL REAL 0 FALSE** — headline: TWO regressions the branch itself introduced (pop_frame's alias-blind cursor destruction; named-fd mis-scoping reorder — the dev's own pre-named weakness measured real), the nonexistent s1 table (BL-4 — which also named the INTEGRATOR's acceptance: fault I-2, acceptances-are-claims), un-re-affirmed s1 pins, dropped chained-dup cell, undeclared compound-dup census gap. Fix round: ONE lifecycle rule closed BL-1+BL-6+pre-existing-N9; the REAL 18-cell s1 table (tty finding: bash ALSO holds at a terminal on plain `read -t`) with the user-guide claim NARROWED to it; two M8 arms WITHDRAWN rather than defended (nothing could trip them — "an arm nothing can trip is not a lock"). Integrator-direct re-verify PASS: 6/6 bounce rows, M8 19/19 fresh-checkout, all faces == bash. Gate-1 MISMATCH handled by STOP (2 static-ratchet trips; fixed by complying, never allowlisting; the forced rewrite produced a STRONGER leg-B pin — the concatenation property). Fault register: dev F-1..F-4 self-disclosed + the ONE-HABIT self-diagnosis (a figure reasoned, an axis assumed, an edit unasserted, a rule applied where risk was expected rather than where it is written); integrator I-2 + E1-4b4 (the un-cited W0-R2 → three-register sweep rule). W0-R2 SUPERSEDED on premise falsification, struck-and-corrected in Part C. ZERO false findings all rounds, both directions | recorded |
| D-4B.4-lessons | (1) **When you add an invariant, audit every site that RELEASES the resource it governs, not just the sites that acquire it** (the round-1 regression: closing the reported gap re-opened the slot's own defect class through the destructor). (2) The TWO-AXIS instrument rule: REGRESSION (base≠tip) and DIVERGENCE (tip≠bash) are different questions with different owners — a one-axis comparison hides a regression wherever the oracle independently differs. (3) "A mutation that cannot fail is not a mutation" extends to EDIT SCRIPTS (assert the match count) and to M8 arms (withdraw untrippable arms; re-point at shell observables). (4) Another agent's inability to verify is NOT evidence against your own first-hand measurement — make citations RESOLVABLE, never delete a measured claim. (5) The collect-only count is a property of the ARGUMENT LIST, not the expected run size; "reading the pass line tells you what ran; it cannot tell you what didn't." (6) Acceptances are claims: ruling acceptances enumerate artifacts BY LOCATION, each verified to exist (I-2). (7) Carry sweeps enumerate THREE registers: Part B carries, Part C rulings, Part D successors (E1-4b4). (8) Every hook gets a tripwire — an emulation whose hook never fires produces silence that reads as a negative result (ID-3). (9) A superseded ruling is struck-and-corrected with its premise-falsification evidence cited, never silently overridden. (10) The emulation/landed distinction: injection results are design evidence; the claim set rests on the landed code, re-derived (audit rows supersede, never inherit) | banked, binding forward |
| D-4B.4-a1 | Post-merge addendum (sign-off record): dev SEVEN-leg sign-off PASS at TAG v0.773.0 (protocol pre-registered in D10 BEFORE the tag existed — now the standing shape alongside 4B.2's six-leg template: the artifact-verification leg is leg 7, and pre-registration-before-the-tag is part of the shape). Legs 1-6 as recorded in `4b.4-rescue/ROUNDS-AND-ERRATA.md`; leg 7 reconciled the attestation against the dev's own gate AND caught its own manifest naming five gitignored-tmp/ paths unresolvable from the tag (the RN-4 shape one level over, self-caught by the leg built for it) — ruled COMMIT-THE-FIVE, landed in this addendum hash-verified, so every manifest line resolves in-tree. Zero flakes; declared move-form residue confirmed as pre-registered. WAVE 4 CLOSED: 4A.1/4A.2/4B.1/4B.2/4B.3/4B.4, v0.768.0-v0.773.0 | recorded |

### Part D — Checkpoint R registrations (2026-08-08, evidence-only, no release)

| item | fact | disposition |
|---|---|---|
| CR-record | Checkpoint R executed at `ae871a16` (v0.773.0): round 1 = 6 independent scoped verifiers (Q1 discriminators / Q2 resurrection / Q3 representations / Q4 new-debt / Q5 wave-5 census / QR queue), each in its own detached worktree, discriminator-asserted, oracle bash 5.2.26 — ZERO BLOCKERs, ZERO REQUIRED-NITs, 23 NOTEs; round 2 = 3 adversarial attack scopes (cross-composition ~84 cells over 11 families; verify-the-verifiers reproducing 14 headline cells byte/count-exact incl. both census bases at `0215279c`, 3 varied-shape offender re-plants all biting, 6-guard NAME-VS-BODY; coverage-gap closure: M7 battery 161/161, 4B.2 face 72 cells, 4B.4 suites 57, all four PTY legs green, HIGH-2 battery 78/78, M13 companion 1→0 HOLDS, HIGH-1 census reconciled 95 modules/243 sites, question-coverage audit found + closed the one unexamined face) — ZERO BLOCKERs, ZERO REQUIRED-NITs, 7 NOTEs. Attack rounds to zero per the pre-registered convergence rule. Zero false findings both directions. Report + charter + digests + 275 instruments + workflow scripts: `checkpoint-r/` | recorded |
| CR-D1 | bg-job reap-timing divergence (q4-F4/atk-c-F1): a finished background job's process remains Z/defunct past subsequent command boundaries until shell exit; bash (async SIGCHLD reap) shows none by the next boundary. PRE-EXISTING (byte-identical v0.749.0/v0.750.0/tip); observable only via ps; no fd leak; bounded by jobs-table size. Mechanism = the absent general async reaper (Part B carry #12, routed with #16). Repro: `checkpoint-r/instruments/q4/q4_14_procleak_followup.py` + `atk-c/p08_bgjob_zombie.sh`. Unpinned; pin owed by the successor owner | attached to carry #12 |
| CR-D2 | psh runs the EXIT trap after a failed `exec PROG` where bash 5.2.26 exits without running it (rc 127 parity; trap-fire divergence; controls prove exec-specificity). PRE-EXISTING at `0215279c`. Unpinned characterization: `checkpoint-r/instruments/atk-a/` (a08b) | successor queue (shutdown/exec family, 4A.2-adjacent) |
| CR-D3 | fork-taxonomy RANDOM inconsistency: subshell and cmdsub forks reseed (bash-mechanism parity) but PIPELINE-MEMBER forks continue the parent's seeded sequence deterministically where bash reseeds. PRE-EXISTING at `0215279c`. Unpinned characterization: `checkpoint-r/instruments/atk-a/` (a10b, mechanism-visible redesign after the bash-5.1+-reseeds instrument-mirror flaw was self-caught) | successor queue (D-3.4 dynamic-specials family) |
| CR-D4 | `history -w` to fd-path targets (`/dev/fd/N`, `/dev/stdout`, `HISTFILE=/dev/fd/N`) succeeds rc 0 and writes in psh where bash 5.2.26 fails rc 1 silently writing nothing; named-file and `/dev/stdin` read routes are parity. PRE-EXISTING; distinct from the declared b1–b5 family. Unpinned characterization: `checkpoint-r/instruments/atk-a/` (a01) | successor queue (history file-op family) |
| CR-D5 | Instrument-portability class (q3-F1 + atk-b-F1): `4b.4-rescue/instruments/instr12_three_way.py` hardcodes two retired worktree paths (REGRESSION axis not re-runnable from the record; successors write fresh divergence-axis equivalents citing committed transcripts for base); `checkpoint-r/instruments/q3/p05` requires a pre-existing `<worktree>/tmp` (fails LOUD in a fresh worktree; byte-identical after mkdir). Portability is part of the record for successor re-verifications | recorded |
| CR-D6 | Dead-by-sweep retirement class (q2-F2 + atk-c-F3): retired authorities with no reintroduction ratchet (`arg_types`/`quote_types`, `_file_synced_len`, and the slot-internal deleted deciders — `visit_word_substitution_bodies`, utils `parse_with_heredocs` facade, `_substitute_scan`, `_seq_nullable`, `OperandResult` internals) verified ZERO resurrections at `ae871a16` by two independent sweeps. Structural retirements; a resurrection requires rewriting the mechanism — record-only, no guard added | recorded |
| CR-watch | v0.773.0 (`ae871a16`) merged AFTER the 2026-08-08 scheduled nightly (which ran at `e3924ed3`); its first scheduled Linux exposure is pending ~2026-08-09 04:00 UTC. Six consecutive green scheduled nightlies 08-03..08-08; Wave-4 suites v0.768–772 passed their first Linux exposure; exit-trap recurrence tally 2 | integrator watch |
