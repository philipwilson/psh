# Slot 3.1 instruments (rescued selection)

Rescued from the dev worktree's `tmp/slot31/` at ceremony. Scripts and
small result summaries are kept; regenerable bulk is omitted.

## Kept

- `corpus1.py` / `corpus2.py` / `corpus3.py` / `corpus4.py` — the
  deterministic corpus generators (v1 pair, widened grammar-v2/corpus3,
  backslash-axis corpus4). Each embeds its own derivation counts and,
  run with `PSH_WORKTREE=<wt> PYTHONPATH=<wt>`, regenerates its
  one-spawn bash script + TSV and prints the derived census.
- `corpus5_equiv.py` — the Path-A equivalence prover (cache-clear
  forcing; the M6 mutation class proves the prover).
- `bash_model.py` — the C-faithful port of bash-5.2 `sm_loop.c` used as
  the measured model; replays any corpus TSV
  (`python3 bash_model.py <results.tsv>`).
- `probe_anchor.py` / `probe_anchor2.py` / `probe_consumers.py` —
  anchor and per-consumer probe drivers.
- `replay_mutations.sh` — six-class mutation replay (M1–M6), cp-backup
  based, __pycache__ hygiene after every revert.
- `restore_battery.py` / `restore_phase_c.py` — idempotence-checked
  restoration instruments from the two checkout-slip recoveries.
- `phase_d_parameter_expansion.patch` — byte-exact Phase-D content
  patch: applies to `git show b49b8e9c:psh/expansion/parameter_expansion.py`
  and produces the tip file exactly (verified by round 3).
- `anchor_results_base.txt` / `anchor_results_base2.txt`,
  `consumer_grid_summary.txt` / `consumer_grid_tip.txt`,
  `consumer_grid.tsv`, `glob_grid.tsv` — small measured-result records
  cited by ledger rows.

## Omitted (regenerable)

- `bash-5.2/` + `bash-5.2.tar.gz` (36M + 10M) — fetch bash 5.2 source;
  the ledger cites `sm_loop.c`, `subst.c`, `glob.c` line ranges.
- `corpus[1-4]_results.tsv` (up to 8.5M) and `corpus[1-4]_bash.sh` —
  regenerated by the corresponding generator against
  `/opt/homebrew/bin/bash` 5.2.26 (oracle version matters: a different
  bash may legitimately change quirk cells).
- `globdir/`, `globdir2/`, `neutral/` — fixture dirs the probes create.
- `xdist_debug_test.py`, `try_*.c`, `*_5.2.c` copies — scratch and
  source excerpts.
