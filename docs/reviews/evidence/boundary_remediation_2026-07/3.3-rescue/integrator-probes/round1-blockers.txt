overall: BOUNCE | blockers: 6 | nits: 13
diffAudit -> FAIL (5 issues)
resurrection -> FAIL (6 issues)
ledgerCheck -> FAIL (5 issues)
reprobe -> FAIL (3 issues)

===== BLOCKER 1 [diffAudit] =====
UNPINNED in-charter behavior change at the REDIRECT-TARGET consumer, recorded in the ledger as the OPPOSITE post-state. Field preservation now reaches redirect targets: a multi-field value operand is `ambiguous redirect` at the tip (bash-identical) where the base silently created a file. No pin anywhere covers it (grep of tests/ for any redirect + value-operand row returns nothing; the flipped pin, the new tests/conformance/bash/test_operand_field_ir_conformance.py, and the 10 new golden_cases.yaml rows contain no redirect row), and ledger row A10.2 (/Users/pwilson/src/psh-r3-3/tmp/remediation-ledgers/3.3.md:318) still records the cell as an out-of-charter finding owned by 'successor (redirect arity)' with the text 'psh silently creates both files' - a post-state claim that is false at the tip. Under the slot typology ('an unpinned behavior change = BLOCKER') and CERT-ROW-BEFORE-CLAIM/discharge-audit discipline this must bounce. Remedy is cheap: one equality pin covering both quote states plus the ${a[@]} operand twin, and rewrite A10.2 as CLOSED-in-slot with its bash probe.

===== BLOCKER 2 [resurrection] =====
UNPINNED BEHAVIOR CHANGE (mutation-proven): the array-view operand-content family. `${x:-"${a[@]}"}` and friends changed from base's one space-joined field to bash's field vector — a real, chartered, correct behavior change that NO pin in the branch detects. Slot typology: "an unpinned behavior change = BLOCKER", and the brief's M8 rule requires the fixed blocker to be un-reintroducible silently. The dev's M8 lock #1 covers `"$@"` operand content only; the nested-operand section pins TRIGGERED nested operands (`${x:-${a[@]:-"$@"}}`), which is a different branch (`_expand_one_dollar` -> OperandValue) from a plain view (`_operand_dollar_fields` -> `expand_to_fields`). Remedy: equality rows for the `[@]`-view operand-content axis (brief subtlety-1 names `"${a[@]}"` / `"${a[*]}"` explicitly as content axes), with the `[*]` twin as the control row — I measured `[*]` as genuinely unchanged, so it is a real control, not filler.

===== BLOCKER 3 [ledgerCheck] =====
False pin-coverage claim + chartered parser axis absent from pins. Dev ledger A2.5 states 'Parser stays an axis in the pins (the existing pin runs `_psh_comb`)' — this is false on both ends: the base test_divergence_operand_at_flattens body runs only `_psh(cmd), _bash(cmd)` (verified at d0f7d929, tests/conformance/bash/test_subscript_keying_conformance.py:1680ff), and at tip 8251ed51 NEITHER the flipped 21-row pin nor the new 64-row test_operand_field_ir_conformance.py contains any `_psh_comb`/combinator leg (grep: 0 matches in the battery; flipped pin body is `_psh`/`_bash` only). The brief's pin section required 'keep parser as an axis'; the brief's own parenthetical was wrong, but the dev repeated it unverified and shipped neither combinator pin coverage nor a declared waiver — a combinator-only regression in the operand-field family would be unpinned. Mitigation (verified by me, so the FIX is cheap): the defect and fix are parser-independent — dev matrix K 16 DIFF -> 0 under both parsers, and my own replays at a detached tip worktree show `python -m psh --parser combinator -c` green on the signature cell (`<a 1><b>`) and the zero-positional cell (`n=0`). Bounce fix: add a `_psh_comb` leg (or combinator param) to the flipped pin and correct the ledger sentence.

===== BLOCKER 4 [ledgerCheck] =====
Brief item silently dropped from the ledger: 'REASON ABOUT LINUX' (required-work 4) / subtlety 7 (portable-alphabet corpora, byte-ASCII IFS probes, no platform surface expected). The dev ledger contains zero mention of Linux, platform, portability, or ASCII-corpus reasoning — no verdict row, no negative statement. The corpora as shipped do appear portable-ASCII (space/tab/glob/colon subjects), so semantic risk is low and the fix is a ledger addendum, but per the campaign's ledger-as-durable-record rule a silent brief item is a bounce.

===== BLOCKER 5 [ledgerCheck] =====
Second silent brief item: the transclusion-rule negative. Brief ('Transcluded LEDGER carries' section) requires the dev to RE-VERIFY at d0f7d929 that no other Part B/D carry row names 3.3 and to honour the transclusion rule 'by stating the negative' in the ledger. The ledger never states it. I re-verified the underlying fact myself and it HOLDS: only LEDGER.md rows 26 (HIGH-6 Part A) and 59 (carry #4, Part B row 4) name slot 3.3; the only other '3.3' hit (line 180) is the perf multiplier '2.2–3.3x' in a 2.6 successor row, not the slot. So this is a ledger-record gap, not a missed obligation — fix is a one-line ledger addendum.

===== BLOCKER 6 [reprobe] =====
Undeclared, unpinned behavior change at the redirect-target consumer, which the ledger affirmatively misstates as unchanged. At base d0f7d929, `unset x; set -- f1 f2; echo hi > ${x:-"$@"}` silently creates ONE file named 'f1 f2' (rc=0); at tip 8251ed51 the same command (and the quoted-outer twin `> "${x:-"$@"}"`) errors 'ambiguous redirect' rc=1, matching bash 5.2.26 exactly. The change is beneficial and mechanically downstream of the chartered field IR (the pre-existing exactly-one-field rule in psh/io_redirect/file_redirect.py, untouched by this branch, now sees real fields), but: (1) no pin anywhere in the branch covers a multi-field operand as redirect target (grep of the full test diff finds only a comment); (2) ledger B2/B13 claim the redirect cell is among the 22 'pre-existing and successor-owned' remaining divergences and B13 declares only TWO exclusions — i.e., the ledger asserts this cell did NOT change when it did; (3) ledger A10.2's base-state description ('psh silently creates both files') is factually wrong — base creates one space-named file, verified with a per-file loop. Per the campaign typology ('an unpinned behavior change = BLOCKER') and brief Required-work 6 ('every [behavior delta] DECLARED in the ledger with its bash probe + pin'), this must bounce. Cheap fix-forward: declare the delta, correct A10.2/B2/B13, add pinned equality rows for both redirect shapes (plus the single-positional agreement control, which I verified agrees at all three shells).

