# Slot 2.4 verification round 8 — full issue list (overall: BOUNCE, single blocker)

## Task verdict: PASS-WITH-NITS

### NIT
Round-8 commit 112435bc is never named in the slot ledger (grep count 0) and round 8 has no per-commit accounting table — its +3 tests are attributed only in aggregate at tip 55edb24f. All other 18 branch commits are named with per-commit attribution.

EVIDENCE:
grep -c 112435bc /Users/pwilson/src/psh-r2-4/tmp/remediation-ledgers/2.4.md -> 0; git log --oneline origin/main..fix/remediation-2-4 shows 19 commits, all others cited in the ledger.

### NIT
HIGH-9 / carry #22 closure is QUALIFIED (R4-C): closes for producer-tagged classes only; the scanner-classified family (paren/quote-balance-defeating bodies, e.g. case patterns) is CARRIED with written carry-row text. The committed campaign LEDGER Part B row 77 says 'CLOSE via slot 2.4' unqualified — the integrator's ceremony LEDGER edit must carry the qualification and the R4-C/R4-E/R8 successor rows (b)-(f).

EVIDENCE:
Slot ledger R4-C 'CLOSURE CLAIMS QUALIFIED' section + carry row text; LEDGER.md row 77 via git show origin/main.

### NIT
FLIP-PINS.md row for test_divergence_eval_source_procsub_joined_i3 still names the wrong file (test_subscript_keying_conformance.py; actual location test_syntax_template_timing_conformance.py). Dev caught it in Phase A; brief amendment 2026-07-30 confirms the correction is integrator-owed at ceremony and it is still uncorrected on origin/main.

EVIDENCE:
git show origin/main:docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md line 16; test actually at tests/conformance/bash/test_syntax_template_timing_conformance.py:607 on the branch.

### NIT
One explicitly open item rides to ceremony: a one-clause scope qualifier for psh/core/internal_errors.py#substitution_child_abort_status's severing-rule docstring (route-general phrasing vs the redirect-procsub counterexample) was STOPPED-AND-REPORTED under the round-8 no-production-changes ruling, not edited. Integrator decision owed.

EVIDENCE:
Slot ledger ROUND 8 'STOP-AND-REPORT — one production docstring, NOT edited' section.

### NIT
Old pre-flip pin names survive only in integrator-owned docs/reviews/ campaign records (FLIP-PINS.md, LEDGER.md, close report, 2.2/2.3 slot ledgers) — zero dangling references in psh/, tests/, or tools/ on the branch. Expected; ceremony updates those records.

EVIDENCE:
grep -rn for the four old pin names across psh/ tests/ tools/ docs/ at tip 55edb24f: hits only under docs/reviews/.

## Task verdict: PASS-WITH-NITS

### NIT
Four test symbols renamed in this branch are still referenced by their OLD names in the LIVE campaign tracking docs (FLIP-PINS.md rows 13/15/16 and LEDGER.md rows 29/161). Not dev-owned: git log on docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md shows those rows are edited in the INTEGRATOR's ceremony commit (7bcf163e 'remediation 2.3 ceremony: ... flip-pins ...'), and the 2.4 brief states 'the integrator's LEDGER edit at ceremony has your confirmation'. Flagged so the ceremony edit records the new names rather than leaving the flip-pin inventory naming functions that no longer exist. Rename map to apply at ceremony: test_divergence_c_mode_exit_code_is_127_in_bash -> test_c_mode_exit_code_is_127_like_bash (tests/conformance/bash/test_nested_substitution_timing_conformance.py:374, 6 params preserved); test_divergence_eval_source_fatality_is_i3 -> test_eval_source_frame_fatality_matches_bash (tests/conformance/bash/test_syntax_template_timing_conformance.py:216); test_divergence_eval_source_procsub_joined_i3 -> test_eval_source_procsub_joined_family_matches_bash (same file, :607); test_taxonomy_tuple_is_the_five_families -> test_taxonomy_tuple_is_the_six_families (tests/unit/executor/test_child_policy.py).

EVIDENCE:
git grep -n over the branch tree (psh/ tests/ docs/ tools/) for all four old names returns hits ONLY under docs/reviews/: docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md:13,15,16; .../LEDGER.md:29,161; plus dated historical records (boundary_campaign_close_2026-07.md:179,249; boundary_remediation_integrator_plan_2026-07-21.md:77; 2.2-rescue/slot-ledger.md:363,496; 2.2-rescue/INTEGRATOR-INBOX.md:50; 2.3-rescue/slot-ledger.md:654,767). ZERO hits in psh/, tests/, or tools/. Ownership precedent: `git log --oneline origin/main -- docs/reviews/evidence/boundary_remediation_2026-07/FLIP-PINS.md` -> 7bcf163e / 2714b0e2 / bf39a7ef, all integrator ceremony or wave-0 commits.

### NIT
FLIP-PINS.md row 16 still names the WRONG FILE for the v0.758.0 co-flip: it says `test_subscript_keying_conformance.py::test_divergence_eval_source_procsub_joined_i3`, but the test lived (and its successor lives) in test_syntax_template_timing_conformance.py. The 2.4 brief already records this as an integrator-fault correction 'owed at ceremony' (dated block 2026-07-30); I confirm the wrong path is still present at branch tip, so the ceremony edit must fix the path AND the name together.

EVIDENCE:
FLIP-PINS.md:16 at origin/main: '| **CO-FLIP (added v0.758.0):** `test_subscript_keying_conformance.py::test_divergence_eval_source_procsub_joined_i3` ...'. Actual location at base 1b271d77 and at tip 55edb24f: tests/conformance/bash/test_syntax_template_timing_conformance.py (successor `def test_eval_source_procsub_joined_family_matches_bash(tmp_path):` at :607). `grep -rn test_divergence_eval_source_procsub_joined_i3 tests/` at base returns only the syntax_template file.

### NIT
`psh/executor/child_policy.py#map_child_exception` gained a REQUIRED positional parameter (`state: ShellState`) — a breaking signature change to a module-level function that psh/executor/CLAUDE.md documents as the shared taxonomy entry point. Every in-tree caller was migrated, so this is informational, not a defect: recorded here so a future out-of-tree/tooling consumer is not surprised, and so the integrator can confirm the CLAUDE.md wording (which now says 'That is why the taxonomy takes a ShellState') is the intended public contract.

EVIDENCE:
Diff: `-def map_child_exception(exc: BaseException) -> int:` / `+def map_child_exception(exc: BaseException, state: 'ShellState') -> int:`. Migrated call sites verified by `git grep -n map_child_exception fix/remediation-2-4 -- psh/ tests/ docs/ tools/`: psh/executor/child_policy.py:329,420,426 and psh/executor/process_launcher.py:367 all pass a state; tests/unit/executor/test_child_policy.py passes `_StubState()` at all 13 call sites. No 1-argument call survives anywhere in psh/, tests/, or tools/. Runtime proof in a detached worktree at 55edb24f (psh.__file__ asserted under the worktree, version 0.758.0): `python -m psh -c 'echo ok'` rc 0, `--parser combinator` rc 0, `ruff check psh tests tools` clean, 291 affected unit tests pass, and 8 novel fork-taxonomy rows (x=$(exit 7); ( exit 5 ); ( break ) in a for; ( f ) with return 4; { exit 3; } & wait $!; echo a | ( exit 9 ); ( continue ) in a while; EXIT trap after ( exit 2 )) match PATH bash 5.2.26 on both parsers, one case per invocation.

## Task verdict: FAIL

### BLOCKER
Slot-introduced MOVED-AWAY behavior regression, unpinned and undeclared, outside every sanctioned flip: at tip 55edb24f the pipeline-member severing leaks EFFECTIVE errexit into an ARGUMENT-spelled procsub child of the severed member, where bash keeps the suppressed context. Row: `set -e; { true | cat <(false; echo A); } || echo GOT rc=$?; echo END` -> bash 5.2.26 prints `A\nEND` (rc 0), base 1b271d77 prints `A\nEND` (matched bash), tip prints `END` only. This is exactly the class the R8-B hunt claims zero of (MOVED-AWAY), sitting outside its 186-row space (which has no procsub bodies/arguments), and it falsifies the brand-new R8-A pin docstring's own rule sentence ('The ARGUMENT spelling carries in both [shells]' / 'psh carries in both') at the SIMPLE-member route — a FOURTH instance of the three-rounds-running root cause the ledger itself records: r8a.py varied the SPELLING axis but held the ROUTE axis constant (non-member contexts only), so the spelling-split claim was never measured at the severed-member route. Same-mechanism sibling: the member x REDIRECT-spelling row `set -e; { true | head -1 < <(false; echo A); } || …` moved TOWARD bash undeclared (base `A\nEND`, tip `END` == bash) — an unpinned co-movement in the same intersection. The dev's suite is blind to both: its nearest row is the pinned ABORT twin `{ true | eval 'cat <(if)'; }` (test_syntax_template_timing_conformance.py:945), which agrees with bash; its ordinary-errexit twin does not. Required (integrator's call on shape): declare+pin or fix the member x argument-procsub carry, correct the R8-A pin docstring universal to state the member-route exception, and pin the member x redirect-spelling toward-bash movement beside it.

EVIDENCE:
Replayed at tip 55edb24f / base 1b271d77 (worktrees /tmp/remv-probe-24r8 and /tmp/remv-base-24r8, discriminators verified), oracle PATH bash = /opt/homebrew/bin/bash GNU bash 5.2.26(1)-release, byte-exact script files. REGRESSION row 10/10 deterministic across -c and file channels (plus stdin), BOTH parsers (rd + combinator): bash=(0,'A\nEND\n') base=(0,'A\nEND\n') tip-rd=tip-cb=(0,'END\n'). Family scoping diagnostics (all deterministic): cmdsub-in-member `{ true | echo "x=$(false; echo A)"; }` -> x=A everywhere (no regression); bg x procsub `{ cat <(false; echo A) & }` -> A,child=0 everywhere; BRACE-member control `{ true | { cat <(false; echo A); }; }` -> A everywhere (compound member carries, matches); top-level active-errexit control `set -e; cat <(false; echo A)` -> END everywhere (bash also kills the child when errexit is genuinely effective — proving the divergence is specifically WHICH context the member's procsub child inherits). Mechanism: psh/executor/pipeline.py#make_execute_fn lines 248-251 (SimpleCommand member sets errexit_suppress_deferred then errexit_suppress=0; the member child's expansion-time procsub fork inherits the severed context and dies at `false`). Transcripts: scratchpad v24r8/outspace-55edb24f.txt (procsub-arg-in-member MOVED-AWAY(REGRESSION)) and v24r8/regress-confirm-55edb24f.txt (10 repeats + d1/d3/d4/d5/d6 twins; d5 member x redirect spelling: bash END / base A,END / tip END).

### NIT
Fresh PRE-EXISTING divergence found one step outside the 186-row space (a body shape the enumeration's 4 bodies omit): a here-string inside a member's eval'd text under the severed member. `set -e; { true | eval 'cat <<< H; false; echo A'; } || echo GOT rc=$?; echo END` -> bash prints H, GOT rc=1, END; psh (base AND tip, identical) prints H, END — the member reports success where bash's severed member fails at `false`. Base-identical, not slot-introduced, no action required in-slot; successor-row material for the co-movement family, and one more data point that the enumeration's body axis is narrower than the rule sentences' domain.

EVIDENCE:
3/3 stable repeats, file channel, script byte-exact: bash=(H|GOT rc=1|END), tip 55edb24f=(H|END), base 1b271d77=(H|END). Scratchpad v24r8/outspace-55edb24f.txt row 'herestring-body' + 3-repeat stability check.

### NIT
The widened co-movement pin docstring (test_ordinary_errexit_co_movements_are_declared) says the 186-row hunt's 'committed output r8b-hunt-TIP.txt is the record' — but no tmp/ file is tracked on the branch (git ls-tree of fix/remediation-2-4 tmp/ is empty); the file exists only in the dev worktree's uncommitted tmp/r24-probes/, rescued at ceremony. A clean checkout cannot follow the pointer and the word 'committed' is factually wrong as git state. Same convention family the integrator already ruled on at round 7 (internal_errors.py tmp-path cites); one-word fix ('recorded'/'rescued-at-ceremony') or promote the file when evidence is rescued.

EVIDENCE:
Docstring at tests/conformance/bash/test_syntax_template_timing_conformance.py (round-8 diff hunk): 'its committed output ``r8b-hunt-TIP.txt`` is the record'. `git -C /Users/pwilson/src/psh ls-tree -r fix/remediation-2-4 --name-only -- tmp/` returns nothing; the file lives at /Users/pwilson/src/psh-r2-4/tmp/r24-probes/r8b-hunt-TIP.txt (uncommitted). Ledger STILL-OPEN row R8-B repeats the word ('186-row hunt committed').

### NIT
Observability gap adjacent to the stop-and-report sentence, for the successor record: psh does not set $! after a process substitution where bash 5.2 does, so the procsub child's exit status cannot be observed via `wait $!` (bash st=1, psh st=0 because empty $! degrades to bare wait). Base-identical (pre-existing, not slot-introduced). This means the channel-rule claim in internal_errors.py#substitution_child_abort_status ('exits 1 even inside a -c shell') is verified for the procsub route only through indirect observables (the dev's p10/p11 byte-match rows, which I reproduced — they do agree in both spellings); the direct-status leg is unobservable in psh. Suggest folding into the integrator's already-recorded successor finding ('psh bare wait does not cover procsub children where bash 5.2's does') — same job-accounting family.

EVIDENCE:
Fresh row, file channel: `read -r line < <(eval 'echo $(if)'); wait $!; echo st=$?` -> bash st=1, tip st=0, base st=0. Companion probe `echo bang=[$!]` after the same procsub: bash bang=[<pid>], psh (base and tip) bang=[]. Scratchpad v24r8/chanrule.sh + bang.sh runs. Dev's p10/p11 rows independently replayed: abort twin agrees byte-exactly in BOTH spellings at bash/base/tip (v24r8/r8a-replay-55edb24f.txt rows abort-twin-redirect GOT rc=1 everywhere, abort-twin-argument END everywhere).

### NIT
Width note (matches the R8-B 'width, not concealment' precedent — no compliance failure): the 186-row space does not close under route composition, and compositions also move. My out-of-space rows bg-inside-member, member-inside-bg, nested-member-in-member, arith-command-body, and for-loop-bg all classify MOVED-TO-BASH at tip (base diverged from bash, tip matches), consistent with the declared families' direction but unpinned and outside the enumerated space; loop-spelling backgrounds (for/while/if ... done &) remain outside both the space and the widened sample (the ruling named only 4 shapes, which the dev delivered — verified red-on-base: command-prefix, assignment-prefix, while-condition all base!=bash/tip==bash; brace-bg control identical everywhere as declared). Ceremony may want one composition row and one loop-bg row added to the pin sample or the enumeration's route axis widened in the successor row.

EVIDENCE:
Scratchpad v24r8/outspace-55edb24f.txt: bg-inside-member bash/tip=(END,c=1) base=(A,END,c=0); member-inside-bg bash/tip=(END,child=1) base=(A,END,child=0); nested-member-in-member and arith-cmd-body similar; for-loop-bg bash/tip=(A,END,child=0) base=(END,child=1). Zero parser splits across all rows. Pin-shape red-on-base replays in the same scratchpad (s1/s2/s3 + spot-replay command-prefix row).

### NIT
Everything else in the Task-4 charter VERIFIED — recorded here so the integrator has the full pass list despite the FAIL verdict: (1) R8-A replays exactly (read side bash F:1 vs psh got:A; write side NOWRITE vs WROTE; eval-body twin; argument-spelling controls agree; errexit-free twins agree; abort family agrees in both spellings; every divergent row base-identical, both parsers, both channels; scope paragraph present and accurate for the two routes it names). (2) Zero production changes 4ea3df9c..55edb24f (diff = 2 test files only); dev's assessment of both production sentences confirmed (first clause route-general but scoped by its final sentence — correctly stopped-and-reported, not edited). (3) R8-B totals verified from the transcript's own rows (60/126/0/0 over 186; 11 ORDER-RACY); 8-row independent spot replay all agree including 2 ORDER-RACY rows reproducing as flagged. (4) R8-C sweep collects and passes in a default invocation; 27 novel both-parser observations across the families (varied params $(fi)/$(do)/<(if), 3 input modes) = 0 splits; teardown-$(case) and embedding-$(do) rows classify inside the declared divergence/flip families (bash -c $(do) = 127 = tip). (5) Three instrument defects recorded with instruments; pinw.txt fix is its own declared commit 55edb24f (relative name -> mktemp -d), nothing tracked, dev worktree clean. (6) Audit replayed in a NEUTRAL worktree: 99/99 exit 0 at 55edb24f; composition counted from the script = 77 chk + 9 nchk + 12 loop-stamped + 1 chain = 99; doctored-header-SHA copy makes exactly 1 row FAIL, exit 1; bounced-rows 84/84 SHA-stamped; chain table 121 MATCH / 0 REGRESSION (128 rows: +5 base-identical-divergence, +2 moved-still-divergent). (7) No-regression suites green at tip: 294 conformance rows both files (must-not-flip rows green by name), 113 guard/unit, PTY module 10/10 in default run (7.67s), golden heredoc row 1 passed/1 skipped; live assignment-alias offender bites the strengthened guard (1 failed with offender present, 16 pass clean). (8) Accounting: gate SHA-stamped 55edb24f, 21,104/0/1,590/10 (20,198+906), +89 = 47+33+6+3 reconciles against gate-r6 21,095 and gate-r7 21,101; compare-bash SHA-stamped 2,986/26. (9) Forbidden files, FLIP-PINS.md, docs/, and test_process_sub_closed_fds.py untouched; version.py 0.758.0. (10) Ledger: STILL-OPEN table empty-or-deferred with the stop-and-report row deferred to the integrator; successor row (e) cross-references 2.3 (LEDGER.md:162) and 1.3b (:133) in its own text; row (f) present; the root-cause pattern statement present — and now confirmed prophetic by the blocker above.

EVIDENCE:
Worktrees: tip /tmp/remv-probe-24r8 @55edb24f, base /tmp/remv-base-24r8 @1b271d77, discriminators verified; oracle /opt/homebrew/bin/bash 5.2.26; all transcripts under scratchpad v24r8/ (r8a-replay-55edb24f.txt, r8b-spot-55edb24f.txt, outspace-55edb24f.txt, regress-confirm-55edb24f.txt, parsersweep-novel-55edb24f.txt, audit-replay-neutral.txt, audit-doctored2.sh). Both worktrees left clean and in place (they pre-existed this task and may be shared by sibling verifier tasks; my additions removed).

## Task verdict: PASS-WITH-NITS

### NIT
SCOPE WIDTH — the errexit severing/deferral machinery is a semantic change to ORDINARY errexit far beyond HIGH-9's frame-outcome charter, and the integrator rulings it cites (R8-A/B, R7-x, R6-x) are NOT present in the brief file I was given. The brief's own text is silent on errexit-suppression threading. It IS declared and pinned, so it is not a bounce under the brief's 'DECLARED + PINNED' clause, but the integrator should confirm those rulings exist in the durable record before ceremony, because the ledger/brief pair alone does not authorise them. Files: psh/executor/pipeline.py (SimpleCommand members zero errexit_suppress), function.py (_function_frame now WRITES the errexit pair and save/restores it), command.py (one-shot deferral consume on non-FUNCTION dispatch), context.py (new errexit_suppress_deferred field), core.py (_execute_background_list now threads errexit_suppress — 'silently took the default 0' before), strategies.py (backgrounded builtin severs), subshell.py (_execute_background_subshell seeds _errexit_suppress_seed).

EVIDENCE:
Independent 576-row hunt (18 routes x 4 bodies x 8 suppression sources, one invocation per row, live bash 5.2.26 vs psh@55edb24f vs psh@1b271d77) over a space DISJOINT from the dev's r8b_hunt: 430 MATCH, 134 MOVED-TO-BASH, 12 MOVED-DIVERGE (all 12 are the already-declared function-frame channel rule, e.g. `f(){ eval 'echo $(if)'; }; set -e; { true | f; } || echo GOT rc=$?` -> bash 127 / tip 1 / base INFUNC-printed), 0 MOVED-AWAY. Regression sample green: tests/unit/tooling+executor+scripting, tests/integration/shell_options, test_posix_special_builtin_exit.py, test_fatal_expansion_model.py = 1260 passed; job_control 391 passed; pipeline+control_flow+functions 517 passed; parser+expansion+parser_differential 4329 passed. ruff clean; mypy 'no issues found in 274 source files'.

### NIT
tests/conftest.py promotes the new PTY bash-DIFFERENTIAL (tests/system/interactive/test_substitution_abort_interactive_pty.py) into the DEFAULT suite, bypassing --run-interactive. The module resolves the bash oracle at import scope and is documented to fail LOUDLY (not skip) when absent, and its _ROWS assert BASH-side PTY values ('1', '2', None). Integrator plan A12 records that the Linux nightly's bash build differs from the local 5.2.26, so a bash-version-dependent PTY value now breaks the default suite rather than an opt-in one. Precedent exists (test_multiline_immediate_error_i3.py is already in that list), so this doubles an existing exposure rather than creating a new class.

EVIDENCE:
tests/conftest.py hunk adds `or "test_substitution_abort_interactive_pty" in str(item.fspath)` to the early-return in pytest_runtest_setup. Ran the file at tip: 10 passed in 7.75s (non-flaky across my runs). resolve_bash() ladder is BASH_PATH -> /opt/homebrew/bin/bash -> /usr/local/bin/bash -> shutil.which('bash'), raising BashOracleUnavailable only if all fail.

### NIT
PTY CORPUS GAP — the committed interactive pin holds the ERROR-KIND axis constant on its DIRECT row: _ROWS uses only `echo $(fi); echo AFTERRC=$?` ('direct_complete_but_invalid'). The UNTERMINATED spelling `echo $(if)` at a real terminal does NOT behave like bash: bash reports the syntax error and returns to PS1 with the follow-up command runnable, while psh consumes the line into a continuation and the follow-up command never executes. No traceback and the process is not crashed, so the brief's bounce condition ('a REPL that dies on echo $(if)') is not met — and I replayed it at BASE 1b271d77 and TIP 55edb24f with identical results, so the slot moved nothing. But the module docstring asserts 'THE INVARIANT THAT MATTERS MOST is the guard rail: whatever the status, the REPL SURVIVES every row' over a corpus that excludes the one direct spelling that diverges. Worth a successor row (neighbours the r18 lexer / PS2-continuation family).

EVIDENCE:
Fresh pexpect probe (v9/h_pty.py, v9/h2_pty.py), PS1='P1> ', TERM=xterm, --norc --force-interactive: row `echo $(if); echo AFTERRC=$?` -> bash sentinel reached (alive=True, transcript shows "bash: syntax error near unexpected token `)'"); tip alive=False; base alive=False; traceback=False for both. Follow-up characterisation: bash state=PS1-back recovers=True; tip state=PS1-back recovers=False; base state=PS1-back recovers=False — byte-identical tip vs base. The dev's own 5 _ROWS all replay exactly as pinned (bash 1/1/2/1/None vs psh 2/2/2/2/None, all alive).

### NIT
Anti-bypass guard 2 (tests/unit/tooling/test_substitution_abort_guards.py::test_only_the_sanctioned_non_fork_catchers_exist) resolves only NAMED catchers (bare, attribute-qualified, import- and assignment-aliased). psh/ contains 9 broad catchers that would silently contain SubstitutionSyntaxAbort without tripping it: psh/core/process_lease.py:261,300; psh/core/state.py:497; psh/io_redirect/file_redirect.py:888; psh/interactive/signal_manager.py:227,302,362,396,434. The dev states this exclusion explicitly and defers to the broad-catch ratchets (test_broad_valueerror_catch_q2.py, test_subscript_no_broad_except.py), which is defensible; recorded so the integrator sees the residual surface.

EVIDENCE:
`grep -rn "except BaseException\|except:" psh/` = 9 hits, all pre-existing (untouched by the diff). Guard 2 DOES bite on a real in-tree offender: inserting `try: pass / except SubstitutionSyntaxAbort: pass` into psh/executor/function.py#_function_frame produced `FAILED test_only_the_sanctioned_non_fork_catchers_exist` with `Extra items in the left set: ('psh/executor/function.py', '_function_frame')` (1 failed, 15 passed). Removing the second consumer site (_run_from_source's trial-parse call) turned 13 pins red in test_nested_substitution_timing_conformance.py plus test_frame_fatality_for_complete_but_invalid_bodies; flattening substitution_abort_status to `return 127` turned 17 pins red across both conformance files.

### NIT
Evidence-citation spelling: test_interactive_dash_c_channel_disposition and the PTY module docstring cite `tmp/r24-probes/r6c-flags-*.txt` and `r6c-pty-*.txt` (hyphen) for the base-and-tip measurement, but the hyphen-globbed artifacts present are only `-base1b271d77.txt` and `-tip360090b2.txt` (a MID-branch commit). The final-tip artifacts exist under the UNDERSCORE spelling and are properly provenanced, so the claims hold — the cited glob just does not match them.

EVIDENCE:
ls tmp/r24-probes | grep r6c -> r6c_flags-TIP.txt, r6c_pty-TIP.txt, r6c-flags-base1b271d77.txt, r6c-flags-tip360090b2.txt, r6c-pty-base1b271d77.txt, r6c-pty-tip360090b2.txt. head of r6c_pty-TIP.txt: `SHA: 55edb24f05c7720d2f556b2a2ed972c30360d1d5 / battery: r6c_pty.py / date: 2026-07-31T05:44:34Z / PSH_ROOT: /Users/pwilson/src/psh-r2-4 / bash: /opt/homebrew/bin/bash` — i.e. measured at the true final tip with a discriminator.

